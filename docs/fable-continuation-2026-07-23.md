# Fable Coordinator Continuation — 2026-07-23 (context-floor port)

**For:** the next Fable orchestrator conversation. **Read AFTER the CLAUDE.md protocol + `seat-handover-2026-07-18.md`.** `backlog-dependency-map.md` is THE current board (kept live through 2026-07-23) — **where it disagrees with the seat-handover §2 queue, the map wins.** This brief carries only the 07-22→07-23 deltas.

## 1. Shipped this window (all coordinator gate-re-run green; trader pushes the ~18-commit stack + refreshes AWS with v60+v61)

v55 display semantics (+E5 band ladder = the F1 instrument) · F4 NO_DATA eval v6 · **v56 geometry arbitration modes** (36-cell study run: nearest-target tops every cell but ⚠DIVERGENT; re-read at W6-1 or book-double — overlays committed in `tools/WhatIfRunner/overlays/`) · v57 on_close stomp-proofing · v58 ASIA session_volume neutral · v59 #7/#8 alerts (+rose cascade accent, 6-level approach; `liq_events.log` = durable A4 evidence) · **v60 LONDON burst ARMED (T=5.5)** — WATCH: fire 8–12% + same-side ≥85%, first 2 weekday LONDON sessions · **v61 absorption geometry rescale** (ATR-fraction distances; the 07-23 STALLED ruling: tick-scaled $2 shell → 0% engagement; clock restarted at the AWS/local v61 refresh).

## 2. Superseded handover rows

- **Soak review ~Jul 26–30 → CALLED CLEAN 2026-07-22** (order-app docs = joint record; engine accepted). Live ladder runs order-app-side. P5 values: F1-gated; CONFIRMED **un-flagged** (D7 re-read = ARTIFACT; §6 clarity fix shipped).
- **#6 activation gates ~Jul 29–31 → SUPERSEDED**: re-collect ~1–1.5 weekday-weeks under v61 geometry → re-run `absorption-engagement-derivation-2026-07-23.md` recipe → THEN the gates (~mid-Aug, Opus month). J1 loose-anchor path rejected (map row has the ruling).
- W6-4: **CeilingAudit tool BUILT** (`tools/CeilingAudit/`, K1–K6 + TargetCapReason correction); RUN at ~8–10k rows (early Aug) on the pooled book.
- AWS supplementary collector **DEPLOYED 07-23** (fresh book; local canonical; `aws-collector-deploy-checklist.md`; record InstanceId at first copy-back; weekend-runtime ruling: keep up for the A4 cascade hunt).

## 3. Standing periodic checks (every session)

(a) `liq_events.log` CASCADE line ⇒ A4 unlocks; (b) report §9 STRONG n ≥ 150 ⇒ F1 re-read ⇒ Kelly CAL + P5 tier values; (c) LONDON burst watch (first 2 weekday sessions post-v60).

## 4. Dated queue

LONDON watch read (~2 sessions post-push) → geometry-study re-read + W6-1 LONDON ruling + **Aug-1 handover doc** (the seat's last deliverable; Pro downgrade Aug 2) → W6-4 run (early Aug) → absorption re-derivation (~Aug 4–6) → gates (~mid-Aug).
