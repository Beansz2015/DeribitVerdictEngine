{
  "_harness": "fixture-parser",
  "_detectors": "FP-1 (provenance verdict) and FP-2 (name vs assertion). FP-Q1 is NOT exercised: the measured run omits -ScopeBaselinePath, so scope is CODE-ONLY and the judged population is exactly the list printed by the no-key run.",
  "_window": "-SubFilter '^A23a_' -- the ONLY residual items never judged by FP-1/FP-2: 4 FP-1 sites + 1 FP-2 sub, all in A23a_AggrVelSteadyRate. Opened by item 8a (in scope by construction via WRAPPER_FORWARDED) plus item 8d (the MECHANISM marker). The other 19 FP-1 sites and 6 FP-2 subs in today's residual were judged in the 2026-09-22 clean run and are excluded.",
  "_why_four_not_six": "seat-handover-2026-09-22b.md section 0 says 'six newly-opened FP-1 sites'. That counts the six newly in-scope PARAMETERS of item 8a. Only sites that are in scope AND carry a provenance marker join the FP-1 residual; fundingBoost and upgradeBonus sites carry no marker, so the new FP-1 population is 4 sites (measured: no-key run at HEAD 2f0381b, FP1_CANDIDATES 23 = the clean run's 19 + A23a's 4).",
  "_by": "orchestrator seat (Opus 5.5), by hand, BEFORE any FP-1 or FP-2 detector output on this window",
  "_written_utc": "2026-09-22T18:55:29Z",
  "_head": "2f0381b",
  "_protocol": "docs/harness-shadow-mode-protocol.md",
  "_input_seen": "Same as the detector. FP-1: the exact state fields (call_line, comment_block, param_name, literal_value, matched_key, ever_shipped_values, literal_equals_ever_shipped), dumped from a scratch copy of tools/checks/fixture-parser.ps1 whose Invoke-Jev was replaced by a throw, run with TYPESAFE_API_KEY stripped. FP-2: the A23a sub body. Also read, beyond the detector's input: settings.json's current aggressor_velocity values (to check the comment's own count, note below).",
  "_CONFLICT_DECLARED": "Before writing this, the seat read docs/fixture-parser-check-spec.md section 8.6, which records the item-8d ruling that all four A23a literals are MECHANISM and why. The seat therefore knew the INTENDED class before reading the comment. The detector does not have that context. The comment states the class explicitly, so the FP-1 reads below come from the comment text; any agreement on them is still worth less than a blind read. The seat has also seen FP-Q1's earlier answers on these four PARAMETERS (all 'input', docs/harness-runs/fixture-parser-scope-run-2026-09-22.md) -- a different detector and a different question. The seat has seen NO FP-1 or FP-2 output on A23a: none exists (grep of docs/ and tools/ for 'A23a_AggrVelSteadyRate#' finds nothing; spec section 8.5 says none was recorded).",
  "_notes": {
    "A23a_AggrVelSteadyRate#3061#tauFastSec": "Comment: 'MECHANISM -- all FOUR literals below'. Reason given: LOAD-BEARING, the expected bands are functions of the taus, mutation-checked 5.0 -> 7.0 fails. The reasoning says deriving from cfg would be WRONG, so it does not depend on tracking matched_key. High confidence.",
    "A23a_AggrVelSteadyRate#3061#tauNormSec": "Same declaration and same LOAD-BEARING group as tauFastSec. Note the comment only mutation-checks tauFast, not tauNorm; the class is still declared and still not tied to the key. State's ever_shipped_values is [120] while the comment says norm_window_sec {60, 120} -- the known FP-D18 default-arm under-statement (spec section 8.5 item 3). It does not change the class. High confidence.",
    "A23a_AggrVelSteadyRate#3064#grossFloorUsdPerSec": "Declared MECHANISM; reason: INERT (grossNorm ~96.8 above the 50 floor), mutation-checked (37.0, 83.0) still passes, 'an inert value asserts nothing, so it cannot be SHIPPED BEHAVIOUR'. Equality with shipped 50 is acknowledged and explicitly licensed. High confidence.",
    "A23a_AggrVelSteadyRate#3064#minCoverageSec": "Declared MECHANISM; INERT (399 s coverage vs 120 s bar); also argues reading it from cfg would fail spuriously. Not tied to tracking the key. High confidence.",
    "A23a_AggrVelSteadyRate": "Lean, not certain. Name = scenario 'aggressor velocity, steady rate'. Body folds 400 s of 100 USD buys at 1/s and asserts warmup, grossFast in (109,112), grossNorm in (95,99), BurstRatio in (1.05,1.25) and Lean > 0.99. Rate, ratio and lean are all accumulator outputs on that one steady tape, and this file names fixtures by scenario (A23b_AggrVelBurstDetection, A3_MicroCvdWindowFromEnd). Weighed against name_understates (Lean and HasWarmup are not rates). Chose name_matches.",
    "_comment_defect_seen_before_run": "The shared comment block says 'Three of the four DO equal currently-shipped values'. Against the tracked settings.json at HEAD, ALL FOUR do: fast_window_sec 5, gross_floor_usd_per_sec 50, default.norm_window_sec 120 (tauNormSec and minCoverageSec both 120). The next line lists three KEYS, which is the likely source of the miscount. Spec section 8.6 itself says 'All five DO equal a shipped value'. A prose miscount in supporting text; it does not contradict the MECHANISM class, so it does not move any FP-1 read above. Recorded here before the run so it cannot be mistaken for something the detector surfaced."
  },
  "A23a_AggrVelSteadyRate#3061#tauFastSec": "mechanism_declared_ok",
  "A23a_AggrVelSteadyRate#3061#tauNormSec": "mechanism_declared_ok",
  "A23a_AggrVelSteadyRate#3064#grossFloorUsdPerSec": "mechanism_declared_ok",
  "A23a_AggrVelSteadyRate#3064#minCoverageSec": "mechanism_declared_ok",
  "A23a_AggrVelSteadyRate": "name_matches"
}
