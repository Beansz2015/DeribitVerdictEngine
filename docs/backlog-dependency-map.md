# Backlog Dependency Map

**Created:** 2026-07-22 (trader-directed). **Purpose:** one referral table for everything outstanding — what blocks what, so any seat can see what must happen first. **Authorities unchanged:** `roadmap.md` = execution order · `profitability-risk-levers.md` = profitability lens · `post-websocket-post-calibration-backlog.md` = full item detail. This doc is the CROSS-REFERENCE, not a new authority. **Maintenance:** update State on every tick/ship; move dead rows to the bottom section rather than deleting.

| Item | Class | Blocked by | Unblocks | State (2026-07-22) |
|---|---|---|---|---|
| **#7 liq-cascade alarm + #8 level alerts** | Display/ops | — | A4's gate instrument now EXISTS | ✅ **SHIPPED v59 2026-07-22** (`46e7614` + follow-up `97e2e24`: rose cascade accent, 6-level approach set incl. 15m swings; options 3/4 = leave-as-is rulings, spec-back addendum) |
| A4 liquidation × OFI flip ⚠ | Scoring | **ONLY the market now**: ≥1 CASCADE line in `liq_events.log` (durable, survives restarts) | — | Gated — instrument live since v59. ⚠ **INSTRUMENT AT RISK 2026-07-31:** the `settings.local.json` overlay spec admits `alerts.` to its per-box whitelist, but `alerts.enabled:false` means `liq_events.log` is **never written** on that box (`AlertsTracker.vb:116` returns before the sidecar appends at `:143`/`:157`; `DeribitWsFeed.vb:439` skips the fold) — and the gate pools BOTH boxes' sidecars. Re-audit rules **REJECT `alerts.` from the whitelist**: `overlay-whitelist-reaudit-2026-07-31.md` F1. Also confirmed: no `liq_events*` on either box as of 2026-07-31, so the gate remains purely market-dependent |
| WS-health line persistence | Ops | — | Feed health = durable record | ✅ **SHIPPED 2026-07-22** (`bbc47c7` — `ws_health.log` transitions-only sidecar, A38a/b) |
| 3-min weekday-ASIA `session_volume` re-verify | Calibration | Data gate likely MET (verify ≥50 weekday-Asia 3-min rows) | Possible dial-back of v34's Saturday-set 1.10/1.05; bundles the OBV re-anchor | **READ DONE 2026-07-22** (`asia-session-volume-reverify-2026-07-22.md`: weekday Asia materially calmer — trade-rate 29.4% vs 63.1%; REC: dial to neutral 1.00/1.00 — ticked → ✅ **SHIPPED as v58** `bd31a1a`; §12 row CLOSED) |
| v28 target-hit vs barrier-hit gap (§12) | Measurement | — | — | **RE-HOMED → W6-1 (trader tick 2026-07-22):** the audit re-run computes these rates on the honest yardstick, and "stops too tight" IS W6-1's stop_max candidate; standalone row CLOSED |
| **D7 CONFIRMED-tag re-read (RE-OPENED)** | Display → possibly ⚠ | None — new evidence (placed-geometry §6: CONFIRMED 13.3% [8–21] vs MEDIUM baseline ~33–35%, CI-separated; the 06-24 "≈ baseline" resolution was on the old yardstick) | P5 policy values; the never-built §6 clarity fix (separate directional vs lean tags) | **READ DONE 2026-07-22 — ARTIFACT** (`d7-confirmed-reread-2026-07-22.md`; 06-24 resolution holds; P5 un-flagged; §6 clarity fix is now the actionable remainder) |
| §6 report clarity fix (directional vs lean tags) | Display | — | Honest context table | ✅ **SHIPPED 2026-07-22** (`d4f278b` — directional/lean sub-tables + not-comparable caption, lean rows as counts, A34f) |
| **Fee-aware min-move floor (engine)** | Settings restructure (no ⚠ — defaults byte-identical) | — (**APPROVED 2026-07-27**, D1–D6 all-as-recommended; Deribit maker 1.5/taker 3.5 bps eff. Aug 1) | Honest net-EV analytics (§6.1 rider, post-Aug-1); the trader's live `min_net_move_pct` knob | **BUILD-READY** — kickoff: "Implement docs/fee-aware-min-move-proposal.md" (Opus, medium) |
| Fee relay → order app (EV-aware chase budget + SL taker delta) | Consumer | — | Chase stops pricing residual net move; `cancelled: ev_floor` disposition keeps the counterfactual measurable | **ACK'D 2026-07-27** (relay §2b): Rec 1 accepted, corrected — counterfactual via host-log CANCEL REASON, not a disposition row (join cardinality); Rec 2 accepted as checklist guidance (~2 bps on M.SL), computed version = later micro-spec |
| Geometry-modes study re-read (v56 instrument) | Measurement | Book ~doubles (~mid-Aug) or the W6-1 audit — DIVERGENT flags must clear | TP-half (nearest-mode) live ⚠ candidate | First run 2026-07-21: nearest tops table, not separable |
| res-3 §5.2 (aggr-vel Asia/London thresholds) | Calibration | ASIA: ~150 fires/session (AWS coverage now accruing daily) | Burst scoring auto-arms on those sessions | **LONDON DONE** — armed v60 (T=5.5, `631a3f5`); watch READ **PASS 2026-07-27** on the AWS book (pooled fire 9.5%, same-side 17/17=100%, live p90 5.46 ≈ T — `london-burst-watch-read-2026-07-27.md`). **ASIA DERIVED 2026-08-01** (`asia-burst-threshold-derivation-2026-08-01.md`) — **gate MET, 323 fires vs a ~150 bar** (the 07-13 pass deferred on 36); **REC T=5.5**, i.e. LONDON's value reached independently: ASIA p90 **5.35** → 9.7% fire, 91.0% same-side, 10.4 fires/day. **Decision-of-record proposed: no ASIA-vs-LONDON split needed — the res-3 burst distribution is ONE distribution** (all percentiles within ±9%; `burstRatio` is scale-free by construction). ⚠ **Awaiting trader: arming ASIA is a live scoring change** (threshold presence = auto-arm) ⇒ own D-table + dataset boundary. Two findings recorded: the **contra-soften arm is dead on res-3 at every T** (≤0.43/day, incl. at LONDON's shipped 5.5 — the modifier is upgrade-only on 3-min sessions), and **per-day stability is ~9–16pp, not NY's ±2pp**, so no res-3 threshold may be re-fitted off one session-day |
| Funding calm-week re-read | Watch | A calm funding week | Fully closes the v52+v53 window | Waiting |
| **Bridge soak review** | Consumer | **CALLED CLEAN 2026-07-22** (order-app `soak-review-reply-orderapp.md` + `review-soak-join-2026-07-22.md` = the joint record; 69/69 would-act≡Placed* parity; engine seat ACCEPTED same day — nothing in our entry exceeded §5.7+join) | Live ladder: ARM → live-at-min-size → then L3 spec slot; P5 reminder RESOLVED (D7 = artifact; tier values still F1-gated) | Done |
| **#6 absorption activation gates ⚠** | Scoring | **STALLED at 0% engagement — coordinator ruling 2026-07-23:** not a units bug (fold + caller verified; same USD field AggrVel validated); the TICK-SCALED geometry is mis-sized for a $67k instrument (band 4t=$2, proximity 12t=$6 vs ATR≈$44) — anchors were tape-scale, mechanism measures a $2 shell; even the loosest re-anchor grid caps at 1.6% vs the 3–8% band (`absorption-engagement-derivation-2026-07-23.md`). NEEDS: small geometry-rescale spec (ATR-fraction distances + rescaled floors, display-only, D-table) → ~1–1.5wk re-collection → re-derive → THEN the gates | Absorption Step-2 penalty wire-in | **Re-derivation DONE 2026-07-30 (`absorption-anchor-rederivation-2026-07-30.md`): anchor loosening CANNOT reach the band** — NY episode numerics are barren (max ratio 0.51, max aggr $35k; ZERO NY passes at ANY grid cell down to ratio 0.05), so the `<1%` branch traces past the anchors to the OBSERVATION mechanism (`window_sec=10` rolling reset + D8 pullFrac inflation on sparse postLB — the §8 residuals). **Coordinator ratifies Path B: HOLD v61 anchors, spec a mechanism revision** (window_sec / episode-cumulative pressing / D8 sparse handling — display-only, no ⚠); Path A V-table (0.5/5000/1.0) on file but projects NY 0.000% and ~10–15 weeks to the gate — not recommended. **Trader ticks the path**; mechanism spec = early Opus-month item; activation slips past mid-Aug on any path (stated honestly) |
| **Bridge v2 feedback file (C1)** | Cross-repo contract (phase 1 = display-only, no ⚠) | Order-app draft RECEIVED 07-28 (`proposal-c1-v2-feedback-file.md`); **engine reply AUTHORED 07-28** (`feedback-file-engine-reply-2026-07-28.md` — D1–D5 concur, 3 refinements, D6 posState shape proposed: feedback-governs/radios-fallback) | Truthful `posState` → engine-managed exits (phase 2) + slippage join + W6-6; engine build slots behind the §6.1 net-EV rider | ✅ **DOCUMENTATION PHASE CLOSED 2026-07-29**: T1–T8 ticked; order-app contract §8 landed (their `f6bea0a`); **engine mirror landed (`signal-bridge-v1-proposal.md` §10)**. Builds queued: their emitter after N2; engine consumption behind the §6.1 net-EV rider |
| W6-1 LONDON ruling | Calibration | — | LONDON stop_max override; swing-buffer design input | ✅ **RULED 2026-07-31: NO CHANGE** (`w6-1-london-ruling-2026-07-31.md`). The two named candidates are **one lever** — `stop_buffer_pct` applies AFTER the clamp and scales the final distance, so with the clamp binding **95.5% of LONDON** structural-stop rows they are algebraically interchangeable; confirmed empirically by matched grid pairs agreeing to 0.002 EV. Widening loses monotonically pooled (EV/ATR −0.475 at 1.6 → −0.647 widest; 2.0 → −0.552, 2.2 → −0.584 both refuted); LONDON-only the top 3 cells are a **tie inside noise** (n=215), so "no separable change" is the outcome. **Depth-gate caveat:** the 556-row gate counts directional rows, but the deciding instrument sees **215 evaluable** — no deeper than the ~227 the original read had; future gates must count evaluable rows. **The real finding:** the clamp binds 95.5–99.6%, so stops are de facto ATR stops — the live question belongs to **L9 un-clamp**, gated on **L3** |
| W6-4 offline ceiling audit | Measurement | ~3–4 wks of v0.8 rows (early Aug); method draftable NOW | **W6-5 (B1)** + any W6-7 Tier-C spend | Prep-ahead OK |
| F1 tier-ladder re-read | Measurement | **COUNT GATE MET 2026-07-30: 183 pooled weekday STRONG** (NY 118 / LONDON 53 / ASIA 12; deduped book). Remaining blocker: the §9 report runs in-app off the LOCAL book (~<150 alone) — needs the pooled-book report mechanics decided (point the runner at a pooled snapshot vs extend it) — **Aug-1 session item** | **Kelly CAL (W6-3/L4)** + P5 tier values | **Count gate RE-VERIFIED 2026-07-31: 201 pooled weekday STRONG** (NY 119 / LONDON 67 / ASIA 15) under the newly-ruled AWS-preferred dedup on a fresh freeze — clears 150 on every basis tried (post-07-08 187, local-preferred 203), so the gate is not sensitive to the dedup ruling. Mechanics decision still pending |
| Kelly CAL (EST→CAL) | Display | F1 re-read passes | L4; consumer sizing reads empirical kelly fields | Gated on F1 |
| L3 stop-distance sizing (order app) | Consumer | Live-at-min-size stabilises (post-soak) | **L9 un-clamp** + the widest-stop mode's live eligibility | Queued |
| L9 structural-stop un-clamp + swing-buffer | Scoring ⚠ | L3 shipped (+ calm-regime re-derivation, DG5) | The trader's real stop method live | End-state |
| Tweaker first fire (W5/W6-2) | Loop | A >40%-failure NY×1 window (post-migration rates only) | Window/MinTier recalibration | Data-gated, supervised dry-run first |
| D2-v2 volume-weighted pivot promotion | Geometry | Revive only with evidence — test via the v56 what-if modes | — | Parked — NOT what-if-testable today (ComputeSideLevels never reads BestPivot*; needs a small candidate-mode extension to the v56 machinery first). CSV raw material: 2151 post-2026-07-08 directional rows carry a non-empty BestPivotByVolume5m. Newly interesting given the NY swing-tier underperformance — candidate topic for the Aug-1 geometry session (trader-checked 2026-07-22) |
| D3 / D4 / D5 / D6-backlog (5m RSI-div · Donchian×BBW · smart OBV · MFI) | Scoring refinements | W6-4 ceiling audit must show combination headroom (NOT the #6 gates — clarified 2026-07-22); W6-4 method spec = next Fable task, its data gate ~early Aug | — | Parked |
| A5 VPFR shape classification | Scoring | 30-day book | — | Data gate: 15 distinct calendar dates in CSV overall (first 2026-07-03, last 2026-07-22); same 15 since 2026-06-24 WS cutover — nearly met (halfway to 30). NOTE: even when met, A5 must clear the W6 new-indicator bar (it is a VPFR refinement, not an orthogonal class) — spec-first with that argument, not automatic |
| C1 / C2 (multi-session VPFR · anchored VWAP) | Scoring | Multi-session state plumbing = the CLI-port run-state restructure the trader deferred to LAST (Q6) — Q6-last KEEPS these parked, it does not open them (clarified 2026-07-22) | — | Parked |
| CLI port (Q6) | Platform | Trader ruling 07-08: AFTER the W6 programme — do not pull forward | O3 | Deferred LAST |
| W6-7 cross-venue lead-lag | New signal class | Current queue done + W6-4; #5-style gates bind activation | The one remaining non-marginal class | Spec candidate authored ONLY — not implemented; sequenced after the current queue AND W6-4 (unchanged; trader-checked 2026-07-22) |
| **AWS supplementary collector** | Ops | Trader deploys per `aws-collector-deploy-checklist.md` (fresh book, NEVER seed the CSV; local stays canonical) | Coverage-bound gates: res-3 §5.2, W6-1 LONDON depth, F1 STRONG, W6-4 depth; second 24/7 A4 cascade instrument | ✅ **DEPLOYED 2026-07-23** — collecting (~921 rows/day, all sessions; clean feed since 07-22 19:25 UTC). First copy-back 2026-07-27; InstanceIds recorded in the checklist §5. **Dedup RULED 2026-07-31 (trader): AWS-preferred, local fills gaps** — reverses the earlier local-preferred proposal, on the ground that AWS is the canonical 24/7 collector and the D1 end-state topology while local is an opportunistic addendum that runs only while the trader is at the desk. Measured impact: on the 2,159 minute-keys present in both books the boxes disagree on verdict **4.49%** (97 bars); the preference moves F1 by 2 and W6-1 by 2, so no gate conclusion turns on it. Key = timestamp floored to the minute (`london-burst-watch-read-2026-07-27.md` follow-ups). **Consequence flagged, NOT ruled: J-B's "silence ⇒ defect" needs a per-box expected-uptime scoping clause before the coverage report is built** — unscoped, it would classify most of the local box's existence as defect (local logged 567/392/268 rows on 07-28/29/30 vs AWS 921/921/914) |
| W6-4 method spec | Measurement | — | The W6-4 RUN (gates W6-5/B1, D3–D6, W6-7 Tier-C) | ✅ **APPROVED 2026-07-23 (K1–K6; §2 scored-inputs-only clarification)** — build any gap (**Opus at highest effort tier, trader-directed**), run at the ~early-Aug data gate on the pooled book |
| **Backtest synthesizer** | Instrument (analysis-only, no ⚠) | **PROPOSED 2026-07-30** (`backtest-synthesizer-proposal.md`, D1–D8 await trader; trader-initiated) | Weeks→days on the geometry/threshold class (W6-1 depth, v56 re-read, D2-v2, ASIA §5.2, D3–D6, all future re-baselines); the overlap validation = first empirical OFI/OI/spread verdict-contribution read | Awaiting tick; build = early Opus month after the absorption mechanism spec (D8) |
| Aug-1 handover doc | Process | ~Aug 1 (Pro downgrade Aug 2) | The credit-rationed month | Scheduled — this seat's last deliverable |
| Untracked strays ruling + UI-automation .ps1 | Hygiene | — | Clean tree | ✅ **EXECUTED 2026-07-22** (`241e791` — `tools/tools/` deleted [dead tweaker state], three keepers gitignored, .ps1s were already tracked) |

| **Effective-source CSV stamp (`DeriveWsHealth` per row)** | Measurement / ops | **Nothing blocks the decision** — blocked only on the **next natural CSV header rotation**, which it rides alongside `TriggerMode` and **must never force** | Makes overlay-D7 option (c) implementable (exclude a REST box's rows) without re-opening D7 · first measurement of the degraded-feed fallback rate · a *positive* per-run state record that strengthens the coverage report's S1 (which today only *infers* app-up from a transition-only log) | **PROPOSED 2026-07-31** (`overlay-d7-and-row-source-stamp-2026-07-31.md` §4). The value already exists — `SignalEmitter.DeriveWsHealth` returns `REST`/`DEGRADED`/`DOWN`/`OK` every run and ships in the bridge payload, but **none of `analysis_log.csv`'s 111 columns records it**. Motivation: a `transport: ws` box **already** emits REST-scored rows whenever `ResolveSource` falls back on a degraded feed (time-averaged OFI reverts to snapshot; the v52 aggressor-velocity modifier never fires) — unmarked, at an unmeasured rate. Column addition, not new computation |
| **Overlay D7 — REST-mode box** | Decision | Trader tick on `settings-local-overlay-proposal.md` D7 | Closes the last open question on the overlay spec | **ANSWERED (a) 2026-07-31** by `overlay-d7-and-row-source-stamp-2026-07-31.md` — tracked change + version bump, adequate because REST is break-glass not routine; **not a permanent close**, (c) unlocks via the source-stamp row above. Rider: a tracked flip to REST propagates to AWS on the next xcopy ⇒ catch it in `aws-collector-deploy-checklist.md` §1a |
| **Candle-backfill fixture gap** | Test debt ⚠ | — | Guards the defect that destroyed a month of store data | ✅ **CLOSED 2026-07-31** (`99221b2`, **A51a–e**) — verified present in `verify/ordercheck/Program.vb`: `A51a_MergePreservesExistingRowsOnPartialFetch` is the exact recipe (backfill a partially-covered month, assert stored rows survive), `A51c_EmptyOrFailedFetchNeverDestroys` covers the destructive case, `A51e_CandleRoundTripThroughShippedParse` pins the format; `A51b`/`A51d` add resolution-aware coverage counting and funding-merge clipping. Row was still reading OPEN at the seat-close handover — corrected here. **Residual (not test debt):** nobody has re-introduced the overwrite and re-run to confirm A51 would catch it (`candle-store-derivation-batch-spec-back.md` §4) |

| **`session_volume` multipliers (LONDON/NY passes)** | Calibration | **D3 forming-bar ruling** — the edge the JOB 2 packet flagged as missing from this board, added 2026-07-31 (decision D-C, RULED park) | The queued LONDON/NY multiplier passes; the v58 follow-on | **BLOCKED.** The lever is inert live, mechanism confirmed in code: `DynamicNorms.vb:36-40` builds the threshold from the last 100 **completed** bars (in-progress bar excluded by explicit comment) while `MainForm_Analysis.vb:237` uses **that same bar** as the ratio's numerator; `CalcVolumeSMA` includes it in the denominator too, compounding it. Volume vote fires **0.69% of NY / 2.66% at ExecRes 3** — quote 2.66% for the queued sessions, not the NY figure. A closed-bar-derived multiplier would describe the D3 closed-bar arm, not the live engine. **Rider:** `auto_run.trigger_mode` is not a CSV column, so a past or future trigger-mode change shifts the volume distribution invisibly; it rides the next natural rotation alongside the J-E stamp and **neither forces one**. Read: `job2-read-2026-07-31.md` §3 |
| **TTM `flat_threshold` re-anchor (D-A)** | Scoring ⚠ | Own spec + dataset boundary; direction-only per the seat-close handover §3 | Restores the FLAT band to doing anything | **FINDING STANDS, VALUES DO NOT.** 0.5 is ~100× too small (median \|delta\| 52.5/76.5; 0.5 below p01 on both resolutions; FLAT fires 0.69%/0.61%) — reproduced on two independent instruments. ⚠ **The recommended 25.0/40.0 ladder must NOT be inherited:** its Principle A anchor ("AWARD% *is* the vote") is false — the award block is nested under `Case "RELEASING","NONE"` and awards nothing on `ACTIVE` rows (29.2%/24.5%), so the real vote rate is already **43.4%/46.3%** and the ladder would push it to ~34–36%, the wrong way. Also: 1.60 is a 40÷25 grid quotient, not the measured **1.45**. Re-derive against the post-gate vote rate. `job2-read-2026-07-31.md` §2 |

**Standing rejections (do not re-propose without new evidence):** backlog §E + trader-profile §4 + roadmap §5b (sub-minute baseline cadence, #9 provisional verdict, auth feeds, Phase-2b autotune, A1 spread-momentum — refuted with evidence 2026-07-03).
