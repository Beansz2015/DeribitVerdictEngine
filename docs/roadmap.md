# DeribitVerdictEngine — Strategic Roadmap

**Created:** 2026-07-02 (Fable seat, post-audit planning session; trader-directed).
**Purpose:** the single cross-project planning document — objectives, workstreams, sequencing rules, and the execution schedule. Engine-local backlogs stay where they are (`DeribitIndicatorProject.md` §12/§13/§16.6, `post-websocket-post-calibration-backlog.md`); this doc says **what runs in what order and why**, across projects. **It is also the history future orchestrators reference** — items get ticked and moved, never dropped.
**Referred from:** `CLAUDE.md` (session-start pointer), `DeribitIndicatorProject.md` §13/§16.

**The three-doc division (restated 2026-08-07, trader-directed):**

| Doc | Holds |
|---|---|
| **this doc** | The **strategic board** — every workstream, done and outstanding, at altitude, **with its history** |
| [`trader-tick-queue.md`](trader-tick-queue.md) | The **scoped immediate slice** — what needs a trader tick or decision, and what is ready to build now |
| [`backlog-dependency-map.md`](backlog-dependency-map.md) | **What blocks what.** Referred to by both. Carries no state — only edges and a pointer to where state lives |

**Maintenance.** Tick items as they land and keep their description. Update §2 by **replacing** it, never appending — it had become a 12-entry log dated 07-02 → 07-22 while the tree ran to v65, the same rot the §15 cap exists to prevent. **Re-plan sections, not sentences.**

> 📚 **Spent sections are ARCHIVED, not deleted** — [`history-archive.md`](history-archive.md) **§H**, verbatim: the twelve dated §2 update paragraphs, the Jul 2–7 Fable window schedule, and the month execution order. §2b's ledger records *what* shipped; §H records *why it was sequenced that way and what was measured at the time* — the #5 correlation coefficients, the 13-column v0.8 CSV manifest, the CLI-port deferral, the F1 gate that still binds Kelly CAL. **Follow the same rule next time: move and point at it.**

---

## 1. Strategic objectives (in priority order)

**O1 — Signal quality to its data-supported ceiling.** The scoring core is correct (2026-07-02 full audit: all invariants hold); remaining alpha is calibration debt and signal-stack hygiene, not new machinery. Everything in W1 serves this. **Status 2026-08-07: the indicator set is BUILD-COMPLETE** (the W6 header ruling realized when #6 shipped v54). What remains under O1 is calibration (W6-1, ruled), combination (W6-5), and loop closure (W6-2/3/6) — and W6-4 has now *measured* that the combination prize is not yet demonstrable.

**O2 — Signal bridge to DeribitOrderPlacementApp (2026-07-02).** The engine's verdict/score/direction/ATR feeds the order-placement app's autotrade function (`C:\Users\user\source\repos\DeribitOrderPlacementApp` — its `FrmIndicators.ProcessAutomatedSignal` → `ExecuteAutomatedTrade(direction, score, atr)` chain ran on a home-grown EMA/VWAP/DMI score; the bridge replaces that with the engine's verdict). **Design invariant: the human display remains the engine's primary output — the machine contract is an *additional* surface**, and it joins the display-parity obligation (signal-file values must equal what the cards/snapshot show for the same run). Autotrade consumption raises the effective cost of a wrong signal from discretionary to mechanical, which raises the priority of W1's calibration items — especially the reach-target calibration (the order app places ATR-derived targets). **Status: v1 LIVE and soak-clean; live-at-min-size unlocked.**

**O3 — Linux CLI port (long-standing, §16.2).** Unchanged destination; the port's hard prerequisite (host-agnostic run-state) is specced so the port becomes "write a renderer + a runner," implementable any time after. The signal bridge is deliberately designed host-agnostic (file emission) so a future headless engine feeds the same contract. **Status: DEFERRED LAST** by trader ruling 2026-07-08 — behind the whole W6 programme.

---

## 2. State snapshot — **2026-08-07, verified in the tree**

Everything here was checked against code or data on the date shown, not inherited from prose. *(The 07-02 → 07-22 snapshot log this replaced is verbatim in [`history-archive.md`](history-archive.md) §H.)*

| | |
|---|---|
| **Settings** | **v65**, tracked. `settings.json:2` |
| **Push state** | **ahead 1** — `8b488cc` (the 08-05 handover doc) is unpushed |
| **Boundary state** | ⚠ **OPEN — the v65/D3 ASIA arming window**, live on both boxes since **2026-08-01 19:02:31Z**. This is the window §5 rule 1 governs |
| **Collectors** | AWS live on v65 (`09c747f8`), capturing tape since 2026-08-01 17:50Z. **Local Debug ran 2026-08-06 23:11 → 2026-08-07 06:27** — it is back in the pool |
| **Local capture** | OFF in **both** `bin\Debug` and `bin\Release` (overlay in each). Trader policy: run **Debug** locally — latest build, tape off, contributes CSV rows |
| **Book (local)** | 10,512 rows, 2026-07-03 → 2026-08-06, **149 weekday STRONG** |
| **Book (AWS copy in hand)** | ⚠ **CORRECTED 2026-08-12 — this row said the AWS trade store "has never been copied back at all". It has.** Copy-backs have run; the **2026-08-12** one is in hand and **not yet merged** (its August file holds **307,141** rows against the repo's 278,761). Re-check the CSV's own end date before quoting it |
| **Candle/trade store** | ⚠ **CORRECTED 2026-08-12 — this row said "2026-01…2026-07 only, no August data". Wrong.** The repo store holds July **122,018** and August **278,761** trade rows. ⚠ **But the store has THREE ERAS and any tape-derived measure must split on them** — identity-less (before 2026-08-10 14:08) · identified but ~50 % complete **and biased toward dropping sweep legs** (to 2026-08-11 17:18) · identified and complete (after) |
| **Next free fixture family** | ⚠ **A56** — **CORRECTED 2026-08-12; this row said A53, and A53a–h plus A55a–g already exist.** An implementer trusting the old value would have collided with a live family. **A55g is the high-water mark.** Re-check: `Select-String verify/ordercheck/Program.vb -Pattern '\bA[0-9]{2}[a-z]_'` |
| **Next free hard constraint** | **HC28** (HC27 is the high-water mark) |
| **F3 observational watch** | ⚠⚠ **THIS ROW CONFLATES TWO DIFFERENT WATCHES AND NEEDS RESOLVING — flagged 2026-08-12, not fixed.** Its *description* (EXIT GUARD strip vs HOLD\EXIT row corroboration during holds) is the **2026-07-02 audit-fixes F3** — see [`audit-fixes-2026-07-02-spec-back.md`](audit-fixes-2026-07-02-spec-back.md) and [`history-archive.md`](history-archive.md). Its *status* ("unevaluable; see the queue") is the **B4b F3 trigger** — the LONDON structural-target inversion, which needs cap-bucket segmentation. **They are not the same watch.** ⚠ **Only the B4b one was RETIRED on 2026-08-12** ([`w6-1-london-ruling-2026-07-31.md`](w6-1-london-ruling-2026-07-31.md) §3). **The 2026-07-02 EXIT GUARD watch is NOT retired and its state is unverified** — someone must read it and say which of the two this row is meant to track |

**Two dated instruments are blocked on the same thing.** The Kelly trigger (≥406 pooled weekday STRONG, ETA ~2026-08-30) and the W6-4 re-run both need a pooled freeze, and **both are unreadable until an AWS copy-back**. Bundle them into one freeze on one span — that is what keeps the overfit counter honest.

### 2b. Shipped ledger — settings versions, newest first

| Version | Date | What |
|---|---|---|
| **v65** | 08-02 | **D3 — ASIA aggressor velocity ARMED at 5.5** ⚠ boundary. Watch live |
| v64 | 07-31 | In-app trade-store capture (`Core/TradeStoreWriter.vb`) + gap-repair backfill. AWS-only by D1 ruling; local held off by the `settings.local.json` overlay. Not a boundary |
| v63 | 07-30 | D2-v2 what-if candidate mode — `use_best_pivot_candidate`, default **false** ⇒ byte-identical, not a boundary |
| v62 | 07-27 | Fee-aware min-move floor — flat `min_tradeable_move_pct` retired for composed `trade_costs`; defaults compose to **0.0008** exactly ⇒ not a boundary |
| v61 | 07-23 | Absorption geometry rescale — tick keys → ATR-fraction keys; floors rescaled. Display/CSV only |
| v60 | 07-23 | LONDON aggressor velocity armed at 5.5 ⚠ (`631a3f5`). Watch READ **PASS** 07-27 (pooled fire 9.5%, same-side 17/17) |
| v59 | 07-22 | #7 liq-cascade alarm + #8 level alerts (`46e7614` + `97e2e24`) — **§11 catalogue COMPLETE**; A4's gate instrument now exists |
| v58 | 07-22 | ASIA `session_volume` dial-to-neutral (1.00/1.00) — §12 re-verify row closed |
| v57 | 07-22 | Trigger-mode stomp-proofing |
| v56 | 07-22 | Geometry arbitration modes (what-if-first) |
| v55 | 07-21 | Eval display pass (F2/F3/F12) + E5 band ladder = the F1 instrument |
| v54 | 07-17 | **#6 book absorption** (`ae6678c`) — `scoring_enabled:false`, display/CSV. **The indicator set is build-complete** |
| v52+v53 | 07-14/15 | #5 aggressor-velocity NY wire-in ⚠ + funding time-anchored window ⚠, bundled at ONE boundary with trader sign-off. Joint watch **PASS** 07-17 |
| v51 | 07-06 | Placed-geometry structural-first ⚠ (`e8dd64a`+`9ab3f04`) — **satisfied the live-at-min-size hard gate**; pushed 07-07, origin `e7f8199` |
| v47–v50 | 07-02/03 | Audit fixes F1/F2/F3+N1–N4; OFI dominance re-baseline (`a86b4f1`); signal-health retune + the v0.8 CSV rotation |

**Non-settings ships since 07-21, at altitude:** placed-target matrix migration (07-21, geometry parity 1335/1335 exact) · eval `NO_DATA`/F4 (07-21, `75a2694`) · `settings.local.json` overlay (08-01, `291457c`+`1611011`) · Kelly EST honesty display (08-02, `18b1ea8`) · **C1 trade-store coverage report, both sessions** (08-05).

**Rulings of record since 07-22:** AWS-preferred dedup (07-31) · W6-1 LONDON **no change** (07-31) · D-cluster sequencing — D3 alone first, D1+D2 bundled after (08-01) · E1 Kelly option (c), wait for separation (08-02) · **J-B scoping** — scope a defect rule by a positive record of intent, never a baseline derived from the behaviour being judged (08-02) · D1 TTM **re-derived then PARKED** (08-02) · **weekday scope** — capture 24/7, evaluation weekday-only (08-03) · tape retention — keep unless it is a copy (08-05) · **briefs carry model+effort**, and **effort is self-set per task** (CLAUDE.md).

---

## 3. Workstreams

### W1 — Calibration & signal health (O1, feeds O2)

| Item | State | Gate |
|---|---|---|
| v48 OFI dominance re-baseline (+ `OFI.Momentum*` retire-or-keep) | ✅ v48 SHIPPED 2026-07-03 (`a86b4f1`); Momentum* answered by the audit → **retire** (retune spec R1, landed at the #5 boundary) | closed (§4a fire-rate watch stays open) |
| Post-WS signal-health audit — fire rates, pairwise agreement, conditional outcomes for OFI/TFI/CVD/MicroCVD + FundingMomentum/RSI-div/OISignal | ✅ DONE 2026-07-03 (`21d1b7f`): `signal-health-audit-2026-07-03.md` + `signal-health-retune-proposal.md` (D1–D6 ticked; TFI found unlogged → columns rode #5's v0.8) | closed |
| Spread revival re-baseline (WIDE/TIGHT thresholds on 100ms-fresh book; A1 spread-momentum rides along) | ✅ RESOLVED 2026-07-03 by the audit — premise refuted (one-tick book; >5bps on 0.1% of WS runs); keep 5.0 as inert tail-guard; **A1 REJECTED with evidence** | closed |
| Directional reach-target calibration (D7 spin-off — 43% reach / 53% window-expiry vs `AtrTargetMultiplier` 2.0; **elevated by O2**) | ✅ **ABSORBED 2026-07-03 → `placed-geometry-structural-first-proposal.md`**, APPROVED same day (D1–D8, D3=clamp): structural-first target ladder + structural stops. **SHIPPED v51, pushed 07-07** | closed |
| 3-min weekday-ASIA `session_volume` re-verify (§12 — v34's 1.10/1.05 was Saturday-set; recompute on weekday 3-min Asia rows, dial toward neutral if calmer; bundles the OBV `\|obvChange\|` re-anchor) | ✅ **READ DONE 07-22** — weekday Asia materially calmer (trade-rate 29.4% vs 63.1%); dialled to neutral → **SHIPPED v58**. ⚠ The bundled OBV re-anchor **split off and is now D2**, still owed | closed for ASIA; OBV half is live in the queue |
| **LONDON/NY `session_volume` multiplier passes** | ⛔ **BLOCKED — ruled park (D-C, 07-31).** The lever is inert live, mechanism confirmed in code: `DynamicNorms.vb:36-40` builds the threshold from the last 100 **completed** bars (in-progress bar excluded by explicit comment) while `MainForm_Analysis.vb:237` uses **that same bar** as the ratio's numerator; `CalcVolumeSMA` includes it in the denominator too, compounding it. Volume vote fires **0.69% of NY / 2.66% at ExecRes 3** — quote 2.66% for the queued sessions, not the NY figure. A closed-bar-derived multiplier would describe the D3 closed-bar arm, not the live engine | Needs the forming-bar question answered, not a multiplier |
| v36 Phase-2 carry-forward **(a)** (§12) — 3-min scaling of `TTM.flat_threshold` | ⚠ **SUPERSEDED — RE-DERIVED then PARKED 08-02.** The **unit** is wrong, not the value: `delta` tracks volatility, not price (per-month CV **26% in USD, 28% price-normalised, 6.5–11.7% in ATR multiples**), so an absolute USD constant cannot survive a regime change and **in ATR units the per-resolution ladder dissolves** (one k, both resolutions, within ~2 pp). The fix is ATR-relative k≈0.25–0.30 and it is a **CODE change with its own spec and ⚠ boundary**, not the settings tick it was carried as. **Both prior numbers retired** — the 25.0/40.0 ladder and the 1.45 ratio (measured **1.774**) | Un-parks on trades-covered replay |
| v36 Phase-2 carry-forward **(b)** (§12) — 3-min scaling of the CVD/RSI `divergence_price_gate`s + the LONDON session-scoped DynamicNorms baseline check | **STILL OPEN, and the expectation is inverted.** Audit F15 (2026-07-03): RSI-div is **NOT** over-eager on 3-min (9.8% vs 10.2%) and CVD-div **UNDER-fires 8×** (1.3% vs 10.3%) — **re-measure the mechanism, do not blind-scale ×2.1.** *(Split out explicitly 2026-08-07: this half was travelling in the same row as (a) and would have been read as parked with it.)* | **Opus + coordinator**, measure-not-proxy, v40/v41 method; rides the next audit re-run's data pass |
| **Geometry-modes study re-read** (v56 instrument) | First run 07-21: nearest-target tops the 36-cell table but **DIVERGENT** | Book ~doubles, or the W6-1 audit — DIVERGENT flags must clear |
| **Funding calm-week re-read** | Waiting. Fully closes the v52+v53 window | A calm funding week |

### W2 — Indicator queue (O1) — **BUILD-COMPLETE**

| Item | State | Sequence |
|---|---|---|
| #5 Aggressor velocity ⚠ | ✅ **COMPLETE, ALL THREE SESSIONS.** NY wire-in SHIPPED v52 07-14 (LIVE 07-15) — gate NOT-redundant 07-13 (ρ(Net,TFI)=0.61 pooled, 0.53–0.68 across all 5 NY session-days against a 0.7 line; overlap 86%), §5.2 NY=4.5, S1–S5 ticked, HC22 rider · LONDON armed v60 07-23, watch **PASS** 07-27 · **ASIA armed v65 08-02** — the res-3 §5.2 re-baseline that was the last open arm, gate met at 323 fires vs a ~150 bar. ⚠ **Mechanism worth carrying: sessions AUTO-ARM when their threshold lands** (`HasExplicitAggrVelBurstThreshold`, D1 of the wire-in spec-back), so adding a session key is a live scoring change, and the POCO default in `EngineSettings.vb` must move in lockstep or the harness pins behaviour the app no longer has | Done; ASIA watch live |
| #6 Book absorption ⚠ | ✅ **BUILD SHIPPED v54 2026-07-17 (`ae6678c`, coordinator-APPROVED, LIVE)** — `scoring_enabled:false`, display/CSV only, rotation-free (reserved columns populated); spec-back `book-absorption-spec-back.md` (15 deviations accepted incl. the break-laundering guard, fill masking, opening-interval skip); HC23. **THE INDICATOR SET IS BUILD-COMPLETE** (W6 header ruling realized). ⚠ **Activation is the open half, and the original gate is unreachable:** the v61 rescale did not lift engagement, and the 07-30 re-derivation proved **anchor loosening cannot reach the band** — NY episode numerics are barren (max ratio 0.51, max aggr $35k, **ZERO NY passes at ANY grid cell down to ratio 0.05**), so the `<1%` branch traces past the anchors to the **observation mechanism** (`window_sec=10` rolling reset + D8 pullFrac inflation on sparse postLB). **Coordinator ratifies Path B: hold v61 anchors, spec a mechanism revision** (display-only, no ⚠). ✅ **TRADER TICKED PATH B, 2026-08-12.** ⚠ **Path A's V-table (0.5/5000/1.0) is REJECTED — do not re-propose it**; it projects NY 0.000 % and ~10–15 weekday-weeks to the gate | ✅ **Path ticked. Anchors HOLD at v61 — no settings change, no version bump, no ⚠ boundary.** The next artifact is a **mechanism-revision proposal** (`window_sec` · episode-cumulative pressing · D8 `pullFrac` on sparse `postLB`), now a build slot in [`trader-tick-queue.md`](trader-tick-queue.md) §2. Activation slips past mid-Aug regardless; pullFrac distribution still doubles as the W4 incremental-book trigger measurement |
| A4 Liquidation × OFI flip ⚠ | Spec: backlog Section A; high payoff; not in the original §11 list. **Gated on liq-feed live validation** (audit F9 2026-07-03: zero liq-marked trades in 8,025 runs, plumbing verified — #7 must carry a first-liq-seen diagnostic and see one real cascade first). ✅ **Instrument LIVE since v59**, so A4 is now **market-gated only** — ≥1 CASCADE line in `liq_events.log`, durable across restarts. Zero seen on either box as of 07-31. ⚠ **Protect the instrument:** `alerts.` is REJECTED from the overlay whitelist, because `alerts.enabled:false` on a box means the sidecar is never written there and the gate pools both boxes | After the market produces one |
| A5 VPFR shape (30-day data gate) | **28 of 30 distinct calendar dates as of 2026-08-07** (2026-07-03 → 2026-08-06), up from 15 on 07-22 — **~2 dates out**. ⚠ Meeting it does **not** authorize the build: A5 must still clear the **W6 new-indicator bar**, since it is a VPFR refinement rather than an orthogonal class | Spec-first with that argument, never automatic |
| Deferred: D3/D4/D5/D6, B1 per-indicator weights (overfit-gated), C1/C2 multi-session VPFR/anchored VWAP (state-plumbing-gated) | Unchanged — see W6 for B1's now-defined trigger, and W4 for the state-plumbing dependency | Per their backlog triggers |

**Display/ops tier — the §11 catalogue** (from `websocket-migration-proposal.md` §11, the 2026-06-12 post-WS list; items 1/2/3/4/10 shipped v39–v46, 5/6 were the ⚠ rows above). All **Opus-tier, display-only, no re-baseline** — gap-fillers on trader demand. ✅ **CATALOGUE COMPLETE with v59.**

- ✅ **#7 Liquidation cascade alarm** — banner/sound when liq-flagged trades stream in a cascade (the scoring penalty already exists; this is timeliness). **SHIPPED v59** — rose cascade accent; reuses the exit-guard strip pattern.
- ✅ **#8 Level-approach alerts** — notify within N ticks of the active swing/HVN/POC level. **SHIPPED v59** in the same spec as #7 — 6-level approach set incl. 15m swings.
- **#11 Early-resolution-on-confirmed-hit** — live perf-strip posts a 3-min winner at the hit instead of T+45. Lowest value in the catalogue; **must carry the contiguous-bars guard** (§11's stated caveat) or a feed gap can resolve on an incomplete prefix. **Unscheduled.**
- **#9 Provisional forming-bar verdict** — **held, not scheduled**: tension with the conservative-bias profile and now doubly suspect with an autotrader consuming output (a PROVISIONAL score must never reach the signal file). Only on explicit trader ask, with the bridge explicitly excluded from it. See §5b.

### W3 — Signal bridge → DeribitOrderPlacementApp (O2)

**Trader rulings (2026-07-02, binding on the spec) — reproduced in full because they bind:**

- **R1 — Full replacement, not gating.** The engine's scoring **replaces `FrmIndicators`' home-grown EMA/VWAP/DMI score entirely** (those are lagging indicators and a strict subset of the engine's inputs — gating the verdict with them would be cross-process double-counting, would selectively block early entries, and would decouple the executed population from the engine's calibration book). The order app keeps **execution-policy gates only**: cooloff, max-age/health checks, tier selection. The action key is the **verdict tier/confidence, never the raw score** (raw scores sit under regime-dependent ceilings; scores travel in the payload for logging only). Optional trust-building during the supervised period: a shadow log of what FrmIndicators would have said — never a gate — decommissioned after.
- **R2 — Engine levels are authoritative; order-app ATR is a checkbox override.** The order app consumes the engine's **final effective levels** (post-Step-5b cap-arbitrated target + stop), not an ATR formula — the verdict only survived the BELOW_MIN_MOVE gate because of that effective target, and the engine's ATR is execution-resolution ATR (1m NY / 3m Asia-London, v36), which the order app's own quote-feed ATR would silently diverge from by session. Raw ATR + multipliers ride in the payload for transparency and for the override math. The override checkbox (order-app side): **default OFF**, applies to distance math only, can never resurrect a verdict the engine suppressed, and when ON both level sets are logged per trade so the divergence is measurable.

| Phase | State |
|---|---|
| **v1** | ✅ **SHIPPED, LIVE, SOAK-CLEAN.** Schema FROZEN 2026-07-03 (reconciled with the order-app field-diff; trader ticked D1–D10; `engine.instance_id` + `engine.autotrade_armed` dual-arm interlock + enum pins incl. `health.ws` REST + neutral path `C:\Dev\DeribitBridge\`). `signal_bridge.enabled` flipped on by the trader 07-16 → log-only soak; **joint soak review CALLED CLEAN 07-22**, 69/69 would-act ≡ CSV `Placed*`. **Live-at-min-size unlocked.** **Frame worth carrying:** per-run atomic-write `verdict_signal.json` emitted after every completed run — **including NO TRADE and SKIPPED** — so the consumer can distinguish "engine says stand down" from "engine dead"; consumer side is FileSystemWatcher + debounce; consumer-side hard gates are max-age staleness, schema-version match, and NO TRADE/BELOW_MIN_MOVE/skip ⇒ no action, with cooloff/sizing/execution policy order-app-owned; emission goes through a **host-agnostic core class**, which serves the CLI port unchanged. **The field list is deliberately NOT duplicated here** — canonical consumer copy is the order-app's `integration-contract-verdictengine.md`, and [`signal-bridge-v1-proposal.md`](signal-bridge-v1-proposal.md) §3 is the verbatim mirror |
| **v2** | Position-state feedback (order app → engine file so `posState` stops being a manual radio during autotrade), which makes `HoldStatus`/EXIT-guard output an actionable exit signal in the contract; plus slippage-aware signal pricing. Gate (v1 supervised ≥1–2 weeks) ✅ **met.** ✅ **Documentation phase CLOSED 07-29** — T1–T8 ticked, order-app contract §8 landed, engine mirror in `signal-bridge-v1-proposal.md` §10. **Standing rule from that exchange: the mirror carries consumption-visible consequences, never emitter mechanics** |
| **T8 amendment** | ⚠ **OPEN — and a different class from every other correction in that exchange: no text is wrong anywhere.** Their emitter is right, our tolerance is right; the defect lives only in the *composition*, which is why neither side's review could find it alone. Gated on their **acceptance 3** (owner-only: mode Live + ARM + START). Until it passes the feedback file is **partially proven** — shape, lifecycle and position semantics observed; **disposition path / `last_signal` / mode strings NOT** |
| **Consumer-side, separable** | **Sizing-by-stop-distance** (their own small spec; all inputs already in the v1 payload; slot = after live-at-min-size stabilises) — **its existence is what later unlocks the engine's structural-stop un-clamp** (placed-geometry derivation F1/§6b). `session-policy-gate-proposal.md` P1–P5 approved and handed over; **P5 tier VALUES stay F1-gated** |

### W4 — Platform & plumbing (O3 + ops)

| Item | State |
|---|---|
| CLI-port run-state extraction (host-agnostic run-context for `_fundingHistory`/`_ofiHistory`/`_oiHistory`/MTF cache/`_prevRegime` + headless runner skeleton) | ✅ Spec DONE 2026-07-06 + **D1–D7 TICKED 2026-07-07**. **DEFERRED TO LAST (trader 2026-07-08)** — runs after the W6 ceiling programme, to avoid restructuring `MainForm_Analysis.vb` mid-calibration: behaviour-neutral, but hot-path churn/regression surface on the file the calibration work keeps editing, and the port was always the post-completion project (O3). When it runs: Opus, 4 stages, gate+review each; **serialize against anything touching that file**; zero behaviour change. Pull forward only on trader reversal |
| WS-health line persistence to log (closes the "feed health is inferred" monitoring caveat) | ✅ **SHIPPED 2026-07-22** (`bbc47c7`) — `ws_health.log` transitions-only sidecar, A38a/b. Later became coverage-report evidence |
| **In-app trade-store capture** | ✅ **SHIPPED v64 2026-07-31**, capturing on AWS since 2026-08-01 17:50Z (first trade to disk anywhere). Streaming write in `DeribitWsFeed.ApplyTrades` + gap-repair backfill on start and every 6h. Removes Deribit's ~24h public-trades retention deadline for everything captured while the app is up. Host-agnostic `Core/TradeStoreWriter.vb` owns naming/rollover/format/parse. ⚠⚠ **A 2026-08-07 claim that the sole capturer was only 78.8 % complete is WITHDRAWN — see [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §4a.** The two boxes disagree on *amounts at shared timestamps*, not on which trades exist; zero timestamps were absent from AWS. ⚠ **The real defect it uncovered: the store has no trade identity.** Deribit supplies `trade_id`; `HistoricalStore.vb:307-317` never reads it, so the five-field row is what both the store dedup and S0's venue diff match on. That means two tape books **cannot be correctly merged**, genuinely distinct trades are **silently dropped at write time** when five fields collide, and a daily venue-diff job would be uninterpretable. ✅ **Trade identity is SPECCED AND BUILD-AUTHORIZED 2026-08-08** — [`trade-store-trade-identity-proposal.md`](trade-store-trade-identity-proposal.md), D1–D7 all ticked. Takes **both** `trade_id` and `trade_seq`. **`trade_seq` is the strategically interesting half:** a per-instrument monotonic sequence gives gap detection *from the store alone*, with no venue call and **no ~24 h retention limit** — so store completeness stops being a thing you must catch within a day. That retires the urgency behind the suspended daily venue-diff job, which is re-specified afterwards rather than before |
| **`settings.local.json` per-box overlay** | ✅ **SHIPPED 2026-08-01** — gitignored, per-box, whitelisted merge; absent overlay ⇒ byte-identical to the pre-overlay engine. What lets the local box run the latest build without capturing tape. ⚠ Re-audit rejected `alerts.` from the whitelist (it would kill A4's instrument) and found **`mtf_gate` — the hard veto — named nowhere** in the original enumeration |
| **C1 trade-store coverage report** | ✅ **BUILT 2026-08-05**, both sessions, reviewed and accepted. `coverage` verb in `BacktestRunner` (Part A store health, Part B venue diff) + a live TAPE STORE strip. **The precondition instrument for every data-gated item** — it decides whether future collection gaps are seen at all. Two remainders: **F1** trailing-edge attribution (build slot) and **F2** split-hour scoping (a spec question) |
| Incremental full-depth order book (public channel, change_id semantics) | **Deferred** — only if #6-v1 proves absorption on snapshots; then its own spec |
| **Authenticated API / raw interval: NOT pursued** (decision 2026-07-02) | Verified vs current Deribit docs: auth unlocks only sub-100ms `raw` feeds — below the noise floor for 2–15 min holds; the material upgrade (full-depth incremental book) is public at 100ms. Auth also means key custody on an unattended box. Revisit trigger: order-management-in-engine ever enters scope (currently excluded), or a specced consumer needs event-time sequencing that 100ms provably blurs |

### W5 — Auto-tweaker ops

First live fire stays data-gated (>40%-failure NY×1 window) and supervised (dry-run first); **window/MinTier recalibration** against the real post-v35 directional rate (~23% NY actionable — §12 MinTier-mismatch row; the interim `window_size_verdicts` 75 holds until then) rides the first-fire follow-up — **Opus + coordinator**. P13 UserManual tier documentation = doc pass (Opus). Phase-2b per-population autotune stays parked (may never be built).

⚠ **New prerequisite, 2026-08-07 — the AutoTweaker weekday filter must land BEFORE the first fire.** The weekday-scope ruling made evaluation weekday-only, but weekday filtering is enforced in code in exactly two places (`CoverageReport`, CeilingAudit's `CsvFeatureBuilder`); `AutoTweaker` is unfiltered and is **the only surface that WRITES `settings.json`**, so unfiltered it would tune the engine on sessions never traded. **Verified 2026-08-07 that it has never fired** (no tweaker state file, `settings_snapshots/` empty) — which is exactly why this is fixable now at zero cost.

### W6 — Accuracy-ceiling programme (O1 terminal phase; added 2026-07-07, trader-directed)

**The ruling this section encodes (Fable coordinator assessment, 2026-07-06, trader-accepted):** the indicator set is essentially COMPLETE once #6 ships — NY×1 runs ~55% SUCC with adverse-stop failures at 0.3% (losses are geometry/horizon, which B4b attacked), and a selective public-data system at this horizon tops out ~60%. **The remaining accuracy is NOT in new indicators; it is in calibration (LONDON), combination (B1), and loop closure (outcomes/fills).** Future orchestrators: work this table top-down, tick items as they land, and refuse new-indicator proposals that don't clear the W6-7 bar (orthogonal signal CLASS, not another angle on flow — the audit measured max pairwise flow agreement at 69%; there is no room for a sixth flow indicator that isn't marginal).

| # | Item | Class | Gate / recipe | State |
|---|---|---|---|---|
| W6-1 | **LONDON×3 calibration debt** — the single largest measured deficit (SUCC 34.5% vs NY 54.9%; ADVERSE 24.1%; structural-target reach INVERTED 33% vs 61%). **Named candidates (2026-07-16):** (1) **LONDON `stop_max` override 2.0–2.2×ATR** (F2 winners-MAE evidence: p75 1.63 — the pooled 1.6 clamp harvests LONDON winners; 76.8% adverse-first, 92% of stops at the clamp); (2) **swing-buffer offset** — stops placed a small buffer BEYOND the swing (ATR-fraction/ticks, not %) — an **un-clamp (§6b) design input**, not a near-term change. Covers: the v36 Phase-2 carry-forward, session-scoped DynamicNorms baseline check, the B4b LONDON struct-target watch (F3/DG4), the F13 adverse-cluster question | Calibration | Rides audit re-runs + the B4b §5 watch | ✅ **RULED NO CHANGE 2026-07-31.** The two candidates are **one lever** — `stop_buffer_pct` applies AFTER the clamp and scales the final distance, so with the clamp binding **95.5%** of LONDON structural-stop rows they are algebraically interchangeable; confirmed empirically by matched grid pairs agreeing to 0.002 EV. Widening loses monotonically pooled (EV/ATR −0.475 at 1.6 → −0.647 widest); LONDON-only the top 3 cells tie inside noise (n=215). ⚠ **Candidate (2) survives as an L9 design input.** **The real finding: the clamp binds 95.5–99.6%, so stops are de facto ATR stops, and the live question is L9 un-clamp, gated on L3.** **Depth caveat binding all future gates: count EVALUABLE rows, not directional rows** (556 directional → 215 evaluable) |
| W6-2 | **Outcome-loop closure (a):** auto-tweaker first live fire (= W5) + failure-matrix-driven threshold moves thereafter | Loop | Data-gated (>40%-failure NY×1 window), supervised dry-run first — **plus the weekday filter, new 2026-08-07** | OPEN (W5) |
| W6-3 | **Outcome-loop closure (b):** per-tier empirical win rates → Kelly CAL mode (the long-promised EST→CAL upgrade). **The M1–M5 placed-target matrix is the p-instrument** (`profitability-risk-levers.md` L4); consumer sizing consumes the payload's kelly fields once CAL lands | Loop | M1–M5 ✅ shipped 07-21 + eval book depth per (tier × session × resolution) | ⚠ **THE LADDER DOES NOT SEPARATE.** §9 read 08-01 (gate met, 203 pooled STRONG): MEDIUM 42.2% ≈ WEAK 42.7%, all CIs overlap, and **LONDON inverts at the top** (MEDIUM 53.0% > STRONG 44.8%). Empirical per-tier rates would size all tiers alike with the ordering partly inverted — worse than EST, because it carries false authority. ✅ **DECIDED 08-02 (trader): option (c) — wait for separation on the measurement, render the assumption honestly now.** Display half shipped and confirmed. **CAL is PARKED, not pending**; `ScoringEngine_Kelly.vb:23` still reads "EST mode only" and under this decision that is the intended state, not a gap |
| W6-4 | **Offline ceiling audit:** one-off regularized-model audit (e.g. logistic on the CSV feature book vs outcomes) — an INSTRUMENT, never a live scorer (interpretability principle intact). Quantifies how much the additive +1/−1 pipeline leaves unharvested. Model ≈ pipeline ⇒ declare the ceiling reached and STOP spending on combination; model ≫ pipeline ⇒ that delta is the B1 prize, measured | Measurement | ≥3–4 weeks of v0.8 rows; deliverable = a report doc. Method spec ✅ APPROVED 07-23 (K1–K6) | ✅ **RUN 2026-08-01 → INCONCLUSIVE.** NY×1 ΔAUC **−0.0291**, CI [−0.197, +0.124] straddling the ±0.030 margin. **The queue does NOT unlock**; instruction is re-run at the next book doubling, **no spend meanwhile**. Also produced the first outcome-linked read on ASIA aggressor velocity — `AggrVelBurstRatio` AUC **0.5179** (n=217), `AggrVelNet` 0.4654 — essentially no demonstrated edge, and D3's D-table says so rather than leading with the distributional case |
| W6-5 | **B1 regime-conditional per-indicator weights** — same indicators, better combination; the audit's conditional-outcome tables are the first evidence base | Combination | Gated on W6-4's **measured** prize + a materially bigger evaluated book (overfit risk is the binding constraint) | **PARKED by W6-4's result** (was OPEN; trigger defined here rather than in §5b) |
| W6-6 | **Bridge v2 realized-outcome calibration** — fills/slippage/actual exits replace mid-price touch labels (= W3 v2 position-state feedback + the disposition log joins) | Loop | v1 supervised 1–2 weeks ✅ met; now gated on the feedback file being fully proven (T8) | OPEN (W3) |
| W6-7 | **Cross-venue lead-lag + external context** — the ONE remaining non-marginal indicator class (where is price discovery happening). Spec candidate AUTHORED: `cross-venue-lead-lag-proposal.md` (Tier A = free real-time Coinbase/Binance spot WS; Tier B = free slow-cadence CME-gap/ETF-flow context; Tier C = paid CME/ETF real-time, deferred) | New signal class | After the current queue AND W6-4; #5-style correlation gate + #6-style outcome gradient bind its activation | SPEC CANDIDATE. **Tier-C spend parked by W6-4's result** |
| W6-8 | Already-scheduled indicator completions: #6 absorption (W2), A4 liq×OFI + the #7 liq-feed validation (W2), full-depth incremental book (W4, gated behind #6-v1) | — | Per their existing entries — cross-referenced here so the programme view is complete | OPEN (W2/W4). #7 liq-feed validation ✅ done via v59 |

---

## 4. What runs next

*(This replaces the spent "Fable window Jul 2–7" schedule and the fully-consumed "month execution order" — both verbatim in [`history-archive.md`](history-archive.md) §H.)*

**Open trader decisions — ✅ NONE.** As of **2026-08-12** every row in [`trader-tick-queue.md`](trader-tick-queue.md) §0a is ruled.

**Closed since this line last read "four":**

| Decision | Outcome | What it became |
|---|---|---|
| **D2** OBV `trend_gate` | ✅ **SHIPPED as settings v66**, 2026-08-11 (18 → 23) | Done; ⚠ dataset boundary |
| **E5** absorption Path B | ✅ **TICKED 2026-08-12 — Path B.** Anchors hold at v61, no settings change, no ⚠ | A **mechanism-revision proposal**, queue §2 |
| **F3 watch** | ✅ **RETIRED 2026-08-12**, explicitly. ⛔ No tooling to be built | Nothing — the tooling row is cancelled. ⚠ The *topic* stays open |
| **C1-coverage F2** split-hour | ✅ **RULED 2026-08-12 — split the hour at the marker**, defective if either part is | A **build slot**, queue §2 |

⚠ **An empty decision table is not an empty board.** Three of these four turned into scheduled work. The queue's §2 is where the board now lives.

**Ready to build, no decision needed** — queue §2: the three weekday filters (**AutoTweaker first**) · the atomic-write total-primitive swap · **C1-coverage F1** · G12 · the CeilingAudit version constant.

> ⚠ **CORRECTED 2026-08-12 — this line used to read "C1-coverage F1 · F2 · F3", and both extra IDs were wrong.** The C1-coverage report has **only F1 and F2**; there is no C1-coverage F3. **F2 is a DECISION, not a build slot** (it is in the open-decisions line above), and the **F3** meant here is the **B4b observational watch**, which is also a decision. ⚠ **`F3` names two unrelated things in this project's docs** — the B4b watch, and a finding ID reused in other spec-backs. Always name the document.

**The one ops action that unblocks the most** — an **AWS copy-back** (CSV *and* store). It is the sole blocker on the Kelly trigger, the W6-4 re-run, the first coverage report against the real AWS store, and resolving the `bin\Release` tape.

**Sequenced after that:** D1+D2 bundled at one ⚠ boundary once the D3 watch reads · absorption mechanism spec → re-collection → activation gates · A4 whenever the market delivers a cascade · A5 at 30 dates *if* it clears the W6 bar · then the W6 tail (W6-5/W6-7) **only if** a re-run of W6-4 shows a prize · **CLI port last**.

---

## 5. Sequencing rules (binding)

1. **One scoring change per dataset boundary.** A ⚠ item never builds while another ⚠ item's collection window is open (bundling several at one boundary is allowed with trader sign-off — v52+v53 is the precedent). ⚠ **Current open window: the v65/D3 ASIA arming watch**, live on both boxes since 2026-08-01 19:02:31Z. *(This line read "the v52+v53 bundle" until 2026-08-07, naming a window spent since 07-17 — a binding rule pointing at the wrong boundary reads as "the slot is free," and D2 is exactly the change that would have taken it.)*
2. **Read a watch over a band, never one session-day.** The D3 ASIA watch triggers on ≈9.7% fire rate and ≥85% same-side, but per-day fire spans **4.5–13.8%** on ~106 rows/day. **NY's ±2pp band does not transfer to res-3**, and no res-3 threshold may be re-fitted off a single session-day.
3. **Spec-first for scoring/novel behaviour; trader signs off; local-first; trader tests + pushes.** Unchanged. Every spec or implementer brief carries a **model + effort recommendation** with the reason, the specific traps, and the escalation trigger (CLAUDE.md).
4. **Display parity has three surfaces:** plaintext snapshot ↔ cards ↔ `verdict_signal.json`. Any commit changing a value on one updates all three or states why not.
5. **Autotrade safety is consumer-gated, engine-informed:** the engine never suppresses information for the bridge (it emits health flags and stands-down states); the order app owns the decision to act. Engine-side trade execution stays out of scope.
6. **A spec's status header is not evidence of code state.** Verify in the tree before offering any spec as available work: `git log --oneline -S'<symbol>' -- <file>`. A 2026-08-01 sweep found 4 of 13 queue rows describing shipped work as outstanding.
7. The rejected-approaches list (trader-profile §4 / backlog Section E / §5b) binds this roadmap; nothing here reintroduces a rejected pattern.

## 5b. Explicitly not scheduled (decisions of record — do not re-propose without new evidence)

| Item | Why | Revisit trigger |
|---|---|---|
| Sub-minute full-run cadence (old §8 baseline) | Conflicts with on-close bar discipline (v44); worst calibration cadence (autocorrelated rows) | Only if a specced consumer needs intra-bar full verdicts |
| §11 #9 provisional forming-bar verdict | Conservative-bias tension; autotrade contamination risk | Explicit trader ask, bridge-excluded |
| Authenticated / `raw` Deribit feeds | W4 ruling 2026-07-02 | Order-management-in-engine, or a proven 100ms-blur case |
| Tweaker Phase-2b per-population autotune | Manual (B)-style re-baselines are cheap enough so far | Manual cadence becomes a real burden (spec §1 gates) |
| A1 spread-momentum | **Refuted with evidence 2026-07-03** — one-tick book; >5bps on 0.1% of WS runs | New evidence only |
| Bundle 5 (C1/C2), Bundle 6 (D5/D6), D3/D4, A5 | Backlog-gated per their entries; Bundle 4 dissolved into W1 | Their documented triggers |
| ~~B1 per-indicator weights~~ | **Moved to W6-5 (2026-07-07)** — its trigger is now defined (post-W6-4 ceiling audit + book size), and **W6-4 ran inconclusive, so it is parked there** | A W6-4 re-run showing a prize |
| ⚠ **Migrating CSV storage to a database (PostgreSQL)** — **PARKED 2026-08-11** | **Analysed, not recommended as a standalone project.** The decisive fact: it fixes almost nothing currently on the board. The `TradeStoreWriter.vb:149` drop happens in `Buffer()` *before* anything reaches storage, so a DB never sees it — and the same is true of the POCO/method-default drifts, gap repair's cursor, and the coverage-scoping gaps. All application logic. Its one genuinely on-point win is that a `UNIQUE(trade_id)` **cannot be bypassed** whereas a code contract can, which is exactly how the write guard drifted from `DedupTrades`. But the merge/dedup class it would have prevented was **already closed by the trade-identity build**. ⚠ **Two direct conflicts with live decisions:** it works against the **AWS cost review** (Postgres on a box measured at ~1474 MB Windows / 86 MB app could make the instance downgrade impossible; RDS adds a line item), and it collides with the **Linux CLI port (O3)**, which will rewrite the host layer anyway — doing storage separately means touching every reader and writer twice. Efficiency is a wash on storage (~900 MB/yr either way, more with indexes) and irrelevant on write throughput (~1.4 trades/sec). ⚠ **The harness is the real casualty:** `verify/ordercheck` runs today with zero external dependencies, and a DB dependency needs a test database or an abstraction layer | **Two triggers, either one.** (1) **Fold it into the Linux port**, where the host layer is being rewritten regardless — that is the only sensible home for it. (2) Ad-hoc query pain becomes the binding constraint. ⚠ **For (2) the cheaper answer is recorded so it is not skipped: DuckDB over the existing files.** Embedded, no server, no cost, no footprint on the collector, runs on Linux, reads CSV/Parquet **directly** — it buys the one real win (SQL for investigation) at **zero migration cost** and keeps CSV's load-bearing properties: append-only, never-throws, no dependencies, greppable. **Try DuckDB before proposing a migration** |
