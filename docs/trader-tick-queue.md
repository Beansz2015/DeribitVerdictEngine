# Trader Tick Queue

**Created:** 2026-08-01 (trader-directed). **Purpose:** the one place that answers *"what is waiting on me, in what order, and what does each one block?"* — the **scoped immediate slice** of [`roadmap.md`](roadmap.md). Derived from the specs and the board; **not a new authority.**

**Authorities:** [`roadmap.md`](roadmap.md) = the strategic board and execution order · [`backlog-dependency-map.md`](backlog-dependency-map.md) = **what blocks what** (edges only — as of 2026-08-07 it carries **no state at all**, by design) · [`profitability-risk-levers.md`](profitability-risk-levers.md) = profitability lens · each spec's own D-table = the decision text. **This doc carries ORDER and GATES; where it disagrees with a spec, the spec wins.**

**Maintenance:** update State on every tick. Move closed rows to §5 rather than deleting. **Re-run the §1b shipped-state sweep whenever this queue is rebuilt — do not inherit a prior assembly.**

**Trimmed 2026-09-14 (UTC).** Superseded and closed text in this file moved verbatim to [`trader-tick-queue-archive.md`](trader-tick-queue-archive.md) §C. Every moved block leaves a pointer carrying its `trim-2026-09-14-NN` id. Ledger: [`doc-trim-log.md`](doc-trim-log.md). Pre-trim original: git tag `doc-trim-2026-09-14-pre`; byte copy under `docs/archive/doc-trim-2026-09-14/originals/`.

**Second trim pass 2026-09-14 (UTC):** closed and stale rows in this file's §2 and §4 moved verbatim to [`trader-tick-queue-archive.md`](trader-tick-queue-archive.md) §D, ids `trim-2026-09-14b-NN`, same ledger. Pre-pass original: git tag `doc-trim-2026-09-14b-pre`.

---

> ## ⛔ STATE BANNER — REFRESHED 2026-09-14 (UTC). Read this before believing any row below.
>
> ⭐⭐ **CURRENT STATE READ: [`seat-handover-2026-09-14b.md`](seat-handover-2026-09-14b.md)** (supersedes [`seat-handover-2026-09-14.md`](seat-handover-2026-09-14.md) for state). Settings v68 (tracked repo-root `settings.json` line 2). ⛔ **Run `git status -sb` - never inherit a push state.** **First, per the trader: the overdue ASIA burst-watch read, and a plan to schedule the `--verify-venue` trade check (handover §1). Then S1 of [`absorption-d2-stage1-rotation-build-spec.md`](absorption-d2-stage1-rotation-build-spec.md) from 2026-09-15 UTC. Nothing is owed by the trader.**
>
> **Standing facts carried from the superseded banners.** Each one still binds. The banners themselves moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-35`](trader-tick-queue-archive.md#trim-2026-09-14-35).
>
> - ⛔ **Run `date -u` before any dated claim.** The workstation is GMT+8; every project date is UTC.
> - ⛔ **`_evalCache` is unbounded, and trimming the in-memory list truncates `analysis_eval_cache.csv` on the next write** (`WriteEvalCache` rewrites the whole file). Decoupling the list from the file is the work; no spec exists. See its row in `trader-tick-queue.md` §2.
> - **The book's `Timestamp` column is UTC** (`AnalysisLogger.vb` writes `DateTime.UtcNow`). The local-time defect claim was refuted 2026-08-25.
> - **Quote the `pagesOUT/s` memory baseline split by time of day** (NY 13:00-23:00 UTC 1.83 %, non-NY 14.29 %). Never pool it and never quote a mean.
> - **Relaunching the retired collector:** `i-08c740e22d507667d` is TERMINATED. It lives as `ami-0247c8b7275de49ac`, backed by `snap-0b5c126dd898c6bd0`; deleting that snapshot silently breaks the AMI. Read [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) §8 first: without the `EC2-SSM-Access` instance profile a relaunched box has no SSM.
> - **A deploy acceptance that counts ONE new row is superseded.** v68's single-shot defect passed it. The gate needs two rows spanning more than one interval.
> - **Any pooled book count must include the rotated `.bak` files** as well as the live `analysis_log.csv`.

---

## 0. Orientation — what each doc is authoritative FOR

**The failure this table fixes is not that docs go wrong — it is that "authority" was never scoped.** Nothing below is demoted; each is pinned to what it actually governs.

| Doc | Authoritative for | **Not** for |
|---|---|---|
| *Ten work-item rows that were misfiled in this table* | **Moved 2026-09-14: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-36`](trader-tick-queue-archive.md#trim-2026-09-14-36).** **Open, now rows in `trader-tick-queue.md` §2:** the `_evalCache` spec (two duplicate rows merged), the `roadmap.md` footprint figures, the `DeribitWsFeed` subscribe reply. **Closed, verified in the tree 2026-09-14:** `collector.ps1` FIX 7 (snapshot before manifest, line 286), FIX 8 (line 571), FIX 9 (rollback runs the row gate, line 658); `tools/mem-probe.csv` is now tracked. **Closed reads:** t2.micro viability, the 24-hour test, the 18-hour plateau series, the 2026-08-22 START HERE row | Current work - read `trader-tick-queue.md` §2 |
| [`trader-tick-queue.md`](trader-tick-queue.md) *(this doc)* | **What is outstanding, in what order, and its gates.** §0a answers "what is owed" at a glance; §1b is the dated shipped-state sweep | Dependencies · decision text · strategic altitude |
| [`trader-tick-queue-archive.md`](trader-tick-queue-archive.md) | ⛔ **History only, never current state.** The 39 finished §2 rows and all of former §5, moved byte-identical 2026-09-10 (UTC) so closed items are not re-investigated | **Available work — nothing in it is a build slot.** Current state — this doc's §2/§5 are current; the archive is not |
| [`roadmap.md`](roadmap.md) | **The strategic board** — every workstream, done and outstanding, at altitude; execution order; the **binding sequencing rules** (§5) | The immediate slice — it points here |
| [`backlog-dependency-map.md`](backlog-dependency-map.md) | **What blocks what** — edges, and a pointer to where each item's state lives | **State. It carries none, deliberately** (restructured 2026-08-07 after 7 stale cells) |
| [`seat-handover-2026-09-14b.md`](seat-handover-2026-09-14b.md) | ⭐ **THE current seat handover - the STATE read.** Its §1 is the FIRST TASKS table | A task list beyond its §1; it points here |
| Superseded handovers, 2026-07-18 to 2026-09-14 (the first 2026-09-14 handover's §1 build plan, §3 absorption facts, §4 instruments and §6 lessons still bind) | **Their rulings and conventions still bind; none is current state.** Detail still worth opening: [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) §6 (`_evalCache`), §7 (local-time taxonomy), §8 (AMI relaunch parameters); [`seat-handover-2026-08-24.md`](seat-handover-2026-08-24.md) §1.1a (memory baseline); [`seat-handover-2026-07-18.md`](seat-handover-2026-07-18.md) §3 (standing rules); [`fable-seat-close-handover-2026-08-01.md`](fable-seat-close-handover-2026-08-01.md) (the 2026-07-31 rulings). The per-handover rows moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-37`](trader-tick-queue-archive.md#trim-2026-09-14-37) | Current state |
| each spec's own D-table | **The decision text** — where this queue disagrees, the spec wins | — |

**Superseded for state, still current for rules:** `seat-handover-2026-07-18.md` (§3 binds), `fable-continuation-2026-07-23.md`. Neither should be cited for what is outstanding today.

---

> **Book rotation rule (from the 2026-09-03 box).** After a header rotation, a book count lives in TWO files: the live `analysis_log.csv` and the rotated `.bak`. They do not overlap. Count both. The `.bak` naming rider is in [`csv-rotation-riders.md`](csv-rotation-riders.md). The full box moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-38`](trader-tick-queue-archive.md#trim-2026-09-14-38).

> **Queue items 17 and 18 were ruled and built 2026-09-03:** 17 = MECHANISM, option (c); 18 = PER-SPAN. Packet: [`queue-17-18-batch-spec-back.md`](queue-17-18-batch-spec-back.md). The full box moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-39`](trader-tick-queue-archive.md#trim-2026-09-14-39).

## 0a. Decisions genuinely unanswered — the short list

**§1 records history with strikethrough, so "what is actually owed" is not answerable at a glance. This is.** ✅ **Re-verified against the tree 2026-08-07** — re-verify rather than inherit.

> **State of the decisions formerly boxed here: all RULED, nothing is owed.** The banners moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-40`](trader-tick-queue-archive.md#trim-2026-09-14-40).
>
> - **Absorption `D-6d`** (the episode-continuity counting gap, [`d6d-episode-continuity-spec.md`](d6d-episode-continuity-spec.md)): `D-6d.3` = (c), so `D-2` (episode-cumulative pressing) and Stage 1 ship together and Stage 2 follows the read; `D-6d.1` = (c), the sidecar plus the `AbsorptionShadowAggrUsd` column. ⛔ Stage 2 before `D-2` makes `absorbRatio` fall. `RD-1` (how a row records a settings load failure) = (b), a separate `SettingsLoadError` column, ruled 2026-09-13. Build spec: [`absorption-d2-stage1-rotation-build-spec.md`](absorption-d2-stage1-rotation-build-spec.md). `D-6c` stays open, gated on data.
> - **`WD-SEMANTICS`** (the unparsed-row counter) was ruled (c) and BUILT (`ab5600f`). ⛔ Option (a), counting an unparsed row as weekend, is rejected outright.
> - **`S2-2`** (the `CalcSpread` split) D-table was ticked and BUILT 2026-09-06.
> - ⚠ **`F3` names two watches.** The B4b LONDON structural-TARGET watch was RETIRED 2026-08-12. The 2026-07-02 audit-fixes EXIT GUARD corroboration watch was NOT retired, and its state is unverified since v47. [`roadmap.md`](roadmap.md)'s F3 row conflates them. A third `F3` is an ordinary finding ID in several spec-backs, so always name the document.
> - **A ruled row is a scheduled build, not a closed item.**

| # | Owed by | Note |
|---|---|---|
| ~~**E5** absorption **Path B**~~ | ✅ **TICKED 2026-08-12 - PATH B** | Anchors HOLD at their v61 values; `scoring_enabled` stays false. ⛔ **Path A is DEAD - do not re-propose its V-table** (0.5 / 5 000 / 1.0). What it bought is the absorption mechanism-revision proposal (`trader-tick-queue.md` §2). Full row: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-41`](trader-tick-queue-archive.md#trim-2026-09-14-41) |
| ~~**F3 watch**~~ (the B4b LONDON structural-target trigger) | ✅ **RETIRED 2026-08-12 (trader)** | Retired as a trigger, not as a topic: whether a LONDON structural target actually gets reached is still unanswered. It returns only as a scoped instrument request with its own spec. No tooling is to be built for the watch. Full row: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-41`](trader-tick-queue-archive.md#trim-2026-09-14-41) |
| ~~**C1-coverage F2** - split-hour rule~~ | ✅ **RULED 2026-08-12 - SPLIT THE HOUR** (shipped as `SH-1`) | Rejected, do not re-propose: (a) scope by the later marker (a false defect on every deploy) and (c) exclude split hours from the denominator (a silent hole). Full row: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-41`](trader-tick-queue-archive.md#trim-2026-09-14-41) |
| ~~**A54a scope - guard or delete the third copy?**~~ | ✅ **RULED 2026-08-11 - (d) reflection guard + scoped (b)** | Built as the A54a arc, closed 2026-09-05. Option (a), a hand-kept parameter-to-key table, is dead: it is a fourth copy that can drift. Full row and its superseded twin: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-41`](trader-tick-queue-archive.md#trim-2026-09-14-41) |
| ~~**Seeded session buckets**~~ | ✅ **RULED 2026-08-11 - DO NOT empty the seed; guard it via (d)** | The code-defaults path is real: a startup `settings.json` parse failure reaches it. ⛔ Option (e), a fatal settings-load failure, is REJECTED for a 24/7 collector. Residual, silent: a `settings.json` that parses but lacks `session_volume` applies the seed with no banner. Full row and its superseded twin: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-41`](trader-tick-queue-archive.md#trim-2026-09-14-41) |

**Explicitly NOT owed — do not re-raise:**

- ✅ **D2 (OBV `trend_gate` 18 to 23) SHIPPED as v66 and DEPLOYED to both boxes 2026-08-10 18:35 UTC.** A live scoring change and dataset boundary. OBV needs NO per-session and NO per-resolution split.
- ✅ **The D3 ASIA burst watch PASSED (2026-08-11).** Ruled tolerance: reference 11.0 %, band 8-14 %, read at least 10 weekday session-days, same-side at least 85 %. Do not quote the derivation's 9.7 %.
- ✅ **C1-coverage is BUILT.** Its F1 shipped (`4032f9c`) and its F2 shipped as `SH-1`.
- **D1 TTM is RE-DERIVED and PARKED.** `flat_threshold` stays 0.5 and the FLAT band stays inert, deliberately. The fix is ATR-relative (k about 0.25-0.30), a code change with its own spec. Do not inherit the 25.0/40.0 ladder or the 1.45 ratio (measured 1.774).
- **D3 (the v65 ASIA arming) is SHIPPED and LIVE.** v65 spans SIX instance ids; split a pooled ASIA read on the deploy ledger, or check it with the classifier edge (armed ASIA rows fire only at `AggrVelBurstRatio` of at least 5.5).
- ✅ **D1-a is REVERSED (2026-08-08): local capture stays OFF.** The quarantined local tape is unmergeable forever. Re-enabling needs a ruling on row D7 of [`trade-store-trade-identity-proposal.md`](trade-store-trade-identity-proposal.md).
- **E1 Kelly is DECIDED** (option (c)); CAL is parked, not pending.
- **E6** fee knob confirmed; **E2** W6-4 run INCONCLUSIVE; **E3** and **E4** built; **A1**, **A2**, **A3** closed; **B1** shipped 2026-07-21.
- **E7** (A4) is market-gated and **E8** (L9) is order-app-gated; neither is a decision. ⚠ Do NOT weekday-filter the A4 gate: a cascade on any day counts.
- The full text of these bullets moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-42`](trader-tick-queue-archive.md#trim-2026-09-14-42).

> **Why this section exists, recorded because it was the third instance.** The E1 row said *"needs an explicit trader decision"* for a decision the trader had already made — written into its own doc and never carried back to the row. §0 designates **this queue** as the state read, so a stale row here is worse than a stale spec header: it is wrong in the doc everything else points at. **The 2026-08-07 sweep found the same shape again** — see §1b.

---

## 1. The queue

### Cluster A — the v64 landing. ✅ **COMPLETE 2026-08-02.**

A1 answered (option **c** — the local box avoids capture through the overlay, not a hand-edit, so there is no standing chore and no silent-restore failure mode) · A2 overlay **built** `291457c`, F1/F2/D-C fixed `1611011`, re-reviewed and accepted · A3 **both boxes live**, AWS capturing from 2026-08-01 17:50 UTC, local capture correctly OFF, Kelly `[EST]` trader-confirmed.

**The reusable part, kept.** `trade_store.enabled` ships `true` — right for AWS, wrong for the local box under D1's AWS-only ruling — and `settings.json` is `PreserveNewest`, so **the build that tested v64 would have been the build that started local capture.** The overlay is gitignored and lives in `bin\`, so it must be placed **before** the first build carrying a newer tracked `settings.json`. Once it is, `PreserveNewest` can refresh the base and the merge still resolves capture to `false`.

**On D7, kept because it is not a permanent close.** Answered 2026-07-31, option (a): a REST-mode box is expressed by a tracked `settings.json` change with a version bump, because a REST box is break-glass rather than routine and a break-glass change *should* be deliberate and version-visible. Option (c) — excluding a REST box's rows from the pooled book — unlocks once the J-E effective-source stamp gives rows a source column.

### Cluster B — ✅ **CLOSED: it was never work. It shipped 2026-07-21.**

**B1 (eval `NO_DATA` / F4) SHIPPED `75a2694`** — N1–N5 built, R1 and R2/N4 with it. **Verified live:** `# schema=v6` in the cache, `analysis_eval_cache.csv.v5.bak` present, **120 `NO_DATA` rows**. **The EV-on-the-strip precondition is therefore already satisfied** — that question is unblocked whenever it is wanted.

### 1a. The convention this produced — verify shipped state before offering work

**This item was carried as outstanding, ticked by the trader, and an implementer conversation was recommended for it. All three were wrong, and the error survived a correction pass.** The proposal's header read *"PROPOSED — D-table awaits trader"* while §3 recorded the tick; the header was corrected **from the D-table alone** and the tree was never checked. That fixed the wrong half.

**Second occurrence of the exact shape** — [`fable-handover-2026-07-31.md`](fable-handover-2026-07-31.md) §6.2. Both times the item was an eval-surface fix whose spec header had gone stale.

> **A spec's status header is not evidence of code state. Before offering any spec as available work, verify it in the tree.**
> One command: `git log --oneline -S'<distinctive-symbol>' -- <file>`.

The cost of skipping it is asymmetric: a wasted implementer conversation, and a queue that misrepresents what is outstanding to the person deciding what to spend on.

### 1b. Shipped-state sweeps

Sweeps recorded: 2026-08-01 found 4 of 13 rows stale; 2026-08-07 found 4 findings; the 2026-09-07 sweep (`trader-tick-queue.md` §2) found 11. The root cause every time: rows written from status prose, not checked against the tree. The sweep records moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-43`](trader-tick-queue-archive.md#trim-2026-09-14-43).

### Cluster D — the ⚠ boundary contention

All three rows are closed: D1 TTM parked (`trader-tick-queue.md` §0a), D2 OBV shipped as v66, D3 ASIA arming shipped as v65. The table and the 2026-08-01 two-boundary sequencing ruling (with its AUC 0.5179 caveat) moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-44`](trader-tick-queue-archive.md#trim-2026-09-14-44).

### Cluster E — data-gated, other repos, or genuinely later

| # | Item | Gate |
|---|---|---|
| **E1** | F1 §9 pooled read, then Kelly CAL, then P5 | The ladder does not separate. DECIDED 2026-08-02: option (c); CAL parked; P5 values gated (LONDON inverts at the top). The 2026-09-09 read still does not separate |
| ~~**E2-E6**~~ | W6-4 run · D2-v2 what-if mode · backtest synthesizer · absorption Path B · fee knob | ✅ All closed: E2 INCONCLUSIVE (no spend until the next book doubling) · E3 and E4 BUILT · E5 TICKED Path B · E6 CONFIRMED (composes 0.0008). Full rows: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-45`](trader-tick-queue-archive.md#trim-2026-09-14-45) |
| **E7** | A4 liquidation × OFI flip | Market-gated only (≥1 CASCADE line). **Protect the instrument — `alerts.` stays REJECTED from the overlay whitelist** |
| **E8** | L9 structural-stop un-clamp | Gated on **L3** (order app). W6-1 ruled no-change and handed the real question here |

---

## 2. Not ticks — these need a build slot, not a decision

Ordered by what each one prevents. **Re-swept against the tree 2026-09-14:** closed rows are collapsed into the index row at the top of the table. Original line: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-12`](trader-tick-queue-archive.md#trim-2026-09-14b-12)

> **Shipped-state sweep 2026-09-07 (UTC): eleven rows in this section described finished work; all were closed against the tree.** Standing lesson: verify a row in the tree before offering it as available work. The weekday-filter row was settled the same day, and all three surfaces have since shipped. The sweep banner moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-46`](trader-tick-queue-archive.md#trim-2026-09-14-46).

| Item | Size | Where |
|---|---|---|
| **Closed rows - collapsed 2026-09-14** | see the list | WD-TIDY `5996f01` · D3-RESIDUAL `4ab0b25` · R-2 residual `1ad7d6d` · OPS-1 `124e154` · I17-A6 `98ed4fd` · I17-SWEEP `1b2adbe` · S2-2 and S2-1 `57b55f9` · F-1 `a86a0cf` · `trade_seq` skip claim (ruling only) · v67 thin-trade skip gate `613cf1e` · `ForwardWindowJoiner` header guard `5b8515e` · streaming capture drop `1cec1ea` · AWS copy-back (2026-08-14) · `trade_id` + `trade_seq` `64d41e7` · gap repair Part A `c6c6942` · DR-1 and DR-2 `91942d6` · DR-3 `5346bc0` · WD-SEMANTICS `ab5600f` · S-4 `1aeae5a` · pooled minute-key dedup (ops) · `BuildResolutionCfg` literal (2026-09-03) · seam-audit doc corrections (2026-08-25) · weekday filters (`A67a`/`A67b`) · atomic writes `5b0f8cc` ([`trader-tick-queue-archive.md` §C, `trim-2026-09-14-50`](trader-tick-queue-archive.md#trim-2026-09-14-50)) · C1-coverage F1 `4032f9c` · A54a drift guard arc · CeilingAudit version constant and check (2026-08-25) · F2 and F3 `e082844` · F3-watch tooling (cancelled) · SH-1 `c19f0ef` · `DeribitIndicatorProject.md` §15 C1 collapse · G12 (2026-09-07). Detail for these: [`trader-tick-queue-archive.md`](trader-tick-queue-archive.md) §A. **Closed by the 2026-09-14 sweep, checked in the tree:** coverage cluster C-1 `trade_seq` completeness and C-2 startup window (`ea8907e`), C-3a declared schedule (`25b8691`), C-3b Part B venue scope (`16b19f6`), all committed but not yet on the collector box (`seat-handover-2026-09-14.md` §5); the stale `roadmap.md` footprint figure, now updated. The moved rows: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-13`](trader-tick-queue-archive.md#trim-2026-09-14b-13) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-15`](trader-tick-queue-archive.md#trim-2026-09-14b-15) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-16`](trader-tick-queue-archive.md#trim-2026-09-14b-16) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-17`](trader-tick-queue-archive.md#trim-2026-09-14b-17) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-18`](trader-tick-queue-archive.md#trim-2026-09-14b-18) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-19`](trader-tick-queue-archive.md#trim-2026-09-14b-19) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-21`](trader-tick-queue-archive.md#trim-2026-09-14b-21) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-23`](trader-tick-queue-archive.md#trim-2026-09-14b-23) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-24`](trader-tick-queue-archive.md#trim-2026-09-14b-24) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-25`](trader-tick-queue-archive.md#trim-2026-09-14b-25) |
| ⚠ **REFUTED ON DIRECTION 2026-08-20 - the 100 ms book feed does not understate the ratio** | Superseded by the row above; the raw-feed test SURVIVES, aimed at `pullFrac` | Blur shrinks the denominator, so the ratio would read LARGER, not smaller. The surviving test: subscribe `book.<i>.none.10.raw` in parallel and compare per-episode `pullLB`/`postLB` against the 100 ms fold. Credentials were ruled available for the collector (2026-08-20). Full row: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-47`](trader-tick-queue-archive.md#trim-2026-09-14-47) |
| **Fills-import tool — outcome GROUND TRUTH from `private/get_user_trades_*`** | Small build, `tools/` | **NEW 2026-08-20, trader-queued explicitly as a LATER item.** ⛔ **It CANNOT substitute for the trade store and must never be proposed as one** — `private/get_user_trades_by_instrument` returns *"the latest **user** trades"*; the `historical` parameter changes **reach** (indefinite), never **scope**. The store needs the **public tape** (~60k trades/day, every participant); this returns **your blotter**. Authentication does not widen it. **WHAT IT ACTUALLY BUYS:** every outcome in this project today is **modelled, not observed** — `LivePerformanceTracker` and `ForwardWindowJoiner.PopulateForwardBars` resolve barriers against forward candles, which **cannot resolve intrabar path** (a 1 m candle touching both target and stop has no orderable answer in OHLCV). ⚠ **DO NOT USE FILLS TO COMPUTE OUTCOMES** — your fills are a sample of *the signals you chose to take*, so any win rate from them carries a selection bias the candle eval does not have; the candle eval's advantage is that it covers **every** run including the skipped ones. **USE FILLS TO VALIDATE THE MODEL THAT COMPUTES OUTCOMES:** measure the **disagreement rate** between candle-derived and realised outcome on trades actually taken. Agreement ⇒ the modelled eval is trustworthy across the whole population, which validates Kelly, the F1 tier ladder and everything built on them. Systematic disagreement ⇒ the bias is **measured** and correctable. ⭐ **This works on a SMALL sample** — it measures agreement, not win rate — so a few dozen fills say something, where every other gate here needs hundreds of rows. **Secondary, all measurement not signal:** slippage (actual entry vs `Placed*` — currently unmeasured), **fee drag** (v62's `trade_costs` **models** 0.0008; fills would measure it), stop-gap behaviour. **NO scoring signal improves — not one.** **HOME: `tools/`, NOT the engine** — host-agnostic per the CLAUDE.md Linux-port rule. ✅ **CREDENTIALS RULED AVAILABLE FOR THE COLLECTOR (trader, 2026-08-20)**, superseding this row's original recommendation to keep the box credential-free; the engine holds none today (`DeribitClient.vb:3`: *"No authentication required -- all endpoints are public"*), so this and the raw-feed absorption test are the **first two** things that would put a key there. Mint a **`trade:read`-only** key (the endpoint needs nothing more); never in the repo, never in the deploy allowlist. ⚠ **The key must be provisioned INTO the Linux cutover (CLI port Stage 5), not onto the Windows box first** — otherwise it is provisioned twice and revoked once. **TIMING: needs fills to EXIST.** The dataset starts accruing the day the bridge trades, not the day the tool is built. **Model: Sonnet, effort medium** — one authenticated fetch, a join on `SignalId`, a disagreement table; escalate to Opus if the join proves ambiguous (**a fill spanning two runs is the trap**) |
| ⚠ **`ws_health.log` under-reports a capture outage — it and the tape disagree by ~34 minutes** | investigation, then a scoping decision | ⚠ **NEW 2026-08-12.** For the 2026-08-11 outage: `ws_health.log` records `DEGRADED` 09:04:21.695Z → `OK` 09:30:39.338Z — **26 minutes** — while the tape shows **zero rows 08:59:56 → 10:00:12, 60 minutes**. **Trades stopped ~5 min BEFORE the DEGRADED marker and resumed ~30 min AFTER the OK marker.** ⚠ **This matters beyond bookkeeping: `ws_health.log` is the supplementary uptime source `CoverageReport`'s S1 join reads**, so an hour the box could not capture is evidenced as ~34 minutes of health. **`WsHealthLog` logs only on state CHANGE by design**, so the question is whether the health classification itself lags the feed, or whether "OK" is being emitted on a completed run that had no trades to process. **Determine which before changing anything.** ⚠ **NEW LEAD, 2026-08-12 — a starting point, NOT a finding. I did not verify the mechanism.** The trader reports that during this outage **both boxes showed the amber `ANALYSIS SKIPPED` hero with no readings** — the P4f skipped card, reached when a required REST fetch returns nothing. **The skip path still writes WS health:** `MainForm_SignalBridge.EmitBridgeSkipped` calls `LogWsHealthTransitionForRun(cfg)`, and its own comment says *"a skip run still carries a valid feed-health reading."* **So every health line in that window was written by a run that produced no analysis at all.** The question to open on is therefore precise: **what does `SignalEmitter.DeriveWsHealth` return on a SKIPPED run whose WS has reconnected but whose tape is still empty?** If it can return `OK` there, that is a candidate explanation for an `OK` marker at 09:30 against trades resuming at 10:00. ⚠ **I did not read `DeriveWsHealth`'s parameter semantics — do not treat this as diagnosed.** Fixture `A22d` pins its precedence (`REST` > `DEGRADED` > `DOWN` > `OK`) and is the place to start. ✅ **ANSWERED 2026-08-13 — `DeriveWsHealth` READ, and the lead is now a FINDING.** `Core/SignalEmitter.vb:94` takes exactly four inputs: transport mode, the per-run stale-stream REST-fallback flag, `feedExists`, `feedConnected`. ⚠⚠ **NOT ONE OF THEM REFLECTS TRADE FLOW OR A STORE WRITE.** So `OK` means *"the socket is up and this run did not fall back"* — **it does not mean capture is healthy, and by construction it cannot.** That explains the 2026-08-11 discrepancy exactly: the socket recovered at 09:30:39 (→ `OK`) while the tape stayed empty until 10:00:12. ⚠ **This reframes the item: it is NOT a lag to tune.** An hour the box could not capture can never appear in a log whose inputs exclude capture. **Two design routes, and the spec must pick one:** (a) `CoverageReport`'s S1 join stops treating `OK` as capture evidence; (b) `DeriveWsHealth` gains a capture-liveness input — ⚠ **`TradeStoreWriter.LastFlushUtc` already exists and is exactly that signal**, and `ClassifyTapeStoreTier` already grades it. ⚠ **Route (b) changes a pinned enum's semantics and would move `A22d`** — say which route in the spec before touching code. **Full write-up: [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5b.** ⚠ **Related but distinct from the intentional-downtime row below** — that one is about hours nobody was capturing on purpose; this is about the record disagreeing with what happened. **Model: Sonnet, effort: medium** |
| ✅ **`S-1` (the fourth trade-parse site in `WsTradeProbe`) - RULED 2026-09-07 (trader): (a) move `TradeRecord` into `Core/TradeRecord.vb`, AS THE DIRECTION, NOT NOW. Nothing is owed** | Not scheduled. Schedule it behind anything with a live consumer. The old sizing does not survive; re-scope when scheduled | The probe parses trades with its own readers (five divergences, re-verified 2026-09-07). ⛔ Option (c), linking `DeribitClient.vb`, is REJECTED on measurement: it drags in `SettingsLoader`, which writes `settings.json` when absent, into a probe guaranteed to write nothing. ⛔ Option (b), accept and document, is rejected: a safety held by a comment is the defect class. Fix shape: `TradeRecord` is self-contained, so the move needs zero call-site changes and one `Compile Include` in five `.vbproj` files. Read: [`item6-wstradeprobe-s1-recheck-2026-09-07.md`](item6-wstradeprobe-s1-recheck-2026-09-07.md). Full row: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-48`](trader-tick-queue-archive.md#trim-2026-09-14-48) |
| **Absorption mechanism revision (Path B)** - fully ruled; the build is date-gated | Build spec S1 from 2026-09-15 UTC, then S2, then one deploy. Opus, effort HIGH | `D-1` to `D-5` ticked 2026-09-01 · `D-6a` and `D-6b` ruled · `D-6c` open, gated on data · `D-6d.1` = (c) and `D-6d.3` = (c), ruled 2026-09-11 · `RD-1` = (b), ruled 2026-09-13. Build spec: [`absorption-d2-stage1-rotation-build-spec.md`](absorption-d2-stage1-rotation-build-spec.md). ⛔ Stage 2 before `D-2` makes `absorbRatio` fall. The 2026-08-20 bimodal-population measurement (the pull veto as the dominant killer) is answered by `D-3` of [`absorption-mechanism-revision-proposal.md`](absorption-mechanism-revision-proposal.md): the pullFrac 1.000 artefact is real but about 3 % of active rows, so `max_pull_frac` stays and the response is instrumentation. Full rows: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-20`](trader-tick-queue-archive.md#trim-2026-09-14b-20) · [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-14`](trader-tick-queue-archive.md#trim-2026-09-14b-14) |
| ✅ **Run S0 `--verify-venue` - RULED 2026-09-14 (trader): OPTION A, run on the dev machine after each `collector.ps1 fetch`, NOT on the AWS box** | Build in progress: wiring fix `dc02219`, then `V-1` + `V-4` `6509a0f` + a post-fetch hook `9b6fd0b` | Brief: [`venue-check-plan-review-2026-09-14.md`](venue-check-plan-review-2026-09-14.md); plan: [`venue-check-schedule-plan.md`](venue-check-schedule-plan.md). Purpose: test the `trade_seq` gap-free assumption that gap repair and the coverage report share. The box-side scheduled task, `deploy-tool` verb and `V-2`/`V-3` are dropped or held. ⏰ **Sample review due on or after 2026-10-05 UTC** (`trader-tick-queue.md` §4). The pre-ruling row text follows, kept for history: Suspended 2026-08-08 until `trade_id` shipped. `trade_id` + `trade_seq` were deployed to AWS 2026-08-10 (instance `d8678d2b…`), and the venue diff now reports identity and fallback matches apart. No scheduled task exists (`tools/ops` has no `--verify-venue` call, checked 2026-09-14). Settled design: AWS only · output to a folder under the app root · the window ends on the previous whole hour · a background task, read manually · ⚠ `--strict` grades Defect hours only and cannot signal venue loss. Original row: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-22`](trader-tick-queue-archive.md#trim-2026-09-14b-22) |
| ~~Run S0 `--verify-venue` on a DAILY cadence~~ | superseded by the SUSPENDED row above | Its "16,459 more trades" figure is WITHDRAWN. Original row: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-49`](trader-tick-queue-archive.md#trim-2026-09-14-49) |
| ⛔ **SPEC NEEDED - decouple `LivePerformanceTracker._evalCache` from `analysis_eval_cache.csv` before ANY trim** (moved from `trader-tick-queue.md` §0 on 2026-09-14; two duplicate rows merged) | Spec first, then a build. Not urgent, but the obvious fix is destructive | `_evalCache` is a static list that is never trimmed; the file grows about 0.29-0.33 MB/day, and the in-memory rate is NOT measured. It is not a leak: handles and threads stayed flat across a 4x process-age gap. `WriteEvalCache` rewrites the whole file from `_evalCache`, so trimming the list truncates the eval history that Kelly and F1 read. The spec must choose (a) MERGE with disk or (b) stream `AggregateRange` from file, and must handle the two reclassification passes that need the full set. `LivePerformanceTracker.vb` is host-agnostic, so the CLI port carries this unchanged. Line numbers in the original rows date from 2026-08-22 and were not re-verified. Still open per [`seat-handover-2026-09-14.md`](seat-handover-2026-09-14.md) §5. Full text of both rows: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-36`](trader-tick-queue-archive.md#trim-2026-09-14-36) |
| **`DeribitWsFeed` never verifies the subscribe reply - it logs the REQUESTED channel count** (moved from `trader-tick-queue.md` §0 on 2026-09-14) | Not trivial: needs request-id correlation. Sonnet, medium; bundle into the next commit that opens `DeribitWsFeed.vb` | Deribit silently drops an unrecognised channel with an empty `result` array, so a bad channel logs as subscribed while its stream never starts. The failure shows in aggregate as every run skipping on "order book unavailable". ✅ **Re-checked 2026-09-14: `DeribitWsFeed.vb:208` still logs `Channels.Length`.** Full text: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-36`](trader-tick-queue-archive.md#trim-2026-09-14-36) |
| ⚠ **Gap repair skips a trade that shares a page's last millisecond** (NEW 2026-09-14, found by the option-A venue-check build) | ✅ **SPEC WRITTEN AND AMENDED 2026-09-14 (UTC): [`gap-repair-same-ms-page-skip-spec.md`](gap-repair-same-ms-page-skip-spec.md).** ✅ **RULED (trader, via the orchestrator):** `GR-1` (d) repair by `trade_seq` range · `GR-2` (b) · `GR-3` closed by (d) · `GR-4` (b) durable `repair_status.log` · `GR-5` (b) the S2 deploy WAITS for this build — decision rows in that spec's §3.2. Seq endpoint measured exact before amending. ✅ **Amendment ACCEPTED by the orchestrator 2026-09-14 (UTC)** (`79dcfe0`, `291111f`). ✅ **BUILT 2026-09-14 (UTC) on the trader's go: `edd4539`, local only, not deployed.** Harness 382 → 389 `ALL PASS`; fixtures `A79a`–`A79g`, `A56a`–`A56f` remapped. ✅ **ACCEPTED by the orchestrator 2026-09-14 (UTC)** for the S2 deploy; no deploy scheduled. Packet: [`gap-repair-same-ms-page-skip-build-spec-back.md`](gap-repair-same-ms-page-skip-build-spec-back.md). Response: [`gap-repair-same-ms-page-skip-spec-back.md`](gap-repair-same-ms-page-skip-spec-back.md) §5 | `HistoricalStore.BackfillTradeMonthAsync` sets `cursorMs = newestMs + 1` (`tools/BacktestRunner/HistoricalStore.vb:314`, verified 2026-09-14), and Deribit's `start_timestamp` and `end_timestamp` both measured inclusive. `TradeStoreGapRepair.vb:127` calls it, so this is the LIVE repair path, engine-binary. ⛔ **ANSWERED: the next pass does NOT refill the hole, ever.** The hole window is `[L.ts + 1, R.ts − 1]` (`Core/TradeStoreWriter.vb:853`), which excludes the millisecond the skipped trades sit in (`gap-repair-same-ms-page-skip-spec.md` §2.1). ⭐ **Already fired in production:** 16 small `trade_seq` holes (70 trades) at 1,000-row page ends inside the 2026-08-17 start-up repair (`InstanceId` `e3781e57…`), still present in the 2026-09-13 copy-back (`gap-repair-same-ms-page-skip-spec.md` §2.2). The tools-side fix already exists in `CoverageReport.vb` (`6509a0f`, fixture `A78b`). Source: [`venue-check-build-spec-back.md`](venue-check-build-spec-back.md) §3. Not scoring, no rendered value; the deploy is reserved |
| ⚠ **Cross-month leading gap not repaired** (NEW 2026-09-14, orchestrator finding `F-1` in the review of `edd4539`) | ✅ **RULED (a) by the trader 2026-09-14 (UTC), and BUILT the same day:** spec [`gap-repair-cross-month-gap-spec.md`](gap-repair-cross-month-gap-spec.md) (`2b57411`), code `165f780`, local only, not deployed. `A79h` flipped from pin to guard (9 of 9); `A79i`–`A79k` added; harness 393 `ALL PASS`. ⛔ **The S2 deploy holds for it; awaiting orchestrator review.** Packet: [`gap-repair-cross-month-gap-build-spec-back.md`](gap-repair-cross-month-gap-build-spec-back.md) | **Mechanism:** `ScanForRepair` scans ONE month file and brackets only against rows below `segStartMs` in that file. When an outage crosses 00:00 UTC on the 1st, the August tail stops at August's segment end (required, trap `GT-3`), the September scan holds no August row, and the September tail starts after September's newest row. So seqs between August's last stored seq and September's first stored seq, dated September, are fetched by nobody. **The race:** if the first repair pass runs while the September file is still empty, the anchored tail at September's `segStartMs` fetches them; if streaming writes September first (an outage the app rides out, or a flush before the pass), the gap is permanent. ✅ **PRE-EXISTING, not introduced by `edd4539`:** verified by reading `2d52fb8`, where the same three facts held for time windows (`Core/TradeStoreWriter.vb:916`, `:918`, `:980`–`:1019`; `tools/BacktestRunner/HistoricalStore.vb:262`, `:300`). **Pinned by fixture `A79h`** (`e13e7cf`): with September already written, 0 of 9 leading-gap trades are repaired; with September empty, 9 of 9. **Fix direction, a sketch not a design:** seed `ScanForRepair`'s bracket from the previous month file's newest row, so the hole spans the file boundary and is fetched as one seq range. The `GT-3` tail stop must stay. `A79h` part 1 flips by design when fixed. **Not verified:** how often a real restart's first pass beats streaming's first flush, and whether any cross-month gap exists in the store today |
| ⚠ **Repair pass rewrites its whole lookback when its store scan fails** (NEW 2026-09-15, found by the September duplicate-rows read) | ⛔ **Needs a ruling:** `DUP-1` (fix now, and whether the S2 deploy waits) and `DUP-2` (fix direction) — decision rows in [`trade-store-duplicate-rows-read-2026-09-15.md`](trade-store-duplicate-rows-read-2026-09-15.md) §7. Spec first; touches `ScanForRepair`, which the gap-repair builds froze | **Measured:** 298,932 of September's 298,934 duplicate rows are 3 repair passes (35, 37, 40; 09-10 09:49 → 09-11 15:49 UTC) that each rewrote the full 20 h lookback. The other 2 rows are the `A79g` streaming overlap. **Mechanism, read from `2d52fb8`:** `ScanForRepair` swallows any exception and returns no rows or only an old bracket, so the tail starts at `segStartMs` and the whole lookback is fetched and appended again. **Still live after `edd4539` + `165f780`:** an empty scan gives an anchored tail at `segStartMs`; a bracket-only scan gives a seq tail from the retention edge (~24 h). ⚠ `repair_status.log` would read that pass `PASS_CLEAN`. **Not loss:** readers dedupe, and September shows 0 `trade_seq` holes. **Not verified:** which exception fired (Console is not captured); the leading candidate is the B-3 file-sharing collision with a streaming flush. This read supersedes the 2026-09-14 duplicate-rows task chip |
| ⚠ **The eval cache double-stores rows and misses hits the candles show** (NEW 2026-09-15, found by the swing-vs-fallback read) | Investigation first, then a scoping decision. It feeds the live performance strip | **Two findings, from [`swing-vs-fallback-target-read-2026-09-15.md`](swing-vs-fallback-target-read-2026-09-15.md):** (1) `analysis_eval_cache.csv` on the box holds every pre-2026-09-01 row twice: a live copy and a cold-start backfill copy with a whole-second `.0000000Z` stamp. ⚠ Probably the defect `S-4` (the eval-cache identity dedup, `1aeae5a`) fixed; `S-4` is committed but not yet on the box. **Not verified.** (2) On 7.7 % of rows the eval cache records no target hit where the 1-minute candles show one, never the reverse. Candidate: a forming bar stored early. **Not verified.** Why it matters: the live performance strip reads this cache, so both effects move its rates. Kelly and band-ladder reads walk candles and are not affected |

---

## 3. Riders — MOVED 2026-09-13 (UTC) to [`csv-rotation-riders.md`](csv-rotation-riders.md)

⛔ **The ledger of record is now [`csv-rotation-riders.md`](csv-rotation-riders.md).** This section's table was moved there verbatim (its §2) for two reasons. **A list inside a file that changes every day cannot be gated on**, and **a sixth rider — the trade-count column, ruled in [`thin-trade-window-skip-gate-proposal.md`](thin-trade-window-skip-gate-proposal.md) `D-5` — had never been in this table at all.** ⭐ **`tools/checks/verify-gate.ps1` now fails a pushed `analysis_log.csv` header change that does not update the ledger.**

**State at the move:** seven riders `TRAVELLING` with the `D-2` + Stage 1 rotation, one `CONDITIONAL`, one historical `CONSUMED`.

---

## 4. Standing watches — periodic, no tick required

liq_events CASCADE ⇒ A4 · burst-watch spot-checks · funding calm-week re-read · absorption episode accrual under Path B · v48 OFI dominance (`v48-ofi-dominance-rebaseline-spec-back.md` §4) · the B4b placed-geometry watch (`DeribitIndicatorProject.md` §12; its F3 arm was retired 2026-08-12) · pullFrac distribution (W4). The STRONG-accrual watch ended when the Kelly trigger was met on 2026-09-09. Original line: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-26`](trader-tick-queue-archive.md#trim-2026-09-14b-26)

✅ **The D3/v65 ASIA arming watch — READ 2026-08-11, PASSED.** [`d3-asia-burst-watch-read-2026-08-10.md`](d3-asia-burst-watch-read-2026-08-10.md). Fire rate **10.97 %** and same-side **89.47 %** over 12 fully-covered AWS weekday session-days (n=1,905); the post-ship-only 5-weekday slice reads 11.99 % / 89.47 %. `burst_ratio_threshold` stays **5.5**; no re-fit triggered. The contra arm stays effectively dead (0.50/day), so **on res-3 this modifier is upgrade-only** — unchanged from the derivation §3.

⚠ **The watch stays LIVE, on the ruled tolerance** (T-1 to T-5, ruled 2026-08-11; they supersede row D3-5 of [`asia-burst-threshold-derivation-2026-08-01.md`](asia-burst-threshold-derivation-2026-08-01.md) §5): reference **11.0 %**, band **8–14 %**, read length **≥10 weekday session-days**, same-side **≥85 %**. A trigger fires a re-derivation READ, not a threshold change. Do not quote the derivation's 9.7 % or its "~106 AggrVel rows/day". ⛔ **SECOND READ 2026-09-14 (UTC): MISS on the fire rate** — [`d3-asia-burst-watch-read-2026-09-14.md`](d3-asia-burst-watch-read-2026-09-14.md). 23 fresh weekday session-days (2026-08-11 → 09-11): fire rate **7.75 %** (below the 8 % floor), same-side **89.75 %** (passes). The rate is bimodal and tracks ATR: **15.97 %** over 08-11 → 08-19, **4.85 %** from 08-20. No deploy or settings edge sits on the step. **T-5 fires: a re-derivation READ is owed. `burst_ratio_threshold` stays 5.5.** Next watch read due after 10 more covered weekdays from 2026-09-14, so **no earlier than 2026-09-26 (UTC)**. Original paragraph: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-27`](trader-tick-queue-archive.md#trim-2026-09-14b-27)

⏰ **DATED REMINDER — venue-check sample review, due on or after 2026-10-05 UTC** (option A, ruled by the trader 2026-09-14). Read `aws_fetch/venue_check_ledger.csv` (gitignored, on the dev machine). **Review only once at least 5 valid samples exist** (verdict `CLEAN` or `LOSS`; `VENUE_SHORT`, `NOT_RUN` and `INEXACT` rows do not count) from builds at or after the `V-1` commit (`6509a0f`); if fewer exist on that date, re-date this line. ⚠ The first live sample is the 2026-09-16 UTC routine fetch; at a fetch every 3–4 days, 5 samples by 2026-10-05 is tight. Decision table: [`venue-check-plan-review-2026-09-14.md`](venue-check-plan-review-2026-09-14.md) §5. The key read (trader ruling `R-2` (a), 2026-09-14 UTC, `venue-check-build-spec-back.md` §2): any `LOSS` row with `missing_inside_seq_span > 0` means the `trade_seq` gap-free assumption is false, so spec option B, a comparison inside the app's repair timer. A `LOSS` row with `missing_inside_seq_span = 0` is an edge effect, not decisive.

**⭐ Kelly dated trigger: MET 2026-09-09** (407 pooled weekday STRONG against at least 406). The ladder did not separate, and the on-screen promise is retired (`DeribitIndicatorProject.md` §15, row dated 2026-09-08). The original paragraph moved verbatim: [`trader-tick-queue-archive.md` §C, `trim-2026-09-14-51`](trader-tick-queue-archive.md#trim-2026-09-14-51).

**Data gate - A5 VPFR shape:** 28 of 30 distinct calendar dates on 2026-08-07 (2026-07-03 → 2026-08-06). Not re-counted since; if collection ran daily the gate passed about 2026-08-08 (an inference, not a count). Meeting it does not authorize the build: A5 must still clear the W6 new-indicator bar as a VPFR refinement rather than an orthogonal class. Original line: [`trader-tick-queue-archive.md` §D, `trim-2026-09-14b-28`](trader-tick-queue-archive.md#trim-2026-09-14b-28)

---

## 5. Closed — MOVED TO ARCHIVE 2026-09-10 (UTC)

⛔ **This section's entries moved to [`trader-tick-queue-archive.md`](trader-tick-queue-archive.md) §B, byte-identical.** They exist so closed items are not re-investigated; read them there. **This section stays empty going forward** — new closed findings still get recorded here first and archive on the next sweep, per §0's authority note on the archive.

---

## 6. Provenance

Assembled 2026-08-01 from: [`seat-close-handover-gap-audit-2026-07-31.md`](seat-close-handover-gap-audit-2026-07-31.md) · [`job1-outstanding-2026-08-01.md`](job1-outstanding-2026-08-01.md) · [`job2-read-2026-07-31.md`](job2-read-2026-07-31.md) · [`overlay-whitelist-reaudit-2026-07-31.md`](overlay-whitelist-reaudit-2026-07-31.md) · [`w6-1-london-ruling-2026-07-31.md`](w6-1-london-ruling-2026-07-31.md) · [`asia-burst-threshold-derivation-2026-08-01.md`](asia-burst-threshold-derivation-2026-08-01.md) · and each spec's own D-table. **Re-swept against the tree 2026-08-07** — findings in §1b.
