# Trader Tick Queue

**Created:** 2026-08-01 (trader-directed). **Purpose:** the one place that answers *"what is waiting on me, in what order, and what does each one block?"* — the **scoped immediate slice** of [`roadmap.md`](roadmap.md). Derived from the specs and the board; **not a new authority.**

**Authorities:** [`roadmap.md`](roadmap.md) = the strategic board and execution order · [`backlog-dependency-map.md`](backlog-dependency-map.md) = **what blocks what** (edges only — as of 2026-08-07 it carries **no state at all**, by design) · [`profitability-risk-levers.md`](profitability-risk-levers.md) = profitability lens · each spec's own D-table = the decision text. **This doc carries ORDER and GATES; where it disagrees with a spec, the spec wins.**

**Maintenance:** update State on every tick. Move closed rows to §5 rather than deleting. **Re-run the §1b shipped-state sweep whenever this queue is rebuilt — do not inherit a prior assembly.**

---

> ## ⛔ STATE BANNER — 2026-08-25. Read this before believing any row below.
>
> ⭐⭐ **CURRENT STATE READ: [`seat-handover-2026-09-07.md`](seat-handover-2026-09-07.md).** Settings **v68**. ⛔ **Run `git status -sb` — never inherit a push state.** ⛔ **FIRST TASK: the Kelly trigger read, and it is dated to a TIME OF DAY now — LATE in the Tuesday 2026-09-08 UTC day, NOT at 00:01.** ⚠⚠ **The GMT+8 clock trap fired THREE times in one session, once in a claim made to the trader. Run `date -u`.** ✅ **Nothing is owed by the trader; nothing is blocked.** ⭐ **Harness 328 → 337 across 17 commits, `settings.json` in NONE of them. The spread seam is CLOSED and the weekday-scope ruling is FULLY DISCHARGED.**
>
> ⚠⚠ **KELLY — PROGRESS SAMPLE 2026-09-07 18:49 UTC. ⛔ THIS IS NOT THE TRIGGER READ AND MUST NEVER BE QUOTED AS ONE.** `337` (the closed `.bak`) `+ 62` (live `analysis_log.csv`) = **399 against ≥406. Shortfall 7.** Taken deliberately one day EARLY, and labelled, to test whether the ~14/weekday rate was real. ⭐ **IT IS — the 2026-09-06 projection said *"Mon 09-07 ends ≈ 400"* and it read 399 with ~5 h of the UTC day still to run.** Live-file weekdays: 09-01 = 4 (partial, post-rotation) · 09-02 = 14 · 09-03 = 21 · 09-04 = 10 · **09-07 = 13 so far**. Book 5,692 rows, `2026-09-01 15:50:01 → 2026-09-07 18:48:01`; one process; collector healthy.
>
> ⭐⭐ **REVISED ETA, SHARPER THAN "TUESDAY": a shortfall of 7 against ~13–14/weekday crosses 406 PARTWAY THROUGH Tuesday 2026-09-08 UTC — late morning to early afternoon UTC, NOT at the day's start.** ⛔ **So read LATE in the Tuesday UTC day. A 00:01 read still returns short and would burn the bundled W6-4 freeze on a below-trigger span.**
>
> ⭐ **THE METHOD WAS CALIBRATED BEFORE USE, because the 337 baseline was measured by another seat and counting differently makes the sum meaningless:** the identical query run against the 2026-09-06 copy-back returned **exactly 49**, matching the recorded figure, with 09-02 = 14 and 09-03 = 21 matching too. ⚠ **Two traps checked rather than assumed — all TEN distinct `Verdict` values were enumerated (there is NO `NO TRADE [STRONG …]` form today, but exact-match is used anyway so the count cannot silently shift if one ever appears), and `ParseExact` with `InvariantCulture` rather than `[datetime]::Parse`, which reads the box locale.** Read-only over SSM; nothing written on the box.
>
> ✅✅ **NOTHING IS OWED — ITEM 6's direction was RULED the same day it was raised, 2026-09-07 (UTC): (a) AS THE DIRECTION, NOT NOW.** Split `TradeRecord` into `Core/TradeRecord.vb`, then route the probe — **but scheduled behind anything with a live consumer.** ⛔ **(c) is dead: linking `DeribitClient.vb` drags `SettingsLoader`, which writes `settings.json` when absent, into a probe whose project file guarantees it writes nothing the collector uses.** ⚠ **The row keeps its measurement; only the sizing is void.** ✅ **`D3-RESIDUAL` was taken instead and is DONE — see its §2 row.** *(Superseded text follows, per the quote-and-label convention.)*
>
> ~~⛔⛔ **ONE THING IS OWED — added 2026-09-07 (UTC): ITEM 6's direction.**~~ `S-1` was re-checked as its row instructs and **it still holds — but its recommended fix is NOT AVAILABLE and the row's own escalation trigger FIRES.** Read [`item6-wstradeprobe-s1-recheck-2026-09-07.md`](item6-wstradeprobe-s1-recheck-2026-09-07.md) **§5** and pick (a), (b) or (c). ⭐ **My read: (a) as the direction, but NOT NOW — nothing is bought today.** ⛔ **(c) is rejected on measurement: linking `DeribitClient.vb` drags `SettingsLoader`, which WRITES `settings.json` when absent, into a probe whose project file guarantees it cannot write any file the collector uses.**
>
> ⚠ **THE GMT+8 CLOCK TRAP FIRED AGAIN, second session running.** At **2026-09-07 17:32 UTC** the workstation read **2026-09-08 01:32**. ⛔ **The Kelly trigger's "not before 2026-09-08" is UTC and had NOT arrived.** **Run `date -u`. Every time.**
>
> ✅✅ **`S2-2` IS BUILT, REVIEWED AND ACCEPTED — 2026-09-06 (UTC).** Reviewer verdict: [`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md) **§5**. ⭐ **Re-run by the reviewer, not taken on report: all seven handles, and ALL THREE MUTATIONS re-applied and rebuilt — `A65c` fails ALONE under the swapped arms, and `A65a` still PASSES under the always-`NORMAL` mutation, which is the whole reason `A65b` exists.** ⚠ **TWO NON-BLOCKING FINDINGS, §5.3–§5.4: `R-1` the packet's headline handle `H-1` is NOT RE-RUNNABLE — its parity instrument was a scratchpad never committed, so *"if you only run one, run H-1"* points at a thing no reviewer can execute. ⭐ The parity claim still holds by a route that DOES survive: `A65a` + `A65b` + `H-2`, all three in the tree and all three re-run. `R-2` deleting that instrument left the composition line at `UI/MainForm_Analysis.vb:423` unguarded and STRUCTURALLY uncoverable — `OrderCheck.vbproj` cannot link `UI/`.** ⛔ **New shape worth carrying: a handle whose INSTRUMENT does not survive the build is evidence, not a handle. Rank handles by whether the READER can run them.** ✅ **`Q-1` ruled (a) and queued below · `Q-2` and `Q-3` closed.** **Committed locally and NOT pushed** (the trader pushes after testing). **Nothing is owed by the trader and no build slot is open.** Harness **328 → 332** (`A65a`–`A65d` — the first fixture coverage this method has ever had); six Release builds 0/0 each run separately; `verify-gate.ps1 -Mode local-fast` **GATE PASSED**; **settings unchanged, still v68**. ⛔ **The display-string parity rule was LIVE here and was PROVED, not asserted** — snapshot `Spread:` line + breakdown note + both card bindings' resolved `(text, colour)`, captured pre- and post-change over **eight book shapes including four degenerate**, **byte-identical, same MD5**; and the three render files are untouched in the diff. **All three of the spec's §6 mutations were RUN, not reasoned.** Packets: [`s2-2-calcspread-split-batch-summary.md`](s2-2-calcspread-split-batch-summary.md) · [`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md). ⚠ **ONE NAMED RESIDUAL IS NOW LIVE, by ruling and not by oversight:** `D-3` (b) left `HasTopOfBook` alone, so the live strip's 3-condition predicate and `CalcSpreadBps`'s 6-condition one still disagree on a **zero-priced top of book** — `HasSpread` says True where the bps says `Nothing`. That is a one-line change with a real if small live-strip effect and it wants its own slot, not a quiet tidy-up inside a refactor. *(Superseded text follows, kept per the quote-and-label convention.)*
>
> ~~✅✅ **NOTHING IS OWED BY THE TRADER — `S2-2`'s D-table was TICKED IN FULL the same day it was raised, 2026-09-06 (UTC).**~~ ⭐ **[`s2-2-calcspread-split-proposal.md`](s2-2-calcspread-split-proposal.md) IS BUILD-AUTHORIZED. `D-1` (a) `Double?` · `D-2` (a) inside `ClassifySpread` · `D-3` (b) leave `HasTopOfBook` · `D-4` (a) delete `CalcSpread` · `D-5` (a) keep the names — every row as recommended, no amendment.** ⛔ **Build from its §4b, NOT its §4** — §4 is the record of how the decisions were reached. **Six build items; Opus/HIGH, one session.** *(Superseded text follows, kept per the quote-and-label convention.)* ~~⛔⛔ **ONE THING IS NOW OWED BY THE TRADER — added 2026-09-06 (UTC). `S2-2`'s D-table.**~~ The proposal is written — [`s2-2-calcspread-split-proposal.md`](s2-2-calcspread-split-proposal.md) — and **its §4 needs a tick before any code.** ⭐⭐ **Writing it falsified the fix as specified**, which is why it is a decision and not a build slot: the split shape named in [`a54a-session2-step1-measurement-2026-09-05.md`](a54a-session2-step1-measurement-2026-09-05.md) §9.2 (i) **silently changes a rendered value on four surfaces.** `CalcSpread` seeds `spreadStatus = "NORMAL"` at `Core/Indicators_OrderFlow.vb:586` **before** its five early-return guards, so `"NORMAL"` is the *degenerate-book answer*; classifying the returned `0.0` instead yields **`TIGHT`** (`0.0 <= 1.5`), flipping the breakdown note, the snapshot line and both card bindings — **grey → green, "no data" rendered as "best possible execution".** ⛔ **Five decisions open (`D-1`…`D-5`), each with my read.** ⛔ **Model UPGRADED to Opus/HIGH.**
>
> ⚠ **THE CLOCK — read this before calling the Kelly read late.** At **2026-09-06 16:09 UTC** the workstation clock read **2026-09-07 00:09** (GMT+8). ⛔ **The trigger's "not before 2026-09-08" is a UTC deadline**, and 09-05/09-06 are Sat/Sun, so **nothing accrued over the weekend**. **Do not treat a local-clock 09-07 as evidence the read is due.**
>
> ✅ **`OPS-1` is RULED AND DONE 2026-09-06** — a LITERAL `analysis_log.csv.v0.7.bak` entry in `tools/ops/collector.ps1`, **not** a `*.bak` glob. See its §2 row: the reason is mechanism (the manifest is name-keyed; a glob rewrites the verification loop) and the literal **cannot** sweep future rotations, because `AnalysisLogger.vb:159-163` timestamps the **incoming** file, never the existing one.
> ⭐⭐ **CURRENT STATE READ: [`seat-handover-2026-09-06.md`](seat-handover-2026-09-06.md).** Settings **v68**. ⛔ **Run `git status -sb` — never inherit a push state.** ⛔⛔ **KELLY TRIGGER MEASURED 2026-09-06 15:58 UTC: 386 against ≥406, shortfall 20. DO NOT READ BEFORE 2026-09-08 (Tuesday).** ⭐⭐ **AND THE READ IS NOW ONE COUNT, NOT TWO: the `.bak` half is a CLOSED file at a cross-validated 337, so the whole read is `337 + (weekday STRONG in the live `analysis_log.csv`)`** — obtainable over SSM in one command, no copy-back needed. **Live-file weekday STRONG measured 49 today; ~14/weekday ⇒ Mon 09-07 ends ≈400, Tue 09-08 ends ≈414.** ⚠ **09-05/09-06 were Sat/Sun — nothing accrued.** ✅ **Collector healthy, read today: 38.3 rows/hour, gap 0.8 min, v68, exe 2026-09-01 15:48, two DEGRADED→OK pairs both recovered (the 09-05 one ran 32 min and NOBODY HAS CHECKED WHETHER IT COST TAPE — verify from `trade_seq`, not the repair log).** ⭐ **The whole A54a arc is COMPLETE and pushed: guard → 7 re-syncs → dict completeness both directions → 44 method defaults removed → 26 literals measured → `A64a`/`A64b`. Harness 326 → 328, settings never moved.** ⚠ **Nine reviewer corrections in that arc; the sharpest is that `D1` specified a SHIPPED value in a MECHANISM fixture one day after the same seat ruled "off-EVER-shipped" into CLAUDE.md.**
>
> ✅✅ **THE A54a ARC IS CLOSED — R-2/R-3 follow-up BUILT, REVIEWED AND ACCEPTED 2026-09-05, commit `cc44e9f`.** Packet: [`a54a-r2-r3-followup-spec-back.md`](a54a-r2-r3-followup-spec-back.md) (its §5 is the reviewer verdict). ⭐ **Re-run by the reviewer, not taken on report: harness 326/326 `ALL PASS`, `verify-gate.ps1 GATE PASSED`, and the reviewer's own probe confirms `Compared` = 261 UNCHANGED — the spec's falsifiable prediction held under a second instrument.** **The parse-failure path now equals shipped on every ROC value.** Settings **v68**. ⭐ **Next free fixture family: `A64`.** ⭐⭐ **THE FINDING WORTH KEEPING — `A63a` was built exactly to the spec's worked description and STILL could not see one of the four mutations the spec itself named.** `ResolveRocMagnitudeForHour` tests the bucket override first, so `resolution_profiles["3"].roc_magnitude_threshold` is masked for every live session — **and the masking was created by the same commit's own D-R3 (i) seeding.** ⛔ **Second instance in this arc of a spec-described mutation being unobservable (`A62f` was the first). Both were caught ONLY because "every mutation RUN, not reasoned" is a hard requirement.** ⭐ **Generalisation endorsed: for any spec touching a resolver with more than one fallback layer, state which LAYER each test input exercises, not just which final value it returns.** ⚠ **One item queued — `F-1` in §2: the REVERSE dictionary direction (a POCO dict key absent from JSON) is still unguarded. Measured 0 today, manually, across all four dicts. It is a RULING, not a patch.**
>
> ✅✅ **NOTHING IS OWED BY THE TRADER — `D-R3` RULED (i) the same day it was raised, 2026-09-04.** ⭐ **[`a54a-r2-r3-followup-spec.md`](a54a-r2-r3-followup-spec.md) is BUILD-AUTHORIZED IN FULL** — R-2 = (b) · R-1 rides it · D-R3 = **(i) seed the nullables** (ASIA `roc_magnitude_threshold` **0.17**, LONDON **0.11**, ⛔ **NY stays `Nothing`** — shipped has no NY override). **The parse-failure path then equals shipped on every ROC value.** **Sonnet, medium, one session. Fixtures `A63a`–`A63b`.** ⛔⛔ **THE THING THAT MUST NOT BE LOST: the D-R3 seed is INVISIBLE to the A54a guard** — both values are `Double?` and the walk skips nullables before comparing, so **`A62a` passes identically whether the seed is right, reverted, or mistyped.** ⭐ **`A63a` is the ONLY instrument in the tree that can detect this class; build it first and prove its teeth by reverting each seeded value separately.** *(Superseded banner text follows, kept per the convention.)* ~~⛔ **ONE THING IS OWED BY THE TRADER — `D-R3`, added 2026-09-04.**~~ **R-2 = (b) and R-1-rides-it are RULED**; the follow-up build spec is [`a54a-r2-r3-followup-spec.md`](a54a-r2-r3-followup-spec.md). ⛔⛔ **Its §3 `D-R3` is open, and it exists because THE REVIEWER'S OWN RECOMMENDATION WAS HALF WRONG.** He argued for (b) partly on *"it fixes R-3"*; the arithmetic, done afterwards, says **(b) fixes the ROC slope half exactly, improves ASIA, and moves LONDON from 0.01 off shipped to 0.10 off — ten times FURTHER.** **`D-R3` is whether the per-session ROC nullables get seeded too (ASIA 0.17 / LONDON 0.11).** ⭐ **Reviewer's read: YES — in-file precedent twelve lines from the edit** (`AggressorVelocitySessionOverride.BurstRatioThreshold` is a `Double?` seeded with real values, comment *"MUST mirror settings.json"*). ⚠ **Without it, (b) ships LONDON knowingly worse.** ⭐⭐ **What R-3 exposes outlives R-3: the guard's ruled scope — *"a `Double?` = Nothing nullable override cannot drift"* — is right about DRIFT and silent about CORRECTNESS. Here the nullable is legitimately absent and the seed is still wrong, because the RESOLUTION it inherits under changed underneath it. No reflection walk scoped by that rule can catch this class**; it needs a behavioural fixture (`A63a`).
>
> ✅✅ **ITEM 8 SESSION 1 IS BUILT, REVIEWED AND ACCEPTED — 2026-09-04, commit `3a89093`.** Packets: [`a54a-drift-guard-batch-summary.md`](a54a-drift-guard-batch-summary.md) · [`a54a-drift-guard-spec-back.md`](a54a-drift-guard-spec-back.md) (its §5 is the reviewer verdict). **Guard `WalkPocoVsJson` + fixtures `A62a`–`A62g` + SEVEN POCO re-syncs, one commit.** ⭐ **Re-run by the reviewer, not taken on report: harness 324/324 `ALL PASS`, `verify-gate.ps1 GATE PASSED`, and the reviewer's own independent probe now reports exactly the TWO allow-listed deliberate divergences — all seven previously-drifting paths clean under a second instrument.** Settings stays **v68**. ⭐ **Next free fixture family: `A63`.** ⚠ **TWO NON-BLOCKING FINDINGS carried, spec-back §5.3: R-1** the `SessionVolumeSettings.Sessions` comment still opens *"aligned to live v30"* — the exact line `seam-audit-2026-08-11.md` **S-3** cited — above buckets now aligned to v68; **R-2** the walk's dictionary branch iterates POCO keys, so a JSON dict key with no POCO counterpart is neither compared nor reported `JsonOnly` — **live twice and benign twice today**, and ⛔ **a ruling, not a silent patch** (fixing it makes `A62a` report 2 and fail). ⛔ **Session 2 (§7 scoped-(b)) NOT started.** ⭐ **Its step 1 is to RE-MEASURE the inherited "9 fixture omissions, 0 production omissions" — still unverified.**
>
> ⚠⚠ ~~**ITEM 8 SESSION 1 STOPPED AT AN ESCALATION 2026-09-04 — AND THE ESCALATION ASKS FOR A RULING THAT ALREADY EXISTS. NOTHING IS OWED BY THE TRADER.**~~ **RESOLVED same day — kept per the quote-and-label convention.** Packet: [`a54a-drift-guard-escalation-2026-09-04.md`](a54a-drift-guard-escalation-2026-09-04.md). ⭐ **The stop was CORRECT** — the implementer measured that the spec classifies **seven** rows DRIFT while its D-table disposed of only **three**, and refused to re-sync four production defaults on their own judgment. ⛔ **But its conclusion is wrong: rows 3–6 were RULED 2026-08-11, in §0a's own "Seeded session buckets" row below — *"DO NOT empty the seed. Guard it via (d)"* and *"a CORRECT seed beats both an empty one and a stale one, and (d) makes it correct and keeps it correct."* Its superseded twin names the exact four values and frames the fork as "re-sync AND guard it".** ⛔⛔ **THE ERROR IS THE SPEC AUTHOR'S: he read that row at session start, used its GUARD half, and dropped its RE-SYNC half.** ⚠⚠ **This is the FOURTH instance of the defect §0a exists to prevent — a decision the trader had already made, presented as still owed — and the stale carrier this time is a SPEC WRITTEN FIVE HOURS EARLIER by the seat that had just read the ruling. A freshly-written document is not evidence that a standing ruling reached it.** ✅ **Resolution: SEVEN re-syncs, not three; spec §4.2 corrected; build resumes with no new ruling.**
>
> ✅✅ **NOTHING IS OWED BY THE TRADER — the item 8 D-table was ticked the same day it was raised, 2026-09-04.** ⭐ **ITEM 8 IS BUILD-AUTHORIZED: [`a54a-json-poco-drift-guard-spec.md`](a54a-json-poco-drift-guard-spec.md), §4 TICKED IN FULL.** D-1 · D-2 · D-4 · D-5 as recommended. ⛔ **D-3 RULED AGAINST THE SPEC'S OWN ABSTENTION — `network.transport` POCO syncs `"rest"` → `"ws"` and the stale comment is fixed.** ⚠ **That ruling tightens the build: the D-1 allow-list is now TWO entries, not three, and the escalation trigger moves with it — a THIRD entry is the stop-and-escalate condition.** ⭐ **Verified before acting (spec §4.1): `ResolveSource` carries two REST fallbacks on the `ws` branch, `WsFallbackToRest` defaults True, and every other `network.*` key already matches — so the parse-failure path IMPROVES.** ⭐⭐ **And D-3 repairs an unnamed inconsistency: `trigger_mode`'s POCO default is `"on_close"` while `transport`'s was `"rest"`, and on-close firing REQUIRES ws — so today's defaults ask for a mode the transport cannot deliver.** *(Original box text follows, kept per the quote-and-label convention.)* ~~⛔⛔ **ONE THING IS NOW OWED BY THE TRADER — added 2026-09-04.**~~ The **item 8 spec is WRITTEN** and **its §4 D-table needs a tick before any code.** ⚠⚠ **Writing it falsified the premise it was commissioned under.** [`seat-handover-2026-09-03.md`](seat-handover-2026-09-03.md) §0 bills item 8 as *"Transcription. The ruling is COMPLETE."* ⛔ **It is not.** The ruling settled how to FIND drift and never settled what to do when a divergence is **deliberate** — and three of the nine are. ⭐⭐ **The measurement, taken against tracked v68 rather than inherited: 261 scalars compared, NINE divergences, not six.** ⛔ **TWO ARE NEW AND TEN TIMES APART — `indicators.CVD.slope_pct_of_value` (POCO 0.01 / JSON 0.10) and `indicators.MicroCVD.accel_threshold_dynamic_pct` (POCO 0.03 / JSON 0.30), both drifting since 2026-06-13, both scoring inputs, both with ZERO fixture references.** **App unaffected — no version bump, no boundary.** ⚠ **Item 8 is re-sized: Sonnet, HIGH, TWO sessions.**
>
> ⭐ **CURRENT STATE READ: [`seat-handover-2026-09-03.md`](seat-handover-2026-09-03.md).** Settings **v68**. ⛔ **NO PUSH STATE IS QUOTED HERE ON PURPOSE — `git status -sb` is the answer.** ~~⭐ **FIRST TASK: write the item 8 spec (transcription — the ruling is complete), THEN item 6.**~~ ⭐ **FIRST TASK IS NOW THE ITEM 8 BUILD, SESSION 1** — the walk + fixtures `A62a`–`A62g` + **THREE** POCO re-syncs, all in ONE commit (D-4 requires it, or the harness ships red). **Sonnet, HIGH.** ⛔ **Its §0 names three traps and the spec author walked into trap 2 himself — read §0 before §5.** **Session 2 (scoped-(b)) must NOT lead.** **Then item 6** — `WsTradeProbe` through the shared trade reader; ⛔ **read the code FIRST, nobody has checked whether S-1 still holds.**
> ⭐ **CURRENT STATE READ: [`seat-handover-2026-08-29.md`](seat-handover-2026-08-29.md).** Settings **v68**. ⛔ **NO PUSH STATE IS QUOTED HERE ON PURPOSE — `git status -sb` is the answer, and this line was wrong twice in four days because the trader pushes mid-session.**
>
> ⭐ **NOTHING IS BLOCKED and nothing is owed.** ✅ **Items 17 and 18 — the two decisions that were hiding in §2 build rows — are RULED (trader, 2026-09-03) AND BUILT the same day.** See the (now-closed) box below §0a and packet [`queue-17-18-batch-spec-back.md`](queue-17-18-batch-spec-back.md). ✅ **Item 21 — `coverage` aimed at a copy-back — is DONE 2026-09-03 (`7794320`), and with it R8, which prints the resolved store and evidence paths on every run.** Items 6 and 8 remain ready to build. ✅ **The absorption D-table is TICKED 2026-09-01** — D-1…D-5 ticked, D-6a/D-6b ruled, D-6c open until §5 reads (~09-15), D-6d parked. Its §5 instrumentation is BUILT, DEPLOYED and accepted.
>
> ⛔ **KELLY DATED TRIGGER — NOT MET, but CLOSE. 376 weekday STRONG against ≥406 — shortfall 30. MEASURED 2026-09-03 17:54 UTC on the box, across BOTH files.** ⛔⛔ **THE BOOK ROTATED 2026-09-01 15:49 UTC AND A ONE-FILE COUNT UNDER-REPORTS BY 90 %:** live `analysis_log.csv` alone reads **39**; `analysis_log.csv.v0.7.bak` holds **337**; the true total is their sum, and they do NOT overlap (the rotation split 09-01 cleanly at 15:49/15:50). **Any Kelly read MUST count both — see the rotation box below §0a.** ⭐ **Recent weekdays: 08-28 = 23 · 08-31 = 4 · 09-01 = 11 · 09-02 = 14 · 09-03 = 21 and still running.** **At the recent ~14/weekday, 09-04 + 09-07 + 09-08 plus the rest of 09-03 clears 406 — realistic ~2026-09-07 to 09-08.** ⛔ **Still do not fetch before ~2026-09-08. W6-4 waits with it.** ⚠ **All dates in this queue are UTC; the workstation is GMT+8. Check which before calling a sequence impossible.** *(Superseded, kept per the convention: ~~NOT MET: 311 weekday STRONG … checked 2026-08-31 … ceiling ≈ 43 against a shortfall of 95 … ~09-10 at the median, ~09-16 if quiet.~~ **That 311 came from the older, partially-cut 08-28 book and was already stale when the rotation box below was added on 09-03; the two disagreed inside one document for a day.**)*
>
> *(The 2026-08-25 banner follows and its DETAIL still binds — [`seat-handover-2026-08-25.md`](seat-handover-2026-08-25.md) §5 is the full hostel-app record and §1.1/§1.1a/§1.1b the memory-baseline corrections.)*
>
> ✅ **#5, C1-coverage F1 — BUILT, REVIEWED AND ACCEPTED 2026-08-26 (`4032f9c`). DONE bar the trader's test + push.** ~~*FIRST TASK … RULED IN FULL AND BUILD-AUTHORIZED.*~~ Eleven decisions across two ticks; the build took all eleven and **the review re-ran both silent-trap mutations independently — each is caught by exactly ONE fixture of 306.** Packets: [`coverage-trailing-edge-f1-batch-summary.md`](coverage-trailing-edge-f1-batch-summary.md) · [`coverage-trailing-edge-f1-spec-back.md`](coverage-trailing-edge-f1-spec-back.md).
>
> ⚠ **One finding worth carrying past this item: only ONE of D-4 (c)'s three bounds ever binds.** `storeEndMs` is range-filtered, so it is always tighter than `walkToUtcMs`; the boundary term is **untestable, not merely untested**, and fixture `F1-c` passes for a different reason than the spec claims. Recorded at [`coverage-trailing-edge-f1-proposal.md`](coverage-trailing-edge-f1-proposal.md) §3a — **no code change, and do not "simplify" the redundant term away.**
>
> ⚠ **The headline finding, because it cost a recommendation:** the spec's own D-6 argument — that leaving `ObservedLongestGapMs`/`GapBreachHours` alone under-reports — **is false by construction.** `AccumulateHourStats` charges the WHOLE gap to the hour containing the ENDING trade, so the full gap is always ≥ the trailing edge and is already recorded. Folding is a **no-op** on one counter and a **double-count** on the other.
>
> ⛔ **ONE OF THE TWO OPEN TRADER DECISIONS IS CLOSED** — the book's `Timestamp` column is **UTC**, not local. [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) §7.0 carries the refutation. **Nothing to rule.**
>
> ⛔ **THE `pagesOUT/s` BASELINE IS TIME-OF-DAY DEPENDENT — corrected 2026-08-25 from a DETACHED 24-hour instrument, 8,640 samples.** ~~*4 non-zero in 90 samples (4.4 %).*~~ **NY 13:00–23:00 UTC = 1.83 % · non-NY 23:00–13:00 UTC = 14.29 %.** Our 4.4 % was a correct reading of **the quietest 80 minutes of the day**, generalised to 24 hours. ⛔ **Do not quote the 9.10 % pooled figure either — quote the SPLIT.** availMB median **168**, routine floor **55**. *(The "0.0 on all thirty was a 1-in-8 draw" correction still stands; only its replacement number was wrong.)*
>
> ⚠⚠ **AND THE LESSON IS NOT SAMPLE SIZE — 30 → 90 → 180 samples ALL came from the SAME 80-minute window. A bigger sample from the same window does not fix a time-of-day confound.** ⛔ **This project segments every market number by session and then pooled 80 minutes for a box number.** **Three causes now refuted: gap repair · Defender · our own write volume** (writes 3× higher in NY, which has 8× fewer bursts — anti-correlated). Full table and the re-grounded stop conditions: [`seat-handover-2026-08-25.md`](seat-handover-2026-08-25.md) §1.1.
>
> *(The 2026-08-24 banner follows, still accurate for what it covers.)*
>
> ⛔ **AUTHORITATIVE STATE READ: [`seat-handover-2026-08-24.md`](seat-handover-2026-08-24.md).** Settings **v68**, master pushed and in sync — **verify with `git status -sb`, never inherit a push state.**
>
> ⭐ **THERE IS NO FIRST TASK. Nothing is blocked.** Every thread opened in the 2026-08-22 → 08-24 session is closed. **That is unusual for this project and is stated plainly so nobody hunts for the catch.** ⛔ **Do not invent work from the two latent defects below** — both have a destructive or contested obvious fix, which is exactly why they are decisions.
>
> ⚠ **The three 08-23 claims below are SUPERSEDED and are struck in place, per the quote-and-label convention.** Corrected 2026-08-24 from the handover named above; **the AWS figures behind them were not independently re-verified by the correcting seat.**
>
> ✅ **THE COLLECTOR MIGRATION IS DONE.** [`seat-handover-2026-08-22.md`](seat-handover-2026-08-22.md) §0 lists it as the first task with steps 2–8 outstanding — **that is now stale for steps 2–7.** The **t2.micro `i-0d6c133058876273e` IS production**, holding production's carried book (24,721 rows at handover, first row `2026-07-22 16:24:54`) and its full 65,088,339 B store. ~~The old `i-08c740e22d507667d` is **stopped, not terminated**, with termination protection ON and snapshot `snap-0a17195c58e850cbd` completed.~~ ⛔ **SUPERSEDED 2026-08-24 — `i-08c740e22d507667d` is TERMINATED and its volume went with it.** It lives as **`ami-0247c8b7275de49ac`**, backed by **`snap-0b5c126dd898c6bd0`**. ⚠ **Deleting that snapshot silently breaks the AMI while leaving it listed as `available`.** **Tier ruled 2026-08-24: keep it STANDARD** — archive tier is ~4× cheaper but imposes a 90-day minimum and a 24–72 h restore, which defeats a disposable target wanted on short notice. ⛔ **[`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) §8 is REQUIRED READING before relaunching it** — it carries the subnet, security group, key pair and above all the **`EC2-SSM-Access` instance profile, without which a relaunched box has no SSM at all.** Those are otherwise unrecoverable: a terminated instance ages out of `describe-instances` within about an hour. **Tape loss across the cutover: ZERO** (`trade_seq` span 1586, distinct 1586, MISSING=0).
>
> ✅ **Step 8 — the full-day watch — PASSED**, 22 h 27 m: +829 rows at 36.9/hour, gap 0.3 min, **zero DEGRADED**, no restart, CPU credits pinned at 144, `pagesOUT/s` 0 across 15 samples. **The migration is closed.** ⚠ **That `pagesOUT/s` 0 is WEAKER evidence than it reads — corrected 2026-08-24.** At the burst rate measured on 08-24 (6.7 % of samples), `P(all 15 zero)` = `0.933^15` ≈ **35 %** — better than a one-in-three coin. **It does not undermine the migration verdict, which rests on rows, cadence and zero DEGRADED. It does mean this line is not a zero-eviction baseline and must not be cited as one.** See [`seat-handover-2026-08-24.md`](seat-handover-2026-08-24.md) §1.1a.
>
> ~~⛔ **THE FIRST TASK IS NOW §0.4 of [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) — verify the `collector.ps1` rollback fix against a DELIBERATELY FAILING deploy, on the RETIRED box, never production.**~~ ✅ **DONE — EXECUTED AND PASSED 2026-08-24.** The fix (`30ca04d`) is proven: `Invoke-Rollback` used to restore over a RUNNING app, so the restore aborted on a file lock and the box ended with TWO collectors — **`Get-Process` returns an array and four sites treated it as scalar; only one threw, the other three failed silently.** ⛔ **The line *"the rollback path has still never completed successfully and `robocopy /MIR` has still never executed"* is SUPERSEDED — both ran successfully for the first time ever** ([`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) §0.5, executed 2026-08-24). ⚠ **The test target was the old box, which is now terminated** — re-testing this path again needs `ami-0247c8b7275de49ac` relaunched with the instance profile named above.
>
> ⚠⚠ **`_evalCache` HAS A CLOCK ON IT NOW — recorded 2026-08-23 UTC. The code did not change; the BOX did.**
>
> **Confirmed from source, not inherited:** `_evalCache` (`LivePerformanceTracker.vb:118`) is **UNBOUNDED**. Mutations are `LoadEvalCache` / `AddRange` / `Add`; there is **no `Remove`, `RemoveAt`, `RemoveRange`, `Clear`, `Take` or `Skip` anywhere**. `AggregateRange` filters at READ time only. This upgrades [`seat-handover-2026-08-22.md`](seat-handover-2026-08-22.md) §4 from an assertion to a verified fact.
>
> ⛔ **What changed is the headroom, not the growth.** §4's "years of headroom on t3.small; it blocks nothing" was true — measured against **~496 MB free on a 2 GB t3.small**. The collector now runs a **1 GB t2.micro with ~150 MB free**. Same growth, roughly one-third the room.
>
> **Measured:** the eval-cache FILE grows **0.29 MB/day** (3.83 → 4.1 MB over 22.5 h), independently matching §4's ~0.33 MB/day prediction. ⚠ **The IN-MEMORY rate is NOT measured** — §4 estimates the list at ~2× the file per entry, and two production readings of app private (74.3 → 71.1 MB over 21 h) went *down*, so GC noise dominates at this timescale. **On that unmeasured 1×–2× range the runway is roughly 8–17 months**, against "years" previously. **Treat the t2.small resize as a WHEN, and treat any specific date as unproven until someone measures the in-memory rate over weeks.**
>
> ⛔ **THE LANDMINE IN §4 STILL STANDS AND IS NOW ON A TIMER.** `WriteEvalCache` rewrites the **entire** file with `StreamWriter(path, append:=False)` iterating `_evalCache`, from **five** call sites. **Trimming the in-memory list truncates `analysis_eval_cache.csv` on the next write** — the file Kelly and F1 both read. **Decoupling the list from the file IS the work, and it must come first.** ⚠ **NO SPEC EXISTS.** It is no longer "years away, blocks nothing"; it is a dated item with a destructive obvious-fix.
>
> ~~⚠ **NEW LATENT DEFECT, recorded 2026-08-23** — `UI/MainForm_Analysis.vb:613` writes `analysis_log.csv`'s `Timestamp` column from **`DateTime.Now` — LOCAL time**, while session bucketing resolves on `DateTime.UtcNow.Hour`. ⛔ **This makes "collector hosts run UTC" a hard constraint, and it is a live risk for the Linux CLI port.**~~ ⛔⛔ **REFUTED 2026-08-25 — THE BOOK IS UTC AND THIS IS NOT A TRADER DECISION.** `AnalysisLogger.vb:180` writes the column from **`DateTime.UtcNow`**; `AnalysisLogger.vb` never reads `VerdictResult.Timestamp` at all. Eval cache and bridge are UTC too. **`verdict.Timestamp` reaches only the rendered `TIME` line and the output dump — both display.** ⚠ **So session bucketing and the stamped hour are the SAME clock**, and changing a box's timezone would shift **zero** book rows. **Full refutation: [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) §7.0; summary closed at [`seat-handover-2026-08-24.md`](seat-handover-2026-08-24.md) §2.1.** ⚠ *"Collector hosts run UTC"* stays sound practice but **must be re-grounded** — the reason on file is not real, and it was relayed to the hostel-app seat on that basis. ⭐ **§7.6's taxonomy survives; line 613 is a DISPLAY site, the class it says not to sweep up.** ⭐ **§7.6 carries the RANKED taxonomy this produced** — persist > transmit >> elapsed / display — validated across two independent codebases. **A blanket ban on `DateTime.Now` is the wrong rule: it buries the 3 real defects among 19 calls, 16 of which are correct as written.** **Found because the hostel app's seat asked a timezone question, not by any test.**
>
> ⚠⚠ **The v68 auto-run defect is the reason this queue's `collector.ps1` rows read the way they do.** v68 as first shipped collected **exactly one analysis row per process start** — and the deploy acceptance gate, which required only ONE new row, **certified that as healthy.** Both are fixed (`9e9fe33` engine, `9eb3329` gate) and both are proven live by a controlled V1/V2 pair. **Any row below that cites the one-row gate as the acceptance criterion is superseded.**
>
> **Authoritative state read: [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md).** §0a below is still correct that **nothing is owed by the TRADER** — the migration was execution, not a decision.

---

## 0. Orientation — what each doc is authoritative FOR

**The failure this table fixes is not that docs go wrong — it is that "authority" was never scoped.** Nothing below is demoted; each is pinned to what it actually governs.

| Doc | Authoritative for | **Not** for |
|---|---|---|
| ⛔⛔ **`collector.ps1` FIX 8 + FIX 9 — the first real `deploy` FAILED SAFELY and exposed two defects, one of which would SILENTLY STOP PRODUCTION COLLECTING** | FIX 8 small; **FIX 9 matters for the cutover** | **FIRST LIVE `deploy`, test box, 2026-08-22. ⭐ THE SAFETY MACHINERY WORKED — nothing was lost.** Pre-flight passed · plan printed with hashes · y/N answered · app stopped · **backup written and ALL SIX ITEMS VERIFIED PRESENT (FIX 2 works)** · uploaded to S3 · **hash check CAUGHT the failure** · rollback restored · **`Start-RemoteApp` relaunched the REAL engine binary into SESSION 2** — the single thing I predicted was most likely to fail, and it did not. ⚠ **FIX 8 — the place step reports success it never checked.** Its remote commands call bare `aws s3 cp`; **`aws` does NOT resolve in an SSM session** — measured: *"The term 'aws' is not recognized"* — because **the SSM agent process PATH predates the CLI install** (`C:/Program Files/Amazon/AWSCLIV2/aws.exe` exists; the agent just cannot see it). Every cp failed to stderr, `$ErrorActionPreference` is `Continue` remotely, and **`'PLACED=done'` printed unconditionally** — so the script announced *"placed on box"* over six files it had not written. ⭐ **The hash check caught it exactly as TRAP 3 intended, and the proof is elegant: the three files that MATCHED (`deps.json`, `runtimeconfig.json`, `OFL.txt`) are precisely the three that are byte-identical between v67 and v68; the three that MISMATCHED (`exe`, `dll`, `settings.json`) are precisely the three that differ.** **FIX 8 = (a) check each remote `aws` exit code and emit `PLACED=done` only on all-success — THIRD instance of the marker-vs-property defect after FIX 2 and FIX 7 — and (b) invoke `aws` by FULL PATH on the box, or refresh PATH from the machine environment.** ⚠⚠ **FIX 9 IS THE SERIOUS ONE AND IT IS NOT ABOUT THIS BOX.** The rollback restored v67, relaunched it, reported `OK relaunched` and exited — **but v67 has no `auto_run.start_engaged`, so the app came back RUNNING AND STOPPED.** Measured: **3.4 minutes past a 3-minute cadence with no new row.** **The rollback verifies the PROCESS started, never that COLLECTION resumed.** ⛔ **On production — which is on v66 and equally has no auto-run — ANY rollback would leave the collector running but IDLE, reporting success, with tape quietly not being captured.** **FIX 9: the rollback must run the same new-CSV-row gate it uses for a successful deploy.** ⚠ Q2 deliberately skipped the gate on the rollback path to avoid a 5-minute wait; **that trade is wrong, and this run is the proof.** |
| ⭐ **t2.micro RULED VIABLE — 5 readings + a 30-sample sweep. ⚠ And my gap-repair explanation for the 24 h spike is REFUTED, not confirmed** | Read complete; trader's call | **FINAL READ 2026-08-22, app up 42.03 h, 30 consecutive samples at 10 s intervals (`ssm-repairwatch.json`).** ⭐ **`pagefile` 39.6 % on ALL THIRTY samples — not one tick of movement. `pagesOUT/s` 0.0 on all thirty. `pagesIN/s` 0.0 on 29 of 30 (one 57.0).** ⛔⛔ **THE `pagesOUT/s` 0.0 IS CORRECTED 2026-08-24 — IT WAS NEVER A PROPERTY, IT WAS A 1-IN-8 DRAW.** Two independent 30-sample sweeps on 2026-08-24 each returned **2 of 30 non-zero** (bursts 169–468 pages/sec). ⚠ **The two readings do NOT conflict — the arithmetic is the finding:** at the measured **6.7 % of samples**, `P(all 30 zero)` = `0.933^30` ≈ **12.6 %**. **This sweep drew the 1-in-8 and the result was written down as "0.0 on all thirty", which reads as a property.** ⚠⚠ **The verdict below — VIABLE — is NOT overturned by this** (the box still does the same work, page file still flat, no DEGRADED); what is overturned is the *baseline number a future change gets judged against*, which had already been handed to the hostel-app seat as a stop condition. **Full correction and the three falsifiable trigger numbers: [`seat-handover-2026-08-24.md`](seat-handover-2026-08-24.md) §1.1a.** ⛔ **Never quote a pagesOUT MEAN — quote the non-zero count and the max.** avail 174–208 MB oscillating with no trend; app private sawtooths 66.8–80.7 MB with a visible GC at 09:45:06. **Work unaffected throughout: 38.6 rows/hour against production's 38.3, 1.0 min behind, ZERO DEGRADED events in 42 h.** ⚠⚠ **THE SWEEP REFUTES MY OWN EXPLANATION OF THE 24 h SPIKE.** I attributed the 67.3 % page file / 1,321 pagesIN to a gap-repair pass. **A repair triggered at 09:41:45 UTC, ~100 s before this sweep began — and the sweep shows NOTHING.** Store growth over the 5 minutes was **50,562 bytes**, which at ~1.4 trades/s × 300 s × ~120 B/row is **streaming capture almost exactly**; no backfill signature at all. ⭐ **The likely truth is better than my hypothesis: a routine repair on a HEALTHY box finds no holes and costs essentially nothing.** So **the 67.3 % reading remains UNEXPLAINED** — but it has not recurred across three later readings plus thirty samples, so it is a one-off transient of unknown cause, not a creep. ⚠ **Recorded rather than quietly dropped: that is the FOURTH time this session I offered a tidy explanation for a surprising number and the measurement did not support it.** ⭐ **CPU is a non-issue, measured: credit balance PINNED at 144.0 (the t2.micro maximum) for 12 h straight with one momentary dip — the app spends less than the box earns, so no throttling risk ever. Peak CPU 52.5 % of 1 vCPU vs production's 26.2 % of 2 — identical absolute work.** ⛔ **t3.micro would buy nothing; the second vCPU would sit idle.** The only thing it buys is peak headroom (~2x vs ~4x), and at 52 % peak with full credits that is not the thin dimension — **memory is.** **VERDICT: VIABLE. ~206 MB sustained headroom against production's 496 — it works, with less margin.** |
| ⛔ **SPEC NEEDED (not written) — decouple `LivePerformanceTracker._evalCache` from `analysis_eval_cache.csv` before ANY trim** | Spec first, then a build. ~~**NOT urgent — years of headroom**~~ ⛔ **SUPERSEDED 2026-08-24 — roughly 8–17 MONTHS, and the cell contradicted this document's own state banner.** **The code did not change; the BOX did** — "years" was measured against ~496 MB free on a 2 GB t3.small, and production is now a 1 GB t2.micro with ~150 MB free. **Measured: the eval-cache FILE grows 0.29 MB/day.** ⚠ **The IN-MEMORY rate is NOT measured** — four production readings ran 74.3 → 90 → 71.1 → 96 MB, noisy with no trend. **Do not derive a date from them.** Treat the t2.small resize as a WHEN | **TRADER-QUEUED 2026-08-22 as a spec-to-be-written, deliberately deferred.** ⛔⛔ **THE OBVIOUS FIX IS DESTRUCTIVE AND I WROTE IT INTO THIS QUEUE MYSELF BEFORE CHECKING.** `WriteEvalCache` (`LivePerformanceTracker.vb:1325`) rewrites the ENTIRE file with `StreamWriter(path, append:=False)` iterating `_evalCache`, from **FIVE call sites (:387, :417, :773, :844, :1076)**. **Trimming the in-memory list truncates `analysis_eval_cache.csv` on the very next write — destroying the eval history the Kelly ladder and the F1 read both depend on.** Verbatim this project's own lesson: *"`append:=False` over a whole file from a partial window is how it becomes destructive."* **The list and the file are THE SAME THING by construction; decoupling them IS the work, and it is the spec's first job — not the trim.** ⚠ **TERMINOLOGY, because it has been called a leak: IT IS NOT A LEAK.** Measured twice — handles **576 vs 548** and threads **13 vs 13** across a **4x process-age gap**, then **540 handles at 41.7 h**, i.e. DECLINING. It is **ONE unbounded static cache** growing with the BOOK at **~0.33 MB/day (~120 MB/yr)** — `EvalCacheEntry` is 8 small fields, so 44,047 entries is **~8 MB, not the 46 MB I first attributed to it**. ⚠ **`_ohlcLookup` is NOT part of this** — both boxes hold ~10,500 rows, so the 7-day cache is genuinely bounded. **WHAT THE SPEC MUST DECIDE:** (a) make `WriteEvalCache` MERGE with what is on disk (the trade-store fix's pattern), or (b) stream `AggregateRange` from file instead of memory — ⚠ **and either way the reclassification passes at `:411` and `:1052` deliberately need the full set in memory to rewrite correctly, so they must be handled EXPLICITLY rather than incidentally.** ⭐ **WHY IT STILL MATTERS DESPITE THE LOW URGENCY: `LivePerformanceTracker.vb` is host-agnostic, so the CLI PORT CARRIES IT ACROSS UNCHANGED.** The trader predicted exactly this before it was measured. **It should land BEFORE the port rather than be inherited by it — but it blocks nothing now, and rushing it is how the eval history gets destroyed.** |
| ⭐ **START HERE — [`seat-handover-2026-08-22.md`](seat-handover-2026-08-22.md) is the STATE read for the collector migration** | Read first | **Settings v68, pushed. §0 is the first task: the four-step migration (memory readings → AWS CLI install → v68 deploy to the TEST box → weekend cutover), in an order that must not be changed.** ⭐ **Carries the simplification that saves a risky step: production NEVER needs a v68 deploy** — the test box becomes production already on v68, so the box holding 57.8 MB of irreplaceable tape is never stopped for one. ⚠ **Its §2 separates what was PROVEN BY EXECUTION (`status`, `fetch`, `deploy -DryRun`, all three pre-flight gates, v67's 24 h soak at 919-vs-920 rows) from what has NEVER RUN (`deploy` steps 4–9, where FIX 1 and FIX 2 both live; `Start-RemoteApp` with the real binary).** ⛔ **Its §4 is a landmine I planted and defused — do not "just trim" `_evalCache`.** **Its §5 carries the three lessons: two points is not a trend (I was wrong 3/3 in one day); execution finds what parsing structurally cannot (3/3 defects); a marker you print is not a property you checked.** |
| ⭐ **THE APP PLATEAUS — settled by an 18-hour series, not two points. `tools/mem-probe.csv` was sitting untracked in the repo the whole time** | Read done | **The trader asked: "there must be a point where it plateaus as it shouldn't keep growing forever, if not the Linux box will be affected too." ANSWER: IT DOES, and the evidence already existed.** `tools/mem-probe.csv` (untracked, dated 2026-08-11, UTF-16, 1,209 rows) is a **21-hour memory series** from 2026-08-10T17:47 to 2026-08-11T14:36. Hourly means of the in-use column: **2209 -> 2219 -> 2243 (peak) -> 1998 (release, almost certainly a restart) -> 1832, and then EIGHTEEN CONSECUTIVE HOURS between 1,828 and 1,884 MB.** Available memory rose over the same window, **216 -> 528 -> 690 -> 671 MB**. **That is a plateau with ±30 MB of noise, not a ramp.** ⚠⚠ **AND IT RETROACTIVELY CONVICTS ALL THREE OF MY TREND ALARMS AS NOISE** — app growth "121 -> 134 MB" (next reading 113.7), page file "67.3 % and climbing" (next reading 39.7 %), and the implied ramp behind both. **Three for three, same error: a fluctuating metric, two samples, a direction asserted. The correct instrument was an 18-hour series, and it was already in the working tree.** ⚠ **THE ONE THING AN 18-HOUR WINDOW CANNOT SEE, so both findings stand together without conflict:** `LivePerformanceTracker._evalCache` grows with the BOOK at ~0.33 MB/day, which is invisible across 18 hours and only shows over months. **SHORT-TERM: plateaus, measured. LONG-TERM: one genuinely unbounded component remains, and it still survives the port.** ⚠ **The series is from the v65/v66 era, before v67/v68 and before the trade store had accumulated — so re-measure on the current build before treating the absolute numbers as current; the SHAPE is the finding, not the level.** ⛔ **`tools/mem-probe.csv` is UNTRACKED and was nearly lost — it blocked the deploy pre-flight and my first instinct was to move it aside. COMMIT IT OR IGNORE IT DELIBERATELY; do not leave a 21-hour measurement series as an untracked file.** |
| ⚠ **`collector.ps1 fetch` FIX 7 — the box-side manifest RACES the live collector, so `fetch` fails on EVERY run** | One-line-ish: snapshot before manifesting | **FOUND BY THE FIRST LIVE `fetch`, 2026-08-21 — the transfer itself is SOUND; the verification is not.** `Invoke-Fetch` builds a manifest on the box by reading `(Get-Item $f).Length` and then counting lines with `ReadLines`, **on files the collector is actively appending to**, and only afterwards uploads to S3. **The three reads happen at three different instants.** Measured: box reported `analysis_log.csv` at **21,943,640 bytes / 23,847 lines**; the download arrived at **21,944,609 bytes / 23,847 lines — SAME line count, +969 bytes.** ⭐ **The manifest was the torn read, not the transfer:** `Length` caught the 16:24:01 row mid-write, `ReadLines` then counted it whole, and the S3 upload (later still) captured the completed row. **The downloaded file is COMPLETE and CORRECT — it is strictly better than what the manifest describes.** ⛔ **But `fetch` prints `FAIL ... SIZE MISMATCH` and `treat this fetch as INCOMPLETE`, and exits 1 — a false failure on a healthy transfer, the SAME CLASS as the `Wait-DeployGate` bug (a correct operation reported as catastrophe).** ⚠⚠ **And it is not intermittent: a 24/7 collector always appends, so this fails EVERY time except by luck in a quiet gap.** The store matched exactly this run only because its buffered flush happened not to land in the window — **same defect, it just did not fire.** **FIX: snapshot on the box FIRST, then manifest the snapshot, then upload the snapshot** — that makes manifest == uploaded == downloaded by construction, and also removes the torn-read risk on a growing file. Costs ~80 MB transiently on a 30 GB disk. ⚠ **A `local >= box` tolerance is the cheap alternative and is NOT recommended — it papers over torn reads instead of preventing them.** ✅ **The run still produced a GOOD copy-back — 23,846 rows (+4,862 over the 2026-08-10 copy), same first timestamp so a strict superset, plus the full 57.8 MB AWS tape. First copy-back since 08-10; W6-4 re-run and the coverage report are unblocked.** |
| ⚠ **`LivePerformanceTracker._evalCache` IS genuinely unbounded — and it SURVIVES THE LINUX PORT. (My "13 MB/day growth" alarm is RETRACTED — that was GC noise.)** | ⛔ **NOT SMALL — the obvious fix is DESTRUCTIVE (see body). Needs a spec; NOT urgent** | **PROFILED 2026-08-21 via SSM, both boxes, same probe.** ⛔ **RETRACTION FIRST: I flagged production's app private going 121 -> 134 MB in a day as a possible trend. It is NOT. One hour later the same metric read 113.7 MB — DOWN 20 MB.** Two points on a GC sawtooth, read as a line. **A number that goes DOWN is neither a leak nor unbounded growth, and I should have said "two points is not a trend" and stopped rather than raising it.** ⭐ **AND IT IS NOT A LEAK, measured:** production's process is **4x older (95.5 h vs 24.2 h) with 25x the book (23,814 vs 932 rows)** and carries **576 handles vs 548, and 13 threads vs 13.** A leaking app climbs into the thousands. **Flat handles + identical thread count across a 4x age gap is a clean bill.** ⚠⚠ **BUT THE TRADER'S UNDERLYING POINT IS CORRECT AND SURVIVES THE RETRACTION.** `LivePerformanceTracker.vb:118` — `Private Shared _evalCache As New List(Of EvalCacheEntry)()` — is **static, one entry per evaluated hold-window, and NEVER trimmed.** Production holds **44,003 eval rows against 23,814 log rows** (~2 entries per row, one per hold window), so it grows at roughly **2x the row rate**. Measured private-memory delta between the two boxes is **46 MB** across a 22,882-row book difference — consistent with a book-proportional component of order **0.5 MB/day** at the current ~920 rows/day. ⚠ **That is unbounded and it never plateaus** — ~250 MB/year on the present trajectory. **Timescale: YEARS on t3.small's 428 MB headroom, MONTHS on t2.micro's ~112 MB.** ⛔ **AND THE CLI PORT DOES NOT FIX IT — `LivePerformanceTracker.vb` is host-agnostic and the port carries it across unchanged.** The trader predicted exactly this ("the Linux box will be affected too") before it was measured. **`_ohlcLookup` is NOT a concern — both boxes hold ~10,000 OHLC rows, so the 7-day rolling cache is genuinely bounded.** ⛔⛔ **DO NOT "JUST TRIM IT" — I WROTE THAT HERE FIRST AND IT IS WRONG. `WriteEvalCache` (LivePerformanceTracker.vb:1325) rewrites the ENTIRE file with `StreamWriter(path, append:=False)` iterating `_evalCache`, and it is called from FIVE sites (:387, :417, :773, :844, :1076). Trimming the in-memory list TRUNCATES `analysis_eval_cache.csv` on the very next write — permanently destroying the eval history the Kelly ladder and the F1 read both depend on.** ⚠ **This is verbatim the project's own recorded lesson: *"`append:=False` over a whole file from a partial window is how it becomes destructive."* The memory list and the file are THE SAME THING by construction; decoupling them IS the work, and it is not small.** **A safe fix means either making `WriteEvalCache` MERGE with what is on disk (the trade-store fix's pattern) or streaming `AggregateRange` from file instead of memory — and the reclassification passes at :411 and :1052 deliberately need the full set in memory to rewrite correctly, so they have to be handled explicitly rather than incidentally.** ⚠ **AND THE URGENCY IS LOWER THAN I FIRST SAID.** `EvalCacheEntry` is 8 small fields; at production's 44,003 entries that is **~8 MB by structure estimate, not 46**. At ~1,840 entries/day the growth is **~0.33 MB/day, ~120 MB/year — YEARS of headroom on t3.small.** ⛔ **The 46 MB cross-box delta is NOT cleanly attributable to `_evalCache`: production's own private memory swung 134 -> 113.7 MB in ONE HOUR, so a 46 MB cross-box gap sits inside GC sawtooth range. I over-attributed twice on this item; the CODE FACT (unbounded, survives the port) is solid, the SIZE ATTRIBUTION is not.** **Verdict: spec it, do not build it ad hoc, and do not let it block anything this week.** ⚠ **NOT CHECKED: the .NET CLR memory counters are unavailable on both boxes**, so heap-vs-native was not separated — the book-proportional attribution is inference from the two-box delta, not a heap read |
| ⚠ **t2.micro 24-HOUR TEST COMPLETE — FUNCTIONAL PASS, MEMORY MARGINAL. The decision is a judgement, not a measurement gap** | Read done; the trader's call remains | **MEASURED 2026-08-21 via SSM, both boxes, same probes, neither with an RDP session attached. App up 23 h 56 m across THREE recurring 6-hourly gap repairs.** ⭐ **FUNCTIONAL: PASS, and not close — 919 rows in 24 h on t2.micro/Server-2019 against 920 on t3.small/Server-2025.** Per-hour min/avg/max 19/36.8/61 vs 20/36.8/61; 22.5 MB of tape captured; **ZERO DEGRADED events**; 0.1 min behind now. **The small box does the same work.** ⚠⚠ **BUT THE EARLIER "not thrashing" READ WAS WRONG AND IS CORRECTED HERE: `Pages Output/sec` 0.0 does NOT mean healthy — it means everything evictable has ALREADY been evicted.** The live signal is the other direction: **`Pages Input/sec` 1,321 avg / 1,866 max on the test box against 0.7 / 8.5 on production — ~1,900x.** ⛔ **NOT probe noise: production ran the IDENTICAL probe and returned 0.7**, which is what kills the mapped-file-read caveat that made the first reading look benign. **The box has settled into continuously RE-READING pages it cannot keep resident.** ⚠ **Page file 36.6 % (1 h) -> 67.3 % (24 h), still climbing and not plateaued — two points, wrong direction, and it is the one metric that could still end this.** Disk is NOT the constraint (1,321 pages/s = ~5 MB/s against gp3 3,000 IOPS). App squeezed to **63 MB private vs production's 134 MB**. **THE DECISION IS NOW A JUDGEMENT — $15/month against an unsupported memory configuration with ~112 MB headroom and no margin for a volatility spike, when the trade buffer grows fastest.** ⚠ **SEPARATE WATCH, found in the same read: production's app private grew 121 MB -> 134 MB in one day** (and `roadmap.md` still records 86 MB). Probably cache warm-up plateauing, but **on any box with ~100 MB headroom the TREND matters more than the reading — take a third measurement before treating either number as settled.** ⛔⛔ **SUPERSEDED AT +25 h — THE METRICS REVERSED AND MY "STILL CLIMBING" READ WAS WRONG.** Measured 2026-08-21 at app-up **25 h**: **avail 222 MB (was 74) · sustained avail 220 (was 112) · pagesIN/s 7.7 avg / 49.6 max (was 1,321 / 1,866 — a 170x DROP) · pagefile 39.7 % (was 67.3 % — DOWN, not up) · eviction still 0.0.** Work unaffected: **955 rows, 38.9/hour, 0.3 min behind.** ⚠ **The likely cause of the alarming 24 h reading is that it was taken during or near a gap-repair pass**, which allocates heavily for a 20 h trade window; the box has since released it. ⚠⚠ **THAT IS THE SECOND TIME IN ONE DAY I INFERRED A TREND FROM TWO POINTS AND GOT THE DIRECTION WRONG** — first the app-growth "121 -> 134 MB" (next reading 113.7), now the page file "67.3 % and climbing" (next reading 39.7 %). **The standing lesson is not about memory: on a metric that fluctuates, TWO POINTS IS NOT A TREND, and saying so is the whole discipline. Take three readings spanning a known-quiet interval before calling any direction.** **On this reading t2.micro looks VIABLE, and that claim carries the same two-point caveat as the alarm it replaces — a third reading, taken away from a repair pass, is what settles it.** Probes: `ssm-mem.json`, `ssm-apphealth.json`, `rows24.json` |
| ⚠ **`roadmap.md`'s box-footprint figures are STALE — the app has grown ~40 %, and the PORT'S BUSINESS CASE IS QUOTED ON THEM** | Trivial doc edit; the measurement is already done | **MEASURED 2026-08-21 via SSM on both live boxes, no RDP session inflating either.** [`roadmap.md`](roadmap.md) records **"~1474 MB Windows / 86 MB app"** from the 08-11 database-migration analysis. **Production t3.small / Server 2025, disconnected session, 3-day uptime: total 1,894 MB · in use 1,483 MB · app 144 MB working set / 121 MB private.** ⚠ **The app is now ~121-144 MB, not 86 MB** — v64 trade-store buffering and the 7-day OHLC cache are the plausible causes and both are legitimate, but **the number moved AGAINST the small-box case and the port's cost argument is quoted on the old one.** Windows-alone is ~1,340 MB on 2025. **Measured comparison, same probe, same conditions:** Server **2019** on t2.micro runs Windows-alone at ~**894 MB** — **~350 MB lighter than 2025, a real and previously unmeasured saving.** ⭐ **THE INSTRUMENT THAT ACTUALLY SEPARATES THE BOXES IS `MemoryPages Output/sec`, NOT memory-in-use** — in-use contracts on a small box regardless and proves nothing; **eviction pressure is the discriminator. Production: 0.0 avg / 0.0 max. t2.micro: 52.0 avg / 779.7 max.** ⚠ **`Pages Input/sec` is NOT usable — it counts mapped-file reads, so an SSM probe generates the faults it reports** (a single sample on production read 60.2/s while eviction was flat zero). Reusable probes committed: `ssm-mem.json`, `ssm-apphealth.json`. **Update the roadmap figures and name the date measured** |
| **`DeribitWsFeed` never verifies the subscribe reply — it logs the REQUESTED channel count** | ⛔ **NOT TRIVIAL — re-sized 2026-08-25, this cell read "Trivial" and it is wrong.** `SubscribeAsync` is **fire-and-forget**: it awaits `SendJsonAsync` and returns `Task`, and the reply arrives **asynchronously in `ReceiveLoopAsync`**. There is no reply to compare at the call site. Verifying it needs **request-id correlation plumbing that does not exist**, on the **live WS feed path** — the engine's data source. ⚠ **The "one line" framing came from the FIX description (compare the reply array), not from what it takes to GET the reply.** **Sonnet, effort medium**, and the row's own standing advice still holds: **bundle it into the next commit that opens `DeribitWsFeed.vb` for another reason; do not give it a session of its own** | **NEW 2026-08-20, found by probing Deribit live.** `DeribitWsFeed.vb:208` logs `"subscribed to " & Channels.Length & " channels"` — the **requested** count. `SubscribeAsync` does not compare the reply's `result` array against the requested list. ⚠ **MEASURED, not hypothesised: `book.BTC-PERPETUAL.none.10.raw` returns `{"result":[]}` with NO error** — Deribit silently drops an unrecognised channel form rather than rejecting the call. So a mistyped or unsupported channel string would log as **subscribed** while its stream never starts. **Blast radius is bounded and it fails visibly in aggregate** — a dead book channel starves `WsMarketDataSource`'s per-shape staleness gate, which returns `Nothing`, and the skip gate then fires `"order book unavailable"` on **every** run. **The cost is diagnosis, not data: the operator sees total skips and a log line saying all channels subscribed.** **FIX: compare the reply `result` array to `Channels` and log the DIFFERENCE, not the count.** Same class as the standing rule that a verification handle must assert the property rather than count a string that mentions it. **Model: Sonnet, effort low** — bundle into the next `DeribitWsFeed` commit, do not give it a session |
| [`trader-tick-queue.md`](trader-tick-queue.md) *(this doc)* | **What is outstanding, in what order, and its gates.** §0a answers "what is owed" at a glance; §1b is the dated shipped-state sweep | Dependencies · decision text · strategic altitude |
| [`roadmap.md`](roadmap.md) | **The strategic board** — every workstream, done and outstanding, at altitude; execution order; the **binding sequencing rules** (§5) | The immediate slice — it points here |
| [`backlog-dependency-map.md`](backlog-dependency-map.md) | **What blocks what** — edges, and a pointer to where each item's state lives | **State. It carries none, deliberately** (restructured 2026-08-07 after 7 stale cells) |
| [`seat-handover-2026-08-25.md`](seat-handover-2026-08-25.md) | ⭐ **THE current seat handover — the STATE read.** Its §2 carries the OUTSTANDING ITEMS table; its §0 is the first task (#5, C1-coverage F1) | A task list beyond §0; it points here |
| [`seat-handover-2026-08-24.md`](seat-handover-2026-08-24.md) | **Superseded for STATE 2026-08-25.** Its §1.1a is the authoritative memory-baseline detail; ⛔ **its §2.1 is CLOSED, not open** ⭐ **Its §0 says there is NO first task; nothing is blocked.** Its two open items are trader DECISIONS, not builds: the book's local-time `Timestamp` column, and `_evalCache` | A task list — it has none. ⛔ **Not the authoritative DETAIL** — it deliberately does not repeat [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md), which remains authoritative for everything it summarises |
| [`seat-handover-2026-08-23.md`](seat-handover-2026-08-23.md) | **The authoritative DETAIL behind the 08-24 summary** — §7 the local-time taxonomy, §6 `_evalCache`, §8 the AMI relaunch parameters | Current state — superseded 2026-08-24 |
| [`seat-handover-2026-08-14.md`](seat-handover-2026-08-14.md) | Its **rulings and conventions**, still binding | ⛔ **Current state — superseded, now three times over (08-22, 08-23, 08-24).** ⚠ **This row read "THE current seat handover" until 2026-08-24, naming the absorption proposal as the first task** — that task is still open but is **no longer the top item**, and the row was ten days stale in the table that exists to say which doc governs what |
| [`seat-handover-2026-08-12.md`](seat-handover-2026-08-12.md) | **Superseded for STATE 2026-08-14; its rulings and conventions still bind.** Its §0 first task (the downtime-repair spec) is **DONE, shipped and deployed** | Current state |
| [`seat-handover-2026-08-10.md`](seat-handover-2026-08-10.md) | **Superseded for STATE 2026-08-12; its rulings and conventions still bind.** Its §0 first task (the D3 ASIA watch) is **DONE and PASSED** | Current state |
| [`seat-handover-2026-08-05.md`](seat-handover-2026-08-05.md) | Its **rulings and conventions**, still binding | **Current state — superseded, now twice over (2026-08-10, then 2026-08-12).** Also not a task list; it points here instead, deliberately. *(Row repaired 2026-08-13 — it carried FOUR cells in a three-column table, so it rendered wrong in the doc everything else points at.)* |
| [`seat-handover-2026-08-02.md`](seat-handover-2026-08-02.md) | Its **rulings and conventions**, all still binding | Current state — superseded |
| [`seat-handover-2026-07-18.md`](seat-handover-2026-07-18.md) §3 | **Standing rules — all still binding** | Current state · its §2 queue is spent |
| [`fable-seat-close-handover-2026-08-01.md`](fable-seat-close-handover-2026-08-01.md) | The **2026-07-31 rulings** (J-B/J-C/J-E/D-F) | Its §4 task list and §1 state — read [`seat-close-handover-gap-audit-2026-07-31.md`](seat-close-handover-gap-audit-2026-07-31.md) beside it |
| each spec's own D-table | **The decision text** — where this queue disagrees, the spec wins | — |

**Superseded for state, still current for rules:** `seat-handover-2026-07-18.md` (§3 binds), `fable-continuation-2026-07-23.md`. Neither should be cited for what is outstanding today.

---

> ## ⛔⛔ READ BEFORE THE KELLY READ — THE COLLECTOR'S BOOK ROTATED 2026-09-01 AND A NAIVE COUNT UNDER-REPORTS BY 93 %.
>
> **Added 2026-09-03, BEFORE the ~09-08 read, deliberately — this is the "stale prose in the doc everything points at" failure, caught in advance instead of at the read.**
>
> **The absorption-instrumentation deploy (2026-09-01 15:49:02 UTC) added five CSV columns. The header changed, so `EnsureLogFile` rotated the book.** Nothing was lost. **But the weekday-STRONG count now lives in TWO files:**
>
> | File on `i-0d6c133058876273e` | weekday STRONG | measured 2026-09-03 |
> |---|---:|---|
> | `C:\DeribitEngine\analysis_log.csv` (live, post-rotation) | **39** | rows from `2026-09-01 15:50:01` |
> | `C:\DeribitEngine\analysis_log.csv.v0.7.bak` (pre-rotation) | **337** | 33,911 rows |
> | **TRUE TOTAL** | **376** | ✅ no overlap — the rotation split 09-01 cleanly at 15:49/15:50 |
>
> ⛔ **Counting `analysis_log.csv` alone gives **39** against a ≥406 trigger. That is a **90 % apparent collapse** and it is PURE ROTATION ARTEFACT.** ⚠ **The `.bak` name carries no timestamp** — there was no pre-existing `.bak`, so it took the hardcoded `v0.7` string. **That name is wrong for a v0.8 book and is a queued decision in [`absorption-instrumentation-spec-back.md`](absorption-instrumentation-spec-back.md) §2 — do not "tidy" it before that is ruled.**
>
> ⭐ **STATE OF THE TRIGGER, measured not projected: 376 against ≥406. Shortfall 30.** Recent weekdays: 08-28 = 23 · 08-31 = 4 · 09-01 = 11 · 09-02 = 14 · **09-03 = 21 and still running**. **At the recent ~14/weekday, the rest of 09-03 plus 09-04, 09-07 and 09-08 clears 406 — realistic ~2026-09-07 to 09-08.** ⚠ **RE-MEASURED 2026-09-03 17:54 UTC; the figures above are that read.** ⛔ **A count taken on any later day is a DIFFERENT number — re-run the two-file count, never quote this one as current.**
>
> ⚠ **The 337 was cross-validated against the local copy-back `AWS-copybacks/aws-copyback-2026-09-01/analysis_log_aws.csv` — same number, independent read.**

> ## ✅ BOTH DECISIONS RULED AND BUILT 2026-09-03 — THIS BOX IS CLOSED.
>
> **Both rulings came from the trader the same day this box was written: 17 = MECHANISM, option (c); 18 = PER-SPAN.** Specs: [`fixture-literal-provenance-a14b-spec.md`](fixture-literal-provenance-a14b-spec.md) · [`coverage-trailing-split-span-spec.md`](coverage-trailing-split-span-spec.md). **Both are BUILT the same day** — item 17 at `verify/ordercheck/Program.vb:1084-1090`/`:1121-1125` (see the row in §2 below), item 18 at `tools/BacktestRunner/CoverageReport.vb` (`HourResult.TrailingMsForHour`, `ClassifySpan`, `ClassifyHour`, `BuildResult` — five new fixtures `A61a`–`A61e`). Harness `ALL PASS`, gate `PASSED`, both before/after proofs run (A61a fails pre-fix / passes post-fix; a real copy-back run showed no change, correctly, since its one trailing-edge hour is not split). **Full packet: [`queue-17-18-batch-spec-back.md`](queue-17-18-batch-spec-back.md) · [`queue-17-18-batch-summary.md`](queue-17-18-batch-summary.md).** *Original box text follows, kept for the record.*
>
> **§0a's own stated purpose is that *"what is actually owed" is not answerable at a glance* anywhere else. Two rows defeat it by carrying a decision inside a row that looks like a build, complete with a model-and-effort tag:**
>
> | Row | Its own words | Actually |
> |---|---|---|
> | **17** — `BuildResolutionCfg` stale fixture literal | *"**Do not just change the number.** First decide what the fixture asserts… **the decision above is the work, not the edit**"* | ⛔ **A DECISION.** SHIPPED BEHAVIOUR (derive from cfg) or MECHANISM (literal + a comment saying why)? |
> | **18** — `ObservedLongestTrailingMs` over-reports | *"**Decide first** whether the honest fix is per-span stats or simply not reporting the figure on split hours"* | ⛔ **A DECISION**, and its first build job is *"a fixture that produces it"* — the case has never been constructed |
>
> ⚠ **Both are tagged "Sonnet, low", which reads as ready-to-build.** **The tag costs the eventual build, not the decision.** ⛔ **Neither can be specced until it is ruled — a spec written today would be inventing the design decision, which the CLAUDE.md spec-first rule forbids.**
>
> ⚠ **Line drift on 17, so nobody greps the wrong place:** the row cites `verify/ordercheck/Program.vb:995`; the literal is at **`:1086`** as of 2026-09-03. **Grep the symbol `RocSlopeDeltaThreshold`, not the line.**

## 0a. Decisions genuinely unanswered — the short list

**§1 records history with strikethrough, so "what is actually owed" is not answerable at a glance. This is.** ✅ **Re-verified against the tree 2026-08-07** — re-verify rather than inherit.

> ## ✅ NOTHING IS OWED — `S2-2`'s D-table was ticked the same day it was raised, 2026-09-06 (UTC).
>
> ✅✅ **AND THE BUILD SLOT IS NOW CLOSED TOO — `S2-2` BUILT 2026-09-06 (UTC), all six §4b items.** Harness **328 → 332** (`A65a`–`A65d`); all six Release builds 0/0; `verify-gate.ps1 -Mode local-fast` **GATE PASSED**; settings unchanged, still **v68**. ⛔ **Display-string parity PROVED, not asserted** — the snapshot `Spread:` line, the breakdown note and both card bindings' resolved `(text, colour)` captured pre- and post-change over **eight book shapes incl. four degenerate**, **byte-identical, same MD5**. Packets: [`s2-2-calcspread-split-batch-summary.md`](s2-2-calcspread-split-batch-summary.md) · [`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md). ⚠ **`D-3` (b)'s residual is now LIVE and NAMED** — `HasTopOfBook` (3 conditions) and `CalcSpreadBps` (6) still disagree on a zero-priced top of book; that is the one open follow-up this build deliberately did not take. *(Superseded text follows, kept per the quote-and-label convention.)*
>
> ~~⭐ **It became a BUILD SLOT, not a closed item** — the standing warning at the top of this section applies again: *"a ruled row is a scheduled build, not a closed item."* Build from [`s2-2-calcspread-split-proposal.md`](s2-2-calcspread-split-proposal.md) **§4b**, never its §4.~~
>
> | # | Owed by | Note |
> |---|---|---|
> | ~~**`S2-2` — the `CalcSpread` split D-table**~~ | ✅ **TICKED IN FULL 2026-09-06, every row as recommended** — `D-1` (a) `Double?` · `D-2` (a) inside `ClassifySpread` · `D-3` (b) leave `HasTopOfBook` · `D-4` (a) delete `CalcSpread` · `D-5` (a) keep the names | **Spec: [`s2-2-calcspread-split-proposal.md`](s2-2-calcspread-split-proposal.md) §4b is the build list; §4 below is the record of how it was reached.** ⭐⭐ **It is a decision and not a build slot because writing it FALSIFIED the fix as specified** — the shape in [`a54a-session2-step1-measurement-2026-09-05.md`](a54a-session2-step1-measurement-2026-09-05.md) §9.2 (i) moves a rendered value on four surfaces (§3 of the proposal). **`D-1` how "no measurable spread" is signalled — my read `Double?`, the repo's own idiom (absorption `Double?`+`InvOpt`; `HourStoreStats.LastTsMs` re-ruled from sentinel to `Long?`) · `D-2` where the `"NORMAL"` degenerate answer lives — my read inside `ClassifySpread`, ONE seam · `D-3` whether `LiveMicrostructureEvaluator.HasSpread` moves onto the nullable — my read NO for this build (⚠ `HasTopOfBook` tests 3 conditions, `CalcSpread` tests 6; collapsing them is a real live-strip behaviour change and should not ride inside a refactor billed as zero-change) · `D-4` delete `CalcSpread` or keep a wrapper — my read DELETE (a wrapper is a third place the composition can drift; zero fixtures call it) · `D-5` naming — my read keep `CalcSpreadBps` / `ClassifySpread`.** ⛔ **Model Opus, effort HIGH** — upgraded from the queue's *"Sonnet, medium"*; it is the only item in the A54a arc where the display-string parity rule is live |
>
> *(Superseded banner text follows, kept per the quote-and-label convention.)*
>
> ~~## ✅ NOTHING IS OWED BY THE TRADER — as of 2026-08-12, every row in this table is ruled.~~
>
> **The last three closed on 2026-08-12:** **E5** absorption Path B (ticked — Path B) · the **F3 watch** (retired) · **C1-coverage F2**'s split-hour rule (ruled — split the hour). ⚠ **Every one of them turned into work, not into nothing** — see §2 for the absorption mechanism proposal and the F2 split build. **A ruled row is a scheduled build, not a closed item.**
>
> ⚠ **Do not read an empty decision table as an empty board.** §2 is where the work is, and [`trade-store-downtime-repair-proposal.md`](trade-store-downtime-repair-proposal.md) is authorised and ready to hand over.
>
> ⚠⚠ **`F3` NAMES TWO DIFFERENT WATCHES AND ONLY ONE WAS RETIRED — found 2026-08-12 while recording the retirement.**
>
> | Which `F3` | What it is | State |
> |---|---|---|
> | **B4b F3** | LONDON structural-**TARGET** inversion trigger; needs cap-bucket segmentation | ✅ **RETIRED 2026-08-12.** This is the one this queue's row meant |
> | **2026-07-02 audit-fixes F3** | EXIT GUARD strip vs HOLD\EXIT row corroboration **during holds** | ⚠ **NOT retired. State unverified — nobody has read it since v47** |
>
> ⚠ **[`roadmap.md`](roadmap.md)'s F3 row carries the FIRST one's status against the SECOND one's description.** That conflation is flagged in `roadmap.md` and **not fixed** — it needs someone to read the 2026-07-02 watch and say which the row tracks. **A third `F3` also exists as an ordinary finding ID in several spec-backs. Always name the document.**

| # | Owed by | Note |
|---|---|---|
| ~~**E5** absorption **Path B**~~ | ✅ **TICKED 2026-08-12 — PATH B** | ✅ **DECIDED, as the coordinator recommended.** **Anchors HOLD at their v61 provisional values** — `absorb_ratio`, `default.min_aggr_usd`, `max_pull_frac` and `depletion_floor_usd` all stay put, `scoring_enabled` stays **false**. ⚠ **Path A is DEAD — do not re-propose its V-table** (0.5 / 5 000 / 1.0). It is on file at [`absorption-anchor-rederivation-2026-07-30.md`](absorption-anchor-rederivation-2026-07-30.md) §5a and it projects **NY 0.000 %**, POOLED 0.064 %, and ~10–15 weekday-weeks to reach the n ≥ 30 activation gate. **No settings change, no version bump, and no ⚠ boundary in this pass.** ✅ **WHAT PATH B OWES — a new proposal file**, re-opening the book-absorption proposal's §8 residuals with the evidence that the v61 geometry rescale alone did not lift flag rates into the 3–8 % design band: **`window_sec` too short** · **episode-cumulative pressing** · **D8 `pullFrac` inflation on sparse `postLB`**. **That proposal is now a §2 build slot** — see the absorption mechanism-revision row. ⚠ **The reasoning, so it is not re-litigated:** no anchor set reachable from the observable v61 population lands NY in-band — loosening moves the problem sideways. The honest failure mode is *"the tracker does not observe enough real pressing"*, not *"the thresholds are one tick too high"*, which is the trader-profile conservative-false-positive posture applied correctly. **The §5 activation clock does NOT restart** — no knob turned, so the §12 watch convention's re-collection reset does not fire |
| ~~**F3 watch**~~ | ✅ **RETIRED 2026-08-12 (trader)** | ✅ **DECIDED — the watch is RETIRED, explicitly, not archived and not stranded.** ⚠ **`F3` here means the B4b post-ship watch item (4), the LONDON structural-TARGET inversion trigger — *"still under 45 % after ≥3 more LONDON session-days"*. It is NOT a coverage-report finding ID; see the note below.** **Why retired:** the trigger needs outcomes segmented by **cap bucket** (`TargetCapReason` swing/hvn = structural vs none = fallback), and the 2026-07-21 placed-target migration **deliberately retired that axis**. Three surfaces were checked and none produces it — `FailureRateMatrix` is `(tier × window)` · the what-if runner reads `TargetCapReason` only to *exclude* `poc` rows (`WhatIfProgram.vb:91-92`) · `CeilingAudit` carries it as an info-categorical for an AUC column. **No tooling is to be built for it.** ⚠ **What is NOT retired, and must not be lost with it:** the underlying question — *does a LONDON structural target actually get reached?* — is still open and still unanswered. **It is retired as a live trigger, not as a topic.** If it is ever wanted again it comes back as a **scoped instrument request** with its own spec, not as a watch nobody can read. **What survives from the last read**, [`w6-1-london-ruling-2026-07-31.md`](w6-1-london-ruling-2026-07-31.md) §3: LONDON directional placement is **structural 41.0 %** (swing 33.6 + hvn 7.4) vs **fallback 59.0 %** — that is F3's *denominator*, computable from the CSV today; the *reach rate* is the half that has no instrument. ⚠ **Do not conflate this with the 95.5 % ATR-clamp figure** in that doc's §4 — that is about structural **stops**, F3 is about structural **targets**, and the clamp finding is not an argument for retiring F3 |
| ~~**C1-coverage F2** — split-hour rule~~ | ✅ **RULED 2026-08-12 (trader) — SPLIT THE HOUR** | ✅ **DECIDED. It is now a BUILD SLOT, not a decision — see §2.** **The rule: an hour containing a capture-state marker is split AT the marker, each part is classified against the state that governed it, and the hour is reported defective if EITHER part is defective.** **What it replaces:** `ResolveScope` selects the marker with the greatest `UtcMs ≤ hourStartMs`, so the hour containing a flip is classified by the **pre-flip** state — a process starting at 10:30 with capture ON leaves hour 10:00 governed by the previous process's marker. ⚠ **Why that had to change: it errs toward NOT flagging**, the opposite of J-B's stated preference that a false defect is the cheaper error. ⚠ **The two options NOT taken, recorded so they are not re-proposed:** **(a) scope by the LATER marker** — one line, moves the error to the preferred side, but manufactures a **false defect on every deploy**, and a check that cries wolf at each deploy gets ignored (the alarm-fatigue failure this project already flagged once, on the F3 observational watch). **(c) exclude split hours from the denominator** — ⛔ **rejected outright: a silent hole in a coverage report is the exact defect class this store keeps producing**, and the report exists to make gaps visible. ⚠ **The implementer must state what the classification UNIT becomes** — the report currently classifies whole hours, and splitting introduces sub-hour spans. Say whether the output row stays one-per-hour with a worst-of verdict, or becomes one-per-span. **Model: Sonnet, effort: medium.** ⚠ **Where it slips: `ResolveScope` is also used where no split is possible** — do not change its contract for every caller; add the split at the classification site. **Fixtures must cover a flip at :00 exactly and a flip at :59** (both boundaries), and an hour with **two** markers |
| ~~**A54a scope — GUARD the third copy, or DELETE it?**~~ | ✅ **RULED 2026-08-11 — option (d) + scoped (b)** | ✅ **DECIDED. Neither of the two options offered was right, and the orchestrator's recommendation was DEFEATED on evidence.** Second opinion: [`seam-audit-decisions-second-opinion-2026-08-11.md`](seam-audit-decisions-second-opinion-2026-08-11.md). **(a) is dead** — the parameter→key mapping is genuinely underivable: verified `mtf_gate.adx_period` → `DmiPeriod`, `min_of` → `RequiredConfirms`, `candle_lookback` → `CandleCount`, and `mtf_gate` sits at JSON **top level**, not under `indicators`. ⚠⚠ **(b) was my recommendation and it is WRONG as the fix: the drift that motivated this whole exercise did not hide behind an omission.** Fixture **A6** passes `trendGate:=10.0` **explicitly** (`verify/ordercheck/Program.vb:661-662`), so making the parameter required leaves it **completely unchanged**, still pinning 10.0 against a shipped 23.0. I framed it as "three copies become two"; **copy 4 is independent of copy 1, and copy 4 is the one that actually failed.** ✅ **THE RULING: (d) — a reflection walk comparing `New EngineSettings()` against the deserialised shipped `settings.json`.** No parameter mapping (it never touches method parameters), **no fifth copy** (reflection reads the `JsonPropertyName` attributes the serialiser itself uses), ~40 lines against the `A52a` template, and it **subsumes the session-bucket decision for free**. Prototyped by the reviewing seat: found all four session-bucket drifts and both known drifts, zero false positives, zero orphans. **Plus scoped (b) as DEAD-CODE REMOVAL, not as the fix** — 9 fixture omissions, **0 production omissions**, and VB positional rules do not bite because the settings-mirroring optionals already precede the trailing genuinely-optional parameters. ⚠ **Leave `maxAgeMs`, `nowUtc` and the documented `CalcSpread` discard alone** — internal conveniences with no settings counterpart. ⛔ **CORRECTED 2026-09-05 (`S2-1`) — this reason was FALSE for `CalcSpread`'s two threshold parameters.** `SpreadSettings.WideThresholdBps`/`TightThresholdBps` exist and have real settings counterparts; the "no settings counterpart" reason is true only of `spreadStatus`, the `ByRef` output the caller (`LiveMicrostructureEvaluator.vb`) declares and never reads. **RULED (B) 2026-09-05: the two thresholds ARE in scope** — made required, `LiveMicrostructureEvaluator.vb` now passes `cfg.Indicators.Spread.*` at its one call site. Detail: [`a54a-session2-step1-measurement-2026-09-05.md`](a54a-session2-step1-measurement-2026-09-05.md) §7.2/§8, queue row `S2-1` below (§2). **Scope the guard by the derived rule: a concrete POCO default can drift; a `Double?` = Nothing nullable override cannot.** ⚠⚠ **STATE PLAINLY IN THE OUTCOME: copy 4 — fixture literals — is fixed by NOTHING here.** `A20a`/`A20b` pass off-spec OFI thresholds **legitimately** (refactor-equivalence tests, any consistent value serves) while `A6` is **stale**, and the two are indistinguishable to a machine. It needs a convention: *a fixture asserting shipped behaviour derives the value from cfg; a fixture asserting mechanism passes a literal and says so in a comment.* **Model: Sonnet, effort: medium, one session.** ⚠ **Escalation to Opus/high:** if the reflection walk needs a hand-written exception to resolve a property to its JSON path, **(d) has quietly become (a)** and the decision must be re-taken |
| ~~⚠ **A54a scope — superseded row, kept per the quote-and-label convention**~~ | ~~trader — a design choice~~ | ⚠ **NEW 2026-08-11**, from [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) S-2. Every indicator threshold exists in **three code copies plus the JSON**: the method `Optional` default, the POCO default, the fixture literal, and `settings.json`. **11 of 42 method defaults disagree with shipped JSON.** **(a) Guard three copies** — the audit's Row 2. ⚠ **My concern with (a):** the guard needs to map a method parameter to a JSON key (`CalcOBV.trendGate` → `indicators.OBV.trend_gate`), which is not mechanical, so it wants a table — **and a hand-maintained table is itself a fourth copy that can drift.** That is guarding the class with an instance of the class. **(b) Delete the method defaults, make the parameters required** — reduces three copies to two rather than watching three. ⚠ **The audit verified every production call site already passes the cfg value by name**, so the defaults serve *only fixtures* — and fixtures pinning stale literals is the defect. **My read: (b).** Bigger diff, removes the class instead of monitoring it. The counter-argument is the settings-exposure spec's stated "caller convenience", which is weak when no caller uses it. ⚠ **Whichever is picked, scope it by the rule the audit derived, not a hand-listed key set: a key with a concrete POCO default can drift; a `Double?` = Nothing nullable override cannot** — that is why v40/v41/v48 produced no drift while v33/v34/v36/v58 did |
| ~~**Seeded session buckets — does the code-defaults path exist at all?**~~ | ✅ **RULED 2026-08-11 — DO NOT empty the seed. Guard it via (d)** | ✅ **DECIDED, and the orchestrator's recommendation was DEFEATED on a factual error.** ⚠ **The question is answered: the code-defaults path is NOT theoretical.** `Core/Settings/SettingsLoader.vb:44` initialises `_current = New EngineSettings()`, and the parse-error handler **deliberately keeps it** — its own comment at `:460-461` reads *"keep the last good settings rather than crashing. At startup 'last good' is the POCO defaults, so the engine is then running on uncalibrated values"*. **So a real non-harness path reaches the seed: a `settings.json` parse failure at startup.** ⚠⚠ **And that kills my argument for emptying it.** I wrote *"wrong values are worse than absent values, because absent is detectable and wrong is not."* **Both are announced by the same banner** — `UI/MainForm_Layout.vb:1898` renders *"settings.json parse failed — running on code defaults"*, recomputed every render and self-clearing. **Emptying buys nothing on the silence axis; it swaps one degraded mode for another and both shout.** ✅ **A CORRECT seed beats both an empty one and a stale one, and (d) makes it correct and keeps it correct** — this decision is a free special case of the row above, not a separate job. **Verified: zero fixtures reach the seed** — all six session-bucket consumers (`A14a/b/d/e/i`, `A15d`) route through `BuildResolutionCfg()`, which fully replaces `SessionVolume.Sessions`. ⚠ **One residual, narrow but genuinely silent and recorded so it is not lost:** the banner covers a *parse failure*, **not** a `settings.json` that parses cleanly while **missing the `session_volume` block** — no exception, no banner, seed applies silently. **(d) guards the drift, which is the risk that matters.** ⚠ **Option (e) — making settings-load failure fatal — was considered and REJECTED: wrong for a 24/7 unattended collector, where "keep last good and shout" beats "refuse to start".** Recorded so it is not re-proposed |
| ~~⚠ **Seeded session buckets — superseded row, kept per the quote-and-label convention**~~ | ~~trader — intent, not a typo fix~~ | ⚠ **NEW 2026-08-11**, [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) S-3. `Core/Settings/EngineSettings.vb:668-671` seeds ASIA/LONDON/NY with a comment reading *"aligned to live **v30**"*. Live is **v66**. ASIA `high_multiplier` 0.8 vs 1.00 · ASIA `mid_multiplier` 0.85 vs 1.00 · ASIA and LONDON `execution_resolution` 1 vs 3. **NY is clean.** The seed's stated purpose is that *"an empty default would not silently skip all session scaling on the code-defaults path"*. **Either that path matters — re-sync AND guard it — or it does not, and an empty list is honest.** ⚠ **Re-syncing without picking one just resets the clock**, which is exactly what happened between v30 and now |

**Explicitly NOT owed — do not re-raise:**

- ✅ **D2 IS SHIPPED as settings v66** (2026-08-11, `fd52299`). `indicators.OBV.trend_gate` **18.0 → 23.0**, one global key. ⚠ **The commit said "POCO in lockstep" and that was wrong — corrected 2026-08-11.** There are **four** copies and v66 moved two; the method default (`Core/Indicators_Structure.vb:44`) and the fixture literal still read **10.0**. App unaffected, harness affected. See [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) S-2. ⚠ **Live scoring change and a dataset boundary** — rows after it carry a less-often-directional OBV, expected ≈52/49/51 % (NY/ASIA/LONDON) against 62.2/58.1/60.3 % before. Its gate — the D3 watch **reading** — was satisfied the same day. Evidence: [`candle-store-derivations-2026-07-31.md`](candle-store-derivations-2026-07-31.md) §2. **Decision-of-record carried with it: OBV needs NO per-session and NO per-resolution split** (p50s span 22.2–24.2, 3m/1m ratio ≈0.95) — nobody should spend a pass splitting it. ~~⚠ **The deploy is still owed and is part of this change**: `settings.json` moved, and it **hot-reloads**, so the version edge can land mid-`InstanceId` and become unfilterable. **Stop → swap → start on both boxes**, then record both ids in [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a.~~ ✅ **CORRECTED 2026-08-13 — THE DEPLOY IS DONE AND HAS BEEN SINCE 2026-08-10.** [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a records it: AWS `3be7f4c9…` at **2026-08-10 18:35:18 UTC** ("THE D2 DEPLOY"), local `7c9d9a59…` 40 s later, and **two later AWS rows read "v66 — UNCHANGED"**. The version edge is clean and spent. ⚠ **This stale line was repeated four times across this queue and was quoted as live guidance on 2026-08-13 before the ledger was checked** — the §1b sweep class exactly. ⚠ **The date looked contradictory and was not: `fd52299` committed 2026-08-11 02:06 +0800 = 2026-08-10 18:07 UTC, 28 minutes before the deploy. Docs here mix GMT+8 commit dates with UTC ledger times — check which before calling a sequence impossible.**
- ✅ **The D3 ASIA watch HAS READ, and it PASSED** (2026-08-11) — [`d3-asia-burst-watch-read-2026-08-10.md`](d3-asia-burst-watch-read-2026-08-10.md). Fire rate **10.97 %** over 12 fully-covered AWS weekday session-days (n=1,905), same-side **89.47 %**. `burst_ratio_threshold` stays **5.5**. ⚠ **Its tolerance T-1…T-5 is now RULED** and **supersedes row D3-5** of the derivation: reference **11.0 %** (not 9.7 %), band **8–14 %**, read length **≥10 weekday session-days**, same-side ≥85 % unchanged. ⚠ **Do not quote the derivation's 9.7 % or its "~106 AggrVel rows/day" as current** — both are artefacts of a partially-covered book; fully covered, ASIA carries ~158–160 rows/day. **Next band-eligible read ~2026-08-17.**
- **C1-coverage is BUILT** (2026-08-05, both sessions, reviewed and ACCEPTED, one review finding fixed `c6a7a63`). Verified in the tree: `coverage` verb at `tools/BacktestRunner/BacktestProgram.vb:282`, Part A `BuildResult`, Part B `RunVenueDiffAsync`, TAPE STORE strip in `UI/MainForm_Layout.vb`. **Its F1 and F2 are the only remainders** — F1 is a build slot (§2), F2 is the decision above.
- **D1 TTM is RE-DERIVED and PARKED** (2026-08-02). The **unit** is wrong, not the value — `delta` tracks volatility (CV 26% in USD vs **6.5–11.7% in ATR multiples**), so an absolute USD constant cannot survive a regime change, and in ATR units the per-resolution ladder dissolves. `flat_threshold` stays **0.5** and the FLAT band stays inert — **recorded and deliberate, not unnoticed.** ⚠ The fix is ATR-relative k≈0.25–0.30 and is a **CODE change with its own spec and ⚠ boundary**, not a settings tick. **Do not inherit either retired number** — neither the 25.0/40.0 ladder (it moves the vote 46.5→35.1%, *away* from its target) nor the 1.45 ratio it was to be rebuilt from (measured **1.774**, stable every month, ≈√3). Un-parks on trades-covered replay.
- **D3 is TICKED, SHIPPED AND LIVE ON BOTH BOXES** — settings **v65**, ASIA armed at 5.5 from 2026-08-01 19:02Z, boxes restarted **1.6 s apart** so the version edge is cleanly attributable. Verified: ASIA 5.5 / NY 4.5 / LONDON 5.5. The ⚠ boundary is spent; the watch is live in §4. **Any pooled ASIA read spanning the boundary must split on the InstanceId ledger** in [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a — there is no settings-version column in the CSV. ⚠ **CORRECTED 2026-08-11 — v65 spans SIX instance ids, not five and not two.** AWS `09c747f8…` → `ec487909…` → **`ffced26c…`** → `d8678d2b…`; local `3916540f…` → `ad7cadf4…`. **All six are v65 and all six are armed.** `ffced26c…` (2026-08-10 09:59:56Z, the post-weekend restart) was in **no document** until the watch read found it in the copy-back. Filtering the D3 watch on the original pair silently drops everything after 2026-08-07. The only v65 version edge is still v64→v65 on 2026-08-01; every id after it is a restart. ⚠ **v66 opens a NEW version edge and it is NOT yet deployed** — both boxes still run v65 until stop → swap → start. **A more robust split than the ledger, found 2026-08-11:** the classifier carries the edge itself — armed ASIA rows have `AggrVelSignal = BURST_*` agreeing exactly with `AggrVelBurstRatio ≥ 5.5`, while unarmed rows fire from **2.507**. Use it to *check* the ledger rather than depending on it.
- ✅ **D1-a is REVERSED — RULED 2026-08-08 (trader), and the action is DONE.** Local capture is **OFF** again: `settings.local.json` restored to `bin\Debug\net8.0-windows` with `{"trade_store": {"enabled": false}}`, verified present; `bin\Release` overlay untouched. The tape the local box captured under D1-a (2026-08-07 16:05 → 2026-08-08 00:55 UTC) is **kept and quarantined** at `AWS-copybacks\quarantine-local-d1a-2026-08-08\` with a README — moved out of `bin\Debug` so a future run cannot append to it and blend two eras in one file. **It stays unmergeable forever**; identity cannot be retrofitted. **AWS is once again the sole capturer, which is D1's undisputed end state.** ⚠ **Do not re-enable local capture until `trade_id` ships** — see [`trade-store-trade-identity-proposal.md`](trade-store-trade-identity-proposal.md) row D7, which re-opens it as a real option at that point. *Superseded text follows, per the quote-and-label convention:* ⚠⚠ **D1-a's PREMISE IS WITHDRAWN 2026-08-08 — the ruling needs a trader re-read, and it is the one open item on this page.** D1-a turned local capture ON in `bin\Debug` on the strength of a measurement that a second box had recovered 16,459 trades AWS lacked. **That measurement is withdrawn** — the boxes disagree on *amounts at shared timestamps*, not on which trades exist, and the root cause is that the store has no `trade_id` (see §2). **My recommendation: turn local capture back OFF until `trade_id` ships.** Three reasons: two tape books **cannot be correctly merged**, so a second box contributes nothing usable; **tape written now is permanently unmergeable**, because identity cannot be retrofitted to rows already on disk; and the merge temptation is a *demonstrated* hazard — I merged the two books within hours of the copy-back and corrupted the store. Reverting also restores D1's undisputed end state. **The action if you agree: restore `settings.local.json` to `bin\Debug\net8.0-windows` with `{"trade_store": {"enabled": false}}`.** Keep the `bin\Release` overlay either way — it is what stops the deploy source repopulating `backtest_data\`.
- **E1 Kelly is DECIDED** (option (c) — wait for separation, display honestly now). Display shipped `18b1ea8` and confirmed. **CAL is parked, not pending.**
- **E6** fee knob confirmed · **E2** W6-4 run (INCONCLUSIVE — the queue does *not* unlock) · **E3 / E4** approved and built · **A1 / A2 / A3** closed · **B1** shipped 07-21.
- **E7** (A4) is market-gated and **E8** (L9) is order-app-gated — neither is a decision. ⚠ **A4 scoping RULED 2026-08-08 (trader), and it needs stating precisely because the obvious reading is wrong.** A missed weekend cascade is **not a defect** and is not to be chased. **But do NOT weekday-filter the gate itself.** A4's gate asks *"has the liquidation feed ever produced a cascade"* — it exists to prove the plumbing works and the event class is real, after the audit found `LiqSignal` never fired in 8,025 runs. **The plumbing is day-agnostic, so a cascade on any day is valid evidence.** Filtering the gate to weekdays would make it strictly harder to satisfy while proving nothing extra. The correct position: **any cascade counts; running part-time simply means fewer observation hours, which is reduced coverage and not a fault.** No code change.

> **Why this section exists, recorded because it was the third instance.** The E1 row said *"needs an explicit trader decision"* for a decision the trader had already made — written into its own doc and never carried back to the row. §0 designates **this queue** as the state read, so a stale row here is worse than a stale spec header: it is wrong in the doc everything else points at. **The 2026-08-07 sweep found the same shape again** — see §1b.

---

## 1. The queue

### Cluster A — the v64 landing. ✅ **COMPLETE 2026-08-02.**

A1 answered (option **c** — the local box avoids capture through the overlay, not a hand-edit, so there is no standing chore and no silent-restore failure mode) · A2 overlay **built** `291457c`, F1/F2/D-C fixed `1611011`, re-reviewed and accepted · A3 **both boxes live**, AWS capturing from 2026-08-01 17:50 UTC, local capture correctly OFF, Kelly `[EST]` trader-confirmed.

**The reusable part, kept.** `trade_store.enabled` ships `true` — right for AWS, wrong for the local box under D1's AWS-only ruling — and `settings.json` is `PreserveNewest`, so **the build that tested v64 would have been the build that started local capture.** The overlay is gitignored and lives in `bin\`, so it must be placed **before** the first build carrying a newer tracked `settings.json`. Once it is, `PreserveNewest` can refresh the base and the merge still resolves capture to `false`.

**On D7, kept because it is not a permanent close.** Answered 2026-07-31, option (a): a REST-mode box is expressed by a tracked `settings.json` change with a version bump, because a REST box is break-glass rather than routine and a break-glass change *should* be deliberate and version-visible. Option (c) — excluding a REST box's rows from the pooled book — unlocks once the J-E effective-source stamp gives rows a source column.

### Cluster B — ✅ **CLOSED: it was never work. It shipped 2026-07-21.**

**B1 (eval `NO_DATA` / F4) SHIPPED `75a2694`** — N1–N5 built, R1 and R2/N4 with it. **Verified live:** `# schema=v6` in the cache, `analysis_eval_cache.csv.v5.bak` present, **120 `NO_DATA` rows**. **The EV-on-the-strip precondition is therefore already satisfied** — that question is unblocked whenever it is wanted.

### 1a. The convention this produced — verify shipped state before offering work

**This item was carried as outstanding, ticked by the trader, and an implementer conversation was recommended for it. All three were wrong, and the error survived a correction pass.** The proposal's header read *"PROPOSED — D-table awaits trader"* while §3 recorded the tick; the header was corrected **from the D-table alone** and the tree was never checked. That fixed the wrong half.

**Second occurrence of the exact shape** — [`fable-handover-2026-07-31.md`](fable-handover-2026-07-31.md) §6.2. Both times the item was an eval-surface fix whose spec header had gone stale.

> **A spec's status header is not evidence of code state. Before offering any spec as available work, verify it in the tree.**
> One command: `git log --oneline -S'<distinctive-symbol>' -- <file>`.

The cost of skipping it is asymmetric: a wasted implementer conversation, and a queue that misrepresents what is outstanding to the person deciding what to spend on.

### 1b. Shipped-state sweeps

**2026-08-01 — 4 of 13 rows wrong, all in the same direction.** B1 (shipped 07-21, live), E3 (built, `SignalEmitter.vb:324-328`), E4 (built, `tools/BacktestRunner/`), E1 (blocker removed 07-30). **Nothing was found in the opposite direction.** Root cause: the queue was assembled partly from a grep for *"await trader"* across `docs/` — **that grep matches prose and section headings, not state.**

**2026-08-07 — re-run against the tree, 4 findings.** The queue held up much better; every settings value, every gate and every "explicitly not owed" claim verified. What did not:

| Finding | Detail |
|---|---|
| **Cluster C read BUILD-AUTHORIZED for work built 08-05** | And §2 of this same file already listed *C1's F1* as a backlog item — which only makes sense if C1 shipped. **The contradiction was internal to one document**, the identical shape as the E1 row that created §0a. Both rows fixed here |
| **Push state** | Handover says "in sync"; tree is **ahead 1** (`8b488cc`, the handover doc committing itself) |
| **Atomic-write site line numbers had drifted** | The comment added in `5ed8862` moved them. Re-anchored in §2 — and re-verified that `Core/Settings/SettingsLoader.vb:346` is real, after a first grep scoped to `Core/*.vb` missed it and nearly produced a false "site absent" report |
| **A blocker that was recorded nowhere** | The AWS copy-back — see §2 and §4. It gates two dated instruments and no doc named it |

### Cluster D — the ⚠ boundary contention

| # | Tick | Gate |
|---|---|---|
| ~~**D1**~~ | ~~TTM `flat_threshold` re-anchor~~ | ⛔ **RE-DERIVED then PARKED 2026-08-02.** See §0a |
| ~~**D2**~~ | ~~OBV `trend_gate` 18 → ~23~~ | ✅ **SHIPPED as settings v66 (`fd52299`, 2026-08-10 18:07 UTC) AND DEPLOYED to both boxes 2026-08-10 18:35 UTC.** ⚠ **CORRECTED 2026-08-13 — this cell read "deploy still owed" and that was stale.** Ledger: [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a, AWS `3be7f4c9…`. Boundary spent, edge clean. Its gate (the D3 watch reading) was satisfied the same day. See §0a |
| ~~**D3**~~ | ~~ASIA `burst_ratio_threshold` = 5.5~~ | ✅ **SHIPPED as settings v65.** ⚠ Boundary spent. **Not the settings-only change the original row implied** — threshold *presence* is the arming mechanism, so the POCO default in `EngineSettings.vb` moved in lockstep (v60 precedent; the harness builds cfgs from `New EngineSettings()`), and **A28c had to be re-pinned**: ASIA *was* the un-armed exemplar, so the un-armed arm is now **constructed** rather than borrowed. New **A52a** (JSON→arming contract + JSON↔POCO drift guard) |

**RULED 2026-08-01 — two boundaries, not one or three: D3 alone, then D1+D2 bundled.** D3 went first because it **arms** rather than retunes, and an activation deserves its own clean window. **Honest caveat carried into its D-table:** the W6-4 ceiling audit gave the first outcome-linked read on ASIA aggressor velocity — `AggrVelBurstRatio` AUC **0.5179** (n=217). Essentially no demonstrated edge. It does not refute arming (NY and LONDON were armed on distributional grounds too, and LONDON's watch passed) — but it is the only outcome evidence that exists on this knob, and it is neutral at best.

### Cluster E — data-gated, other repos, or genuinely later

| # | Item | Gate |
|---|---|---|
| **E1** | F1 §9 pooled read → Kelly CAL → P5 | 📖 **READ DONE → THE LADDER DOES NOT SEPARATE.** MEDIUM 42.2% ≈ WEAK 42.7%, all CIs overlap, **LONDON inverts at the top** (MEDIUM 53.0% > STRONG 44.8%). ✅ **DECIDED 08-02: option (c).** Display shipped; **CAL parked**; **P5 values remain gated** — the LONDON inversion contradicts a STRONG-only policy there. Re-reads at the §4 dated trigger |
| ~~**E2**~~ | W6-4 ceiling audit run | ✅ **RUN → INCONCLUSIVE.** ΔAUC −0.0291, CI [−0.197, +0.124] straddling ±0.030. **The queue does not unlock**; re-run at the next book doubling, **no spend meanwhile** |
| ~~**E3**~~ | D2-v2 what-if candidate mode | ✅ **BUILT** — `use_best_pivot_candidate` wired at `SignalEmitter.vb:324-328`, default `false`. Live-enable is a later separate ⚠ |
| ~~**E4**~~ | Backtest synthesizer | ✅ **BUILT** — `tools/BacktestRunner/`, in active use. Its §6 heading still reads *"D-table (await trader)"* — **that heading is the stale artefact, not the status line** |
| ~~**E5**~~ | ~~Absorption Path B~~ | ✅ **TICKED 2026-08-12 — PATH B.** Anchors HOLD at v61; **no settings change, no version bump, no ⚠**. Path A's V-table is dead. **What it owes is a proposal file, not a knob** — see §0a and the §2 absorption mechanism-revision row |
| ~~**E6**~~ | Fee knob | ✅ **CONFIRMED** — the 5-bps-net state stands, composing **0.0008**. Verified in `settings.json` |
| **E7** | A4 liquidation × OFI flip | Market-gated only (≥1 CASCADE line). **Protect the instrument — `alerts.` stays REJECTED from the overlay whitelist** |
| **E8** | L9 structural-stop un-clamp | Gated on **L3** (order app). W6-1 ruled no-change and handed the real question here |

---

## 2. Not ticks — these need a build slot, not a decision

Ordered by what each one prevents. **Verified present in the tree 2026-08-07** unless noted.

> ### ⛔⛔ SHIPPED-STATE SWEEP, 2026-09-07 (UTC) — ELEVEN ROWS IN THIS SECTION DESCRIBED WORK THAT WAS ALREADY DONE
>
> **All eleven are closed below, in the quote-and-label shape the section already uses** — the item struck, the corrected status in front, the original text kept. Closed: `I17-A6` · `I17-SWEEP` · `F-1` · the streaming-capture ~50 % drop · `C1-coverage F1` · `SH-1` · the A54a JSON↔POCO drift guard · `trade_id` + `trade_seq` · the downtime-repair deploy · the [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15 three-row C1 tidy · the two seam-audit doc corrections. ⭐ **Every one was spot-checked against the TREE — a commit hash, a `grep -c` output, or a deploy-ledger instance id — never against a status header.**
>
> ⛔ **THE STANDING LESSON: verify a row against the tree before offering it as available work.** The 2026-08-01 sweep recorded in [`trader-tick-queue.md`](trader-tick-queue.md) §1b found **4 of 13** rows describing shipped work as outstanding. **This one found 11.** ⚠ **A stale row HERE is worse than a stale spec header**, because [`trader-tick-queue.md`](trader-tick-queue.md) §0 designates this queue as *the state read*. ⭐ **The worst single cell was the streaming-capture row: it read *"LIVE, ongoing, and time-critical"* for four weeks after the identity-keyed write guard had shipped AND deployed.**
>
> ⚠ **Rows deliberately LEFT OPEN by this sweep, so the closures are not read as a clean board:** `F2` (the `ResetBufferState` race) · `F3` (the collector User-Agent) · `S-4` (eval-cache backfill) · `G12` (three manual gaps) · `S-1` · the coverage-report scoping rows · `ws_health.log` under-reporting · intentional-downtime scoping · atomic writes · the fills-import tool · the absorption rows. **Downtime-repair Part B is also still forbidden** — see its row; only the *deploy* claim was closed.
>
> ✅✅ **SETTLED 2026-09-07 by the scoping read the note below asked for. THE ROW IS DETERMINATE AFTER ALL: 1 of 3 surfaces done, 2 open.** ⛔ **Both the note below and the orchestrator briefing behind it were WRONG to call it unsettled — the cell names its three surfaces explicitly (`AutoTweaker` → `LivePerformanceTracker` → `AnalysisRunner`/`WhatIfRunner`); neither of us had read the cell to its end.** ⭐ **The SEVEN `DayOfWeek` files never contradicted "3 surfaces" — they count FILES, not surfaces, and decompose cleanly: THREE are the `AutoTweaker` filter, already done (`AutoTweakerCore.vb:831-832` · `ConditionsExtractor.vb:148-149` · `UI/TweakSettingsForm.vb:314-315`, all three genuinely excluding Sat/Sun) · ONE is the fixtures that guard it (`verify/ordercheck/Program.vb`, `A59a`–`A59e`) · TWO were ALREADY weekday-enforcing before the ruling and are not part of this item (`CoverageReport.vb` · `CeilingAudit/CsvFeatureBuilder.vb` — precisely the "exactly one place" the 2026-08-03 ruling verified) · and ONE is a DECOY.**
>
> ⛔⛔ **THE DECOY IS THE FINDING, and it is why a file count could never settle this: `LivePerformanceTracker.vb:560`'s `DayOfWeek` is MONDAY WEEK-ANCHORING for the perf strip's UTC+8 week window — NOT a filter.** The tracker has **no weekday filter at all**. Anyone scoping this by counting files reads that hit as "already handled" and closes the row wrongly; the cell warned about exactly this and the warning earned its place. ⛔ **`analysis/AnalysisRunner.vb` and `tools/WhatIfRunner/*` have ZERO weekday awareness of any kind** — no `DayOfWeek`, no `Saturday`, no `Sunday`, no `weekday`.
>
> ⭐ **STATE: `AutoTweaker` ✅ DONE (2026-08-25, the sharp one — the only surface that WRITES `settings.json`). REMAINING: (2) `LivePerformanceTracker` — ⚠ expect FEWER rendered cells, not more, since excluding weekends shrinks n against `min_sample_for_render`; that is honest, not regrettable, and should be expected rather than discovered. (3) `AnalysisRunner` / `WhatIfRunner`.** **None is a ⚠ boundary. Sonnet, medium.** *(Superseded note follows, kept per the convention.)*
>
> ~~⚠⚠ **"Weekday filters — 3 surfaces" is LEFT UNTOUCHED because it is UNSETTLED, not merely open.**~~ Its own cell records `AutoTweaker` **DONE 2026-08-25** and names `LivePerformanceTracker`, then `AnalysisRunner` / `WhatIfRunner`, as remaining — but `DayOfWeek` now appears in **SEVEN** `.vb` files (measured 2026-09-07 UTC: `LivePerformanceTracker.vb` · `UI/TweakSettingsForm.vb` · `tools/AutoTweaker/AutoTweakerCore.vb` · `tools/AutoTweaker/ConditionsExtractor.vb` · `tools/BacktestRunner/CoverageReport.vb` · `tools/CeilingAudit/CsvFeatureBuilder.vb` · `verify/ordercheck/Program.vb`), so **"3 surfaces" no longer names a determinate set.** ⛔ **Do not close it, and do not re-size it from that count alone** — several of those hits are week-anchoring or fixture code, not filters. It needs a scoping read, not a build slot.

| Item | Size | Where |
|---|---|---|
| ⭐ **Converge the two remaining inline weekday predicates onto `ForwardWindowJoiner.IsWeekdayRow` (`WD-TIDY`)** | **two call sites, both already linked. Sonnet, effort LOW** | ⚠ **NEW 2026-09-07**, residual from the weekday-scope ruling's surface 3. ⛔ **RECORDED AS ITS OWN ROW ON PURPOSE — it was first written inside the now-CLOSED weekday row's cell, and a closed row is not where anyone looks for available work. That is the same "recorded where its reader cannot use it" shape as finding `R-1`, caught by the trader.** **The weekday-scope ruling is DISCHARGED; this is tidy, NOT a defect, and nothing is wrong today.** ⭐ **State: there are THREE copies of the Sat/Sun predicate. Surface 3 added the shared one (`analysis/ForwardWindowJoiner.IsWeekdayRow`, with the `MinValue` guard); the other two predate it — `tools/AutoTweaker/AutoTweakerCore.vb:829` `MatchesWeekday(r As CsvRow)` and `tools/CeilingAudit/CsvFeatureBuilder.vb:199-200` inline.** ✅ **FEASIBILITY VERIFIED, not assumed: both `AutoTweaker.vbproj` and `CeilingAudit.vbproj` ALREADY link `analysis/ForwardWindowJoiner.vb`, so no project-file change is needed.** ⭐ **THE SHAPE THAT KEEPS THE HARNESS GREEN: do NOT delete `MatchesWeekday` — 7 harness references (`A59a`–`A59e`) call it. Keep its signature and delegate its BODY to `IsWeekdayRow(r.Timestamp)`. The fixtures stay valid and the duplicated logic goes.** ⛔ **WHY IT IS WORTH DOING AT ALL, and it is not aesthetics: the predicate is 3 lines but one of them is the `MinValue` guard, and `DateTime.MinValue.DayOfWeek` is MONDAY — a copy written without it silently admits every unparsed timestamp as a weekday row. Three copies means three chances to omit the non-obvious line.** ⚠ **Do NOT re-route the tweaker's LOAD path while doing this** — surface 1 is shipped and reviewed; only the predicate body moves |
|---|---|---|
| ✅✅ **`D3-RESIDUAL` — BUILT AND DONE 2026-09-07. The live strip no longer prints a fabricated `0.0 bps`** | ✅ **CLOSED.** `snap.HasSpread` now reads `bps.HasValue`; `HasTopOfBook` **DELETED** (one call site, dead the moment the assignment moved). Harness **332 → 333** (`A66a`), `GATE PASSED`, solution builds 0/0, settings **untouched at v68** | ⭐⭐ **THE FINDING WORTH KEEPING IS `A66a`'s SHAPE, not the fix.** An **empty-ladder** book returns `False` under **both** the old and new predicates, so **only the ZERO-PRICED-TOP case observes the change**. ⛔ **A fixture built from the empty ladder alone would have passed identically before and after and read as coverage** — the `A62f`/`A63a` shape, caught this time by naming which input carries the assertion before writing it. ⭐ **Mutation RUN, not reasoned:** restoring the retired predicate inline fails `A66a` **ALONE** (whole-harness FAIL = 1), printing `emptyAsks: hasSpread=False bps=0 \| zeroBid: hasSpread=True bps=0` — **case 1 provably inert, case 2 the defect verbatim.** ⭐ **`A66a` is also the harness's SECOND `UpdateBook` call EVER, which closes the evaluator half of finding `R-2`** ([`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md) §5.4) — the absence of a second one is exactly why that composition was recorded unreachable. ⚠ **`R-2`'s OTHER half is untouched and stays open:** `UI/MainForm_Analysis.vb:423` is still structurally uncoverable, because `OrderCheck.vbproj` cannot link `UI/`. ⛔ **NO parity instrument was built, and `R-2` (b) is NOT triggered — correcting the reviewer's own earlier read.** This build is a **deliberate** rendered change, so it needed a before/after enumeration, not a byte-identical capture: on a zero-priced top of book the strip moves `0.0 bps` → `-- bps`; **every other input is unchanged** (healthy book both `True`; empty ladder both `False`; `Nothing` book skips the block). **Strip-only surface — no snapshot line, no card binding — the #3/#5/#6 precedent.** §15 **EXTENDS the `S2-2` row** rather than adding one, per the one-item-one-row rule: this is `D-3`'s follow-up, the DR-1/DR-2/DR-3 shape. **Original row follows.** |
| ~~⭐ **`D3-RESIDUAL` — the live strip prints a FABRICATED `0.0 bps` on an unreadable book**~~ | ~~one line + its own parity capture. LOW priority, ⛔ NOT bundled~~ | ⚠ **NEW 2026-09-06**, ruled `Q-1` **(a)** at [`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md) §5.5. **`S2-2`'s `D-3` was ruled (b) — correctly, for that build — leaving `snap.HasSpread = HasTopOfBook(book)` (3 conditions) diverging from `CalcSpreadBps` (6; it adds `bestBid > 0`, `bestAsk > 0`, `mid > 0`).** ⛔ **`UI/MainForm_LiveStrip.vb:221` settles what that costs — `parts.Add(If(s.HasSpread, s.SpreadBps.ToString("0.0") & " bps", "-- bps"))`. `HasSpread` gates EXACTLY whether a number or `-- bps` is printed, so on a zero-priced top of book the strip prints `0.0 bps` where `-- bps` is honest.** ⛔⛔ **SAME fabricated-measurement class `S2-2` existed to remove — [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15's absorption row states it: *"a zero … would be a fabricated measurement."* `S2-2` cleared it from four surfaces and left it on a fifth.** ⭐ **THE FIX: `snap.HasSpread = bps.HasValue` at `LiveMicrostructureEvaluator.vb:141`.** ⭐ **Bonus: that also puts the evaluator's composition line under fixture coverage, closing half of finding `R-2`.** ⚠ **WHY LOW PRIORITY: a zero-priced top of book on a Deribit BTC-PERP ladder is not a real market state — it is a defensive guard, reachability effectively nil, nothing is lost by waiting.** ⛔ **It IS a live-strip behaviour change, so the display-string parity rule is live and it needs its own before/after capture — that capture is the cost, not the line.** ⛔ **Do NOT add a CSV column to measure how often it happens (`Q-2` ruled): the state is synthetic and constructible, so the parity capture IS the instrument.** **Model: Sonnet, effort medium** |
| ✅✅ **`R-2` residual — CLOSED 2026-09-07 by EXTRACTION, not by acceptance. The whole spread seam is now finished** | ✅ **DONE.** The composition moved into `IndicatorEngine.ApplySpread`; harness **333 → 334** (`A66b`), `GATE PASSED`, solution 0/0, settings **v68** | ⭐ **Neither disposition (a) nor (b) was taken — a third became available once the shape was measured.** The obstacle was never the *line*, it was that `UI/MainForm_Analysis.vb` cannot be linked (`OrderCheck.vbproj` is `net8.0`, no WinForms; the file is a `Partial Public Class MainForm` needing `MainForm_Layout.vb` + the designer). ⭐ **`IndicatorResults` being a `Class` rather than a `Structure` is what made the fix clean: `ApplySpread(orderBook, r, wide, tight)` writes both fields, so ALL THREE lines left the untestable file and what remains is one call with ZERO logic — nothing left to get wrong, rather than a smaller thing to get wrong.** ⛔ **It does NOT re-merge what `S2-2` split:** `CalcSpreadBps`/`ClassifySpread` stay public and unchanged and the evaluator still calls `CalcSpreadBps` **alone**, so the no-`indicators.spread.*` guarantee survives. ⭐⭐ **BOTH mutations RUN, and the first one is the justification of the whole build: `0.0` → `-1.0` now fails `A66b` ALONE, where before the extraction it failed NOTHING and the harness stayed green at 333.** Second: classifying from the defaulted `Double` rather than the nullable — `S2-2`'s own defect re-entering where `A65a` cannot see it — fails `A66b` alone with `degenerate: bps=0 status=TIGHT`. ⭐ **`ApplySpread` deliberately carries no `If r Is Nothing` guard — a silent no-op on a caller error is the shape this project removes; a Nothing target should throw loudly.** ⚠ **Behaviour byte-identical; pure extraction.** ⛔ **THE SPREAD SEAM IS CLOSED — no further follow-ups on it.** **Original row follows.** |
| ~~⚠ **`R-2` residual — `UI/MainForm_Analysis.vb:423` is structurally uncoverable by the harness**~~ | ~~accept explicitly, or build an instrument. NOT a defect~~ | ⚠ **NEW 2026-09-06**, [`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md) §5.4. **`verify/ordercheck/OrderCheck.vbproj` links `LiveMicrostructureEvaluator.vb` but CANNOT link `UI/MainForm_Analysis.vb` — `UI/` needs WinForms.** So `r.SpreadBps = If(spreadBps.HasValue, spreadBps.Value, 0.0)` is guarded by nothing: **swap the `0.0` for `-1.0` and the harness still prints 332.** ⭐ **The line is CORRECT — read, not assumed. The finding is the absence of a guard, not a bug.** ⛔ **It was covered during the build by `H-1`'s instrument, which copied the call-site body verbatim and was then thrown away** — which is exactly why finding `R-1` (a handle whose instrument does not survive) matters in practice and not only in principle. **Two dispositions, both legitimate: (a) accept it explicitly as uncoverable and say so once, so it stops looking like an oversight; (b) commit a small host-agnostic parity instrument so this and any future render-surface build has a re-runnable handle.** ⭐ **My read: (a) now, and (b) only if a second build needs the same instrument** — building one for a single use is how the F3 watch outlived its own tooling. **No model needed for (a); Sonnet/low for (b)** |
| ✅ **`OPS-1` — RULED AND DONE 2026-09-06. `collector.ps1 fetch` now collects the rotated `analysis_log.csv.v0.7.bak`** | ✅ **CLOSED** — one literal entry, `tools/ops/collector.ps1:97` (now `:115`) | ✅ **RULED: a LITERAL entry, NOT a `*.bak` glob — and the reason is MECHANISM, not preference.** ⛔ **(1) A glob is not "one entry".** `$FetchFiles` entries are **name keys**, used three times each: box-side snapshot (`Join-Path $dir $f`), manifest keying (`$manifest[$f]`), and the local size-verification loop (`tools/ops/collector.ps1:361-372`). `$manifest['*.bak']` is one key for many files, and `Test-Path` on a literal wildcard path is not a verification. **A glob makes the manifest list-valued and rewrites the exact loop that caught the FIX 8 defect.** ⭐ **(2) The literal CANNOT sweep future rotations, so the concern that made this a ruling is answered by the code.** Read at `AnalysisLogger.vb:159-163`: a new rotation is named `analysis_log.csv.v0.7.bak`, and **only if that already exists** does the **INCOMING** file get a timestamped name instead. ⛔ **So the existing `.bak` is never renamed and never overwritten** — the name is a stable, permanent handle on the closed 2026-09-01 15:49 UTC book. *(⚠ [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15's absorption row reads as though the EXISTING `.bak` gets the timestamp. It is the other way round — corrected here from the code.)* **Cost bounded:** `Test-Path`-guarded on the box, so a no-op wherever absent; ~33,911 rows against a `backtest_data\` transfer already an order of magnitude larger. ⚠ **KNOWN RESIDUAL, named not silent: a SECOND rotation's timestamped `.bak` is still not fetched.** Fetching every historical `.bak` grows without bound, so that wants its own ruling **if a second rotation ever happens**. Verified: script parses clean, list is 5 entries. **Original finding follows.** ⚠ **NEW 2026-09-06**, found by running `fetch` — [`seat-handover-2026-09-06.md`](seat-handover-2026-09-06.md) §1.1. **The fetch itself is SOUND** (`aws_fetch/20260906-155831`, all transfers size-verified; FIX 7's snapshot-then-manifest works). ⛔ **But its box-side manifest lists only `analysis_log.csv` · `ws_health.log` · `capture_marker.log` · `analysis_eval_cache.csv` + `backtest_data`. The `.bak` is not among them.** ⚠ **So the pre-rotation book is STILL box-only five days after [`seat-handover-2026-09-03.md`](seat-handover-2026-09-03.md) §3 flagged it — including the ~11 minutes of rows (2026-09-01 15:38 → 15:49 UTC) that exist NOWHERE ELSE.** ⭐ **It does NOT endanger the Kelly read** — the `.bak` is a CLOSED file at a cross-validated 337. **A box loss would take those 11 minutes with it.** ⛔ **A RULING, not a patch:** a bare `*.bak` glob would also sweep every future rotation, which may be wanted (completeness) or not (transfer size — the book is already 4.3 MB and the store 163 MB). **Model: Sonnet, effort low once ruled** |
| ~~⭐ **Item 17 applied to `A6`'s stale `trendGate:=10.0` (`I17-A6`)**~~ | ✅ **DONE — commit `98ed4fd`, *"I17-A6 — A6's trendGate literal 10.0 -> synthetic 1.0"*.** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC):** `verify/ordercheck/Program.vb:929-930` passes `trendGate:=1.0` — the ruled obviously-synthetic value — at **both** `CalcOBV` call sites, and the comment above them records the `trend_gate` history (0.001 → 10.0 at v31 → 18.0 at v33 → 23.0 at v66) and states that 1.0 has never been shipped in `settings.json` nor held as a method default. **The stale 10.0 is gone.** *Original size estimate follows.* ~~one literal + one comment; **the analysis is already done — see this cell**~~ | ⚠ **NEW 2026-09-05.** Item 17 RULED the convention 2026-09-03 (MECHANISM, option (c): keep the literal, add the declaring comment, **AND change the value to an obviously-synthetic one** *"so nobody reads it as a claim about settings.json"*) and BUILT it for `BuildResolutionCfg`. ⛔ **It was never applied to `A6`**, which pins **10.0 against a shipped 23.0** — a value that is not synthetic at all: it is the **pre-v33 SHIPPED value**, so a reader greps it, finds it in the version history, and cannot tell stale from deliberate. **Session 2 touched the same call site and deliberately did not fix it** (spec §7.2 trap C). ⭐ **MECHANISM is CONFIRMED by reading `A6`, not assumed:** its `Check()` asserts `trendA = "RISING" AndAlso trendA = trendB` — the *equality of two paths*, which is gate-independent because both use the same gate. ⭐⭐ **And deriving from cfg would be WRONG here, which is the non-obvious part:** `ObvRiseCandles` builds 50 bars, volume 10, close +10/bar ⇒ `obvChange` ≈ **48**; that clears 10.0 and 23.0 alike, **but a legitimate retune of `trend_gate` above 48 would flip both paths to FLAT and BREAK `A6` on a settings change it was never meant to track.** So: **MECHANISM, option (c).** **The value must be one never shipped** — the `trend_gate` history is 8.0 → 10.0 → 18.0 → 23.0, so ⭐ **`1.0` is the clean pick** (reads as *"just needs to be cleared"*, and the band where the gate could matter is `obvChange ∈ (1, 10)`, far from any plausible normalisation break). **Comment: extend the one session 2 already left there** — do not add a second block beside it. **Model: Sonnet, effort low** |
| ~~⚠⚠ **21 of session 2's 26 MECHANISM literals EQUAL their shipped value — MEASURE INSENSITIVITY, THEN DECIDE (`I17-SWEEP`)**~~ | ✅ **DONE — commit `1b2adbe`.** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC):** all **26** literals were MEASURED rather than argued (608 probe points + 5 joint grids) — **17 made synthetic · 5 kept (4 load-bearing + 1 design constant) · 4 already never-shipped.** Packets [`i17-sweep-batch-summary.md`](i17-sweep-batch-summary.md) (the band table and the arithmetic identity) + [`i17-sweep-spec-back.md`](i17-sweep-spec-back.md) (handles + the ruled D-table). ⛔ **Take the counts from the batch summary, NOT from the commit message — `1b2adbe`'s subject carries the superseded 13 / 8 figures, and the batch summary's own superseding note records the correction to 17 / 5 / 4.** ⭐ **The sweep produced the MASKED class, which outlives the item:** a wide one-at-a-time band can be a sibling artefact rather than insensitivity. *Original size estimate follows.* ~~⭐ **RULED 2026-09-05: measure in depth first, then consider FULL SYNTHETIC on the results.** Two steps, one session~~ | ⚠ **NEW 2026-09-05**, found while scoping `I17-A6` — measured against the tracked `settings.json`, not inferred. **Item 17's convention has TWO halves: a declaring comment AND an obviously-synthetic value.** Session 2 delivered the first at all 26 call sites and, by design, the **opposite** of the second: each literal was set to *"the method's own former default, byte-identical to the pre-S2 implicit value."* ⛔ **21 of the 26 therefore equal shipped exactly** — `CalcMTFGate` all four (9/20.0/2/60) ×2 sites · `CalcTFI` both (30/0.15) · `CalcMicroCVD` three of four (50/10000/0.25) ×2 sites · `CalcCVD` late/early (2.0/1.0) · `CalcOBV` `divergenceGate` (0.001) ×2 sites. **A MECHANISM literal that equals shipped is maximally confusable, and goes silently stale the moment shipped moves — `A6`'s entire history.** ⭐ **The two rules are NOT in conflict, they are SEQUENTIAL:** session 2 was right to freeze behaviour during the edit; item 17's convention applies afterwards. ⛔⛔ **THE TRADER RULED (2026-09-05): do NOT decide from the packet's argued insensitivity — MEASURE it per literal, then consider full synthetic on the results.** **STEP 1 — the sensitivity sweep:** for each of the 26, re-run the harness with the literal set across a range spanning orders of magnitude plus a clearly-synthetic candidate; record the band over which the fixture's assertion is unchanged. Wide band ⇒ genuinely inert ⇒ synthetic is safe, **and the sweep hands you the value as a by-product** (pick from the middle, far from shipped). Narrow band containing shipped ⇒ load-bearing ⇒ leave the value, and the comment must say the equality is **INCIDENTAL**, not a claim. ⛔⛔ **THE TRAP THAT MAKES A NAIVE SWEEP WORSE THAN USELESS: a fixture can keep PASSING for the WRONG REASON at an extreme value.** ⭐ **`A6` is the exemplar — its `trendA = trendB` half is satisfied by both being FLAT, so a huge `trendGate` would look "inert" while the fixture had become vacuous.** *(`A6` survives only because it ALSO asserts `trendA = "RISING"` explicitly.)* **So the sweep must assert the ASSERTED VALUES are unchanged, not merely the pass/fail bit** — and any fixture whose `Check()` compares two computed values without pinning either to a literal expectation must be flagged, because it cannot be swept safely. ⚠ **Second trap: interacting literals** — `accelThreshold` + `dynamicPct` + `floorPct` combine into one `effThreshold`, so vary one at a time FIRST, then the group together. **STEP 2:** apply — synthetic where step 1 licenses it, incidental-equality comment where it does not. **Bundle `I17-A6` into the same session.** **Model: Sonnet, effort medium** — the sweep is mechanical but the vacuous-pass trap is not |
| ✅✅ **`S2-2` — BUILT AND CLOSED 2026-09-06. `CalcSpread` is DELETED; `CalcSpreadBps` (`Double?`) + `ClassifySpread` replace it** | ✅ **DONE — all six §4b items, committed locally, NOT pushed** (the trader pushes after testing). Packets: [`s2-2-calcspread-split-batch-summary.md`](s2-2-calcspread-split-batch-summary.md) · [`s2-2-calcspread-split-spec-back.md`](s2-2-calcspread-split-spec-back.md) | ✅ **OUTCOME FIRST, then the original row.** Harness **328 → 332** (`A65a`–`A65d`); six Release builds 0/0 each run separately; `verify-gate.ps1 -Mode local-fast` **GATE PASSED**; **settings unchanged, still v68**; **two production call sites edited, exactly as scoped** — no escalation trigger fired. ⛔ **The display-string parity rule was LIVE and was PROVED, not asserted:** snapshot `Spread:` line + breakdown note + both card bindings' resolved `(text, colour)`, captured pre- and post-change over **eight book shapes including four degenerate**, **byte-identical, same MD5**; the three render files are untouched in the diff. ⭐ **The baseline was RE-MEASURED at the build's own base commit `f6a2c08` (328 PASS / 0 FAIL / ALL PASS), not inherited from the spec's `2e8bc76` reading.** ⭐ **All three of the spec's §6 mutations were RUN:** return `0.0` on the empty-ladder guard ⇒ `A65a` FAILS with the literal defect signature (`hasValue=True status=TIGHT`) and the other three pass · `ClassifySpread` always `"NORMAL"` ⇒ `A65b` FAILS **while `A65a` still passes**, which is exactly why §6 insists the two are a pair · arms swapped ⇒ `A65c` FAILS alone. ⚠ **ONE RESIDUAL IS LIVE BY RULING:** `D-3` (b) left `HasTopOfBook` (3 conditions) diverging from `CalcSpreadBps` (6) on a zero-priced top of book — confirmed in the parity capture, where that shape reads `snap.HasSpread = True` beside a `Nothing` bps. **Its own slot, not a tidy-up.** **Original row follows.** ⛔⛔ **WRITING IT FOUND A DEFECT IN THE FIX AS SPECIFIED, and that is why it needs a tick rather than a builder.** The analysis's own shape — `CalcSpreadBps(orderBook) As Double` + `ClassifySpread(bps, wide, tight)` — **silently changes a rendered value on FOUR surfaces.** ⭐ **`CalcSpread` sets `spreadStatus = "NORMAL"` at `Core/Indicators_OrderFlow.vb:586`, BEFORE its five early-return guards — so the `"NORMAL"` is the DEGENERATE-BOOK ANSWER, not a default.** Split naively, a degenerate book returns `0.0`, and `0.0 <= tight_threshold_bps` (1.5) classifies it **`TIGHT`**. ⛔ **Shipped distinguishes two zeros — a LOCKED book (`bestBid = bestAsk > 0`) is genuinely `TIGHT`, a DEGENERATE book is `NORMAL` — and the naive split collapses them.** ⚠ **Rendered effect at 5.0/1.5: the breakdown note, the snapshot line and BOTH card bindings all move, and the card colour flips `FG_TERTIARY` grey → `ACC_STRONG_LONG` green.** **A book the engine could not read would render green "TIGHT" — best-possible execution — when the truth is no data.** ⭐ **Scoring is unaffected** (only `"WIDE"` is scored). ⭐ **Reachability read, not assumed: `orderBook Is Nothing` is NOT reachable at `UI/MainForm_Analysis.vb:422` (`:139` skips the run first), but the other four degenerate shapes ARE.** ⚠ **Frequency NOT measured and not measurable from the book — `SpreadStatus` has no CSV column and both cases log `0.00`.** **Five decisions open: `D-1` the degenerate contract (my read `Double?`) · `D-2` where `"NORMAL"` lives · `D-3` whether `HasSpread` moves onto it (⚠ `HasTopOfBook` tests 3 conditions, `CalcSpread` tests 6 — NOT equivalent) · `D-4` delete the old method or keep a wrapper · `D-5` naming.** ⭐ **Baseline MEASURED at commit `2e8bc76`: harness 328 PASS, `ALL PASS`.** ⛔ **Model UPGRADED to Opus, effort HIGH** — the only item in the arc where the display-string parity rule is live, and the obvious implementation reads correct. **Original row follows.** ⚠ **NEW 2026-09-05**, raised by the trader while ruling `S2-1` — analysis at [`a54a-session2-step1-measurement-2026-09-05.md`](a54a-session2-step1-measurement-2026-09-05.md) §9. **`LiveMicrostructureEvaluator.vb:135` wants only `spreadBps`, so it declares a throwaway `sStatus` it never reads** (`snap.HasSpread` comes from `HasTopOfBook`). ⚠ **`S2-1` (B) makes this slightly worse before better — that call site will pass two cfg thresholds purely to compute a status it discards.** ⭐ **THE FIX: split — `CalcSpreadBps(orderBook) As Double` (pure) + `ClassifySpread(bps, wide, tight) As String`.** ⭐⭐ **The benefit is not tidiness: the evaluator would stop referencing the spread thresholds AT ALL, so the tweaker-retune divergence `S2-1` exists to guard becomes IMPOSSIBLE rather than "currently correct" — this project's own strongest pattern (`ComputeSideLevels` one-seam-no-copies, `TradeStoreWriter` writer-and-reader-cannot-drift).** It also turns a stale-able comment (*"the bps is threshold-independent"*) into a type signature, and deletes `S2-1`'s one call-site line. ⛔ **NOT session 2's job — it is a refactor of a shipped indicator's public API and needs its own proposal + D-table per CLAUDE.md's spec-first rule.** ⚠ **The classify half is REAL and must not be lost: `SpreadStatus` feeds the Step-2 WIDE penalty, the breakdown note, the snapshot line and TWO card bindings — FOUR rendered surfaces, so the display-string parity rule is LIVE for this one, unlike everything else in this arc.** ⭐ **Zero fixture churn** — fixtures set `r.SpreadStatus` directly and never call the method. **Model: Sonnet, effort medium** |
| ⛔ **Session 2 `CalcSpread` exclusion — the ruling's stated REASON is FALSE (`S2-1`)** | ✅ **IMPLEMENTED 2026-09-05** | ⚠ **NEW 2026-09-05**, from the session-2 step-1 measurement review — [`a54a-session2-step1-measurement-2026-09-05.md`](a54a-session2-step1-measurement-2026-09-05.md) §3 (implementer disclosure) + §7.2 (reviewer finding). ⭐ **Step 1's headline is 0 production omissions and 26 fixture omissions — but the 0 is CONDITIONAL.** `LiveMicrostructureEvaluator.vb:132-135` IS a production site omitting BOTH `CalcSpread` threshold params; it counts as 0 only because [`trader-tick-queue.md`](trader-tick-queue.md) §0a excludes *"the documented `CalcSpread` discard"* as an *"internal convenience with no settings counterpart"*. ⛔ **That reason is FALSE for those two params — `SpreadSettings.WideThresholdBps`/`TightThresholdBps` exist.** ⭐ **It IS true for `spreadStatus`, the ByRef output the caller declares and never reads — which is what "the discard" most naturally names.** Under that reading the count is **2** and the trigger fires. ✅ **RULED (B) 2026-09-05 — make both required and pass `cfg.Indicators.Spread.*` at the one call site (`cfg` is in scope at `:103`).** ⛔ **The reviewer had recommended (A) and was WRONG on two unchecked facts — fifth correction in this arc, recorded at the measurement doc §8.** ⭐⭐ **`indicators.spread.` is NOT in `SettingsDiffApplier.RejectedPathPrefixes` (19 prefixes read in full) — it is AUTO-TWEAKER-TUNABLE, and it gates the Step-2 WIDE scoring penalty. So a tweaker retune moves `MainForm_Analysis` and leaves `LiveMicrostructureEvaluator` on the compiled-in 5.0: the OBV drift shape, created by a TOOL rather than by a human forgetting.** ⭐ **Zero behaviour change today** (all three copies read 5.0/1.5); population 42 → 44, production omissions 2, both closed by the one edit; **no fixture touched** — `CalcSpread` has exactly two call sites tree-wide and fixtures set `r.SpreadStatus` directly. ⛔ **Either way REWRITE the exclusion note with a true reason.** ⚠ **Root defect is the spec author's — he carried the exclusion forward while already knowing its reason was false. Fourth correction in this arc.** **Model: Sonnet, effort low** |
| ~~⚠ **A54a walk — the REVERSE dictionary direction is unguarded (`F-1`)**~~ | ✅ **DONE — commit `a86a0cf`, *"F-1 - report a POCO-only dict key as Orphans (reverse of R-2 (a))"*.** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC):** `grep -c "Orphans" verify/ordercheck/Program.vb` prints **21**. A POCO-only dictionary key is now reported as `Orphans` in all four dictionary properties, and `A62a`'s existing `Orphans = 0` guard covers it free. *Original size estimate follows.* ~~one classifier arm behind a short ruling~~ | ⚠ **NEW 2026-09-05**, from the R-2/R-3 review — [`a54a-r2-r3-followup-spec-back.md`](a54a-r2-r3-followup-spec-back.md) §4 (implementer disclosure) + §5.3 (queued by the reviewer). **R-2 (a) closed the JSON→POCO direction: a JSON dict key with no POCO counterpart is now reported as `JsonOnly`.** ⛔ **The reverse — a POCO dict key absent from JSON — is still SILENTLY SKIPPED**, in all four dictionary properties (`ResolutionProfiles` `:113` · `AggressorVelocitySettings.Sessions` `:487` · `AbsorptionSettings.Sessions` `:566` · `StructuralLevelsSettings.Sessions` `:1062`). ⭐ **Measured 0 today** by a manual key-set comparison against the tracked `settings.json` — **all four are `{NY,LONDON,ASIA}` on both sides and `resolution_profiles` is `{"1","3"}` on both sides** — ⚠ **but nothing in the tree will flag it if that changes, and the check was manual, not a fixture.** ✅ **DISPOSED 2026-09-05 — it is a PATCH, not a ruling; the reviewer OVER-CLASSIFIED it and the correction is at [`a54a-r2-r3-followup-spec-back.md`](a54a-r2-r3-followup-spec-back.md) §6.** ⭐ **MEASURED: `System.Text.Json` REPLACES a pre-seeded dictionary rather than merging, so a POCO-only key never reaches the normal path — but `HasExplicitAggrVelBurstThreshold` keys on PRESENCE, so on the parse-failure path it would ARM a session production leaves inert.** No legitimate case exists ⇒ **it is DRIFT. Build it: report as `Orphans` (not a new category — `A62a`'s existing `Orphans = 0` guards it free), extend `A63b`'s arms, fix the step-6 comment.** ✅ **BUILT AND ACCEPTED 2026-09-05, commit `a86a0cf`** — spec-back §7 (outcome) + §8 (reviewer verdict). ⭐ **The escalation did NOT fire: `Orphans` = 0.** ⭐⭐ **Confirmed under TWO independently-written instruments — the walk's own `A62a`, and a reviewer probe that does not reimplement it and compares POCO-vs-JSON dict key sets in both directions: 4 dictionaries reached, POCO-only 0, JSON-only 0.** The manual key-set comparison is now corroborated rather than believed. **Both `A63b` arms independently mutation-proven, neither masked by the other.** ⭐ **The step-6 comment was edited in place — the first time in this arc handle 9's class was avoided by construction rather than caught in review.** ⚠ **The disclosure was correct and the spec caused it** ([`a54a-r2-r3-followup-spec.md`](a54a-r2-r3-followup-spec.md) §1 said *"report the count and stop rather than inventing a rule for it"*); it is queued here because an honest disclosure inside a spec-back is not a tracked item, which is what the §1b sweeps exist to catch. **Model: Sonnet, effort low** for the fixture once ruled |
| ✅ **RESOLVED 2026-08-20 — `trade_seq` skip claim is UNSOURCED. Both candidate sources checked. Do not re-open without a MEASUREMENT.** | Closed | **Deribit's API reference documents `trade_seq` as exactly one line — *"The sequence number of the trade within instrument"* — and `trade_id` as *"Unique (per currency) trade identifier"*. NO sequentiality claim, NO skip claim, in either.** The best-practices page (pasted in full by the trader) does not carry it either, and states the opposite in spirit: *"events are delivered in the exact order they occurred"*, with a sequence gap framed as **the CLIENT missing a message**. **RULING: treat `trade_seq` as strictly sequential.** ⭐ **And note the store's own gap detector IS the instrument that would find out** — a venue skip is permanently unfillable, so it would surface as a hole that survives every repair pass inside the lookback. **If venue skips exist, D-2's detector reports them; no doc read can beat that.** Supporting: the write-guard sample measured **100 % on 2,033 trades.** ⛔ **`docs/trade-store-downtime-repair-proposal.md` D-2 needs NO caveat and MUST NOT be reversed.** Historical detail below kept for the lesson only. ⚠ **DOWNGRADED FROM "SETTLE FIRST" THE SAME DAY, 2026-08-20.** A web search returned *"under rare circumstances, it is possible for the trade sequence to skip a few numbers"*, attributed to Deribit's support page **Market Data Collection — Best Practices**. **The trader pasted that page in full. THE SENTENCE IS NOT ON IT.** ⚠⚠ **THE LESSON IS WORTH MORE THAN THE ITEM: a search-engine summary invented a source attribution, and it survived because the page 403s to automated fetch — so "unverified" was recorded honestly and the claim STILL nearly drove a settle-first action.** *An unverifiable claim is not a weak claim; it is not yet a claim.* **What the page ACTUALLY says cuts mildly the OTHER way:** *"Deribit guarantees that within each instrument's feed, events are delivered in the exact order they occurred"*, and it names **`trade_id`** (not `trade_seq`) for trades, framing a sequence gap as **the CLIENT missing a message**, never the venue skipping. **NOT disproved** — it may still live in the API reference's field description for the trades object, which is the ONLY place left worth looking. ⛔ **Do NOT re-read the best-practices page; it has been read in full.** **If it is ever confirmed:** a venue skip and a capture hole become indistinguishable — [`trade-store-downtime-repair-proposal.md`](trade-store-downtime-repair-proposal.md) **D-2 rules detection on `trade_seq` ALONE**, and its stated correctness property is two-sided (no missed holes AND no phantom holes). This is a **phantom-hole source the spec did not anticipate**, and it is **pre-existing venue behaviour, not a Starbase change** — which makes it worse, because the assumption may never have held. ⚠ **D-2 MUST NOT BE REVERSED** — a time tolerance is strictly worse, and this project's own lesson is that a fixed tolerance turns one bad fetch into permanent silent loss. **WHY THIS IS FIRST AND NOT QUEUED:** [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15's downtime-repair row **arms a falsifiable prediction** — hole filled within 6 h, and *"if a hole survives a full pass cycle, Part A did not find the whole problem"* — to be **verified from `trade_seq` completeness, NOT the repair log.** The next outage produces that reading, on a schedule nobody controls. Unsettled, a seat reads sub-100 % completeness and either declares Part A failed or opens the **Part B session §15 explicitly forbids** until Part A heals a real outage. **THE DISCRIMINATOR ALREADY EXISTS AND COSTS NOTHING:** a venue skip is **unfillable** (fetch the bracketing range, venue returns nothing); a capture hole is **fillable**. The repair already distinguishes them by outcome — **the open question is only whether it RECORDS the distinction.** The per-hole clamp at `TradeStoreWriter.vb:842` handles *unfetchable by time window*, **not** *nonexistent at the venue*. ⚠ **NOT VERIFIED FROM PRIMARY SOURCE** — the sentence appeared consistently across two independent searches attributed to Deribit's docs, but its source page (Deribit support, *Market Data Collection — Best Practices*) **returns HTTP 403 to automated fetch and needs a human browser.** **Deliverable: read the page, then one caveat line in [`trade-store-downtime-repair-proposal.md`](trade-store-downtime-repair-proposal.md) against D-2 and against the completeness instrument.** Weak counter-evidence, recorded so it is not mistaken for proof: the write-guard sample measured **100 %** on 2,033 trades, consistent with skips being genuinely rare — **consistent with, not proof of** |
| ⚠⚠ **THE ABSORPTION POPULATION IS BIMODAL AND THE TWO HALVES HAVE DIFFERENT BINDING GATES — measured 2026-08-20, and it outranks every anchor question** | Medium; changes what a mechanism fix must target | **MEASURED, both books, weekday, since v61 (2026-07-23). n=232 active episodes with `ratio>0`.** The denominator is recoverable from the CSV without new columns — `absorbRatio = aggrUsd / max(sizeStart − sizeMin, depletion_floor_usd)`, so **`den = AbsorptionAggrUsd / AbsorptionRatio`**. **`depletion_floor_usd` is 5000** (rescaled from 25000 at v61 — do not quote the v54 value). **THE SPLIT, and both books agree within 1.6 pp:** **FLOOR-BOUND** (den on the 5000 clamp ⇒ true depletion < 5000) **48.7 %** · **REAL DEPLETION** (den ≥ 5000, mean ~50k, max 788k) **51.3 %**. ⚠ **THE TWO HALVES ARE KILLED BY DIFFERENT GATES, so any single-anchor fix helps one and not the other:** **FLOOR half (n=113)** — `min_aggr_usd` is the SOLE binding gate: `aggr≥20k` **8.8 %** and both-gates **8.8 %**, *identical*, because den=5000 makes `aggr≥20000 ⇒ ratio≥4.0` pass `absorb_ratio` **automatically**. **REAL half (n=119)** — `absorb_ratio` binds hard: **twice** the money-gate pass rate (**17.6 %**) but ratio≥1.5 only **5.9 %**, because the large denominator kills it. ⚠⚠ **AND THE D8 PULL VETO OUTRANKS BOTH IN BOTH HALVES — REAL 77.3 %, FLOOR 56.6 %.** `pullFrac>1.0` alone is **50.4 %** of the REAL half; the FLOOR half carries a **12.4 % spike at EXACTLY 1.000**, the degenerate signature. **All three gates together: 5 survivors of 232 (1 REAL, 4 FLOOR)** — which reconciles exactly with the 5 fired signals in the 08-19 blind pass. **CONSEQUENCE FOR THE MECHANISM SPEC: the pull veto, not the anchors and not `window_sec`, is the dominant killer** — and `roadmap.md` already named its mechanism (*"D8 pullFrac inflation on sparse postLB"*) without knowing it was the top term. **Attack pullFrac first.** |
| ⚠ **REFUTED ON DIRECTION 2026-08-20, then REDIRECTED — the 100 ms book feed and the ratio** | Superseded by the row above; the raw-feed test SURVIVES with a new target | ⚠ **MY HYPOTHESIS WAS BACKWARDS AND THE MEASUREMENT KILLED IT. Recorded in full because the error is instructive.** I argued that `book.<i>.none.10.100ms` (`DeribitWsFeed.vb:27`) blurs the trough, so observed `sizeMin` sits too HIGH, so `sizeStart − sizeMin` is too small — **and then concluded the ratio was UNDERSTATED. A smaller denominator makes the ratio LARGER.** Blur would make absorption fire **MORE**, not less, which is the opposite of the 0.09 %-vs-3–8 % problem. **The data confirms the refutation cleanly: the FLOOR-bound half — by definition the SMALLEST denominators — has the HIGHER ratio pass rate, 16.8 % vs 5.9 %.** ⭐ **THE LESSON: a mechanism read from a config file is a hypothesis, not a finding, and its DIRECTION is the cheapest thing to check and the easiest to invert.** I had the arithmetic available the whole time. ✅ **BUT THE RAW-FEED TEST SURVIVES WITH A REDIRECTED TARGET — `pullFrac`, not the ratio.** D8 accumulates `pullLB`/`postLB` from **per-interval** ΔSize; posts and pulls that cancel *inside* one 100 ms bucket are invisible to both, so `postLB` stays sparse, the `depletion_floor_usd` clamp binds it to 5000, and **`pullFrac = pullLB/5000` inflates** — which is exactly the 12.4 % spike at 1.000 and exactly the mechanism `roadmap.md` already names. **TEST: subscribe `book.<i>.none.10.raw` in parallel and compare per-episode `pullLB`/`postLB` against the 100 ms fold.** ✅ **CREDENTIALS RULED AVAILABLE FOR THE COLLECTOR (trader, 2026-08-20)**, so the auth prerequisite is no longer a blocker — raw public feeds require an authenticated connection and that is now permitted. ⛔ **Still gate the mechanism-revision build on this**: if `pullFrac` is a measurement artefact, the veto threshold is not the thing to tune |
| ✅✅ **DEGRADED-FEED AUDIT — RUN 2026-08-20. SHIPPED AS v67 THE SAME DAY — the thin-trade-window skip gate.** ⚠ **THE GAP (below, historical): no MINIMUM trade count anywhere on the trade-derived path** | ✅ **CLOSED.** Spec [`thin-trade-window-skip-gate-proposal.md`](thin-trade-window-skip-gate-proposal.md), D1–D5 all ticked as recommended (trader-directed). `UI/MainForm_Analysis.vb` skip gate + `ExitGuardEvaluator.vb` (D-4) both now call new `ScoringEngine.MinTradesForScoring(cfg)` — derived, never a literal, = `Max(TFI.window_size, MicroCVD.window_size)` = 50 at shipped defaults, with `scoring.min_trades_for_scoring_override` (default 0) escape hatch. Settings v66 → v67; `DeribitIndicatorProject.md` §15 v67 row. HC28 fences the override off the tweaker surface. Fixtures A57a–d, mutation-proved (A57c fails against a hardcoded 50). D3 ruled **NOT** a dataset boundary — revisit if the post-ship watch (§8 of the spec: `_lastSkipReason` carrying `recent trades thin`, first weekday-week) shows a non-negligible skip rate. Local commit — trader tests + pushes. Historical audit detail below, kept for the lesson. | **`"transport": "ws"` is live (`settings.json:503`), so the WS seed path is the live path.** **WHAT IS CORRECT, verified site by site:** `IsDegraded()` (`DeribitWsFeed.vb:596`) fires on `Not _connected`, `_coolingDown`, or **all three** streams stale — a deliberate conjunction, because one stale stream is handled a tier lower by `WsMarketDataSource`'s per-shape gate returning `Nothing`, which the skip gate then treats like a REST failure. **The skip gate (`MainForm_Analysis.vb:123-149`) null-checks every required shape** — 1m/5m/exec candles with minimum bar counts, funding, book summary, order book, recent trades — plus `IsFresh` on exec and 5m. **A skip writes NO CSV row and emits the SKIPPED bridge payload** (stand down, previous signal stale), so the CSV→payload join stays total. **The three WS-only signals degrade correctly on REST fallback:** OFI averaging falls through to book-snapshot OFI (`:387`); aggressor velocity and absorption are simply never set (`:442`, `:463`) and keep their score-neutral defaults `"NORMAL"` / `"NONE"`. ⭐ **And all four trade-derived indicators SELF-INITIALISE to score-neutral BEFORE their empty guard** — `CalcCVD` sets `"FLAT"`/`"NONE"`, `CalcTFI` sets `"NEUTRAL"` — so they never rely on the caller's defaults. **That is the right pattern and it is applied consistently.** ✅ **The reconnect window I expected to find DOES NOT EXIST: `_connected = True` is set AFTER `Await SeedAsync` completes (`DeribitWsFeed.vb:205, 211`), so the health gate cannot pass on an unseeded ring.** ⚠⚠ **THE GAP — every guard tests `Count = 0`; NOTHING tests for a THIN list.** `WsMarketDataSource.GetRecentTradesAsync` returns `Math.Min(count, all.Count)` — however few. **Reachability: `SeedAsync` line 318 reads `If trades IsNot Nothing Then _state.SeedTrades(...)`. A transient REST failure on that ONE call silently skips the trade seed while candles seed fine, and the connect still proceeds to `_connected = True`.** The ring then holds only post-subscribe streamed trades. **At the measured ~1.4 trades/sec: TFI's 30-trade window needs ~21 s to fill, MicroCVD's 50 needs ~36 s, and CVD walks whatever exists.** ⚠ **CONSEQUENCE — it lands on the two heaviest scoring inputs: `TFIValue` from ONE trade is ±1.0 exactly, and `\|1.0\| > 0.15` ⇒ `"BUY PRESSURE"`/`"SELL PRESSURE"` at MAXIMUM magnitude from a single print.** MicroCVD (9 scoring references) computes thirds of a handful; CVD's `weightedSlope` over 3 trades is noise that can trip `slopeThreshold`. **A spurious TFI direction also opens the aggressor-velocity burst path, which is gated on `tfiLong Xor tfiShort`.** ⛔ **AND IT IS INVISIBLE: no skip, no log, no CSV marker, and there is no trade-count column — the row looks completely normal in the book, so this cannot be found retrospectively.** **`ExitGuardEvaluator.vb:58-64` mirrors the same window with the same `Count = 0` guard and no minimum, so an OPEN POSITION carries the same exposure.** **FIX: add a minimum trade count to the skip gate, set to the largest window any trade-derived signal consumes (MicroCVD's 50 is the natural floor).** ⚠ **Open spec question: rows that used to score would now skip — decide whether that is a dataset boundary** (the v32 D5 candle-freshness gate is the closest precedent and did not take one). **NOT CHECKED:** nothing was run live; `ExecuteWithRetry`'s retry count on the seed call was not traced; `_coolingDown`'s minimum duration was not checked against the seed-failure window. **Model: Sonnet, effort medium** — the change is small, but the boundary question and the fixture (a thin-list run must skip, a 50-trade run must not) are the judgment |
| ~~**`ForwardWindowJoiner` does not skip embedded header lines**~~ | ✅ **DONE 2026-08-25** — one guard at `analysis/ForwardWindowJoiner.vb`, matching `CsvFeatureBuilder.vb:139`'s form. **No counter added** — this class has no `LoadStats` equivalent and inventing one for a single guard is the scope creep the AutoTweaker D-4 ruling declined for the same reason. ⚠ **Confirmed NOT a live defect before fixing:** every consumer is offline (`AnalysisRunner`, `AutoTweakerCore`, `WhatIfProgram`, the harness) — **no live scoring path reads it** — and such a row carries `Verdict = "Verdict"`, which never matches a tier. Closed because the file was open. | **NEW 2026-08-20.** `tools/CeilingAudit/CsvFeatureBuilder.vb` skips them and counts `RepeatedHeadersSkipped`; `analysis/ForwardWindowJoiner.vb:108` does **not** — it would parse an embedded header as a data row. **NOT a live defect:** [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §3b's concat strips the second header, and the junk row would carry `Verdict = "Verdict"`, which never counts as STRONG, so **the Kelly count is unaffected either way.** A missing guard on the *unguarded* half of a pair, worth closing when that file is next open. **Both readers DO bounds-check column reads** (`CsvFeatureBuilder.vb:484`, `ForwardWindowJoiner.vb:219`), so a short v0.8 row read against a longer header map returns empty rather than throwing — **verified, and it is why a schema rotation does not crash either reader** |
| **Fills-import tool — outcome GROUND TRUTH from `private/get_user_trades_*`** | Small build, `tools/` | **NEW 2026-08-20, trader-queued explicitly as a LATER item.** ⛔ **It CANNOT substitute for the trade store and must never be proposed as one** — `private/get_user_trades_by_instrument` returns *"the latest **user** trades"*; the `historical` parameter changes **reach** (indefinite), never **scope**. The store needs the **public tape** (~60k trades/day, every participant); this returns **your blotter**. Authentication does not widen it. **WHAT IT ACTUALLY BUYS:** every outcome in this project today is **modelled, not observed** — `LivePerformanceTracker` and `ForwardWindowJoiner.PopulateForwardBars` resolve barriers against forward candles, which **cannot resolve intrabar path** (a 1 m candle touching both target and stop has no orderable answer in OHLCV). ⚠ **DO NOT USE FILLS TO COMPUTE OUTCOMES** — your fills are a sample of *the signals you chose to take*, so any win rate from them carries a selection bias the candle eval does not have; the candle eval's advantage is that it covers **every** run including the skipped ones. **USE FILLS TO VALIDATE THE MODEL THAT COMPUTES OUTCOMES:** measure the **disagreement rate** between candle-derived and realised outcome on trades actually taken. Agreement ⇒ the modelled eval is trustworthy across the whole population, which validates Kelly, the F1 tier ladder and everything built on them. Systematic disagreement ⇒ the bias is **measured** and correctable. ⭐ **This works on a SMALL sample** — it measures agreement, not win rate — so a few dozen fills say something, where every other gate here needs hundreds of rows. **Secondary, all measurement not signal:** slippage (actual entry vs `Placed*` — currently unmeasured), **fee drag** (v62's `trade_costs` **models** 0.0008; fills would measure it), stop-gap behaviour. **NO scoring signal improves — not one.** **HOME: `tools/`, NOT the engine** — host-agnostic per the CLAUDE.md Linux-port rule. ✅ **CREDENTIALS RULED AVAILABLE FOR THE COLLECTOR (trader, 2026-08-20)**, superseding this row's original recommendation to keep the box credential-free; the engine holds none today (`DeribitClient.vb:3`: *"No authentication required -- all endpoints are public"*), so this and the raw-feed absorption test are the **first two** things that would put a key there. Mint a **`trade:read`-only** key (the endpoint needs nothing more); never in the repo, never in the deploy allowlist. ⚠ **The key must be provisioned INTO the Linux cutover (CLI port Stage 5), not onto the Windows box first** — otherwise it is provisioned twice and revoked once. **TIMING: needs fills to EXIST.** The dataset starts accruing the day the bridge trades, not the day the tool is built. **Model: Sonnet, effort medium** — one authenticated fetch, a join on `SignalId`, a disagreement table; escalate to Opus if the join proves ambiguous (**a fill spanning two runs is the trap**) |
| ~~⚠⚠ **THE STREAMING CAPTURE PATH DROPS ~50 % OF TRADES — `TradeStoreWriter.vb:149`**~~ ✅✅ **FIXED, SHIPPED AND DEPLOYED — it does not drop them any more.** | ✅✅ **DONE — the identity-keyed write guard shipped 2026-08-11 and DEPLOYED the same day: AWS `a5d701ad-eea1-4ba0-97a5-2ea05274c8c5` at 2026-08-11 17:18:36Z.** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC).** ⛔⛔ **THIS WAS THE MOST MISLEADING CELL IN THIS SECTION AND IS THE REASON THE 2026-09-07 SWEEP EXISTS — it read *"LIVE, ongoing, and time-critical"* for four weeks after the fix was live.** **The evidence, run rather than quoted:** `Core/TradeStoreWriter.vb:76` now describes the old guard in the **PAST** tense — *"The shipped guard **was** `If t.Timestamp <= _lastTs Then Return False`"* — and `grep -c "Private _lastTs" Core/TradeStoreWriter.vb` prints **0**, so the field is **deleted**, not demoted to a pre-filter (a pre-filter would have reinstated the defect exactly). `Buffer` now tests MEMBERSHIP of a bounded recent-trade window using the relation `DedupTrades` already defines. ⭐ **And the falsifiable prediction HELD on a 2,033× larger sample:** the 2026-08-14 copy-back gives **134,204 rows, `trade_seq` span 134,204, ZERO missing — 100.000 %.** Record: [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15, the *"Trade-store write guard keyed on IDENTITY, not on a millisecond"* row. *Original size estimate follows.* ~~Small change, **high**-risk seam; needs its own spec~~ | ⚠⚠ **NEW 2026-08-11 and it is LIVE, ongoing, and time-critical in the one way nothing else here is: tape lost past ~24 h is unrecoverable at any price.** Full write-up: [`trade-store-same-millisecond-drop-2026-08-11.md`](trade-store-same-millisecond-drop-2026-08-11.md). **The guard reads `If t.Timestamp <= _lastTs Then Return False`** — a millisecond timestamp used as an identity. Deribit reports a sweep across price levels as **several records at the same millisecond**, so the writer keeps the first leg and silently discards the rest. **Confirmed by two independent instruments that were never told about each other:** (a) a 20 h window captured twice by the same box — REST repair holds **41,013** distinct rows against streaming's **20,477**, with repair ⊇ stream at **20,443 of 20,443 timestamps and zero reverse**; (b) ⚠ **`trade_seq` gap detection on the 431 identified rows — 385 of 816 sequence numbers MISSING (47.2 %)**, which needs no second book, no venue call and no merge. ⚠ **This is NOT the withdrawn 78.8 % shape** — that was two boxes with no mechanism; this is one box, strict one-directional containment, and a mechanism read in the code. Two rival explanations were tested and rejected first (row multiplicity; WS aggregation — refuted because aggregation preserves volume and the repair's mean trade is *larger*, 3,850 vs 3,013). ⚠ **NOT a scoring problem** — the engine reads trades from `MarketState`'s in-memory ring, never the store; nothing has ever been calibrated off the tape, so **nothing needs redoing.** The cost is future optionality. ⚠ **And the loss is BIASED, which is worse than its size**: it preferentially drops the later legs of sweeps — exactly the aggressive prints CVD / TFI / aggressor velocity exist to measure. ✅ **THE DELIVERY GATE HAS RUN AND PASSED — 2026-08-11 on AWS, all three answers green.** G1: **max 24 trades on one millisecond** ⇒ the feed does deliver siblings. ⚠ **G2: the delivered `trade_seq` stream is PERFECTLY CONTIGUOUS** — span 313, distinct 313, zero gaps ⇒ **the feed loses nothing, so the guard is the SOLE cause and the spec is SUFFICIENT; no second workstream is owed.** G3: **49.2 % dropped**, against 50.1 % and 47.2 % from the two stored-data routes — **three independent methods agree.** ⚠ **The mechanism is now observed, not inferred:** `accepted` (159) equals `distinct timestamps` (159) exactly, and **69 of 70 multi-trade timestamps arrive in a single notification batch**, which is where `_lastTs` advances on `Buffer`. **The worked example:** one aggressive sell walking the book 63904.00 → 63875.00, 24 unbroken `trade_seq`, **239,990 USD** — the guard keeps the first leg (50,000) and drops **189,990 USD, 79 % of the sweep.** ⚠ **Sample bias runs the safe way** — Deribit had just left maintenance so volume may be depressed, meaning fewer sweeps and a **conservative floor**. **The spec is now gate-authorized; only its §4 D-table awaits a tick.** **The fix is not `<` — that re-admits every reconnect duplicate the guard was written to stop.** It needs `trade_id`, which now ships. **Model: Opus. Effort: high** — a guard whose failure mode looks like correct behaviour, in a never-throws path. ⚠ **Escalation trigger: if the new fixture cannot be made to FAIL on current code before the fix lands, stop — the test is not testing what it claims** |
| ⚠ **AWS copy-back — CSV *and* store** | ✅ **DONE 2026-08-14 and INSTALLED.** Store `trades_2026-08.csv` **278,761 → 441,279** (+162,518), July **untouched at 122,018**; `analysis_log_aws.csv` **14,584 → 18,984**, header identical and proven a strict superset first. ⚠⚠ **TWO TRAPS FIRED — both recorded in [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §4b.** (1) **July must NOT be copied** — the repo file holds **115,029 distinct lines AWS lacks** (AWS's July is only what repair reached back into; the repo's is a full REST backfill), so a copy destroys 118,775 rows. (2) ⚠⚠ **Whole-line dedup — which step 5 specified — would have destroyed 23,806 genuinely distinct rows**: the legacy era is five-field and two distinct trades can share all five. **The rule is now CONCATENATE, never dedup on disk** — duplicates are harmless by design and the read path applies the identity-first `DedupTrades`, a stronger relation than whole-line. ✅ **All 11 AWS `InstanceId`s cross-check clean against the §5a ledger** — the step that was skipped last time now passes. ⛔ **`capture_marker.log` was NOT copied back** and step 1 never asked for it; that blocks the SH-1/D-3 real-world check | **Newly recorded 2026-08-07 and it gates the most.** `analysis_log_aws.csv` in hand ends **2026-07-31 13:59** (7 days stale) and the AWS *trade store* has **never** been copied back. It blocks: the **Kelly dated trigger**, the **W6-4 re-run**, the first coverage report against the real AWS store, and resolving the `bin\Release` tape. Bundle the two dated reads into **one pooled freeze on one span** |
| ~~⚠⚠ **`trade_id` + `trade_seq` in the trade-store schema — ✅ BUILT + ✅ REVIEWED 2026-08-08 — ONLY THE AWS DEPLOY REMAINS**~~ ✅✅ **THE DEPLOY IS DONE. NOTHING REMAINS.** | ✅✅ **DONE — DEPLOYED 2026-08-10 14:08:39.770 UTC, AWS `d8678d2b-94c4-4308-adc1-a88a51c2feea`** ([`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a, *"THE TRADE-IDENTITY DEPLOY"* — a 4.7-second connect, NOT a scoring boundary because `settings.json` did not move). ✅ **CLOSED, verified in the tree 2026-09-07 (UTC)** against that ledger row and against [`trader-tick-queue.md`](trader-tick-queue.md) §0a's *"Explicitly NOT owed"* list. **Every tape row after that instance id carries `trade_id` + `trade_seq`; the five-field clock stopped there.** ⚠ **`trades_2026-08.csv` is a mixed-shape file from that id onward** — the case ratified as Q1 while the affected population was still zero. *Original build record follows.* ✅ **BUILT — §1 gate PASSED** (both feeds carry both fields; `trade_id` is a JSON **string**, `trade_seq` a JSON number ~296 M so `Long` not `Integer`; a trade seen on both feeds carried the same values). Six projects 0/0 Release · harness ALL PASS, 265 checks, A53a–h added · verify-gate `prepush` GATE PASSED. Review packet: [`trade-store-trade-identity-spec-back.md`](trade-store-trade-identity-spec-back.md). ⚠ **ONE RULING GATES THE DEPLOY — spec-back Q1:** the spec's §3.4 dedup contract is **not a well-defined equivalence relation** (a legacy row can equal two identified rows that differ from each other — and Deribit returned exactly that pair in the first three trades fetched), so the build had to choose a resolution. It resolves **identity-first**, order-independently, erring toward not double-counting. **Ratify before AWS writes identified rows into files that already hold legacy rows** — changing it afterwards re-interprets tape already on disk. ~~⚠ **The AWS redeploy is still owed and is part of this change**; every hour until it lands is permanently unmergeable five-field tape.~~ ✅ **CORRECTED 2026-08-13 — DEPLOYED 2026-08-10 14:08:39 UTC**, AWS `d8678d2b…` ("THE TRADE-IDENTITY DEPLOY", [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a). **Every tape row after that id carries `trade_id` + `trade_seq`.** The five-field clock stopped there. *Original authorisation text follows.* ✅ **D1–D7 ALL TICKED 2026-08-08 (trader), every row as recommended** — [`trade-store-trade-identity-proposal.md`](trade-store-trade-identity-proposal.md). Row **D1** = take **both** fields. ⚠ **D7 ticked means the local-sampler question RE-OPENS after this ships — it is not authorisation to resume capture now.** ⚠ **The AWS redeploy is part of this change, not a follow-up:** `settings.json` does not move, so AWS keeps writing five-field rows until it gets the new binary, and every hour in between is permanently unmergeable tape. **Model: Opus, effort: high** (§0 of that spec). Two findings from writing it: **`TryParseRow` guards on `parts.Length < 5`, not `= 5`, so appending columns is already backward-compatible** — no rotation, no rewrite of existing months; and **`trade_seq` is the more valuable field** — a per-instrument monotonic sequence gives gap detection *from the store alone*, with no network and **no ~24 h retention limit**, which retires the clock that made the daily S0 job urgent. ⚠ **Gate before any build: confirm the WS channel actually carries both fields.** Our code never reads them, so the codebase is no evidence about what Deribit sends | ⚠ **NEW 2026-08-08.** Deribit's trade records carry a `trade_id`. `HistoricalStore.vb:307-317` reads price/amount/direction/timestamp/liquidation and **never reads it**, so `TradeRecord` has no identity and the store row is five fields. `TradeStoreWriter.FormatRow` — that row — is what the store dedups on *and* what S0's venue diff matches on (`CoverageReport.vb:310, 486, 492`). **Three consequences, all live:** two tape books **cannot be correctly merged** (proved 2026-08-08 — a merge doubled volume in the tested window); the store **silently drops genuinely distinct trades at write time** when five fields collide (22,376 of AWS's 228,163 August rows are exact duplicates and nothing can say how many were real); and any venue diff is **uninterpretable**. ⚠ **Retrofit is impossible** — rows already on disk have no identity to recover, so every day of tape captured before this ships is permanently unmergeable. **Write path + dedup + venue diff + a CSV rotation.** **Model: Opus. Effort: high** — the thing being fixed is precisely a case where wrong looks right |
| ~~⚠⚠ **Gap repair cannot heal DOWNTIME either — the cursor skips the hole within seconds**~~ ✅✅ **PART A IS BUILT AND DEPLOYED — this row is CLOSED.** ⚠ **Part B is a SEPARATE and still-forbidden item; the closure does NOT cover it.** | ✅ **PART A BUILT (`c6c6942`) + REVIEWED AND ACCEPTED 2026-08-13.** ✅✅ **DEPLOYED 2026-08-13 13:10:32.637 UTC, AWS `e551f15e-b245-4392-8e71-89e749636f1c`** ([`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a, *"THE DOWNTIME-REPAIR DEPLOY"*) — it carries Part A **plus** `DR-1` (no width floor) and `DR-2` (time-contiguous truncation cut) from `91942d6`, and is a restart/code edge only, **not** a version edge (AWS had been on v66 since `3be7f4c9…` on 2026-08-10). ✅ **CLOSED, verified in the tree 2026-09-07 (UTC)** against that ledger row. ⭐ **AND THE FALSIFIABLE PREDICTION HELD ON ITS FIRST REAL TEST:** the 2026-08-27 00:51→00:54 `DOWN` is a genuine feed outage, not a restart (the instance id is identical either side, where every prior `DOWN` carries a new one), and across the outage window there are **188 rows, 188 distinct `trade_seq`, span 188, MISSING 0** — see [`seat-handover-2026-08-29.md`](seat-handover-2026-08-29.md). ⛔ **PART B IS STILL NOT BUILT AND IS STILL FORBIDDEN.** Part A has now healed one real outage, so Part B's `G-1` gate **can be re-argued — but nobody has re-argued it**, and re-arguing it is a decision, not this row. ~~⛔ **NOT DEPLOYED — that is all that remains.**~~ Spec [`trade-store-downtime-repair-proposal.md`](trade-store-downtime-repair-proposal.md) · review packet [`trade-store-downtime-repair-spec-back.md`](trade-store-downtime-repair-spec-back.md), **its §6 carries the rulings and two new findings**. Harness re-run independently: **ALL PASS**, six A56 fixtures. Q1 (BREAK not SKIP) ratified · Q2 (three constants) ruled · the spec's `V4` handle **withdrawn** — it counted a name and would have rejected a correct build. ⚠⚠ **The deploy bundles the v66 settings, NOT "the write-guard fix" as the packet's Q4 said — that shipped 2026-08-11 17:18:42Z and is verified.** `settings.json` **hot-reloads**, so **stop → swap → start on both boxes** and record both ids in [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5a. ⚠ **Part B is NOT built and must not be started** until Part A is deployed and one outage is observed to self-heal — that spec's §2.4 | ⚠⚠ **The original finding (2026-08-12), kept because it is the reason the fix looks wrong to a hurried reader.** Full trace: [`trade-store-same-millisecond-drop-2026-08-11.md`](trade-store-same-millisecond-drop-2026-08-11.md) **§5a-bis**. **Measured hole: 2026-08-11 08:59:56 → 10:00:12, 60.3 minutes, zero rows, never filled** despite repair having seven hours and two scheduled passes. **Why:** `3be7f4c9` ran unbroken across the outage, so **no restart ⇒ no start-triggered pass**; and `ResolveResumeCursorMs` begins at **the file's last written row**, which streaming made current again the moment it reconnected. **The rule: repair recovered downtime ONLY if the app was RESTARTED after the outage, or a scheduled pass landed inside it. An app that rode through a blip and reconnected — the common case — lost the hole permanently.** ⚠ **Do NOT "fix" this by widening `gap_repair_lookback_hours`** — the cursor skips the hole, not the window. ✅ **Both candidates were ranked and (a) shipped:** the cursor is now seeded from the **hole**, not the tail. Firing a pass on WS reconnect is Part B and is deferred |
| **`DR-1` — `MinHoleMs` filters completeness on a TIME axis, the wrong axis** | ✅ **BUILT, REVIEWED, ACCEPTED AND COMMITTED — `91942d6`.** ⚠ **This cell read "⛔ UNCOMMITTED — working tree only" until 2026-08-25, twelve days after the commit landed** — the same shipped-state rot §1a exists to prevent, in the row that records the fix for it. Decision §1.2 ticked as recommended: **the width floor is REMOVED, not replaced with a count floor.** The one drop kept is the inverted (sub-millisecond) range, now **counted and LOGGED** in the shared conditional where it was silent. Packet: [`downtime-repair-followups-spec-back.md`](downtime-repair-followups-spec-back.md), **its §5 carries the review**; outcome record: [`downtime-repair-followups-batch-summary.md`](downtime-repair-followups-batch-summary.md). Harness re-run independently: **0/0 Release, ALL PASS** | ⚠ **NEW 2026-08-13**, [`trade-store-downtime-repair-spec-back.md`](trade-store-downtime-repair-spec-back.md) §6.2. ⚠ **RENAMED from `F-1` 2026-08-13 — `F1`/`F2` already name C1-coverage findings and the `ResetBufferState` race; the old names would send a grep to the wrong defect.** **A defect in the SPEC, faithfully built.** D-2 rules detection on `trade_seq` only *"never on a time gap"*, then step 6 drops any hole under **2,000 ms** however many sequences are missing. ⚠ **It blinds repair to exactly the sub-2-second losses `F2` (`ResetBufferState` race, still open) produces**, and the drop is **silent** while the `MaxHolesPerPass` cap logs. **On healthy tape it protects against nothing** — `delta ≤ 1` already emits no window. **The floor belongs on missing-sequence COUNT, or nowhere.** ⚠ **`A56b` part 4 pins the current behaviour as correct**, so the cost of changing it rises with time. Not deploy-blocking; it can only fail to recover, never lose. **Model: Sonnet, effort: low** |
| **`DR-2` — repair scan truncation removes rows in FILE order while claiming to keep the newest** | ✅ **BUILT, REVIEWED, ACCEPTED AND COMMITTED — `91942d6`, alongside DR-1.** ⚠ **This cell also read "⛔ UNCOMMITTED" until 2026-08-25.** The cut is now **time-contiguous**: sort once per drop block, drop the oldest by time, remember `floorMs` and discard later rows below it on arrival. ⚠ **I re-derived the contiguity invariant from the code rather than accepting it — it holds.** `MaxScanRows` stayed `Public Const`; `ScanForRepair` took a `maxScanRows` parameter and moved `Private`→`Friend` (with `SeqPoint` following it). New fixture **`A56g`**, mutation-proven. Packet §5 carries the review | ⚠ **NEW 2026-08-13**, [`trade-store-downtime-repair-spec-back.md`](trade-store-downtime-repair-spec-back.md) §6.3. ⚠ **RENAMED from `F-2` 2026-08-13** — see the `DR-1` row. **LATENT, not live** — needs >500,000 rows in one scan against a 20 h lookback that yields 37–72k, so **~7× margin**. `ScanForRepair`'s `RemoveRange(0, N)` runs while the list is still in **file order**; the sort happens later. For an out-of-order store those are not the oldest rows, so the retained set gets interior timeline gaps and the walk emits **phantom holes with huge missing-seq counts that evict every real hole** from the cap. ⚠ **Reachable only via `gap_repair_lookback_hours`, an unguarded settings key** (bites past ~140 h — pointless against a ~24 h retention, but nothing stops it). ⚠ **Fix the COMMENT either way** — it asserts a property the code does not have. **Model: Sonnet, effort: low** |
| **`DR-3` — `TotalRowsRepaired` over-reports: it sums the whole FILE, not rows appended** | ✅ **DONE — BUILT, REVIEWED AND COMMITTED as `5346bc0`, packet `feee138`.** ⚠ **This cell described DR-3 as available work — model, effort, brief and a "step 1 is to read…" — until 2026-08-25, twelve days after it shipped.** ⛔ **All three DR rows were stale in the same direction on the same day: two said UNCOMMITTED, this one read as unstarted.** *(Original scoping text retained below.)* Sonnet / **medium**; brief: [`downtime-repair-followups-implementer-briefs.md`](downtime-repair-followups-implementer-briefs.md) §3. **No decision owed.** ⚠ **Its OWN conversation — different seam from `DR-1`/`DR-2`.** ⚠ **Step 1 is to read what `BackfillAllAsync` does with the return value; the scoping IS the work.** ⚠⚠ **RULED 2026-08-13: this does NOT gate the deploy — DEPLOY `DR-1`/`DR-2` FIRST, do this after.** `V6` reads `trade_seq` completeness from the **store**, not the repair log. Three reasons in the brief's §0.1, the decisive one being that the defect being fixed is **unrecoverable tape loss on a ~24 h clock** while this is a logging fix. ⚠ **And this is the only one of the three that can touch a path OUTSIDE gap repair** — bundling it would widen the deploy's blast radius for zero capture benefit. **Meanwhile: `pass complete — N row(s) appended` is NOT a recovery metric. Do not read it as one** | ⚠ **NEW 2026-08-13** (pre-existing behaviour, newly consequential), [`trade-store-downtime-repair-spec-back.md`](trade-store-downtime-repair-spec-back.md) R7 and §6.5. ⚠ **RENAMED from `R7` 2026-08-13** — every spec-back in `docs/` reuses that index. `BackfillTradeMonthAsync` returns `CountDataRows(path)` when there is nothing to fetch — **the common case on a healthy box** — so every healthy pass logs the entire month file (~300,000) as *"N row(s) appended"*. ⚠ **This degrades the instrument that verifies the downtime fix**: its falsifiable prediction is checked by watching a hole get filled. **Verify from the store's `trade_seq` completeness, not the repair log — or fix this first.** **Model: Sonnet, effort: low** |
| ⚠ **An up-interval starts at the `DOWN` line, so a CONNECT WINDOW reads as capture time — and any span inside it with no rows reads `Defect`** | needs a spec; ⚠ **the obvious fix trades a false defect for a missed one** | ⚠ **NEW 2026-08-14, observed in the first real coverage run after SH-1 shipped, mechanism read in the code.** `CoverageReport.BuildUpIntervals` sets `FirstUtcMs` to the **earliest evidence** for an instance, and for a restart that is the **`DOWN`** line. But the checklist's own reading rule says *"a lone `DOWN` means the app started and nothing else has happened"* while *"an `OK` line is positive evidence that at least one analysis run completed"* — **so `DOWN` evidences that the process EXISTS, not that it can CAPTURE.** ✅ **Confirmed instance:** hour **2026-08-10 09:00** classified `Defect`, with the report's own Reason reading *split@09:59 — first span `Captured`, second span `Defect(empty)`*. That second sub-span is **3.4 seconds** long (marker 09:59:56.601 → hour end), and `ws_health` shows the process connected at **10:00:03.328** — so it was still starting up and **zero rows was correct behaviour, not a defect.** **That was 1 of the 4 defects in the window.** ⚠ **Rare — it needs a marker in the last few seconds of an hour** — but it fired on the very first production run. ⚠⚠ **Do NOT just seed from `OK` instead:** that SHRINKS the up-interval, so hours before the `OK` fall to `before-first` ⇒ `ExpectedMissing` — **trading a false defect for a possibly missed one, the wrong direction under J-B.** Connect latency is real and measured: **4.7 s, 5.5 s, 5.8 s, 6.7 s, 18 s, 20 s, 44.3 s** across the six recorded deploys. **The likely shape is a distinct "starting" state for the `DOWN`→`OK` window, not a boundary move.** **Model: Sonnet, effort: medium** |
| **`CoverageReport`'s `gapMs` is a TIME tolerance standing in for a COMPLETENESS check — the DR-1 pattern, third instance** | one classifier arm; needs a short spec | ⚠ **NEW 2026-08-14, measured on the copy-back.** `ClassifyHour` reads `storeClean = stats.RowCount > 0 AndAlso stats.LongestGapMs <= gapMs`. **`gapMs` (300,000 ms) is a time tolerance** — exactly what D-2 of the downtime spec forbids for hole detection and what DR-1 removed from `ResolveRepairWindowsMs`. ✅ **Measured, so the severity is not guessed: over 63.1 h of tape that `trade_seq` proves 100.000 % complete (134,204 rows, ZERO missing), exactly ONE gap exceeds the threshold — 302,145 ms at 2026-08-13 21:46:50 UTC, beating it by 2.1 s.** That hour would classify **`Defect`** on provably complete tape. ✅ **CONFIRMED BY THE REPORT ITSELF 2026-08-14** — the coverage run classified hour **2026-08-13 21:00** as `Defect` under instance `e551f15e…` with reason `gap-breach(302145ms)`, **the exact figure predicted before the run.** ⚠ **Severity is LOW and the direction is SAFE** — it errs toward flagging, which is J-B's cheaper error, and it is one false defect in 63 hours. **Not urgent.** ⚠ **But `trade_seq` now makes completeness directly measurable per hour, so the tolerance is redundant where sequences are present.** The spec must handle the mixed store: **legacy hours have no sequence and still need the time heuristic**, so this is a *second* signal, not a replacement. **Model: Sonnet, effort: medium** |
| ⚠ **`ws_health.log` under-reports a capture outage — it and the tape disagree by ~34 minutes** | investigation, then a scoping decision | ⚠ **NEW 2026-08-12.** For the 2026-08-11 outage: `ws_health.log` records `DEGRADED` 09:04:21.695Z → `OK` 09:30:39.338Z — **26 minutes** — while the tape shows **zero rows 08:59:56 → 10:00:12, 60 minutes**. **Trades stopped ~5 min BEFORE the DEGRADED marker and resumed ~30 min AFTER the OK marker.** ⚠ **This matters beyond bookkeeping: `ws_health.log` is the supplementary uptime source `CoverageReport`'s S1 join reads**, so an hour the box could not capture is evidenced as ~34 minutes of health. **`WsHealthLog` logs only on state CHANGE by design**, so the question is whether the health classification itself lags the feed, or whether "OK" is being emitted on a completed run that had no trades to process. **Determine which before changing anything.** ⚠ **NEW LEAD, 2026-08-12 — a starting point, NOT a finding. I did not verify the mechanism.** The trader reports that during this outage **both boxes showed the amber `ANALYSIS SKIPPED` hero with no readings** — the P4f skipped card, reached when a required REST fetch returns nothing. **The skip path still writes WS health:** `MainForm_SignalBridge.EmitBridgeSkipped` calls `LogWsHealthTransitionForRun(cfg)`, and its own comment says *"a skip run still carries a valid feed-health reading."* **So every health line in that window was written by a run that produced no analysis at all.** The question to open on is therefore precise: **what does `SignalEmitter.DeriveWsHealth` return on a SKIPPED run whose WS has reconnected but whose tape is still empty?** If it can return `OK` there, that is a candidate explanation for an `OK` marker at 09:30 against trades resuming at 10:00. ⚠ **I did not read `DeriveWsHealth`'s parameter semantics — do not treat this as diagnosed.** Fixture `A22d` pins its precedence (`REST` > `DEGRADED` > `DOWN` > `OK`) and is the place to start. ✅ **ANSWERED 2026-08-13 — `DeriveWsHealth` READ, and the lead is now a FINDING.** `Core/SignalEmitter.vb:94` takes exactly four inputs: transport mode, the per-run stale-stream REST-fallback flag, `feedExists`, `feedConnected`. ⚠⚠ **NOT ONE OF THEM REFLECTS TRADE FLOW OR A STORE WRITE.** So `OK` means *"the socket is up and this run did not fall back"* — **it does not mean capture is healthy, and by construction it cannot.** That explains the 2026-08-11 discrepancy exactly: the socket recovered at 09:30:39 (→ `OK`) while the tape stayed empty until 10:00:12. ⚠ **This reframes the item: it is NOT a lag to tune.** An hour the box could not capture can never appear in a log whose inputs exclude capture. **Two design routes, and the spec must pick one:** (a) `CoverageReport`'s S1 join stops treating `OK` as capture evidence; (b) `DeriveWsHealth` gains a capture-liveness input — ⚠ **`TradeStoreWriter.LastFlushUtc` already exists and is exactly that signal**, and `ClassifyTapeStoreTier` already grades it. ⚠ **Route (b) changes a pinned enum's semantics and would move `A22d`** — say which route in the spec before touching code. **Full write-up: [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §5b.** ⚠ **Related but distinct from the intentional-downtime row below** — that one is about hours nobody was capturing on purpose; this is about the record disagreeing with what happened. **Model: Sonnet, effort: medium** |
| ⛔⛔ **`S-1` RE-CHECKED 2026-09-07 — IT STILL HOLDS, BUT ITS FIX IS NOT AVAILABLE AND THE ROW'S OWN ESCALATION TRIGGER FIRES. ONE DECISION IS OWED** | ⛔ **The sizing below — *"project ref + two call swaps · Sonnet, medium"* — DOES NOT SURVIVE. Re-scope or drop** | ⭐ **Full read: [`item6-wstradeprobe-s1-recheck-2026-09-07.md`](item6-wstradeprobe-s1-recheck-2026-09-07.md).** ✅ **`S-1` re-verified line by line at `eb05a4b`, not inherited — probe fields `:53-54`, own readers used `:203-204`, helpers `:333`/`:353`, `TradeSeq > 0` at `:250`, and the `.vbproj` still has NO `ItemGroup` at all. All five divergences still live. Four weeks older, nothing healed.** ⛔⛔ **MEASURED BY BUILDING IT, not predicted: linking `DeribitClient.vb` alone → `Build FAILED`, `BC30451 'SettingsLoader' is not declared` at `(15,46)` and `(30,19)`. Adding `SettingsLoader.vb` + `EngineSettings.vb` → `Build succeeded`. Cascade is BOUNDED at three files — but they are the wrong three.** ⛔ **`DeribitClient.vb:10` holds a static `HttpClient`; `SettingsLoader.vb:46-47` holds TWO `FileSystemWatcher`s; `SettingsLoader.vb:245-247` WRITES a default `settings.json` if none exists.** ⛔⛔ **The probe's own `WsTradeProbe.vbproj:12-14` states the disqualifying guarantee in capitals — *"DELIBERATELY STANDALONE … no settings loader … It cannot read or write any file the collector uses, so it can be run on the same box as a live collector without touching its state."* The `S-1` audit never weighed that comment.** ⚠ **At runtime it would still be safe TODAY — `TradeRecord`'s shared readers never run `DeribitClient`'s static ctor — but that is an ACCIDENT of VB static-init semantics, not a declared property. One future line adding a REST cross-check silently arms a settings write on the production collector box.** ⭐ **THE STRUCTURAL FIX IS CLEAN AND MEASURED: `TradeRecord` is fully self-contained (zero references to `DeribitClient`/`HttpClient`/`SettingsLoader`; needs only `System.Globalization` + `System.Text.Json`). Move it to `Core/TradeRecord.vb` — ZERO call-site changes, root project picks it up by glob, and exactly 5 `.vbproj` files gain one `Compile Include` line each (`AutoTweaker`, `BacktestRunner`, `CeilingAudit`, `WhatIfRunner`, `ordercheck`). The probe then links ONE dependency-free file and its guarantee SURVIVES.** ⛔ **DECISION OWED — (a) split `TradeRecord` into `Core/` then route the probe · (b) accept `S-1` and record why it cannot fire · (c) link the three files as originally scoped.** ⭐ **My read: (a) as the DIRECTION — the trigger pre-authorises it and a dependency-free trade parser serves every future tool, not just this probe — but NOT NOW: `S-1` cannot fire at `trade_seq` ≈ 296 M, the write-guard fix it gated shipped long ago, and the probe has not been run since 2026-08-11. Schedule it behind anything with a live consumer.** ⛔ **(c) is REJECTED on the measurement above.** ⚠ **Not (b): *"held safe by an assumption in a comment"* is the exact defect class this project has hit four times, and (b) is writing it down a fifth.** ✅ **Probe's own baseline verified green — Release build 0 errors 0 warnings at `eb05a4b`.** **Original row follows.** ⚠ **NEW 2026-08-11**, [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) **S-1 — the fourth instance of the defect class, and it is one day old.** `WsTradeProbeProgram.vb:203-204` uses its own private `ReadString`/`ReadLong` instead of `TradeRecord.ReadTradeId`/`ReadTradeSeq`, making it a **fourth trade-parse site** against a documented "one seam, three parse sites" claim. **Verified divergences:** absent `trade_seq` ⇒ shared reader returns `AbsentSeq` = **−1**, probe returns **0** · shared reader rejects negatives, probe returns them · shared reader treats `n >= 0` as a real value, probe tests presence as `> 0` · `trade_id` whitespace trimmed vs returned raw. ⚠ **Why it matters more than a duplicate parser: this probe is the DELIVERY GATE for the `TradeStoreWriter.vb:149` fix.** A gate whose parsing differs from production's does not measure what production will do. ⚠ **Honest bound, and it changes the urgency:** **G1 and G3 use `timestamp` only and are completely unaffected**; G2's exposure needs `trade_seq ≤ 0` against a venue at ~296 million. **So this does not block the gate run — but Deribit is down and the fix is free, so do it now.** **Model: Sonnet, effort: medium.** ⚠ **Where it slips:** editing the probe's sentinel from `0` to `−1` makes the two agree *today* and leaves two readers in the tree — **that reproduces the class instead of removing it.** ⚠ **Escalation trigger:** if linking `DeribitClient.vb` drags an `HttpClient` into the probe, **stop** — that is the same network/format split that put `TradeStoreWriter` in `Core/`, and it needs the same structural treatment, not a workaround |
| **Eval-cache backfill — key on identity, and fix the loop** | two small changes, one seam | ⚠ **NEW 2026-08-11**, [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) S-4. `LivePerformanceTracker.vb:431` guards a run on a **1-second timestamp** — instance 1's shape in a different seam — while the CSV already carries `InstanceId` and `SignalId`, so **the identity exists and is unused**. **Two defects in one guard:** the key is not an identity, *and* `:358` builds `existingTs` once before the loop and never updates it inside, so **a cold start admits two same-second rows while a warm start drops one — the bias flips with cache state.** **Latent, and measured rather than assumed: zero duplicate timestamps** in the local book (10,779 rows) and the AWS copy-back (15,499 rows); at 60 s/180 s cadences it is barely reachable. Dates to settings **v26**. ⚠ **Needs no schema change — do NOT bundle it with a CSV rotation.** **Model: Sonnet, effort: medium.** ⚠ **Where it slips:** *"just add the row to `existingTs` inside the loop"* fixes the batch inconsistency and leaves the key wrong. Both halves, or state why only one |
| ~~**Bound the pooled minute-key dedup — before the next pooled read**~~ | ✅ **DONE 2026-08-11 — and the ruling REVERSED the recommendation** | ✅ **CLOSED.** [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §3b amended with an independent re-measurement. ⚠ **KEEP the minute key; do NOT change it for the Kelly/W6-4 freeze.** The measurement did not support the original "fix it first" call: of the **573** rows a minute key drops (AWS 25 = 0.16 % · local 548 = 5.08 %), **exactly ONE is a weekday STRONG**, against **281** across both books — so it costs the Kelly dated trigger **0.36 %**. Against that, changing the key breaks §3b's own reproducibility note: **every pooled figure published since 2026-07-31 was built on the minute key.** ⚠ **The clustering is real and is recorded for reads that do not exist yet: 21 of the 25 AWS losses sit at 13:00 UTC**, the `execution_resolution` 3 → 1 handover. **Any future pooled read segmented by session, resolution, or the 13:00 boundary must switch to second-resolution or `InstanceId`+`SignalId`+timestamp keying and say the figures will not reconcile with earlier pooled reads.** Not code — no minute-key dedup exists in any `.vb` file |
| ~~**superseded row, kept per the quote-and-label convention**~~ | ~~procedure edit + a re-measure, **not code**~~ | ⚠ **NEW 2026-08-11**, [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) S-5. [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §3b specifies an AWS-preferred **minute-key** dedup for pooling. Measured against the books in hand it would drop **548 local rows (5.08 %)** and **25 AWS rows (0.16 %)**. ⚠ **The AWS losses are not random — they cluster at `13:00` UTC**, the NY `session_volume` boundary where `execution_resolution` switches 3 → 1, so the cadence changes and two runs land in one minute. **A session-correlated loss is the same "biased, and worse than its size" property that made the same-millisecond drop serious.** **The Kelly dated trigger and the W6-4 re-run are both scheduled to consume one pooled freeze**, so fix the key first: second-resolution, or `InstanceId` + timestamp, costs nothing extra. ⚠ **No minute-key dedup exists in any `.vb` file** — this is a documented ops procedure, so the fix is a doc edit. **Model: Sonnet, effort: low** |
| ~~**A SECOND stale fixture literal — `BuildResolutionCfg` pins a pre-v40 ROC value**~~ | ✅ **RULED + BUILT 2026-09-03** | ✅ **RULED 2026-09-03 (trader) — MECHANISM, option (c): keep the literals, add the declaring comment at the call site, AND change both values to obviously-synthetic ones so neither reads as a shipped assertion** (`0.21`→**`0.50`**, `0.105`→**`0.25`** — shipped stays `0.21`/`0.06`). Spec: [`fixture-literal-provenance-a14b-spec.md`](fixture-literal-provenance-a14b-spec.md). ✅ **BUILT the same day** — `verify/ordercheck/Program.vb:1084-1090` (`BuildResolutionCfg`, the new literals plus the provenance comment) and `:1121-1125` (`A14b`'s check name and assertions, updated to match — trap T2). **The resolution-1 literals (`0.1`/`0.05`) are POCO defaults, explicitly out of scope, left untouched.** All seven consumers (`A14a/b/d/e/i/j`, `A15a`) re-verified unchanged; harness `ALL PASS`; gate `PASSED`. Packet: [`queue-17-18-batch-spec-back.md`](queue-17-18-batch-spec-back.md). *Original text, kept for the record:* ⚠ **NEW 2026-08-11**, found by the reviewing seat while checking something else, **not** by the audit. `verify/ordercheck/Program.vb:995` hard-codes `.RocSlopeDeltaThreshold = 0.105`; **settings v40 re-baselined the 3-min value to 0.06.** ⚠ **This is COPY 4 — the class of stale literal that nothing in the A54a ruling can catch**, and it is the second confirmed instance after `A6`'s `trendGate:=10.0`. **Model: Sonnet, effort: low** — but the decision above is the work, not the edit |
| **Absorption mechanism revision — the Path B proposal** | ✅ **WRITTEN 2026-08-14 and BLIND-CHECKED 2026-08-19. §6 STILL AWAITS A TRADER TICK — that is the only thing outstanding on this row.** ⚠ **Do NOT open an implementer session until it is ticked.** ✅ **The embargo is SPENT** — [`seat-handover-2026-08-14.md`](seat-handover-2026-08-14.md) §0's blind re-derivation has run: [`absorption-blind-rederivation-2026-08-19.md`](absorption-blind-rederivation-2026-08-19.md), committed `8485173` before the proposal was opened. **Outcome: the population is CONFIRMED (pressed/active 10.76 % / 10.05 %, and `active/rows` 14.68 % / 14.71 % — a second ratio the original pass did not use), so Path A stays dead.** ❌ **Two of the four load-bearing claims FAILED:** `pullFrac` **does** carry a point mass at exactly 1.000 (200–380× local density, both books) and §8's residual **is** diagnosable from the shipped book (`postLB` floored on ~27 % of non-zero rows, 5.4× chance). ⚠ **Two arithmetic corrections:** the "~49 % arming rate" is **11.78 %** (all-row numerator over a directional-only denominator, and that denominator counted `NO TRADE [WEAK *]` as directional), and §3.1's tape-scale argument uses a mean where the median is **15×** smaller. ⭐ **The observation loss SEPARATES from logged data — ≈ 77 % is GEOMETRIC** (`proximity_atr_frac` 0.30 admits into a shell 3× the `band_atr_frac` 0.10 that measures), **plus a third cause nobody listed: the engine counts only 31 % of the in-band flow its own 10 s window admits.** ⚠ **A new D-6 is proposed in that D-table (reconcile proximity with band) and is UNTICKED like the rest.** ⚠ **D-3 and D-4's ACTIONS survive; their stated REASONS do not and are re-grounded in place** | ⚠ **NEW 2026-08-12 — this is what ticking E5 Path B bought.** Re-open the book-absorption proposal's **§8 residuals** with the evidence that the v61 geometry rescale alone did not lift flag rates into the 3–8 % design band: **`window_sec` too short** · **episode-cumulative pressing** · **D8 `pullFrac` inflation on sparse `postLB`**. Evidence base: [`absorption-anchor-rederivation-2026-07-30.md`](absorption-anchor-rederivation-2026-07-30.md) §4–§5 (208 episode rows, 6 weekday days) and [`absorption-engagement-derivation-2026-07-23.md`](absorption-engagement-derivation-2026-07-23.md). ⚠ **Do NOT re-derive anchors in this pass** — E5 ruled they hold, and the finding is that no anchor set on the observable population reaches the band. ⚠ **Read that doc's §5c sanity note first:** every projected flag rate assumes 208 rows over 6 weekday days are representative, and they may not be. **A fresh population count on the post-2026-08-11 book is the honest first step**, not an assumption carried forward. `scoring_enabled` stays **false** throughout; this is a display-only surface. **Model: Opus, effort: high** — it is a derivation against a mechanism nobody has yet shown to observe what it claims, and the prior pass already produced one wrong answer by re-tuning instead of re-mechanising |
| ~~**Two doc corrections found by the seam audit**~~ | ✅ **DONE 2026-08-25 — BOTH parts.** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC).** **Part (1):** [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §4's Tier 2 table now names `Spread.WideThresholdBps` (JSON `wide_threshold_bps`, default 5.0) and carries the correction note that `WidePenaltyThresholdBps` **has never existed** — confirmed here by a tree-wide grep: the name appears in **no `.vb` file at all.** **Part (2):** `CeilingAuditProgram.vb`'s `expectedVersion` was bumped 59 → 68 and has since been **deleted outright** — `grep -c "expectedVersion" tools/CeilingAudit/CeilingAuditProgram.vb` prints **0**. See the two CeilingAudit rows below. *Original size estimate follows.* ~~trivial~~ | ⚠ [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) Row 6. (1) [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §4's Tier 2 table names `cfg.Indicators.Spread.WidePenaltyThresholdBps`; **no such property exists** — it is `WideThresholdBps` (`EngineSettings.vb:632`). **Verified.** Anyone grepping the documented name finds nothing, and it cost the auditor a false finding mid-sweep. (2) `tools/CeilingAudit/CeilingAuditProgram.vb:79` holds `expectedVersion = 59` against live **v66** — the row below says "six versions stale"; **it is now seven.** ✅ **PART (2) DONE 2026-08-25 — bumped to 68, see the CeilingAudit row below.** ✅ **PART (1) DONE 2026-08-25** — `DeribitIndicatorProject.md` §4 now names `Spread.WideThresholdBps` (JSON `wide_threshold_bps`, default 5.0), verified against `Core/Settings/EngineSettings.vb:632`. **BOTH PARTS OF THIS ROW ARE NOW CLOSED.** **Model: Haiku, effort: low.** Blocks nothing |
| ⛔ **Run S0 `--verify-venue` on a cadence — SUSPENDED, do not schedule** | was an ops slot; now blocked | ⚠ **SUSPENDED 2026-08-08, pending `trade_id` above.** The design work is done and recorded below, but scheduling it now would produce a daily report whose central number cannot be interpreted — S0 matches on the same identity-blind five-field row. **Resume once `trade_id` ships.** Settled design, for when it does: **AWS only** (trader) · output to a folder under the app root · window **ends on the previous whole hour**, never "now", to clear the 30 s `trade_store.flush_seconds` lag · the run is a background scheduled task needing no interactive login, but **the reading is manual**, so fold a one-line verdict into the §3 daily glance · ⚠ **`--strict` grades on Defect hours only** (`BacktestProgram.vb:321`) and never reads the venue result, so **the exit code cannot signal venue loss** — parse the output, or extend `--strict` in the same spec |
| ~~⚠ **Run S0 `--verify-venue` on a DAILY cadence**~~ | ~~superseded by the SUSPENDED row above~~ | ⚠ **Original text, retained per the quote-and-label convention. Its "16,459 more trades" figure is WITHDRAWN (§5, 2026-08-08); the S3 point survives on arithmetic alone.** **NEW 2026-08-07, and it is time-critical in a way nothing else on this list is.** The coverage report's **S3 longest-gap metric cannot detect scattered per-trade loss**: AWS-only max gap over the measured window is **153.1 s** and the merged store carrying 16,459 more trades reads **exactly the same 153.1 s** (density ~1 trade/2.3 s, threshold 300 s). **S0 `--verify-venue` is the intended completeness check** — it diffs the store against Deribit — but it is **opt-in and bounded to the 24 h before `--to`** (`BacktestProgram.vb:298-299`), and that bound is not a design choice: **Deribit's public-trades retention is ~24 h**, so a day's completeness is checkable **only within ~24 h of capture and never again.** Every day it does not run is a day whose tape can never be verified. **It has never been run.** Cheap: one scheduled command per box |
| ⚠ **Intentional-downtime scoping for the coverage report** | a build behind a short spec | ⚠ **NEW 2026-08-08**, from the AWS cost review — it blocks nothing today and blocks the coverage report the moment AWS goes part-time. ✅ **WEEKENDS ARE ALREADY HANDLED — no work there.** `CoverageReport.vb:430-431` classifies Saturday and Sunday as `OutOfScopeWeekend` ahead of every other test, and the 2026-08-07 run reported 48 such hours. **The gap is WEEKDAY downtime.** A part-time box creates a **cross-GUID restart gap every single day**, and the C1 build resolves those to **`defect`** per [`j-b-scoping-ruling-2026-08-02.md`](j-b-scoping-ruling-2026-08-02.md) §3.1 — so a part-time box would report a defect daily and the report becomes noise. ⚠ **J-B rejected an expected-uptime BASELINE and that rejection still binds** — a baseline built from observed behaviour cannot, even in principle, flag that behaviour as wrong. **But a DECLARED OPERATING SCHEDULE is not a baseline. It is a positive record of intent, which is precisely what J-B asks for.** That distinction is the whole spec. ⚠ **Prefer a declared schedule over a marker line written at shutdown** — an EC2 stop can kill the process without running any shutdown code, so a shutdown marker is unreliable by construction. ⚠⚠ **SCOPE WIDENED 2026-08-11 by a live instance: the report cannot distinguish a VENUE outage from our own box dying.** Deribit entered system maintenance on 2026-08-11 (`system_maintenance`, code 11051, HTTP 503 on every endpoint). During it the box is **up and healthy** — `analysis_log.csv` and `ws_health.log` both show life — while the store is empty, which resolves to **`defect`** under [`j-b-scoping-ruling-2026-08-02.md`](j-b-scoping-ruling-2026-08-02.md) §3.1. **It is not our defect and the report will say it is.** This is the same failure as unscoped intentional downtime and wants the same fix, so spec them together rather than twice: **a positive record of why an hour is out of scope.** ⚠ **But note the asymmetry** — a declared schedule is known in advance, whereas a venue outage is not, so the venue arm needs a *recorded* source rather than a declared one. `ws_health.log` already distinguishes `DOWN` (our socket) from a healthy socket returning nothing; a venue-maintenance hour is the second shape and is detectable. **Do not invent an uptime baseline for it** — J-B's rejection still binds. **Model: Sonnet, effort: medium** — one new class or scope source, and the judgment is already made here |
| ✅✅✅ **Weekday filters — ALL THREE SURFACES SHIPPED 2026-09-07. THE WEEKDAY-SCOPE RULING IS FULLY DISCHARGED** | ✅ **(1) `AutoTweaker` 2026-08-25 · (2) `LivePerformanceTracker` 2026-09-07 (`A67a`) · (3) `AnalysisRunner` + `WhatIfRunner` 2026-09-07 (`A67b`).** Harness **334 → 337** across surfaces 2–3; `GATE PASSED`; settings **v68** throughout | ⭐⭐ **THE TWO REJECTED SEAMS ARE WORTH MORE THAN THE FIX. (1) NOT `FailureRateMatrix.Compute` — FIVE callers incl. `AutoTweaker` (already filtered upstream) and many fixtures; filtering inside a shared primitive silently changes its contract from *"compute over these rows"* to *"compute over the weekday subset"*. (2) NOT `ForwardWindowJoiner.Load` — five callers, and `A46a` asserts an EXACT row count against it, so that change breaks a fixture and re-scopes three others.** ⭐ **What survives is the pattern surface 1 set: LOAD, THEN FILTER, at each consumer — with ONE shared predicate, `ForwardWindowJoiner.IsWeekdayRow`, in `analysis/` where every offline project links it, so copies four and five were never written.** ⛔⛔ **`DateTime.MinValue.DayOfWeek` is MONDAY — a naive day check ADMITS every unparsed timestamp as a weekday row. Both pre-existing implementations guard it; extracting the predicate is what stops the next copy omitting it, and `A67b` is what stops it being "simplified" back out.** **Mutation RUN: deleting the guard fails `A67b` ALONE, detail line `minValue=True (MinValue.DayOfWeek=Monday)`.** ⚠ **NOT fixture-covered and said so: the two call sites' wiring — `AnalysisRunner.Run` is async and fetches OHLC, so it is not harness-reachable. `A67b` pins the predicate; the wiring rests on reading.** ⚠ **RESIDUAL, named not silent: `AutoTweaker` and `CeilingAudit` still carry inline copies of the predicate. Deliberately untouched — `A59a`–`e` pin the tweaker's and re-routing a shipped, reviewed surface was out of scope. Converging them on `IsWeekdayRow` is a queued tidy, NOT a defect. Sonnet, low.** **Original row follows.** | ⭐ **Surface 2 detail: filter placed in `AggregateRange` beside the resolution filter and the E2a WEAK exclusion — the seam whose own comment sets the discipline, *"Storage stays permissive; the exclusion is applied here at display time."* The eval cache is UNTOUCHED and it is fully reversible.** ⛔ **UTC day-of-week, NOT the UTC+8 display calendar** — the ruling's named precedent (`CsvFeatureBuilder.vb:199-200`) reads the raw UTC stamp, as does the tweaker; diverging here would make the strip's population disagree with both. ⚠ **Consequence stated: the trader's Monday-morning window opens Sunday 16:00 UTC and that portion is now out of scope — the window defines the RANGE, the filter defines the SCOPE.** ⛔ **`MinValue` guard FIRST (`DateTime.MinValue.DayOfWeek` is MONDAY).** ⭐ **`WeekendExcluded` counts the drops and the tooltip renders `Weekend excl.: n=N`, mirroring the `WEAK excl.` line — otherwise the counter would be computed and discarded.** ⚠ **No weekend RATE is shown: out of scope is not a comparison population.** ⚠ **Expect FEWER rendered cells — n shrinks against `min_sample_for_render` (10); predicted by the ruling, not discovered.** ⭐⭐ **`A67a` was REQUIRED because the existing fixtures prove nothing: `A14f`/`A33b`/E2a all drive `AggregateRange` but sit on 2026-07-20 (MONDAY) and 2026-01-01 (THURSDAY) — measured — so each passes identically with the filter present or absent.** **Mutation RUN — disabling it fails `A67a` ALONE at `TotalRange=4 WeekendExcluded=0 success=3 failure=1`: 75 % against the correct 50 %, so the RATE moves too.** ⚠ **The fixture caught its own author — its first draft used `EvalOutcome = "FAILURE"`, which is not a real outcome, and an unrecognised string falls through both arms silently.** **Original row follows.** | From the [weekday-scope ruling](weekday-scope-ruling-2026-08-03.md). Weekday-only is enforced in code in **exactly one place** — verified: only `CoverageReport.vb:346` and CeilingAudit's `CsvFeatureBuilder.vb:199-200`. **`AutoTweaker` is the sharp one** — the only surface that WRITES `settings.json`, so unfiltered it tunes the engine on sessions never traded. ~~**verified never fired** (no tweaker state file, `settings_snapshots/` empty)~~ ⚠ **CORRECTED 2026-08-25 — WRONG, per the weekday-filter proposal's own §1 correction: there IS a state file** (`tools/AutoTweaker/state.json`, untracked) — **it ran, skipped on tier-eligibility, and its cursor advanced to 81.** The conclusion survives (it never proposed or applied a settings change, `settings_snapshots/` is genuinely empty), but **it was LIVE, not dormant** — the gate keeping it quiet was data, not design. Then **`LivePerformanceTracker`** — expect *fewer* rendered cells once filtered, since n shrinks against `min_sample_for_render`; note its `DayOfWeek` at `:560` is **Monday week-anchoring, not a filter** — then `AnalysisRunner` / `WhatIfRunner`. **None is a ⚠ boundary.** ✅ **`AutoTweaker` DONE 2026-08-25** — [`autotweaker-weekday-filter-proposal.md`](autotweaker-weekday-filter-proposal.md), D-1–D-5 ticked, fixtures A59a–e, harness ALL PASS, no settings key. **Remaining: `LivePerformanceTracker`, then `AnalysisRunner` / `WhatIfRunner`.** |
| **Atomic writes: swap the partial primitive for a total one** | 5 sites, mechanical, but includes `SettingsLoader.Save` so it wants fixtures | **The framing is the order-app seat's and it is the useful part: a guard is what you OWE when the primitive is PARTIAL. Choosing a TOTAL primitive removes the obligation rather than satisfying it.** `File.Replace` throws on a missing destination, so all five sites need an existence guard; four have one, and `OhlcCache` is safe only via a distant early-out. `File.Move(src, dst, overwrite:=True)` is **total** — works whether or not the destination exists, atomic on the same volume, available on .NET 8. Swapping it in **deletes the hazard class.** **Sites, re-anchored 2026-08-07:** `OhlcCache.vb:99,134` · `SignalEmitter.vb:563` · `Core/Settings/SettingsLoader.vb:346` · `SettingsDiffApplier.vb:352` · `TweakerState.vb:162`. Not a boundary, no settings keys |
| ~~**C1-coverage F1** — trailing-edge gap mis-attribution~~ | ✅✅ **DONE 2026-08-26 — commit `4032f9c`, *"C1-coverage F1 - trailing-edge gap mis-attribution"*, with two follow-ups: `5cd7269` (the trailing gap measured per-span) and `6a6f93e` (`R7` — the trailing gap is the MAX across an hour's trailing spans, not the first).** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC):** `grep -c "TrailingEdge" tools/BacktestRunner/CoverageReport.vb` prints **26**. ⚠ **[`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15's C1 row still calls F1 *"an open build slot"* — that text is STALE; it was not edited by this sweep, which is scoped to [`trader-tick-queue.md`](trader-tick-queue.md) §2 alone.** *Original re-sizing note follows.* ⛔ **RE-SIZED 2026-08-25 — this cell read "Small-medium; one new fixture" and BOTH halves were wrong.** Spec written: [`coverage-trailing-edge-f1-proposal.md`](coverage-trailing-edge-f1-proposal.md), **§4 D-1…D-6 await a trader tick — SIX coupled decisions, not one.** Investigation found **six existing fixtures a naive fix BREAKS** plus three at risk, and it **moves the CLI `--strict` exit code**. ⭐ **The crux is not the one the origin ruling names:** `ResolveBoundaryUtc` returns `toUtc` when there is no evidence, so it does **NOT** protect `A49e`/`A49g` — **"the store simply ends" is a DIFFERENT bound from "the evidence boundary"**, and a fix with only the latter false-flags the last hour of most manual runs. **Sonnet, effort HIGH**, one session | `Captured` means "rows present and no gap *ending* in this hour breached", **not** "fully covered": trades :00–:05 then silence until the next hour's :30 charges the gap to the FOLLOWING hour, so the hour it started in reads `Captured` despite ~55 min of silence. Not data loss — the next hour still flags `Defect` — but a reader reasonably takes `Captured` to mean complete. Currently in the report's legend only. ⚠ **The obvious fix false-positives every window's final hour** — bound `hourEnd − lastTradeInHour` against the SAME trailing-evidence boundary `ResolveBoundaryUtc` already computes. `tools/BacktestRunner/CoverageReport.vb` (`AccumulateHourStats`) |
| ~~⚠ **A54a — a JSON↔POCO drift guard for the plain indicator keys**~~ ✅✅ **THE WHOLE ARC IS BUILT AND CLOSED.** | ✅✅ **DONE — commit `3a89093` (the guard + seven POCO re-syncs) plus the follow-ups `cc44e9f` (`R-1`/`R-2`/`R-3`), `a86a0cf` (`F-1`), and `fded077` + `94b68d5` (session 2's scoped-(b) removal).** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC):** `WalkPocoVsJson` is present in `verify/ordercheck/Program.vb` (16 references), and the arc carries **one** [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15 row covering all five commits — settings untouched at **v68**, no version bump, not a dataset boundary. Packets: [`a54a-drift-guard-batch-summary.md`](a54a-drift-guard-batch-summary.md) · [`a54a-drift-guard-spec-back.md`](a54a-drift-guard-spec-back.md) · [`a54a-r2-r3-followup-spec-back.md`](a54a-r2-r3-followup-spec-back.md) · [`a54a-session2-step1-measurement-2026-09-05.md`](a54a-session2-step1-measurement-2026-09-05.md) · [`a54a-session2-step2-spec-back.md`](a54a-session2-step2-spec-back.md). ⚠ **[`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15's A54a row names `S2-2` as *"STILL OPEN"* — that text is STALE (`S2-2` was built and closed 2026-09-06, commit `57b55f9`); it was not edited by this sweep, which is scoped to [`trader-tick-queue.md`](trader-tick-queue.md) §2 alone.** *Original authorisation text follows.* ✅ **BUILD-AUTHORIZED 2026-09-04 — [`a54a-json-poco-drift-guard-spec.md`](a54a-json-poco-drift-guard-spec.md), §4 TICKED IN FULL.** ⛔ **D-3 ruled AGAINST the spec's abstention: `network.transport` POCO `"rest"` → `"ws"` + fix the stale comment, so session 1 carries THREE re-syncs, not two, and the D-1 allow-list is TWO entries, not three.** ⛔ **RE-SIZED — this cell read "One fixture" and that is wrong.** ⚠⚠ **AND IT IS NOT TRANSCRIPTION, which is how [`seat-handover-2026-09-03.md`](seat-handover-2026-09-03.md) §0 bills it.** The ruling settled how to FIND drift; it did not settle what to do when the walk finds a divergence that is **deliberate** — and the tree holds **three** of those. ⭐⭐ **MEASURED 2026-09-04 against tracked v68, not inherited: the walk compares 261 scalars and finds NINE divergences, not the six the prototype reported.** ⛔ **TWO ARE NEW, LIVE, AND TEN TIMES APART: `indicators.CVD.slope_pct_of_value` POCO 0.01 vs JSON 0.10, and `indicators.MicroCVD.accel_threshold_dynamic_pct` POCO 0.03 vs JSON 0.30.** Both were BORN IN AGREEMENT and the JSON moved without the POCO — traced to `61b4532` (v34) and `1e9df84` (v33), **both 2026-06-13, the same pair of re-baseline commits that produced the OBV defect.** ⚠ **Both are scoring inputs; both have ZERO fixture references, so nothing would ever have noticed.** **App unaffected** (all six production call sites pass by name) — so **no version bump, no dataset boundary.** **Model: Sonnet, effort HIGH, TWO sessions** — the guard + re-syncs first, scoped-(b) second. ⚠ **The old "medium, one session" tag is superseded; see the spec's §0 for the three named traps and the escalation trigger.** *(Original cell text follows, kept per the quote-and-label convention.)* ⚠ **NEW 2026-08-11, and it is named because a real drift went unnoticed for two months.** `ObvSettings.TrendGate` stood at **10.0** — the pre-v33 value — against a shipped `settings.json` of **18.0**, from 2026-06-13 until v66 moved both in lockstep. **Latent, not live** (the app reads JSON; the harness passes `trendGate` as an explicit literal), which is precisely why nothing caught it. **A52a is the shape to copy** — it pins "the shipped POCO agrees with the shipped JSON" — but it exists **only** for the ASIA arming key. Generalise it to the flat indicator scalars, or at minimum to `OBV.trend_gate` / `TTM.flat_threshold` / the `ROC` pair. ⚠ **Check the others before assuming OBV was the only one** — nobody has looked. ⚠⚠ **SCOPE CHANGED 2026-08-11 — as originally written this guard would NOT have caught the copy that is still wrong.** [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md) S-2 found a **third** code copy: the method `Optional` default. After v66, `OBV.trend_gate` reads 23.0 in JSON and POCO and **10.0** in `Core/Indicators_Structure.vb:44` and `verify/ordercheck/Program.vb:661-662`. **11 of 42 method defaults disagree with shipped JSON.** ⚠ **This row is now BLOCKED on the §0a "guard or delete" decision** — the two options need different builds. ⚠ **And the audit answered the "which keys" question this row left open, with a rule rather than a list: a key with a concrete POCO default can drift; a `Double?` = Nothing nullable override cannot.** That is why v40/v41/v48 produced no drift while v33/v34/v36/v58 did — use the rule, not a hand-listed key set. **Model: Sonnet, effort: medium** for option (a) against the `A52a` template; ⚠ **Opus/high if the parameter→key mapping needs a hand-maintained table**, which would be a fourth copy and a design problem rather than a build step |
| ~~**CeilingAudit expected-version constant**~~ | ✅ **DONE 2026-08-25** | ✅ **BUMPED 59 → 68, and the CONFIRM step was done properly rather than skipped.** The check is a proxy for exactly THREE values, now named in the code comment so the next seat does not re-derive them: `Scoring.AtrTargetMultiplier` (last moved v51) · `Scoring.TradeCosts.EffectiveMinMovePct` · `SessionVolume.Sessions[].StartHour` (last moved v12 — v58 moved the MULTIPLIERS, which this tool never reads). **None changed value across v59→v68.** ⚠ **v62 DID restructure `scoring.trade_costs` inside that window — but its defaults compose to 0.0008 exactly**, identical to the flat `min_tradeable_move_pct` it replaced, and the tool already reads the shared `EffectiveMinMovePct` resolver rather than either literal. **Proven two-sided:** silent against the tracked v68, and mutating the constant to 67 emits `WARN: settings.json version = 68 (expected 67 at build time)` — so the check is live, not dead code. ⚠ **It will go stale again at v69** — it has three times already — because settings bumps for reasons this tool does not read. *Original text:* Verified: `tools/CeilingAudit/CeilingAuditProgram.vb:79` reads `expectedVersion As Integer = 59` against a live **v65** — six versions stale. Confirm nothing the audit reads changed v59→v65, then bump. Otherwise the warning is noise that hides a real mismatch |
| ~~**CeilingAudit version check — replace the equality WARN with a PROVENANCE RECORD**~~ | ✅ **DONE 2026-08-25, same day it was raised** | ⚠ **NEW 2026-08-25, raised while bumping the constant 59 → 68 (the row above).** **The check rots by construction and has done so three times** — it compares `cfg.Version` against a build-time literal, but `settings.json` bumps for reasons this tool cannot read, so the WARN fires on versions that cannot affect it and gets ignored. **That is alarm fatigue on a check nobody can act on.** ⭐ **THE FIX CONVERTS A CHECK THAT ROTS INTO A RECORD THAT CANNOT: print the three values the audit actually consumed** — `Scoring.AtrTargetMultiplier` · `Scoring.TradeCosts.EffectiveMinMovePct` · the `SessionVolume` bucket start-hours — **instead of a version comparison.** A reader then sees what the run was actually parameterised by, which is the question the version number was a poor proxy for. ✅ **Verified small before proposing it:** `versionCheck` is a **free-form `String`** touched at four sites in `CeilingAuditProgram.vb` (declared `:70`, set `:96`, printed `:97`, assigned `:142`) and rendered in exactly ONE line — `AuditReport.vb:71`, `"**Settings source:** ... (version N) — " & m.VersionCheck`. **The version number is already printed separately by `m.SettingsVersion`, so that slot is free**, the field type does not change, and `AuditReport.vb` needs no edit. Delete `expectedVersion` entirely. ⚠ **No fixture** — it is a report string with no consumer. **No parity obligation** — the offline report is the documented non-parity surface (the `BacktestRunner coverage` precedent). ⚠ **Where it slips:** do NOT reintroduce a baseline to compare the three values against — that is a fourth copy that drifts, the exact defect `seam-audit-2026-08-11.md` S-2 found. **Record the values; do not judge them.** ✅ **BUILT AS SPECIFIED.** `expectedVersion` deleted; the report slot now reads `consumed atr_target_mult=1.75 · min_move_pct=0.0008 · buckets=ASIA@0/LONDON@8/NY@13`, verified by running the tool. `AuditReport.vb` untouched as predicted. ⚠ **One addition beyond the plan:** unreadable session buckets render `NONE READABLE`, not an empty list — a report showing `buckets=` reads as "none configured", a different and worse claim than "could not read them". |
| ✅✅ **`F2` — FIXED 2026-09-07 (`e082844`). `ResetBufferState` dropped tape through a LOCK GAP** | ✅ **CLOSED.** Whole body under ONE `SyncLock`; `Monitor` re-entrancy makes `Flush()`'s inner lock a no-op recursion | ⭐ **The mechanism, now that it is understood: `Flush()` releases `_pending` BEFORE `AppendRows` does its disk I/O, and `ResetBufferState` then re-acquired the lock separately to clear. A trade buffered by the WS receive loop inside that window was erased by the `Clear()` without ever being written — the SAME loss class as the 49.2 % write-guard defect, reached by a different route.** ⚠ **VERIFIED BEFORE WIDENING A LOCK OVER FILE I/O, because that is how deadlocks get introduced: NOTHING in the tree acquires `_appendLock` then `_pending` — zero `_pending` references inside that region, so the ordering is strictly `_pending → _appendLock` and never the reverse.** ⚠ **Consequence stated, not hidden: the WS buffering path now blocks for the reconnect write. Stalling briefly beats dropping the trades it holds.** ⛔⛔ **NOT FIXTURE-OBSERVABLE, and no fixture pretends it is.** It is a thread interleaving: a deterministic single-threaded fixture passes under BOTH versions, so writing one would be coverage that observes nothing; a stress test catches it only probabilistically, and a flaky fixture in an `ALL PASS` harness is worse than none. **Regression cover is `A48b`, which already drives the real reconnect path. The correctness argument is the lock-order proof, recorded in the method's own remarks block so the next reader does not redo it.** ⚠ **The old cell cited `DeribitWsFeed.vb:298`; the call had moved to `:305`, and the defect itself lives in `Core/TradeStoreWriter.vb`.** **Original row follows.** Verified still open: `DeribitWsFeed.vb:298`. [`job1-outstanding-2026-08-01.md`](job1-outstanding-2026-08-01.md) §2. ⚠ **Do NOT conflate this with the `TradeStoreWriter.vb:149` same-millisecond drop above** (new 2026-08-11). F2 is an occasional race; that one is **deterministic and costs ~half the tape at all times.** Different bugs, different fixes, and F2's "narrow race" framing would lead someone to under-prioritise the larger one |
| ✅✅ **`F3` — FIXED 2026-09-07 (`e082844`). The repair `User-Agent` now names the RUNNING host** | ✅ **CLOSED** — and ⛔ **it was NOT "one line, cosmetic"** | ⭐ **Why the obvious fix was wrong: `HistoricalStore` is linked by the APP as well as by `tools/BacktestRunner` — `TradeStoreGapRepair` calls `BackfillTradeMonthAsync` — so the LIVE COLLECTOR was announcing itself to Deribit as `DeribitBacktestRunner/1.0`, and venue-side logs could not separate live repair traffic from an offline replay.** ⛔ **Swapping the literal to `DeribitVerdictEngine` would simply have moved the same lie onto the offline tool.** ✅ **Resolved from the entry assembly instead — the only form TRUE from every host: the app reports `DeribitVerdictEngine`, `BacktestRunner` reports itself, a future host needs no edit. `GetEntryAssembly()` can be `Nothing`, so the literal survives as a last-resort fallback.** ⭐ **Extracted as `Friend Shared ResolveUserAgent()` because `_http` is `Private Shared ReadOnly` and a fixture cannot read its headers — testing the constructor would have meant reflection over a private field.** ⭐⭐ **`A66c` asserts the property RELATIVELY, not as a literal: pinning `"OrderCheck/1.0"` would pass against a hardcoded string that happened to match AND would break the moment the harness is renamed, so it computes the expectation the way the environment defines it. MUTATION RUN — restoring the old literal fails `A66c` ALONE (`ua=DeribitBacktestRunner/1.0 expected=OrderCheck/1.0`).** ⚠ **Found in passing, NOT fixed: `verify/ordercheck/OrderCheck.vbproj:72-74` says `HistoricalStore` is *"deliberately NOT here"* while `:151` links it — the comment contradicts the project file eleven lines below. Left for whoever owns that split's rationale.** **Original row follows.** Verified still open: `tools/BacktestRunner/HistoricalStore.vb:52` |
| ~~**F3-watch tooling**~~ | ⛔ **CANCELLED 2026-08-12 — the watch was RETIRED, so no tooling is owed** | ✅ **Do not build cap-bucket segmentation for this.** The decision is in §0a. ⚠ **`F3` here is the B4b watch, not a coverage-report finding.** The underlying question (does a LONDON structural TARGET get reached?) stays open as a topic and returns only as a scoped instrument request with its own spec |
| ~~**`SH-1` — split the coverage hour at the capture-state marker** (was "C1-coverage F2" — ⚠ **RENAMED 2026-08-13**, `F2` also names the `ResetBufferState` race and an audit finding)~~ ✅✅ **BUILT, COMMITTED AND CONFIRMED IN PRODUCTION.** | ✅✅ **DONE AND COMMITTED — `c19f0ef`, *"SH-1 split the coverage hour at a capture-state marker"*.** ⛔ **THIS CELL READ *"clear to commit; not yet committed"* UNTIL THE 2026-09-07 SWEEP, AND THAT WAS WRONG:** it shipped, and it carries its own [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15 row dated **2026-08-14** (settings-untouched, no version bump, settings stayed v66, fixtures `A49o`–`A49w`). ✅ **CLOSED, verified in the tree 2026-09-07 (UTC)** by `git log -S'A49o' -- verify/ordercheck/Program.vb`, which returns `c19f0ef`. *Original build note follows.* ~~✅ **BUILT + REVIEWED AND ACCEPTED 2026-08-13 — clear to commit; not yet committed.**~~ Brief: [`coverage-split-hour-implementer-brief.md`](coverage-split-hour-implementer-brief.md) (its **§5a carries D-3**, ruled after the build) · packet: [`coverage-split-hour-sh1-spec-back.md`](coverage-split-hour-sh1-spec-back.md). Gate **PASSED**, harness **ALL PASS**, nine fixtures `A49o`–`A49w`. ⚠ **D-3 RULED: the residual combine is `Defect` > `Captured` > `UnknownScope` > `ExpectedMissing` > `NotCapturing`** — the implementer built `UnknownScope` at the BOTTOM, which laundered an uncharacterisable span into a confident `NotCapturing` and silently reversed `ClassifySpan`'s own precedence. ⚠ **One open real-world check, not a blocker: AWS hour 2026-08-07 16:00** (where `capture_marker.log` begins mid-hour) **is a free confirmation of D-3 at the next copy-back.** *Original brief note follows.* Sonnet / medium, one session. ⚠ **The brief found what the ruling did not name: `HourStoreStats` carries only `RowCount` + `LongestGapMs` PER WHOLE HOUR, so "was the ON part clean?" is unanswerable from the data `ClassifyHour` receives today — that is the actual work, and the brief routes it (a targeted second pass over split hours only, never a change to `AccumulateHourStats`).** Two small decisions carry my read and do not block. ✅✅ **CONFIRMED IN PRODUCTION 2026-08-14, on the real AWS store** — the first coverage run after the copy-back rendered a split hour in its own Reason field — hour **2026-08-10 09:00**, instance `ffced26c…`, reason *split@09:59, first span `Captured`, second span `Defect(empty)`*. ✅ **And the case the ruling was written against behaved as designed:** hour **2026-08-07 16:00** — where `capture_marker.log` begins mid-hour — no longer classifies `UnknownScope`. It splits at 16:02:41 and its 57 captured minutes carry it to **`Captured`**; the report's `unknown-scope` count is **16** (hours 00:00–15:00), which would read 17 if the hour were still stranded. ⚠ **D-3's residual ORDER is still untested against real data** — every AWS marker records `True`, so no `UnknownScope`+`NotCapturing` pair exists in this store. `A49w` remains the only evidence for it | ✅ **RULED 2026-08-12 — promoted from a decision to a build slot.** Full ruling text in §0a; it is the decision of record. **Split the hour AT the marker, classify each part against its governing state, report the hour defective if EITHER part is.** ⚠ **State what the output UNIT becomes** — one row per hour with a worst-of verdict, or one row per span. ⚠ **Do not change `ResolveScope`'s contract for callers that cannot split**; add the split at the classification site. **Fixtures: a flip at :00, a flip at :59, and an hour carrying two markers.** **Model: Sonnet, effort: medium** |
| ~~**`DeribitIndicatorProject.md` §15 — C1 occupies THREE rows against §15's own one-row cap**~~ | ✅ **DONE — collapsed 3 rows → 1 by the 2026-08-14 trim.** ✅ **CLOSED, verified in the tree 2026-09-07 (UTC):** [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15's C1 row now opens *"ONE ROW for one queue item, per §15's own rule — collapsed 2026-08-14 from the THREE rows this item had taken (Session 1, Session 2, a rider)"*, so §15 records the fix itself. **The same trim is the exemplar the one-row rule now cites as its own growth mechanism.** *Original size estimate follows.* ~~trivial doc tidy~~ | Flagged by the trade-identity implementer, who correctly declined to edit another item's record. §15 caps at one row per item precisely because it grew to 69 % of the session-start read. Consolidate C1's Session 1 + Session 2 + rider into one. Blocks nothing |
| ✅✅ **`G12` — DONE 2026-09-07. All three manual gaps closed AND both tracked PDFs regenerated** | ✅ **CLOSED.** `docs/UserManual.md` +137 · `docs/TraderGuide.md` +33 · **170 insertions, ZERO deletions** · `UserManual.pdf` 359,407 → **379,681 B** · `TraderGuide.pdf` 167,669 → **173,833 B** · zero pandoc glyph warnings, exit 0 | ⭐ **The row's own rule was honoured — *"editing the `.md` WITHOUT regenerating is worse than leaving it"* — so the pass was scoped to end with the regen or revert entirely. Toolchain verified present BEFORE starting (pandoc 3.9.0.2 + XeLaTeX/MiKTeX), rather than discovering it missing halfway.** **Content: `use_best_pivot_candidate` in BOTH manuals (stated explicitly as OFF by default and a what-if instrument, never as live behaviour) · a new `MIN NET MOVE %` section in `UserManual.md` only, as scoped · `BacktestRunner` in both, five verbs with the exit-code table.** ⭐⭐ **THE VALUE WAS NOT THE PROSE — the authoring pass caught TWO facts that would otherwise have shipped WRONG into a user manual: (1) `SettingsLoader.Save` DROPS `changeNote` entirely when `bumpVersion:=False` (`:268-276`) — only `last_modified` is refreshed, so the drafted "records its change note" sentence was false; (2) `HourClass` has SEVEN members, not the six `tools/BacktestRunner/BacktestProgram.vb:20` claimed — stale since the C1-coverage F1 build inserted `TrailingEdge`.** ✅ **Both verified independently and the `BacktestProgram.vb` count corrected in the same sweep.** ⭐ **The general lesson: writing documentation against the code is itself an audit — it found a stale count that no fixture, gate or reflection walk was ever going to catch.** **Original row follows.** ⛔ **NOT TRIVIA — re-sized 2026-08-25.** It was pulled into a "trivia bundle" on 2026-08-25 and pulled back out: the work is **authoring three missing manual sections**, then **regenerating two TRACKED PDFs** (`docs/UserManual.pdf`, `docs/TraderGuide.pdf`) via `tools/build-manual-pdfs.ps1`. ⚠ **Editing the `.md` WITHOUT regenerating is worse than leaving it** — it puts the two surfaces out of sync, which is the doc-rot class this queue exists to catch. **Sonnet, effort medium**, one session, and it must end with the regen | `use_best_pivot_candidate` in neither manual · the `MIN NET MOVE %` row label (in `UserManual.md`, **not** `TraderGuide.md`) · **`BacktestRunner` absent from both** | [`seat-close-handover-gap-audit-2026-07-31.md`](seat-close-handover-gap-audit-2026-07-31.md) §5 |

---

## 3. Riders — cannot travel alone; attach to the next qualifying event

| Rider | Attaches to |
|---|---|
| ⚠ **The rotation backup name is STALE and mislabels the book it saves** — `AnalysisLogger.vb:143` | **The next CSV header rotation — which absorption `D-1` IS**, since any header change rotates (`EnsureLogFile` compares line 0 to `Header`). ⚠ **Must land IN that commit, not after it — once the rotation runs, the misnamed file exists.** The path is **hardcoded** to `analysis_log.csv.v0.7.bak`; that was correct for v0.7→v0.8 and is now stale for every future rotation. **Verified 2026-08-20: the name is already TAKEN** — the existing file holds **95 columns**, the real v0.7 schema — so a v0.9 rotation takes the timestamped branch and writes the **v0.8** book (111 columns) into `analysis_log.csv.v0.7.<ts>.bak`. **Anyone later hunting "the v0.8 book" will not find it under that name.** **Fix: derive the suffix from the superseded header, never a literal** — the same defect class as the fixture-literal provenance rule. **Model: Sonnet, effort low** — bundle into `D-1`, do not give it a session |
| ⚠ **`.bak` inclusion is missing from the pooled-concat rule** — [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) §3b step 2 | **The same rotation. Doc-only, and it is the half that actually costs rows.** §3b step 2 already handles *"a rotation happened on ONE BOX only ⇒ DO NOT pool"* — it says **nothing** about including the rotated `.bak` from the **same** box. ⚠ **Verified 2026-08-20: NEITHER reader discovers `.bak` files.** `CsvFeatureBuilder.vb:112` (W6-4) and `ForwardWindowJoiner.vb:108` (the Kelly ladder) each take **one path**; nothing in the tree globs `analysis_log.csv*`. **Pooling is a MANUAL concat, so nothing warns when the `.bak` is omitted and the population silently truncates to post-rotation rows.** ⚠ **Cost if dropped: the Kelly trigger needs ≥406 pooled weekday STRONG; at the measured 12.4/weekday, re-accumulating ~400 is ~32 weekday-days ≈ 6.5 weeks.** ✅ **CONCAT ORDER IS FREE — SETTLED 2026-08-20, and it settled the OPPOSITE way to this row's first draft.** The earlier ruling ("keep chronological order") was written while `PopulateForwardBars` was untraced. **It has now been read: `ForwardWindowJoiner.vb:190-210` iterates rows independently, floors EACH row to its own UTC minute, and looks bars up from a `Dictionary` — row order is never consulted.** No top-level sort on `Timestamp` exists in `ForwardWindowJoiner.vb` or `BandLadder.vb` either (`AnalysisRunner.vb` sorts only within-population value lists), and `.Index` is positional alignment within one load, not a chronological assumption. **RULED: put the NEW schema FIRST in the concat**, so the header map carries the added column names and post-rotation rows stay readable; pre-rotation short rows return empty on those columns via the verified bounds checks. **This is strictly better than the chronological ordering and costs nothing** |
| ~~`change_log` v64 reversibility wording~~ | ✅ **CONSUMED 2026-08-02 — travelled with D3/v65** |
| **`TriggerMode` CSV column** | The next natural CSV header rotation — **never force one** |
| **Effective-source per-row stamp** (`DeriveWsHealth`) — ruled **J-E** | The same rotation. Until it ships, treat every REST-fallback-sensitive figure as a **bound, not an estimate** |
| **`SettingsVersion` per-row column** | The same rotation, third of the attribution set. **The deploy checklist §4.5 asserted this column already existed; it does not** — `AnalysisLogger.vb`'s header runs `Timestamp` → `SignalId` with `InstanceId,SignalId` as the only attribution fields. A version straddle is **not filterable from the data**, and because `settings.json` hot-reloads it can land **mid-`InstanceId`**. Until it ships: **make every settings-version change coincide with a process restart** so `InstanceId` is a usable proxy, and keep the checklist §5 ledger — now the only place that mapping exists. **Sharpest at a scoring boundary** — v65/D3's armed and unarmed ASIA rows are identical in shape, so a mixed fleet contaminates the D3 watch's own numerator |

---

## 4. Standing watches — periodic, no tick required

liq_events CASCADE ⇒ A4 · §9 STRONG accrual · burst-watch spot-checks · funding calm-week re-read · absorption episode accrual under Path B · **v48 §4a OFI dominance** · **B4b §12** (its F3 arm is blocked — §2) · **pullFrac distribution (W4)**.

✅ **The D3/v65 ASIA arming watch — READ 2026-08-11, PASSED.** [`d3-asia-burst-watch-read-2026-08-10.md`](d3-asia-burst-watch-read-2026-08-10.md). Fire rate **10.97 %** and same-side **89.47 %** over 12 fully-covered AWS weekday session-days (n=1,905); the post-ship-only 5-weekday slice reads 11.99 % / 89.47 %. `burst_ratio_threshold` stays **5.5**; no re-fit triggered. The contra arm stays effectively dead (0.50/day), so **on res-3 this modifier is upgrade-only** — unchanged from the derivation §3.

⚠ **The watch stays LIVE, on the ruled tolerance — which is NOT the one in the derivation.** Reference **11.0 %**, band **8–14 %**, read length **≥10 weekday session-days**, same-side **≥85 %** (T-1…T-5, ruled 2026-08-11; they supersede row D3-5 of [`asia-burst-threshold-derivation-2026-08-01.md`](asia-burst-threshold-derivation-2026-08-01.md) §5). **A trigger fires a re-derivation READ, not a threshold change.** ⚠ **Do not quote the derivation's 9.7 % or its "~106 AggrVel rows/day"** — both come from a partially-covered book; fully covered, ASIA carries **~158–160 rows/day**, which makes the small-sample argument ~50 % weaker than recorded. **Next band-eligible read ~2026-08-17**, since 2026-08-10's ASIA was lost to the intentional weekend instance stop.

**⭐ Dated trigger — the only watch with a computed date.** The Kelly EST advisory renders *"Actual numbers after next book doubling"* — a **forward promise on screen**. **Trigger: ≥406 pooled weekday STRONG** (double the 201 at the F1 read), **ETA ~2026-08-30** at the measured two-box rate of **12.4 STRONG/weekday**. **Bundle with the W6-4 re-run**, which lands in the same window on its own basis — one pooled freeze, one session, both instruments, and the overfit counter stays honest because they consume the same span. **If the ladder still does not separate, the line must be re-worded or the block suppressed — it must not silently promise another doubling.** ⚠ **Blocked on the §2 copy-back:** as of 2026-08-07 the local book holds **149** weekday STRONG (2026-07-03 → 2026-08-06) and the AWS copy stops 07-31, so **the pooled figure cannot be read at all right now.** [`kelly-est-honesty-decision-2026-08-02.md`](kelly-est-honesty-decision-2026-08-02.md) §2.1.

**Data gates worth watching, with fresh readings 2026-08-07:** **A5 VPFR shape — 28 of 30 distinct calendar dates** (2026-07-03 → 2026-08-06), up from 15 on 07-22, so **~2 dates out**. ⚠ Meeting it does not authorize the build: A5 must still clear the **W6 new-indicator bar** as a VPFR refinement rather than an orthogonal class.

---

## 5. Closed — recorded so they are not re-asked

**2026-08-21 — 📌 NAMED REFERENCE WINDOW: the 2026-08-18 VENUE HALT, `09:04:04Z → 10:26:55Z`. A known-good 87-minute void in the tape that is NOT capture loss. Read this before investigating it as one.**

⚠ **THE TRAP THIS ENTRY EXISTS TO DISARM: any time-based completeness check that crosses 2026-08-18 will see an 87-minute hole in `trades_2026-08.csv` and report a catastrophe. There is no hole.** Measured on production via SSM 2026-08-21, window `08:54–10:37Z`: **338 rows · `trade_seq` 296,333,287 → 296,333,624 · span 338 · present 338 · MISSING 0.** **Sequence numbers are perfectly CONTIGUOUS across the wall-clock void**, so the venue assigned no seqs — **no trades occurred.** Corroboration: the per-minute rate resumes at **122 then 109 trades/min against a ~22/min baseline**, the signature of a post-halt catch-up. Matches [`roadmap.md`](roadmap.md)'s note that gate G-1 returned *"venue-wide Deribit outage"*. `ws_health.log` on production carries the transitions: `2026-08-18T09:04:04.739Z | DEGRADED` → `2026-08-18T10:26:55.270Z | OK`.

⭐ **WHAT IT VALIDATES — [`trade-store-downtime-repair-proposal.md`](trade-store-downtime-repair-proposal.md) `D-2`, on live data for the first time.** D-2 rules hole detection on **`trade_seq` alone, never a time gap**, and that was argued rather than demonstrated. **Here a time-based detector reports an 87-minute catastrophe and `trade_seq` correctly reports zero missing.** That is exactly the distinction D-2 exists to make. ⛔ **Do not revisit D-2.** It also bears on the queued `trade_seq`-skip question in §2: **338 contiguous seqs across a venue halt is evidence AGAINST the venue skipping numbers** — one window, not a proof, but it is the only live evidence either way.

⛔ **WHAT IT DOES *NOT* VALIDATE, AND THIS IS THE HALF THAT WILL BE MISREAD: the downtime-repair prediction did NOT pass — IT DID NOT FIRE.** [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) §15 arms it as *"after one outage the hole is filled within `gap_repair_interval_hours`"*. **This event produced NO hole, so there was nothing to heal. Gap repair's healing path remains UNTESTED. Do not tick it.** ⚠ **The probe itself asserted the wrong cause** — it printed *"Gap repair healed the DEGRADED window"* on zero missing seqs, a causal claim no completeness count can support; corrected in `964e48e`. **Second instrument in two days to carry a confident headline its own data did not license** (the other: a search engine inventing a source attribution for the `trade_seq`-skip sentence). **Read the evidence rows, never the summary line.**

**REPRODUCE:** `aws ssm send-command --instance-ids <prod> --document-name AWS-RunPowerShellScript --parameters file://ssm-tapecheck.json` — the window is hardcoded in the probe; edit the two `[DateTimeOffset]::new(...)` lines to point it elsewhere. Sibling probes: `ssm-mem.json` (memory + eviction), `ssm-apphealth.json` (rows/hour, cadence lag, store size, runtimes, session state). **All three read-only, none needs RDP** — they run over SSM against the instance profile.

**2026-08-20 — ✅ STARBASE (the Coinbase matching engine on Deribit) CHANGES NOTHING ON OUR SIDE. Checked against Deribit's own material; do not re-check.** Starbase went to production early July 2026 with migration through August. It adds **new binary APIs** (SBE order entry, FIX Drop Copy, multicast market data) — **additive, not replacements.** Deribit's [Starbase API changelog](https://docs.deribit.com/changelogs/starbase) states *"All existing APIs will continue to work throughout the ME replacement"*; Deribit Insights confirms all current APIs remain fully supported and backward-compatible. **Verified unchanged, one by one:** `public/get_last_trades_by_instrument` · `..._and_time` · `get_order_book` · `get_book_summary_by_instrument` · `get_tradingview_chart_data` · `get_funding_rate_history`. The changelog covers **only** the SBE/FIX/multicast systems — **none are on our path** (the engine speaks JSON-RPC over REST and WebSocket). The one real breaking change applies to **institutional clients migrating from Coinbase International Exchange** (old API keys stop working) — **not us.** ⚠ **AND THE RETENTION HOPE IS DEAD — this is the half worth remembering.** Deribit documents *"recent trades available for 24 hours before removal, while historical records persist indefinitely"* via a **`historical` parameter**, which reads like the store's problem solved. **It is not.** Every endpoint that accepts it is `private/`: `get_order_history_by_instrument` · `get_order_history_by_currency` · `get_user_trades_by_instrument` · `..._by_instrument_and_time` · `..._by_currency` · `..._by_currency_and_time` · `..._by_order`. **`historical` changes REACH, never SCOPE.** ⛔ **The ~24 h PUBLIC trade retention is UNCHANGED and the store's premise stands exactly as `settings.json` line 8 records it.** Related: **TradingView cannot substitute either** — export is Plus/Premium only and capped at 10,000/20,000 **bars**, tick data is Ultimate-only and is **7-day Bar Replay, not a download**, and there is no public API for automated pulls; decisively, **bar aggregation destroys `Direction`, `Liquidation`, per-trade `Amount`, `TradeId` and `TradeSeq`**, so no plan at any price reconstructs the tape. **TradingView sells the data we already get free and uncapped from Deribit (candles), and cannot sell the data we are missing.** Tardis remains the only real product class and stays declined on cost (~$350–700/mo, 2026-07-31).

**2026-08-20 — ✅ THE ENGINE'S WS CHANNEL CONFIG CHECKED AGAINST DERIBIT'S BEST-PRACTICES PAGE. No live problem; three things settled so they are not re-asked.** `DeribitWsFeed.vb:27-33` subscribes `book.<i>.none.10.100ms` · `trades.<i>.100ms` · `ticker.<i>.100ms` · `chart.trades.<i>.{1,3,5,15}` — **all aggregated, none raw.** **(1) "Raw public feeds require an authenticated connection"** ⇒ **no exposure**; the unauthenticated feed is legitimate. ⚠ **But it prices any future move to raw: raw ⇒ a credential on the collector**, which is the constraint behind the absorption §2 row above. **(2) Raw-feed coalescing under extreme load** (*"you might occasionally see a raw order book change that actually represents two or three changes at once"*) ⇒ **does not apply** — on aggregated channels batching **is** the contract, not an anomaly. ⚠ **This does NOT resurrect the WS-aggregation explanation for the `TradeStoreWriter.vb:149` drop**, which was refuted on volume grounds (repair mean 3,850 vs stream 3,013) and whose real cause — a millisecond timestamp used as identity — was found in the code and fixed. **It does explain why the defect had the shape it did:** a 100 ms trades batch contains every trade in that window, many sharing a millisecond, which is precisely what the guard discarded. **(3) "Avoid excessive REST polling"** ⇒ **compliant.** Deribit asks clients not to call `get_order_book`/`get_last_trades` in a loop; the engine fetches per run at 60 s / 180 s bar closes and gap repair runs 6-hourly — neither is a poll loop. ⛔ **AWS Multicast (London `eu-west-2` / Tokyo `ap-northeast-1`) — CONSIDERED AND REJECTED, recorded so the AWS-cost work does not re-derive it.** It buys microsecond latency for HFT; this engine acts on 60 s / 180 s bar closes, so the benefit is **structurally zero**, while the cost is SBE binary decoding added to a codebase **mid-port to Linux**. **Revisit only if the strategy itself becomes latency-sensitive** — which the trader profile says it is not.

**2026-08-20 — recorded as a KNOWN LIMITATION, not a defect: the raw OI series is never persisted.** `_oiHistory` is an in-memory ring with **70-minute eviction** (`UI/MainForm_Analysis.vb:333-334`) and **dies with the process**; only the derived `OI_Current` / `OIChange15m` / `OIChange60m` / `OISignal` reach the CSV. Same shape for the order book — **no depth snapshot is ever persisted**, which is why [`backtest-synthesizer-proposal.md`](backtest-synthesizer-proposal.md) §1 lists OFI, spread, OI and absorption as **MUTED** in any replay. ⚠ **Stated precisely because a looser phrasing misled once already in the 08-20 session: their DERIVED OUTPUTS ARE captured** — `analysis_log.csv` carries `OFIRatio,OFIBidVol,OFIAskVol,OFISignal`, the four `OI*`, `SpreadBps`, `OFIMomentum` and the five `Absorption*`, and liquidations get a **second durable capture** in `liq_events.log` (`Core/AlertsTracker.vb:379`, append-only, never rotated, the sole A4 gate instrument). **What is lost is the INPUTS: you can never re-derive OFI under a different depth weighting, or `OISignal` under a different window.** Exactly `settings.json` line 8's *"keeps the answer and discards the question"* — recorded here so the distinction is not re-litigated as either "captured" or "not captured", because both are wrong.

**2026-08-11 (later):** ✅ **SEAM AUDIT RUN — [`seam-audit-2026-08-11.md`](seam-audit-2026-08-11.md), read-only, seven findings, nothing fixed.** Brief: [`seam-audit-brief-2026-08-11.md`](seam-audit-brief-2026-08-11.md). Scoped to one **defect class** — *two things that must agree, with nothing asserting they do, where the failure looks like correct behaviour* — deliberately **not** an indicator review and **not** a signal-health re-run, since none of the known instances is a statistical anomaly. **It found the fourth instance the brief asked for, and it is one day old and mine: `tools/WsTradeProbe` (S-1).** ⚠ **Three corrections to things previously recorded as fact:** (1) the brief's "175 defaults" is **255**; (2) the brief's "all three POCO drifts come from one commit, v33" is wrong — **v34 moved CVD again and set the current divergence**, and **v36 and v58 also produced drift**; (3) **`CLAUDE.md` step 1's "~24K tokens" for [`DeribitIndicatorProject.md`](DeribitIndicatorProject.md) is stale by ~2× again — measured 47K**, exactly the re-growth `CLAUDE.md`'s own note predicts, so §15 wants another trim to the archive. ⚠ **The single most reusable output is the rule that explains the negative results:** v40/v41/v48 produced no drift because their keys are `Double?` = Nothing **nullable overrides, which carry no competing value and therefore cannot drift** — that rule should scope A54a instead of a hand-listed key set. **Also found clean, and it bounds the class:** zero JSON orphans, zero case mismatches, zero unreachable POCO classes, and **three of four "one seam, no copies" claims hold** (two of them understate themselves). ⚠ **Sweep 4 is a genuine negative result worth keeping** — `LivePerformanceTracker` now checks OHLC completeness **three** ways including a row-count-against-unique-keys check, which is the 2026-07-31 store-hole lesson applied in code. **What the audit did NOT check is stated in its §8, eight items** — most notably that it ran **read-only, so every fixture claim is hand-arithmetic rather than observed**, and that `CoverageReport` was not swept. **Its closing judgement, recorded because it is the actionable part: the class reproduces faster than it is being repaired** — v66 fixed a drift "in lockstep" and left two of four copies stale on the same day a new tool reintroduced a parse copy.

**2026-08-11 (newest):** ✅ **THE D3 ASIA WATCH READ FOR THE FIRST TIME SINCE ARMING — PASSED**, and ✅ **D2 SHIPPED as settings v66 (`fd52299`)** on the strength of it. Watch: fire rate **10.97 %**, same-side **89.47 %**, 12 fully-covered AWS weekday session-days. D2: `indicators.OBV.trend_gate` **18.0 → 23.0**, one global key, ⚠ live scoring change and a dataset boundary, ~~**deploy still owed**~~ ✅ **DEPLOYED 2026-08-10 18:35 UTC to both boxes — corrected 2026-08-13**. Gate PASSED — six projects 0/0 Release, harness ALL PASS at 265 checks, display-parity clean, version-bump guard satisfied. ✅ **T-1…T-5 ruled**, superseding row D3-5 of the derivation. **Four findings worth keeping, all of which correct something previously recorded as fact:** (1) ⚠ **a SIXTH v65 InstanceId existed in no document** — `ffced26c…`, now ledgered; the §5a table is hand-maintained, has missed an entry once, and **stops being viable at ~one new id per day if AWS ever moves to a scheduled start**. (2) ⚠ **a POCO drift of 8.0 had gone unnoticed since 2026-06-13** — `ObvSettings.TrendGate` sat at the pre-v33 value 10.0 against a shipped JSON 18.0. Latent, not live, and fixed in lockstep; **there is still no JSON↔POCO drift guard for OBV — queued as A54a**, and its absence is exactly what let this live two months. (3) **The "~106 AggrVel rows/day" figure quoted in four documents is a mixed-coverage artefact** — fully covered, ASIA carries ~158–160/day. (4) ⚠ **a ~49.5 h AWS collection hole**, 2026-08-08 08:33 → 2026-08-10 10:00 UTC, from an intentional weekend instance stop; **the tape hole is smaller at 29.4 h** because gap repair back-filled exactly `gap_repair_lookback_hours`, and **an EC2 stop leaves no `DOWN` line — absence of one is not evidence the box stayed up.** **The method note worth inheriting:** a tempting coverage-composition explanation for the fire rate was formed, **tested and REJECTED**, and the rejection is recorded — the `seat-handover-2026-08-10.md` §0 rule that a ready explanation for a surprising result is a warning sign, applied and paying off.

**2026-08-07:** **three-doc division restated** (roadmap = strategic board · this queue = immediate scoped slice · dependency map = edges, referred to by both) · **`backlog-dependency-map.md` RESTRUCTURED** — its State column removed, because it was a copy of state that lives here and 7 of its cells were stale; it now carries `Blocked by` / `Unblocks` / a **pointer** to where state lives, and moves rows between "live edge" and "opened edge" as its only maintenance action · **`roadmap.md` re-planned** — §2 replaced rather than appended (it had become a 12-entry log dated 07-02…07-22 against a v65 tree), and ⚠ **§6 rule 1 corrected: it named the v52+v53 bundle as the open window**, spent since 07-17, when the open window is the v65/D3 ASIA watch — a *binding* sequencing rule pointing at the wrong boundary reads as "the slot is free," and **D2 is exactly the change that would have taken it** · **new blocker recorded: the AWS copy-back** · **C1-coverage marked BUILT** in both places that still said BUILD-AUTHORIZED.

**2026-08-08 (newest):** ✅ **TRADE IDENTITY BUILT — `64d41e7`, spec-back `d482935`.** §1 gate passed and is recorded either way as the spec required: `trade_id` is a JSON **string** on all three shapes, `trade_seq` a number ~296 M, and a trade seen on both the WS channel and REST carried identical values — **§0 trap 3 retired on evidence.** Row appends two columns; `TryParseRow`'s `< 5` guard makes it backward-compatible both ways, so no rotation and no rewrite. Dedup moved to one shared contract that **never keys on an absent identity**; venue diff reports identity-matched and fallback-matched **separately**; `trade_seq` gap detection added, reporting **NOT CHECKABLE** rather than "clean" on a store with no sequences. **Fixture teeth proven by mutation, not asserted** — injecting each §0 trap failed exactly the fixtures written for it, and trap 1 was independently caught by the pre-existing A48d. ⚠ **Two things still owed:** the **spec-back Q1 ruling** (the spec's dedup contract is not a well-defined equivalence relation; the build chose identity-first and it needs ratifying **before** the deploy), and the **AWS redeploy itself**. ⚠ **A found-and-fixed trap worth keeping:** `AutoTweaker` / `WhatIfRunner` / `CeilingAudit` link `DeribitClient.vb` but **not** `Core/TradeStoreWriter.vb` — so "the writer links everywhere" is false, and neither the app build nor the harness caught it. **Only Release-building all six projects did.**
**2026-08-08 (earlier):** ✅ **TRADE-IDENTITY SPEC APPROVED — D1–D7 all ticked as recommended, BUILD-AUTHORIZED** behind its own verification gate ([`trade-store-trade-identity-proposal.md`](trade-store-trade-identity-proposal.md), **Opus / high**). Takes **both** `trade_id` and `trade_seq`. Fixture family **A53**; no settings keys so **HC28 stays free**; **not a dataset boundary**. ⚠ **Two things a builder must not miss:** the **AWS redeploy is part of this change** — `settings.json` does not move, so AWS keeps writing five-field rows until it gets the new binary, and that tape is permanently unmergeable; and **D7 ticked re-opens the local-sampler question after the build, it does not authorise resuming capture now.** · ✅ **D1-a REVERSED and executed** — local capture OFF, `bin\Debug` overlay restored, its 2026-08-07/08 tape quarantined at `AWS-copybacks\quarantine-local-d1a-2026-08-08\` rather than deleted, and moved out of `bin\Debug` so a future run cannot append to it.
**2026-08-08 (earlier):** ⚠⚠ **THE 78.8 % COMPLETENESS CLAIM IS WITHDRAWN, and the defect it uncovered is larger than the claim was.** Trader challenged it on the right ground — a box in Deribit's datacentre running 24/7 should hold *more* trades than an intermittent laptop, not fewer. Re-check: **zero timestamps absent from AWS** across 16,190 rows and 6,808 timestamps; the disagreement is **different amounts at shared timestamps**; volume nearly **doubles** on merge; **98.4 %** of it sits in the period the local tape was REST-backfilled. **Root cause: the store has no trade identity** — Deribit supplies `trade_id`, `HistoricalStore.vb:307-317` never reads it, and the five-field row is what both the store dedup and S0's venue diff match on. **Consequences: two tape books cannot be merged; distinct trades are silently dropped at write time; a venue diff is uninterpretable; and retrofit is impossible for rows already written.** Actions taken: the merged store was **un-merged to single provenance per file** (July = pure REST fetch, August = pure AWS capture), mixed versions **quarantined not deleted** in `AWS-copybacks\quarantine-mixed-2026-08-08\`; `trade_id` queued in §2 as an Opus/high build; the S0 daily job **SUSPENDED** behind it; **D1-a's premise withdrawn** and returned to §0a for a trader re-read. **What still stands:** the S3 gap-metric blindness, which rests on arithmetic (~1 trade/2.3 s against a 300 s threshold) and not on the withdrawn number — and **every CSV-derived figure**, because `analysis_log.csv` carries `InstanceId` and `SignalId` and therefore has the identity the tape lacks. **Nothing has ever been calibrated off the tape store**, so there is nothing to redo.
**2026-08-08:** ✅ **D1-a IMPLEMENTED AND THE AWS APP UPDATED — both verified.** Local `bin\Debug` capture is **ON** from **2026-08-07 16:05:56 UTC**, InstanceId `ad7cadf4…`; confirmed from `capture_marker.log`, whose line for that id reads `True`, and from `backtest_data\trades_2026-08.csv` appearing. `bin\Release` correctly **kept** its overlay. **AWS updated to the C1 code**, InstanceId `ec487909…` — so **`capture_marker.log` now exists on AWS** and coverage reports can resolve scope from here on. **AWS hours before that id stay `unknown-scope` permanently; the marker cannot be backfilled.** ⚠ **Neither restart is a scoring boundary** — `settings.json` did not move, both boxes are v65 either side. ⚠ **But v65 now spans FOUR instance ids** (AWS `09c747f8…`+`ec487909…`, local `3916540f…`+`ad7cadf4…`) and **all four are armed** — see §0a. Deploy hardening from the same pass: `aws-collector-deploy-checklist.md` §1.2 is now a **positive allowlist of six items**, after "copy the whole output folder" was found to include `settings.local.json` (which would have **stopped AWS capturing**), `backtest_data\` (~150,000 trades overwritten), and two harmless runtime files. §1b is a PowerShell pre-flight; the first version used `ls`/`grep` and did not run on this box.
**2026-08-07 (latest):** ⚠ **D1-a RULED (trader): local capture returns as a SECOND SAMPLER, temporary.** Decision text in `in-app-trade-store-capture-proposal.md` §7 row **D1-a**. **D1's original ground is untouched** — the end state is still AWS-only, and this retires with the local box. What D1 did not price is a sole capturer being *systematically incomplete while healthy*. ⚠ **Bias recorded with the ruling because it binds every tape analysis:** the local box runs during active hours, mostly NY, so **tape completeness will be higher for NY than for ASIA or LONDON**. The CSV book barely moves (AWS-preferred minute-key dedup drops ~99.9 % of overlapping local rows) but **the tape store merges as a plain union with no preference**, so the bias lands there in full. **No tape-derived measure may compare across sessions without accounting for uneven completeness.** Also amended: `aws-collector-deploy-checklist.md` §1a (both boxes now `true`) and its §3 daily glance (**inverted** — the local box no longer shows `+local`). **ID collisions recorded** in `backlog-dependency-map.md` §0a — **F1, F2 and F3 each mean two different things and all are live in this queue simultaneously.**
**2026-08-07 (later):** ⚠ **FIRST AWS STORE COPY-BACK — executed.** Both dated instruments are now readable: **Kelly gate at 237 pooled weekday STRONG** (was 201 at the F1 read; 169 to go, measured rate **9.5/weekday** — *not* the 12.4 the watch assumed, so **ETA slips ~08-30 → ~09-01**). ⚠⚠ **THE NEXT TWO SENTENCES ARE WITHDRAWN — see the 2026-08-08 entry above. Quoted, not deleted, per the convention.** *"The `bin\Release` tape is NOT a duplicate — 16,459 trades existed only there; merged, zero loss (July 115,029→118,144, August 222,246). The retention rule is what stopped it being deleted."* **The merge has since been reversed and the store restored to single provenance per file.** Two findings queued above: **D1's AWS-only ruling needs a re-read** (~~sole capturer measured 78.8 % complete while healthy~~ — **that figure is retracted; the re-read is still owed, on different grounds**) and **S0 must run daily** (S3 cannot see scattered loss; Deribit's ~24 h retention means a day is checkable once and never again). Procedure written up as [`aws-collector-deploy-checklist.md`](aws-collector-deploy-checklist.md) **§4b** — it had none, and three traps fired during the run: `BacktestRunner` **sets its own CWD** to the repo root so a staging folder cannot be the target; `sort -u` **with a key** dedups on the key and would have destroyed ~44,000 trades (10,199 timestamps carry >1 trade); a `\r`-stripping filter turns a comparison into a false "0 rows in common". **All three were caught by counting rows** — the 2026-07-31 store-hole lesson, applied.
**2026-08-05:** **C1 trade-store coverage report BUILT**, both sessions, reviewed and ACCEPTED (`c6a7a63` fixes the one review finding) · **tape retention RULED** — keep all tape unless it is a copy; merge first, judge duplication after · **atomic-write total-primitive swap queued** as a build slot · **T8 amendment** to the bridge mirror — an unrecognised enum must render DISTINGUISHABLY · session-start doc cut **37 %** by applying §15's own five-row rule, and three doc headers carrying stale version numbers killed.

**2026-08-03:** **RULED — every spec/brief for a new implementer MUST carry a model + effort recommendation** (binding text in **CLAUDE.md**; worked example [`trade-store-coverage-report-implementer-brief.md`](trade-store-coverage-report-implementer-brief.md) §0). Not a bare tier: it must name *why that tier*, *where that model will specifically slip*, the *escalation trigger*, and a *session split* when the build is large · **C1 FULLY TICKED (D1–D7), BUILD-AUTHORIZED** · **WEEKDAY SCOPE RULED** (correcting J-C): capture stays 24/7, *evaluation* is weekday-only; confirms D4's 300,000 ms, cancels the REST-backfill task, Part B stays unconditional.

**2026-08-02:** **J-B scoping clause RULED** — scope a defect rule by a **positive record of intent**, never a baseline derived from the behaviour being judged (a box that dies permanently converges to its own baseline and reports healthy); **C1 unblocked**, D7=(b) ruled out · **D1 TTM RE-DERIVED then PARKED** · **D3 TICKED + SHIPPED as v65**, ⚠ boundary spent · **CLUSTER A COMPLETE** · **E1 decided** (option c) · **Kelly display shipped `18b1ea8`** and trader-confirmed · deploy checklist **§3b dedup corrected** to AWS-preferred minute-key · this queue's **§0a added**.

**2026-08-01:** **37 commits PUSHED** · **E6 fee knob CONFIRMED** · **E2 W6-4 RUN → INCONCLUSIVE**, no spend meanwhile · **D-cluster sequencing RULED** · **B1 correction** — build-authorized since 07-21, not awaiting a tick · J-A ratified · G7 ASIA derivation.

**2026-07-31:** the "both collectors down" verdict **withdrawn** (date-premise error) · AWS-preferred dedup **ruled** · F1 count gate and W6-1 depth **re-verified** · **W6-1 ruled NO CHANGE** · candle-backfill fixture gap confirmed closed (A51a–e).

---

## 6. Provenance

Assembled 2026-08-01 from: [`seat-close-handover-gap-audit-2026-07-31.md`](seat-close-handover-gap-audit-2026-07-31.md) · [`job1-outstanding-2026-08-01.md`](job1-outstanding-2026-08-01.md) · [`job2-read-2026-07-31.md`](job2-read-2026-07-31.md) · [`overlay-whitelist-reaudit-2026-07-31.md`](overlay-whitelist-reaudit-2026-07-31.md) · [`w6-1-london-ruling-2026-07-31.md`](w6-1-london-ruling-2026-07-31.md) · [`asia-burst-threshold-derivation-2026-08-01.md`](asia-burst-threshold-derivation-2026-08-01.md) · and each spec's own D-table. **Re-swept against the tree 2026-08-07** — findings in §1b.
