{
  "version": 61,
  "last_modified": "2026-07-23T00:00:00Z",
  "modified_by": "v61 ABSORPTION GEOMETRY RESCALE (V1-V4 all ticked 2026-07-23) - three tick keys (proximity_ticks/band_ticks/break_tol_ticks) retired for ATR-fraction keys (proximity_atr_frac 0.30 / band_atr_frac 0.10 / break_tol_atr_frac 0.05); floors rescaled to the widened shell (depletion_floor_usd 25000->5000, default.min_aggr_usd 150000->20000, absorb_ratio 3.0->1.5, max_pull_frac 0.5->0.75) - all PROVISIONAL until the post-rescale re-derivation. Display/CSV-only surface (scoring_enabled stays false) - ZERO scoring impact, strip-only display, no card/snapshot obligation. Spec: docs/absorption-geometry-rescale-proposal.md; spec-back: docs/absorption-geometry-rescale-spec-back.md",
  "change_log": [
    "v61 (2026-07-23): ABSORPTION GEOMETRY RESCALE - display/CSV-only surface (scoring_enabled stays false), ZERO scoring impact, NOT a dataset boundary (the CSV Absorption* columns already exist since v54's rotation; column values change meaning per the widened geometry but no header rotation, no .bak). Spec: docs/absorption-geometry-rescale-proposal.md (APPROVED 2026-07-23 by trader after coordinator ruling; V1-V4 all ticked); evidence: docs/absorption-engagement-derivation-2026-07-23.md + the map ruling (0 pct engagement because the tick-scaled geometry - band 4t=$2, proximity 12t=$6 vs ATR~=44 - measures a shell almost nothing prints in; all three anchors bind at 100 pct; loosest re-anchor caps at 1.6 pct vs the 3-8 pct design band). Spec-back: docs/absorption-geometry-rescale-spec-back.md. MECHANISM: three retired keys (indicators.absorption.proximity_ticks / band_ticks / break_tol_ticks) become applier-unresolvable (v53 pattern - deliberately NOT added to RejectedPathFragments, the v47-F1 snapshot-poisoning lesson); three new ATR-fraction keys populate at defaults 0.30/0.10/0.05 (~=$13.2/$4.4/$2.2 at ATR 44 - the design point of the 07-23 derivation). Resolved to absolute dollars ONCE PER RUN at the SetAbsorptionLevels carry site in MainForm_Analysis (execution-resolution ATR by construction, since r.ATR carries the exec-resolution value); the LevelAbsorptionTracker keeps working in absolute dollars internally - proximity_usd / band_usd / break_tol_usd become tracker state, refreshed at SetLevels alongside the four candidate-level fields; FoldBook / FoldTrade no longer read cfg tick fields (no SignalEmitter.TickSize multiplication in the tracker at all). Warmup: r.ATR = 0 collapses the resolved distances to zero, the proximity gate cannot open, tracker stays IDLE by construction (the same posture the pre-v61 uninitialised carry would give). FLOOR RESCALE (all provisional, all against the widened shell): depletion_floor_usd 25000->5000 (divide-by-nothing guard, no longer masks the low-volume events the widened shell will collect), default.min_aggr_usd 150000->20000 (the loosest defensible NY p75-ish anchor after the derivation's grid sweep - keeps sub-$10k noise out), absorb_ratio 3.0->1.5 (mid-band on the observed max 2.0 rather than triple it), max_pull_frac 0.5->0.75 (the 07-23 derivation showed 0.5 vetoing 50-83 pct at tiny volumes where the lower-bound ratio is noise). D8 conservation / visibility mask UNCHANGED - the formulas follow whatever band is passed in, so widening the band flows through by construction (proposal §1). TWEAKER: HC23 unchanged in SHAPE (enabled/scoring_enabled exact-match reject; default./sessions. prefix reject); comment text in SettingsDiffApplier + rule 23 in PromptBuilder updated to name the new proximity_atr_frac/band_atr_frac/break_tol_atr_frac tunables (the flat surface). CSV: header unchanged (v0.8 columns keep their names + positions); Absorption* row VALUES will start populating rather than staying empty - the 07-23 derivation projects ~1-3 flagged rows per NY session-day at these anchors, ~0.5 per LONDON, ~0 per ASIA (per §5 notes; re-verified by the post-rescale watch below). RETRO-FILTER: NOT POSSIBLE (proposal §2) - the tracker folds ~100ms book snapshots + per-trade stream at fold time; neither is persisted, and episodes that never opened (price within $6 of a level was rare) left no record. Re-collection under honest geometry is unavoidable: ~1-1.5 weekday-weeks -> §5 engagement re-derivation (recipe = docs/absorption-engagement-derivation-2026-07-23.md, re-run) -> then the activation gates (~mid-Aug, the Opus month). POCO DEFAULTS RIDE THIS COMMIT (v33/v34 precedent): AbsorptionSettings.ProximityAtrFrac=0.30, BandAtrFrac=0.10, BreakTolAtrFrac=0.05, AbsorbRatio=1.5, DepletionFloorUsd=5000, MaxPullFrac=0.75; AbsorptionDefaults.MinAggrUsd=20000; the three tick property/JsonPropertyName pairs REMOVED from the POCO (an absent JSON key resolves to the new default; the retired JSON keys are ignored on load - System.Text.Json permissive parse). DISPLAY-STRING PARITY: strip-only surface (the ABS-tag TAPE-strip line), NO card/snapshot obligation - the ABS-tag renders the same 'ABS! N (Rx)' shape (a shape parity with the state values), and the plaintext-snapshot / card surfaces neither emit an Absorption line (v54 D6 precedent under the display-string parity rule). Fixtures: A31a-g re-pinned to fraction-resolved dollars via the extended SetLevels signature (6/2/1 USD to preserve the previous v54 test geometry byte-identical against unchanged books); A31b/A31c threshold-edge and D8-veto values re-checked under the rescaled defaults (absorb_ratio 1.5 accepts ratio 3.5 -> ABSORB_ABOVE; max_pull_frac 0.75 still vetoes pullFrac 2.0 - the veto identity holds by construction); A31g HC23 fence-test JSON literal updated to the new key names + the proposable check now targets proximity_atr_frac (the renamed flat tunable). NEW A31h - two-ATR scale-invariance: same episode geometry replayed at ATR=44 (prox $13.2 / band $4.4 / break-tol $2.2) and ATR=88 (prox $26.4 / band $8.8 / break-tol $4.4) on a 2x-scaled ladder, verifying the tracker's resolution arithmetic gives identical classification (the V3 pin). Acceptance: solution + AutoTweaker + WhatIfRunner + CeilingAudit + OrderCheck build 0/0 Release; A1-A39e unregressed; verify-gate prepush GATE PASSED. POST-SHIP WATCH (§12 addendum, mirror of the v54 build watch on the widened geometry): NY Absorption engagement rate on directional rows over the first weekday-week (target 3-8 pct band; if <1 pct after 5 weekday sessions -> re-derive again with a looser default; if >12 pct -> raise absorb_ratio); LONDON + ASIA on their own longer clocks per the derivation §4 note. Local commit - trader tests + pushes; coordinator review follows.",
    "scoring boundary (2026-07-23): LONDON AGGRESSOR-VELOCITY BURST ARMING (v59->v60) - one settings delta: indicators.aggressor_velocity.sessions.LONDON.burst_ratio_threshold = 5.5 (S1-S5 ticked 2026-07-23; derivation docs/aggr-vel-s52-london-derivation-2026-07-23.md - p90 knee 5.69 -> 5.5 per the NY rounding rule; 10.1 pct fire rate, 93.1 pct same-side, n=572 weekday LONDON rows / 145 recipe-filtered fires). ARMS the v52 TFI-modifier on LONDON rows via the shipped D1 auto-arm mechanism (HasExplicitAggrVelBurstThreshold) - NO code change; the narrowest possible dataset boundary (rows after this build carry the burst-modified TFI vote on LONDON; NY unchanged at 4.5; ASIA stays display-only at ~70/150 fires). CAVEAT recorded: LONDON contra-soften arm is thin (~0.3/day) - effectively upgrade-only; POST-SHIP WATCH: LONDON fire rate 8-12 pct band + same-side >=85 pct over the first 2 weekday sessions, re-fit T by the recipe on drift (threshold is revisable at any boundary on richer data). POCO default rides (LONDON session-override BurstRatioThreshold=5.5, v52 precedent).",
    "v59 (2026-07-22): #7 LIQUIDATION-CASCADE ALARM + #8 LEVEL-APPROACH ALERTS — display/alert-only surface (H1-H5 ALL TICKED 2026-07-22, H4 AMENDED same day: first-liq-seen event PERSISTED to a tiny append-only sidecar `liq_events.log` beside the CSV — one line per event `utc | kind(FIRST_SEEN|CASCADE) | side | usd | instance_id`, so the file's EXISTENCE is the durable A4 unlock evidence). ZERO scoring impact — no CSV column, no card, no snapshot line; TAPE-strip tag + status-bar flash + tooltip only (the exit-guard / #6 absorption precedent). Spec: docs/liq-cascade-level-alerts-proposal.md; spec-back: docs/liq-cascade-level-alerts-spec-back.md. NEW alerts block {enabled:true, cascade_min_trades:3, cascade_window_sec:10, level_ticks:12, sound_enabled:false} — ALL provisional anchors: nobody has seen a live cascade, so re-anchor is the trader's job after the first real one; level_ticks:12 = the #6 proximity_ticks anchor so 'near a level' reads with one mental model across features. MECHANISM: new host-agnostic Core/AlertsTracker.vb — owned by MarketState under its ONE SyncLock (the #6 discipline), folded once per streamed trade in DeribitWsFeed.ApplyTrades (the trade analogue of #5 / #6's trade fold), reset in SeedAsync on (re)connect. Fields: a time-stamped ring of liq-flagged trades pruned per cascade_window_sec (H2 — cascade = >= cascade_min_trades in the window, EITHER SIDE; dominant side by USD names the direction CASCADE_ABOVE / CASCADE_BELOW); per-side approach episode with re-arm on leave (H3 — the #6 absorption-episode pattern; carried levels are the SAME candidate set the strip + #6 tracker use — LastSwingHigh/Low5m + VPFRNearestHvn Above/Below, refreshed per full run via MarketState.SetAlertsLevels at the _lastSuccessfulIndicators capture site); a per-process first-liq-seen flag that ALSO reads the sidecar file's existence (H4 — so 'liq_events.log contains >=1 CASCADE line' is the A4 unlock check, surviving restarts). CASCADE / FIRST_SEEN events fire ONCE per active window (edge-detector re-arms when the count drops below threshold); the tracker appends every event to the sidecar in-place (never rotated, never throws — the SignalEmitter.TryWrite discipline) and drains pending events on Snapshot for the host to play the audible cue (sound_enabled opt-in, default OFF) + extend the status-bar flash. DISPLAY: LiveMicrostructureEvaluator + MicrostructureSnapshot carry the alerts state; MainForm_LiveStrip appends compact 'LIQ↑ 3× ($2.1M)' / 'NEAR SH 60103 (7t)' tags to the TAPE strip while active (H1 — strip-only surface, no card/snapshot obligation — the #3/#5/#6 precedent), swaps the strip's ForeColor to the amber attention accent for a ~6s flash on each event (Theme.ACC_WARN), and keeps a persistent ToolTip on lblLiveStrip citing the sidecar path once liq_events.log exists. TWEAKER: HARD CONSTRAINT 25 — `alerts.` PREFIX fenced in SettingsDiffApplier (same shape as exit_guard./live_strip./signal_bridge. — display/alert plumbing has no failure-rate linkage) + PromptBuilder rule 25. FIXTURES: A37a-e (verify/ordercheck) — cascade window math (3 in 10s across sides), episode re-arm on leave-proximity + level re-map close, sidecar append shape (utc | kind | side | usd | instance_id) never-throws, FIRST_SEEN + CASCADE alignment, HC25 fence rejects + sibling tunable pass. REVERSIBILITY: enabled:false ⇒ tracker Fold+Snapshot early-out, strip renders no alert tags — byte-identical to pre-build (harness-proven). Local commit — trader tests + pushes; coordinator review follows.",
    "v58 (2026-07-22): ASIA session_volume dial-to-neutral - high_multiplier 1.10 -> 1.00, mid_multiplier 1.05 -> 1.00 (trader-ticked 2026-07-22). Evidence: docs/asia-session-volume-reverify-2026-07-22.md - the v34 values were set on a 100 pct SATURDAY sample; the weekday 3-min re-verify (n=344, burst-instance excluded) shows weekday Asia materially calmer (trade-rate 29.4 pct vs 63.1 pct, RANGE_BOUND 10.2 pct vs 27.5 pct), so the above-neutral notch was suppressing trades the calmer weekday book does not warrant (fewer trades than warranted also starves the stats - trader note). Closes the section-12 re-verify row. Hand-tuned calibration pass, never the tweaker (session_volume.sessions[] is off-surface by construction).",
    "v57 (2026-07-22): auto_run.trigger_mode default interval -> on_close (stomp-proofing, trader-approved). The tracked seed predated the v44 on-close switch; any Debug test build copies tracked -> bin (PreserveNewest) and was silently flipping the live collector to interval mode (observed 2026-07-21 22:30 local: a 192-row ~13s-cadence burst inside the NY block - exclude InstanceId 8706ebae from watch reads). on_close has been the operating mode since v44 and stays the de facto; interval mode REMAINS available as an operational UI choice (ad-hoc sub-minute use fine post-calibration - exclude those instances from watch reads via the attribution columns). POCO default synced; A22a fixture made explicit (pins pass-through, not the default). Zero behaviour change on a correctly-configured bin.",
    "v56 (2026-07-22): GEOMETRY ARBITRATION MODES + SIGNED BUFFERS — engine seam extension in SignalEmitter.ComputeSideLevels, ALL DEFAULTS BYTE-IDENTICAL to v51 B4b, ZERO live impact at build (what-if-first instrument for the geometry-shape question §1/§2). Spec: docs/geometry-arbitration-modes-proposal.md (APPROVED 2026-07-21/22, G1-G6 ALL TICKED, BUILD-AUTHORIZED); spec-back: docs/geometry-arbitration-modes-spec-back.md. NOT a dataset boundary — enabling any non-default mode/buffer LIVE is a later, separate ⚠ D-table gated on replay evidence (and, for the stop side, on consumer sizing-by-stop-distance L3). FOUR new keys under scoring.structural_levels, all defaults preserve v51 B4b geometry byte-identical: target_arbitration_mode (int, default 0) — 0 = ladder verbatim (swing→HVN→POC→session-ATR fallback, priority-with-bound); 1 = NEAREST among qualifying candidates + session ATR fallback, closest-first (pre-B4b cap philosophy generalised); stop_arbitration_mode (int, default 0) — 0 = tightest min(structural, stop_max×ATR) with STOP_CLAMPED verbatim; 1 = WIDEST max(structural swing dist, stop_max×ATR), still ≥4-tick floor, UNCLAMPED above (mode-1 live-enable ALSO hard-gated on L3 sizing-by-stop-distance regardless of replay evidence); target_buffer_pct (double, default 0.0) — signed % of placed distance from entry, applied AFTER arbitration (placed' = entry + (placed − entry) × (1 + pct/100)); stop_buffer_pct (double, default 0.0) — same shape on stop side, floor applies to buffered stop. Buffers ≠ 0 add a ' BUF ±N%' reason-string suffix; at defaults (all zero) reasons are byte-identical. Min-move gate (Step 5c) evaluates the BUFFERED target (buffers apply INSIDE the seam before outputs leave). WhatIfOverlay.Whitelist accepts the four dotted paths (scoring.structural_levels.{target_arbitration_mode,stop_arbitration_mode,target_buffer_pct,stop_buffer_pct}); integer modes sweep via the existing numeric machinery (0→1 step 1 works). TWEAKER FENCE — HARD CONSTRAINT 24 (hand-ruled geometry, HC11 class): SettingsDiffApplier exact-match rejects all four keys + PromptBuilder HC24 text. The flat structural_levels numerics (target_max_atr_mult / stop_max_atr_mult / stop_min_floor_ticks) STAY tunable — HC21 unchanged. No rendered-surface change at defaults (mode 0 + buffers 0 ⇒ output identical → four-surface parity untouched by construction). Recommended first study: 36-cell grid {mode T,mode S,tBuf,sBuf} on full book + post-07-08 window separately, EV-in-ATR + split-half. Acceptance (§3): solution + AutoTweaker + WhatIfRunner + OrderCheck build 0/0 Release; harness ALL PASS incl. NEW A36a-f family (defaults byte-identical to A26 case set / NEAREST picks min-distance incl. fallback beating a farther swing / WIDEST stop picks max + respects floor / signed buffers move each side correct direction + min-move gate reads buffered / whitelist accepts the 4 keys, HC24 fence rejects them, a sibling tunable still passes / mode-1 overlay replays through the what-if adapter). verify-gate prepush GATE PASSED (version bump present).",
    "v55 (2026-07-21): EVAL DISPLAY-SEMANTICS PASS (F2/F3/F12) — display/report semantics change, ZERO scoring impact, NOT a dataset boundary (verdict pipeline, thresholds, and stored/wire strings untouched). Spec: docs/eval-display-semantics-proposal.md (APPROVED 2026-07-21, E1-E4 ticked; E3a revised same day to DISPLAY-RENDERING only — the superseded rename inventory is retained as decision-of-record); spec-back: docs/eval-display-semantics-spec-back.md. ONE settings delta: performance_display.min_sample_for_render 4 -> 10 (E2b — the WEAK exclusion in E2a drops the denominator ~2.6x, so the pre-flip floor of 4 was far too low for a rate moving in ~3pp steps; session cells will show --% a bit longer, and mean it). No config keys added or removed. CODE-SIDE (recorded here for the audit trail — none of it changes settings): E1 report render text flips to SUCCESS rates at the MarkdownReportWriter + PromptBuilder render boundaries (§2 matrix, §3 before/after, §4 recommended, §5 decomposition captions, §6 context×outcome, §7 hold-window, §8 pending, summary CSV column name renamed FailureRate->SuccessRate); the internal FailureCellResult.Failures/FailureRate stay failure-oriented (the auto-tweaker's trigger comparison aggregateRatePct < FailureRateThresholdPct is UNCHANGED — internal truth stays failure-oriented, conversion to success happens ONLY at render boundaries). E2a WEAK exclusion applied at DISPLAY time in LivePerformanceTracker.AggregateRange (IsEligibleVerdict stays permissive at storage — the cache keeps WEAK rows, no rotation, reversible); a WEAK line joins the EXISTING _perfTip tooltip ('WEAK excl.: N% (n=M)'). E3a band rendering: the plaintext snapshot AND the card render the middle band as 'MEDIUM LONG' / 'MEDIUM SHORT' (prefix form, not the superseded 'LONG [MEDIUM]' suffix); STRONG/WEAK/NO TRADE unchanged; STORED/WIRE strings stay bare LONG/SHORT everywhere else (CSV Verdict, payload verdict, eval cache, every string-matching site, every fixture stored-string pin — all untouched; the four-surface parity rule is deliberately diverged on the two render surfaces, precedented by the cap-reason rich string vs CSV bucket). F7 rider: session-cell tooltip gains the word 'block' (most-recent Asia/London/NY block). Docs pass: UserManual/TraderGuide verdict tables gain the displayed forms with the display<->stored mapping stated; DeribitIndicatorProject.md §5 gets the same note; UserManual.md §18 retired threshold constants (~line 1861) refreshed to the placed-target migration reality (F11 leftover). Acceptance: 4 Release builds 0/0 (solution, AutoTweaker, WhatIfRunner, OrderCheck); harness ALL PASS (A34a-e added — success-render flip, tweaker trigger unchanged, WEAK-excluded aggregate + tooltip, band prefix on stored LONG, stored-form pins); verify-gate prepush green.",
    "v54 (2026-07-17): #6 BOOK ABSORPTION AT STRUCTURAL LEVELS — BUILD SUB-VERSION ONLY (scoring_enabled:false; display/CSV, ZERO scoring impact — NOT a dataset boundary for classification; the CSV gains VALUES in 5 previously-reserved columns, no header rotation). AUTHORIZATION: proposal APPROVED 2026-07-03 (D1-D8 all ticked incl. the D8 pull-fraction spoof guard); build green-lit by the joint v52+v53 watch PASS (commit c8ed6c6, 2026-07-16). Spec: docs/book-absorption-proposal.md; brief: docs/book-absorption-implementer-brief.md; spec-back: docs/book-absorption-spec-back.md. NEW indicators.absorption block {enabled:true, scoring_enabled:false, proximity_ticks:12, band_ticks:4, window_sec:10, break_tol_ticks:2, absorb_ratio:3.0, depletion_floor_usd:25000, max_pull_frac:0.5, penalty:1 (inert at the build), default:{min_aggr_usd:150000}, sessions:{NY:{},LONDON:{},ASIA:{}}} — ALL anchors PROVISIONAL until the §5 target-engagement calibration (penalty-class band 3-8% of directional runs; min_aggr_usd expected to split per-session). MECHANISM: new host-agnostic Core/LevelAbsorptionTracker.vb — the first DUAL-FED tracker: owned by MarketState under its ONE SyncLock, folded from BOTH the ~100ms depth-limited book snapshots (FoldBook) AND the trades stream (FoldTrade), the fold sites mirroring FoldOfi / FoldAggressorVelocity exactly; reset in DeribitWsFeed.SeedAsync on every (re)connect. Watched levels = the nearest CARRIED structural level above+below (the TAPE strip's candidate set: LastSwingHigh/Low5m + VPFRNearestHvnAbove/Below, refreshed per full run via MarketState.SetAbsorptionLevels at the _lastSuccessfulIndicators capture site); per side an EPISODE runs from proximity-entry (touch within proximity_ticks, level inside the visible ladder — min(proximity,visible) by construction) until leave-proximity / level re-map (no cross-level bleed) / break-through (trade or opposing touch beyond level±break_tol ⇒ state cleared INSTANTLY; re-arm requires leaving proximity). absorbRatio = rolling window_sec pressing USD / max(sizeStart−sizeMin, depletion_floor_usd). D8 SPOOF GUARD: per snapshot interval the band obeys ΔSize = Posts − Pulls − Fills with ΔSize (snapshots) + Fills (trades at band prices) observed ⇒ pullLB += max(0,−(ΔSize+Fills)) / postLB += max(0,ΔSize+Fills), masked to the band portion visible in BOTH consecutive top-10 snapshots (a shifting ladder window cannot fake deltas); pullFrac = pullLB/max(postLB,floor) > max_pull_frac ⇒ painted defense ⇒ NONE veto. Pure IndicatorEngine.ClassifyAbsorption (ACTIVE + no-progress-by-construction + aggrUsd ≥ session-resolved min_aggr_usd + absorbRatio ≥ absorb_ratio, D8 veto, both-sides tie → higher ratio) → ABSORB_ABOVE (resistance defended, adverse to longs) / ABSORB_BELOW / NONE (modal by construction — proximity-gated, no non-directional payout). Per-session min_aggr_usd via NEW ExecutionResolution.ResolveAbsorptionMinAggrUsd (v40 nullable-override pattern). READ PATH: RunAnalysisAsync WS-live only (the #5 gate shape: enabled AND src Is _wsSource) → NEW IndicatorResults.AbsorptionSignal/Level/Ratio/AggrUsd/PullFrac; REST/fallback/cold/no-episode ⇒ NONE + null numerics (never blocks, never guesses); numerics populate for ANY active episode — pullFrac logs on vetoed episodes too (the W4 fidelity-binds evidence). CSV: the 5 columns RESERVED at the v0.8 rotation now populate — rotation-free, header unchanged, no .bak. DISPLAY: TAPE-strip 'ABS↑ 60510 (3.4×)' tag while a state is active (D6) — strip-only surface, NO card/snapshot line (the #3/#5 precedent under the display-string parity rule). TWEAKER: three-tier surface (HARD CONSTRAINT 23) — flat params ON (proximity_ticks, band_ticks, window_sec, break_tol_ticks, absorb_ratio, depletion_floor_usd, max_pull_frac, penalty); default./sessions. prefixes OFF (hand-tuned §5 re-baseline tier, HC11 class); enabled + scoring_enabled exact-match OFF (scoring_enabled is the TWICE-evidence-gated ⚠ activation: §5.1 independence vs OFI/burst/TFI AND §5.2 ≥10pp adverse outcome gradient on n≥30 flagged evaluated rows — no gradient ⇒ stays display-only). REVERSIBILITY: enabled:false stops folds+reads hot, byte-identical to pre-build (harness-proven at the tracker/CSV surface); scoring_enabled stays false so there is nothing to unwire. ACTIVATION (later, its own ⚠ boundary): §5 gates + target-engagement table + penalty wire-in (Step 2 entry-quality class, penalty-only against the pressing side, NO fade bonus — D2) + breakdown row w/ snapshot+card parity + §12 watch row. Acceptance: 3 Release builds 0/0; A1-A30d unregressed + NEW A31a-g (episode lifecycle open/accumulate/close-on-level-change, absorbRatio vs an analytic case, D8 conservation bounds + pullFrac veto + sitting-defender pass, break-through instant-NONE + leave-proximity re-arm, reset/cold/degenerate never-throw + reserved-column population empty⇒values no rotation, session min_aggr_usd resolution, HC23 fences); verify-gate prepush green. Local commit — trader tests + pushes; coordinator review follows.",
    "scoring boundary (2026-07-15): FUNDING MOMENTUM TIME-ANCHORED WINDOW (v52->v53) — ⚠ scoring-behaviour change (Step 3b's INPUT STATE becomes cadence-independent; Step 3b's own mechanics are untouched — same crowding gate, same ±amplify/soften). DATASET NOTE: BUNDLED AT THE v52 BOUNDARY rather than taking its own, on trader sign-off 2026-07-15 (rule-1 bundling precedent). The v52 aggressor-velocity wire-in went live the same evening and only a negligible number of rows sit between the two builds, so v52 and v53 share ONE dataset boundary: rows after this build carry BOTH the burst-modified TFI vote AND the anchored funding-momentum state. This supersedes the proposal §5 D4 tick (own boundary post-#5-gate), which was written before the two builds collapsed onto the same evening. AUTHORIZATION: proposal APPROVED 2026-07-07, D1-D5 all ticked as recommended; values final (W=5min, T=2e-7). Spec: docs/funding-momentum-time-anchored-window-proposal.md; trigger: docs/signal-health-retune-proposal.md §5 watch finding; brief: docs/funding-window-implementer-brief.md; spec-back: docs/funding-window-spec-back.md. WHY: the momentum window was 3 funding CHANGES, and on the WS feed funding changes nearly every run — so the window's wall-clock span was ~3x the run cadence. At the collector's on_close cadence that broke the adjunct invariant by arithmetic: 60s NY runs -> ~3min span -> FLAT 52.1% / 3b-engaged 27.6% (vs the retune §5 bands 60-70% / 15-25%), and 180s Asia/London runs -> ~9min span, where a SINGLE 3-min step (p50 6.5e-7) already exceeds the whole-window threshold — Step 3b moved scores on 95.8% of London rows and res-3 FundingMomentum was uninformative. No per-cadence threshold fixes this (backstop timer, gaps and session hand-offs move the effective cadence WITHIN a session); the window had to be anchored in time. MECHANISM (Core/Indicators_OrderFlow.vb): the count-indexed ring becomes a TIMESTAMPED ring — appended every completed run (the [S9] value-change dedup is RETIRED with the count window it protected; age-anchoring makes repeat samples harmless and informative), age-evicted at 30 min (the audit's segment-reset horizon), no count cap (the old FundingHistoryMax=10 would starve the anchor at a 30s cadence). CalcFundingMomentum(history, nowUtcMs, cfg) classifies delta = current - the NEWEST sample aged >= momentum_window_minutes (newest, not oldest-in-window — the oldest would re-import cadence dependence via the ring span); no anchor yet (cold start / post-gap) => FLAT, the same warm-up posture as before. Semantics: 'funding moved more than T per >= W minutes', identical at EVERY cadence. TWO settings deltas: indicators.funding.momentum_window (count, 3) REMOVED — the key + POCO field are gone, which makes it applier-unresolvable so C-6 rejects it with no fence needed (deliberately NOT added to RejectedPathFragments — the v47-F1 snapshot-poisoning lesson; the fragment list is substring-matched and 'momentum_window' would also swallow the new key); NEW indicators.funding.momentum_window_minutes = 5 (D2 — >= 1 full bar at both execution resolutions, >= 2 samples at every cadence the engine has run, front edge of the 2-15min hold horizon; empirically 5-min anchored deltas run SMALLER than the old ~90s count-window deltas, p50 3.0e-8 vs 8.0e-8, because the funding premium oscillates at short horizons and partially cancels over 5 min — so the anchor reads sustained drift rather than wiggle). momentum_threshold STAYS 2e-7 (D5): re-derived at W=5min on a pooled 9-day / 2-regime fit (proposal §4, n=1,994 anchored windows — the exact v0.8 book + the reconstructed .bak WS-era book) landing FLAT 68.7% / 3b-engaged 17.1%, mid-band on both. Same NUMBER v50 R2 shipped, different MEANING — coincidence of scale, not of meaning. TWEAKER: momentum_window_minutes is ON the surface (the OFI.avg_window_sec precedent — it shapes the signal), with the window<->threshold coupling caveat recorded (T is fit GIVEN W; re-fit T if W moves); momentum_threshold/amplify/soften unchanged; momentum_enabled status unchanged. SURFACES: the FUNDING momentum row renders the same three states in the same format — no line added/removed/renamed, so no snapshot/card/payload change (display-string parity rule satisfied; the state VALUES will differ on 3-min sessions, which is the entire point). CSV: FundingMomentum column semantics shift (cadence-independent states) — documented, NO header rotation (v31/v36 rule). FundingDelta (D3) becomes the per-RUN step (0 on unchanged runs) now that the dedup is gone — column KEPT, semantics documented; the audit's funding-path reconstruction survives (it segments on resets and sums steps). FIXTURES: A29a-e (verify/ordercheck) — anchored RISING/FALLING/FLAT, newest->=W anchor selection (not oldest), 30-min eviction, cold-start + post-gap FLAT, and the cadence-invariance pin (the same funding path sampled at 30s vs 180s yields the SAME states at the same wall-clock instants — the fixture that pins the whole point). POST-SHIP WATCH: re-run the retune §5 check PER-RESOLUTION — FLAT 60-70% + Step-3b engagement 15-25%, and BOTH res-1 AND res-3 must sit in-band; that dual-resolution pass is the success criterion the count window could never meet. Expect engagement above band on hot-funding weeks and below on quiet ones (10-34% across the two fit segments) — by design; once the window is time-anchored, above-band engagement during a genuine crowding build is honest signal, not the cadence artifact R2 chased.",
    "scoring boundary (2026-07-14): #5 AGGRESSOR-VELOCITY TFI-MODIFIER SCORING WIRE-IN (v51->v52) — its own ⚠ DATASET BOUNDARY (rows after this build carry the burst-modified TFI vote; the funding time-anchored window build queues AFTER it, handover Q3/D4 — one ⚠ at a time). AUTHORIZATION: the §5.1 correlation gate CLEARED (docs/aggressor-velocity-correlation-gate-verdict-2026-07-13.md — NOT redundant, ρ(Net,TFI)=0.61 < 0.7 on every session-day) + the §5.2 per-session derivation (docs/aggressor-velocity-s52-derivation-2026-07-13.md, S1-S5 ticked 2026-07-14). Spec: docs/aggressor-velocity-proposal.md §4.5 (modifier mechanics, confirmed §10.1); brief: docs/aggr-vel-wirein-implementer-brief.md; spec-back: docs/aggr-vel-wirein-spec-back.md. TWO settings deltas: indicators.aggressor_velocity.scoring_enabled false->TRUE (arms the modifier) + indicators.aggressor_velocity.sessions.NY.burst_ratio_threshold 4.5 (S1 — the p90 knee, ~9.9% NY fire rate, same-side 89.7%; the shared default 2.5 STAYS for res-3 display/collection, S2a). MECHANISM (ScoringEngine_Calculate_Scoring.vb, proposal §4.5 — a MODIFIER on TFI's existing Step-2 vote, appears exactly ONCE, never a parallel vote, so it cannot double-count): TFI directional (BUY/SELL PRESSURE) + AggrVelSignal BURST_* SAME side -> +upgrade_bonus (1) on that side capped at regimeMax (the tick-resolution entry-impulse confirmation the trader's break rule wants); BURST_* CONTRA the TFI lean -> -contra_penalty (1) soften (the genuine-warning half, MicroCVD-stall shape); NORMAL -> no-op (a fired TFI is still valid, just not burst-confirmed — no penalty). S2(a) SCOPING: the modifier fires ONLY when the run's session carries an EXPLICIT sessions[s].burst_ratio_threshold (NY today) via ExecutionResolution.HasExplicitAggrVelBurstThreshold(cfg, r.SessionUtcHour); res-3 (Asia/London) inherit the exploratory default -> classifier + TAPE display + CSV keep running everywhere, but the SCORING modifier is inert there until their own §5.2 pass (~150 fires/session away). scoring_enabled:false => byte-identical current behaviour (harness-proven — the hot rollback). DISPLAY PARITY: a burst suffix ('| BURST_BUY +1[L] confirmed' / '| BURST_SELL -1[L] contra') appends to the EXISTING TFI signal-breakdown note row (OFI 'MOM:' precedent) — no line added/removed/renamed, and the card binds the same SignalBreakdownItem.Note, so no card-surface change (parity rule satisfied by construction). S5 RIDER: session_volume.enabled — the unfenced feature switch found in the 07-13 A15g re-check — now exact-match fenced off the tweaker surface (SettingsDiffApplier + PromptBuilder HARD CONSTRAINT 22, HC16 class; the array-nested session_volume.sessions[] keys stay UNRESOLVED-rejected, unaffected). POCO DEFAULTS RIDE THE COMMIT (v50/v51 precedent): AggressorVelocitySettings.ScoringEnabled False->True + the NY session-override .BurstRatioThreshold=4.5 (absent settings.json now seeds the shipped behaviour). POST-SHIP WATCH (§12): NY burst fire rate 8-12% band + same-side >=85% over the first 2 weekday sessions; TFI-modifier engagement ~5-10% of NY directional votes (~35 upgrades + ~2 softens/NY session-day, re-weighting ~7% of TFI's directional votes; net Microstructure fire count unchanged by construction — it's a modifier, not a vote). Acceptance: solution(Release)+AutoTweaker+OrderCheck builds 0/0; A1-A26g unregressed + new A28a-d (upgrade/soften/no-op through the real Calculate(); regimeMax cap holds; S2a res-3 inert + scoring_enabled:false byte-identical; HC22 fence rejects session_volume.enabled, accepts a sibling tunable) + A15g refreshed to the HC22 reality; verify-gate prepush green. DISPLAY RIDER (folded in, card-only, zero engine/config impact): ATR-levels card (UI/MainForm_Render_Cards.vb) CAPPED->PLACED caption (now PLACED on the structural-first default path, CAPPED only on the enabled:false legacy rollback — a parity fix, the snapshot already emitted PLACED @ via AppendPlacedAtrRow) + STOP_CLAMPED cell-wrap fix (the placed-stop source label moves onto its own full-width small-font sub-line StopReasonSub so a 12-char label can't wrap inside the narrow STRUCT|STOP sub-cell). Local commit — trader tests + pushes; coordinator review follows.",
    "eval-semantics boundary (2026-07-14): D6 EVAL-BARRIER MIGRATION ONTO PLACED LEVELS — NO settings-version bump (this is a code-only, eval/measurement-layer change; zero scoring impact, the verdict pipeline is untouched, no config keys added/changed, so version stays 51). It resolves the v51 'EVAL STAYS ATR-YARDSTICK (D6)' documented divergence below: both eval surfaces scored the adverse barrier against the raw 5m swing stop (median ~9×ATR away, unreachable intrabar on 93% of directional rows) while the engine places + the autotrader executes a stop clamped to 1.6×ATR, so 'failure' had collapsed to window-expiry. NOW the adverse barrier IS the placed stop (SignalEmitter.ComputeSideLevels for live rows via LivePerformanceTracker.BuildLiveEntry; the logged CSV Placed* columns for backfill + offline). SURFACES: (1) live perf strip — BuildEntry sources FavBar/AdvBar from the placed levels, the stale FAV_ATR_MULT/ADV_ATR_MULT fallback consts deleted; (2) eval cache schema v4→v5 — every stored outcome re-bases, so a pre-v5 cache ROTATES to .v4.bak and rebuilds via the cold-start backfill (perf-strip history resets; the raw analysis_log.csv + its .bak history are untouched); (3) offline FailureRateMatrix/AnalysisRunner — adverse = placed stop when the row carries Placed*, else the legacy swing-else-ATR yardstick, with rows lacking Placed* split into a labelled LEGACY_YARDSTICK population (no silent mixing); the per-tier favourable ATR grid is unchanged (out of scope); (4) D4 before/after report section — the SAME rows re-walked under both barrier bases per session×resolution×tier (the continuity bridge, first honest read of executed stop-out risk). AUTO-TWEAKER: no code change — its FailureRateMatrix.Compute call picks up the placed adverse via the default AdverseBarrierMode.Placed, so its NY×1 first-fire gate readings re-base; read them against post-migration rates, not mixed ones. EXPECTED EFFECT: failure rates RISE materially (the 1.6×ATR stop now binds); [B]/[T] perf-strip modes diverge again. Harness A27a-d + A1-A26g unregressed; verify-gate prepush green.",
    "v51 (2026-07-06): B4b PLACED-GEOMETRY STRUCTURAL-FIRST LEVELS — its own ⚠ DATASET BOUNDARY, pre-agreed to land WITHOUT waiting for the #5 correlation gate (proposal D7 + roadmap 3b: the #5 gate measures signal-level quantities [burst vs TFI], which are geometry-invariant; both collection windows continue, rows after this build carry structural-first Placed*/levels). Spec: docs/placed-geometry-structural-first-proposal.md (APPROVED D1-D8, D3=clamp, 2026-07-03); values: docs/placed-geometry-derivation-2026-07-06.md §4 (DG1-DG5 ticked 2026-07-06); spec-back: docs/placed-geometry-spec-back.md. MOTIVE: the O2 bridge made the emitted levels EXECUTED values — the ATR-fallback target (2.0x) reached only ~43% (window-expiry-dominated) and the placed stop was pure ATR (1.2x), the profile's explicitly REJECTED pattern for execution ('swing structure defines natural invalidation'); autotrading them mechanically reintroduced it. MECHANISM (ONE seam — SignalEmitter.ComputeSideLevels, the shared arbitration payload levels + CSV Placed* + snapshot + card ALL read; Step 5b delegates to it and copies the outputs, the v35 min-move gate evaluates the PLACED target): TARGET ladder per side = swing -> nearest HVN -> POC (HVN-gated, as today) -> ATR fallback; first structural tier with 0 < dist <= target_max_atr_mult x ATR places (structure now wins even when FARTHER than the ATR level, up to the bound — the key behavioral delta); labels SWING_HIGH_5M/SWING_LOW_5M/NEAREST_HVN_ABOVE/NEAREST_HVN_BELOW/POC/FALLBACK_ATR, reason string 'PLACED @ p (LABEL)' (was 'CAPPED @' — vocabulary extension, contract-safe pinned-informational). STOP (DG1 amends D2 for v1): placed = min(structural swing stop, stop_max_atr_mult x ATR) — structure places only when TIGHTER and >= stop_min_floor_ticks (F1: 5m swing stops run p50 4-9x ATR = inoperative at v1 fixed sizing; true wide structural stops defer to the sizing-by-stop-distance era, derivation §6b); labels SWING_STOP (structural placed) / STOP_CLAMPED (structural exists, bound binds) / FALLBACK_ATR (no structural stop or sub-floor). VALUES: scoring.atr_target_multiplier 2.0 -> 1.75 (NY reach 56.8% = the DG3 55-60% design point) + scoring.atr_stop_multiplier 1.2 -> 1.6 (survives ~80-85% of winners pooled; the old 1.2 stopped ~30% of eventual LONDON winners — DG2) — SAME KEYS re-purposed as the fallback multipliers (no duplication). NEW scoring.structural_levels block {enabled:true, target_max_atr_mult:3.5 (DG4 confirmed — in-bound structural reach 66.7% pooled), stop_max_atr_mult:1.6 (deliberately = the fallback stop, DG1), stop_min_floor_ticks:4 (tick $0.5), stop_too_loose_mode:'clamp' (D3; 'skip' = the D3-b alternative, NOT built — unrecognised values behave as clamp, flagged in the spec-back), sessions:{NY:{}, LONDON:{fallback_target_atr_mult:2.0}, ASIA:{fallback_target_atr_mult:1.25}} (DG3 hand-tuned tier, v40 nullable-override pattern; session resolved from the NEW IndicatorResults.SessionUtcHour stamped once per run in RunAnalysisAsync — all four surfaces session-consistent by construction)}. enabled:false = BYTE-IDENTICAL v50 geometry (the rollback; harness-proven A26g + the legacy-pinned A12/A24-style fixtures). DISPLAY PARITY (FOUR surfaces, one commit): ATR ENTRY LEVELS rows gain the placed-stop source label + the FALLBACK_ATR target label; structural-placed targets render 'raw --> placed [PLACED @ p (LABEL)]'; fallback rows show the session-resolved multiplier + true placed R:R (snapshot BuildPlaintextSnapshot + card BindCardAtrLevels/BindAtrRow in the same commit — hard rule); payload cap_reason vocabulary grows (schema UNTOUCHED, prices are prices; A22 re-pinned); CSV Placed* columns change VALUES not names (v31/v36 semantics-shift precedent — PlacedTargetLong/StopLong/TargetShort/StopShort now carry structural-first levels; TargetCapReason categorical buckets swing/hvn/poc/none UNCHANGED, fallback rows project 'none'). EXPECTED DISPLAY/GATE SHIFTS (derivation §4): fallback R:R reads ~1:1.1 (was 1:1.7; Kelly advisory b follows — display-only), BELOW_MIN_MOVE +4-6pp on NY, ~0 elsewhere; structural targets are NEVER under the floor (gate-safe, §2). EVAL STAYS ATR-YARDSTICK (D6): LivePerformanceTracker/AnalysisConstants/FailureRateMatrix deliberately UNTOUCHED — the eval cache's favourable barrier follows v.Adjusted* (now the structural-placed target when one places) while its 2.0x fallback const and the offline mirrors keep the old anchors; bounded, documented divergence — the eval migration onto the logged Placed* columns is the D6 follow-up analysis pass (F4). TWEAKER: three-tier surface (HARD CONSTRAINT 21) — flat numerics ON (target_max_atr_mult/stop_max_atr_mult/stop_min_floor_ticks; the re-purposed atr_*_multiplier keys stay ON as before); enabled + stop_too_loose_mode exact-match OFF; sessions. prefix OFF (HC11 class). POCO defaults ride this commit (v33/v34 precedent): atr mults 1.75/1.6, the structural_levels block, + the outstanding v37 hygiene one-liner AtrSettings.StaticRef 115.0 -> 38.0. SEQUENCING: ships BEFORE autotrade live-at-minimum-size (the unlock — proposal §8 hard rule); the bridge log-only soak was never gated on this. POST-SHIP §12 WATCH: realized structural-target reach-rate + STOP_CLAMPED frequency + BELOW_MIN_MOVE rate over the first weekday sessions + the LONDON structural-target watch (F3: LONDON in-bound structural reach 33% INVERTED vs fallback 61% on n=227 — trigger: still <45% after >=3 more London session-days -> London session override or bound tightening). Acceptance: 3 Release builds 0/0; harness A1-A25b unregressed (A12 re-anchored as the legacy-pin rollback proof, A13 refloored to the 1.75 fallback, A22a/A24a re-pinned on the new arbitration) + new A26a-h (ladder tier selection incl. farther-than-ATR structural wins, too-loose -> next tier -> fallback, stop SWING/CLAMPED/floored/fallback, min-move gate reads PLACED values both directions, session fallback resolution, enabled:false byte-identical, HC21 fences); verify-gate prepush green. Local commit stack — trader tests + pushes; coordinator review follows.",
    "v50 (2026-07-03): #5 AGGRESSOR-VELOCITY BUILD + APPROVED RETUNE BUNDLE — the one-boundary commit event (roadmap §5 item 3 manifest; retune §4 sequencing D6; small sequential commit stack, v31 precedent). FOUR parts. (1) #5 BUILD (docs/aggressor-velocity-proposal.md, §10 all RESOLVED 2026-07-01; BUILD SUB-VERSION ONLY — scoring_enabled:false, NO TFI-modifier wire-in; that is a later data-gated sub-version behind the §5.1 correlation gate vs TFI on post-build data): new indicators.aggressor_velocity block {enabled:true, scoring_enabled:false, fast_window_sec:5, direction_lean_floor:0.2, gross_floor_usd_per_sec:50, upgrade_bonus:1, contra_penalty:1, default:{norm_window_sec:120, burst_ratio_threshold:2.5}, sessions:{NY:{norm_window_sec:60}, LONDON:{}, ASIA:{}}}. MECHANISM: new host-agnostic Core/AggressorVelocityAccumulator.vb — TWO time-decayed taker-USD horizons (fast burst tau=fast_window_sec vs per-session rolling-norm tau; A*=r*tau so rate=A/tau, O(1)/trade, trade's own exchange stamp = dt basis) folded per streamed trade in DeribitWsFeed.ApplyTrades (trade analogue of the OFI book fold), owned by MarketState under its lock, reset in SeedAsync on every (re)connect; burstRatio = grossFast/max(grossNorm, gross_floor), lean = netFast/gross in [-1,+1]; pure IndicatorEngine.ClassifyAggressorBurst (burstRatio>=threshold AND |lean|>=floor -> BURST_BUY/SELL else NORMAL — a balanced firehose and a one-sided trickle are both NORMAL, the non-directional-reward guard); per-session norm/threshold resolution via ExecutionResolution.ResolveAggrVelNormWindow/BurstThreshold (v40 nullable-override pattern). WARMUP: until a full norm window of fold coverage the run emits NORMAL + null numerics (never divides a half-filled baseline); REST/fallback runs likewise (WS-only signal). DISPLAY: the LIVE TAPE strip's tape-speed field is enriched to 'N tr/s ($X/s) R.Rx [BURST^/BURSTv]' — strip-only surface, no card/snapshot line (the #3 precedent under the display-string parity rule). TWEAKER: three-tier surface — flat params ON (fast_window_sec, direction_lean_floor, gross_floor_usd_per_sec, upgrade_bonus, contra_penalty); default./sessions. prefixes OFF (hand-tuned §5.2 re-baseline tier, HC11 class); enabled+scoring_enabled exact-match OFF (PromptBuilder HARD CONSTRAINT 19). (2) CSV v0.7->v0.8 ROTATION — 16 columns appended in ONE rotation: AggrVelBurstRatio/AggrVelNet/AggrVelSignal (native; numerics EMPTY when unavailable), TFIValue/TFISignal (retune C1 — closes the F11 audit blindness), AbsorptionSignal/Level/Ratio/AggrUsd/PullFrac (RESERVED empty per book-absorption D4+D8 — #6 lands rotation-free), PlacedTargetLong/PlacedStopLong/PlacedTargetShort/PlacedStopShort (placed-geometry D5 — populated from day one with the CURRENT effective levels via the NEW shared SignalEmitter.ComputeSideLevels, the SAME arbitration the bridge payload levels emit, so CSV ≡ payload by construction), InstanceId/SignalId (attribution — ProcessIdentity; id ticks BEFORE LogRun so CSV SignalId ≡ payload signal_id; SKIPPED runs burn ids with no row, join total CSV->payload). Old book rotates to analysis_log.csv.v0.7.bak — KEPT, the v48 section-4a fire-rate watch reads it. Same pass, retune C2: FundingRate logging F6->F8 (same column, no header change). (3) RETUNE R1 (docs/signal-health-retune-proposal.md §2, D1-D6 ticked 2026-07-03): indicators.OFI.momentum_enabled true->false — the OFI momentum SCORING modifier is RETIRED (active ~90% of runs in every era = no discrimination; unfixable by threshold — fitted 0.136 ~= current 0.15; conditional outcomes adverse). KEPT: CalcOFIMomentum, r.OFIMomentum, the CSV column, the _ofiHistory ring, and the MOM:state note segment (free diagnostics) — only the modifier suffix stops appearing (exactly as on FLAT/BALANCED rows today; no line added/removed/renamed, snapshot+card render the same composed Note). Hygiene rider D5: 'indicators.OFI.momentum_' prefix fenced off the tweaker (SettingsDiffApplier prefix + PromptBuilder HARD CONSTRAINT 20 — inert keys would recreate the recorded-APPLIED-no-op class v47 F1 closed; book_depth/buy+sell_dominant_ratio/averaging_enabled/avg_window_sec stay on their current surface). Reversal = flip the flag + drop the fence in one commit. (4) RETUNE R2: indicators.funding.momentum_threshold 5e-8 -> 2e-7 — WS funding_8h changes on 96.5% of runs, so the v34 threshold had Step 3b moving scores on 36.8% of runs vs the REST-era ~16% adjunct profile (invariant violated by feed drift, not design); 2e-7 restores FLAT-modal ~67.6% / 3b engagement ~18.8%. Window/amplify/soften unchanged; direction stays informative (retune, not retire). POST-SHIP WATCH (retune §5): FundingMomentum FLAT% 60-70% + 3b engagement 15-25% over the first 2 weekday sessions; OFI row renders MOM:state with no modifier suffix. POCO defaults ride this code commit (v33/v34 precedent): OFI.MomentumEnabled=false, Funding.MomentumThreshold=2e-7, + the v48-promised dominance-default sync (1.60/0.625). DATASET BOUNDARY: one boundary, one reset — rows after this build classify with R1/R2 active and carry the 16 new columns; #5's own multi-session collection (the §5.1 correlation-gate data) begins here. Acceptance: 3 Release builds 0/0 (solution + AutoTweaker + OrderCheck); harness A1-A22g unregressed + new A23a-g (accumulator rate math vs analytic fixed point, two-horizon burst detection, cold-start suppression, reset re-arm, classifier edges, per-session resolution, HC19 surface) + A24a (Placed* ≡ payload levels across the three cap cases) + A25a/b (R1 retire byte-identical to the no-modifier path through the real Calculate(), flag still gates, HC20 fence); verify-gate prepush green. Spec-back: docs/aggressor-velocity-spec-back.md. Local commit stack — trader tests + pushes; coordinator review follows.",
    "v49 (2026-07-03): SIGNAL BRIDGE v1 EMITTER — verdict_signal.json to DeribitOrderPlacementApp (docs/signal-bridge-v1-proposal.md, schema v1 FROZEN 2026-07-03 D1-D10; roadmap O2/W3; canonical consumer copy DeribitOrderPlacementApp/docs/integration-contract-verdictengine.md, proposal section 3 = the verbatim emission/parity mirror). ONE new top-level block signal_bridge {enabled:false, output_path:'C:\\Dev\\DeribitBridge\\verdict_signal.json'}. DEFAULT OFF (D3) — the enabled flip is the trader's dated action after the consumer's log-only validation. TRANSPORT PLUMBING, zero scoring/CSV/render impact: after EVERY completed RunAnalysisAsync the engine atomically writes (tmp + File.Replace, create-dir-if-missing, never-throws) one JSON file — full payload on success (values sourced from the SAME VerdictResult/IndicatorResults fields the snapshot renders, incl. the v30 sub-tick cap-noise suppression on target_capped; written AFTER snapshot + card binds so Kelly is populated = the THIRD parity surface), reduced payload on skip (signal_state SKIPPED = stand down, never 'hold last signal'), nothing on mid-run exceptions (silence = dead; the consumer's max-age gate covers it). direction NONE on ALL 'NO TRADE*' verdicts (D2 — leans stay in verdict for logging); WEAK carries direction + LOW (tier gate refuses consumer-side). Enum pins: signal_state OK|SKIPPED; direction LONG|SHORT|NONE; confidence HIGH|MEDIUM|LOW|N/A; health.ws OK|DEGRADED|DOWN|REST (D8 — REST = deliberately on transport=rest, only DOWN blocks); trigger_mode interval|on_close. CODE: new host-agnostic Core/SignalEmitter.vb (pure Build/BuildSkipped + atomic TryWrite) + Core/ProcessIdentity.vb (the shared process-identity primitive: instance_id GUID per process start + signal_id ticked once per completed run INCLUDING skips, readable with the bridge disabled — consumed by the emitter now and by the CSV InstanceId/SignalId attribution columns at the #5 v0.8 rotation; CSV SignalId will equal payload signal_id per run by construction, the id ticks BEFORE LogRun); WinForms glue = UI/MainForm_SignalBridge.vb (two call sites in RunAnalysisAsync) + the ARM AUTOTRADE checkbox (engine half of the dual-arm interlock D7: visible when the bridge is enabled, RUNTIME-ONLY, default OFF every start, deliberately NOT persisted — restart = disarmed; state rides every payload as engine.autotrade_armed, emission UNCONDITIONAL on arming). OFF the auto-tweaker surface: 'signal_bridge.' added to SettingsDiffApplier.RejectedPathPrefixes + PromptBuilder HARD CONSTRAINT 18 (transport plumbing, network.* class). v2 (position-state + disposition feedback file) is out of scope, gated on 1-2 supervised weeks of v1. Acceptance: 3 Release builds 0/0; A1-A21b unregressed + new A22a-g (field-by-field serialisation incl. capped/uncapped/cap-noise-suppressed targets, SKIPPED shape, NO TRADE* lean fixtures => direction NONE, enum pins, armed flag + instance_id stability + signal_id monotonicity, invariant-culture serialization under de-DE, tweaker rejects signal_bridge.* / accepts sibling); live smoke = trader enables on the bin copy, confirms per-run file updates (OK + forced-skip), values eyeball-match the card, ARM toggle flips autotrade_armed in the next payload. Heads-up carried in the ack: the consumer must not hard-pin engine.settings_version (this bump makes it 49; the #5-boundary retune moves it again).",
    "v48 (2026-07-03): OFI DOMINANCE RE-BASELINE on the geometric distribution — buy_dominant_ratio 2.0 -> 1.60, sell_dominant_ratio 0.5 -> 0.625 (exact reciprocal pair; raw pooled fit b=1.593). Spec docs/v48-ofi-dominance-rebaseline-proposal.md (APPROVED 2026-07-03, D1-D4 as recommended + the §4a divergence watch); spec-back docs/v48-ofi-dominance-rebaseline-spec-back.md. METHOD: firing-rate-match (v40/v41 style) — reference = the snapshot-on-WS book 2026-06-24 -> 2026-06-30 13:00 UTC (pre-arith-averaging deploy; n=1067; BUY 35.0% / SELL 28.2% / DOM 63.2% at 2.0/0.5); geometric sample = 991 rows since the geometric build went live 2026-07-01 07:02 UTC (NYx1 722 incl. a near-full 07-01 NY session hrs 13-21, LONDON 200, ASIA 69). The stale pair UNDER-FIRED on geometric (DOM 49.0%, as the v46 entry predicted); the fitted reciprocal pair restores DOM 63.2% (BUY 38.5% / SELL 24.6% — per-side split more buy-lean than the reference 1.24:1 -> 1.56:1; ACCEPTED per D2 as the genuine resting bid-lean of the book, geometric ln-median +0.15, not forced symmetric). PER-SESSION (D3, numbers brought to the trader): NY fit -5.5% vs pooled (fine); LONDON +11.7% (marginally over the 10% line but within n=200 percentile-fit sampling error -> GLOBAL pair shipped); ASIA unfittable on both sides (geo n=69 + ZERO reference-period Asia rows). LONDON + ASIA are named in the section-12 'v48 OFI per-session fire-rate watch': trigger = combined dominance rate outside 0.6x-1.5x of the fitted 63.2% target across 2 consecutive weekday sessions; response ladder = verdict-impact check -> pooled retune -> per-session bucket overrides (last resort, hand-tuned/off-tweaker by construction). MOMENTUM (D4): momentum_threshold 0.15 UNCHANGED — the modifier's active rate is ~90% in BOTH eras (reference 90.6% / geometric 89.2%; fitted threshold 0.136 ~= current), so the averaging construction did not move it; the ~90% ALWAYS-ACTIVE rate itself is handed to the roadmap W1 signal-health audit as the retire-or-keep question (needs conditional-outcome evidence, not tonight's fit). DATASET BOUNDARY: rows after this flip classify OFISignal at 1.60/0.625; OFIRatio VALUES are unchanged (construction stable since v46-geometric) so the ratio column remains comparable across the boundary. POCO defaults deliberately untouched (ride the next code commit — v33/v34/v37/v41 precedent). Settings-only; no CSV/eval-cache/render change (CSV v0.7, eval cache v4). Both dominance keys REMAIN on the tweaker surface. Acceptance: 3 Release builds 0/0; harness A1-A21b unregressed (fixtures pass explicit ratios -> byte-identical).",
    "v47 (2026-07-02): AUDIT FIXES F1+F2+D4 — settings hygiene only, ZERO behaviour change (docs/audit-fixes-2026-07-02-proposal.md, APPROVED 2026-07-02; evidence docs/fable5-audit-2026-07-02.md). F1: REMOVED dead key regime_gates.transitional_adx_penalty_low (20.0) from both settings.json copies + the POCO (EngineSettings.RegimeGateSettings) — orphaned by the v31 F8 fix (the first TRANSITIONAL penalty arm covers [0, penalty_mid); only Mid/High are read). Closes the last found recorded-APPLIED-no-op tweaker path: the key resolved in JSON and sat on no reject list, so a tweaker proposal against it would validate → apply → bump → be recorded APPLIED while changing nothing (the C-6 class); post-removal the path is unresolvable and C-6 rejects it. Deliberately NOT added to RejectedPathFragments — every settings_snapshots/ snapshot since v31 contains the key, and ValidateSnapshotContent checks fragments on wholesale reverts, so a fragment entry would poison all existing snapshots; pre-removal snapshots remain revertable and would harmlessly restore the (unread, ignored-on-deserialise) key. F2: SYNCED the 8 POCO-backed keys the bin copy carries but the tracked file was missing (SettingsLoader.Save serialises the full POCO, so any UI save materialises them) — indicators.CVD.divergence_penalty (1) + indicators.MicroCVD.decel_penalty (1) (both live Step-2 scoring penalties, now legitimately tweaker-tunable on BOTH copies — v46 avg_window_sec philosophy) + the 6 scoring.hold_* CalcHoldStatus thresholds (hold_roc_take_profit_long 0.6 / _short -0.6, hold_rsi_hold_long 60 / _short 40, hold_rsi_evaluate_long 40 / _short 60). All values = POCO defaults = bin values → zero behaviour change; tracked and bin now present the SAME config/tweaker surface. D4: FENCED scoring.hold_* off the auto-tweaker surface — 'scoring.hold_' added to SettingsDiffApplier.RejectedPathPrefixes + PromptBuilder HARD CONSTRAINT 17 (trader hold/exit discipline, HoldStatus never feeds the failure matrix → no failure-rate linkage; same class as kelly.*; sibling scoring.* tunables stay proposable — prefix-safe, no tunable key shares the prefix). D3/F3 companion (separate docs commit): ExitGuardEvaluator deliberately stays on snapshot CalcOFI — ruled + documented, see time-averaged-ofi-spec-back.md §3. No scoring/CSV/render change; CSV stays v0.7, eval cache v4; the OFI dominance re-baseline slides to v48 (D2). Acceptance: 3 Release builds 0/0; A1–A20i unregressed + new A21a (Validate rejects the removed key as unresolvable, accepts sibling regime_gates.transitional_penalty_mid) + A21b (Validate rejects scoring.hold_rsi_hold_long per HC17, accepts scoring.verdict_med_pct).",
    "v46 (2026-06-30): TIME-AVERAGED OFI — build behind the dataset boundary, P4 #4 (docs/time-averaged-ofi-proposal.md, APPROVED 2026-06-29; §10 all-recommended + the tweaker-exposure directive). FIRST P4 scoring re-baseline upgrade — but THIS commit is the BUILD ONLY; the threshold re-derivation is a LATER data-gated pass (v47-ish), mirroring v36→v40. TWO new keys under indicators.OFI: averaging_enabled (true) + avg_window_sec (10). MECHANISM: on the live WS path the single-snapshot OFI is replaced by a TIME-WEIGHTED average of the top-book imbalance over the run window — a feed-side rolling accumulator (Core/OfiAccumulator.vb, host-agnostic) folds each ~100ms book update's weighted bid/ask imbalance into a time-AWARE **geometric (log-ratio)** EMA (Ratio = exp(EMA(ln ratio)); alpha = 1 - exp(-dt/tau), tau = avg_window_sec, applied in log space; NOT fixed-alpha — the window must mean what it says under irregular arrivals) — multiplicatively unbiased for the symmetric bid/ask ratio, chosen over arithmetic per the NY DIAG test (2026-06-30, commit eee6e4b, reverted): arithmetic gave a 12:1 buy-dominant skew on a net-flat 5.1h session vs 1.4:1 geometric (AM≥GM bias; see docs/ofi-geometric-construction-spec.md). The weighted bid/ask volumes (OFIBidVol/OFIAskVol, display context) stay arithmetic. The folded scalar is the SAME sanity-bounded weighted ratio CalcOFI computes (the math is now a shared pure helper IndicatorEngine.ComputeOfiImbalance + ClassifyOfiRatio, so CalcOFI is byte-identical), so the OFISignal classification, the _ofiHistory momentum ring, and the breakdown/card render are UNCHANGED in mechanism — only the VALUE of r.OFIRatio changes (averaged vs snapshot). ⚠ DATASET BOUNDARY: the analysis_log.csv OFIRatio/OFISignal/OFIBidVol/OFIAskVol columns keep their names but their SEMANTICS shift (averaged on the WS majority) — documented here + §15, NO header rotation (same rule as v31/v36). The v47-ish re-baseline re-derives buy_dominant_ratio (2.0) / sell_dominant_ratio (0.5) by firing-rate-matching to the snapshot-OFI history (v40/v41 method) once post-build data accumulates, since averaging tightens the ratio distribution and the spiky-snapshot thresholds would under-fire on it — that re-baseline (and the OFI.Momentum* review) now run on the GEOMETRIC distribution, not the arithmetic one this entry originally described. WARMUP/RESET: until the accumulator has >= avg_window_sec of fold coverage (+ a small min fold count) the run falls back to the snapshot CalcOFI; the accumulator resets on (re)connect so a stale pre-gap average can't bleed across — the fallback re-arms after every reconnect. WS-ONLY: at transport=rest or on a per-run REST fallback there is one book/run → snapshot CalcOFI (today's path; never blocks). TWEAKER SURFACE (trader directive): avg_window_sec is ON the surface (it shapes the OFI signal — a real failure-rate lever) alongside book_depth + the dominance ratios; only averaging_enabled (the feature on/off flag, not a threshold) is OFF — exact-match rejected in SettingsDiffApplier.Validate + PromptBuilder HARD CONSTRAINT 16 (siblings remain tunable). COUPLING CAVEAT: the dominance ratios are re-derived FOR a given window, so a manual avg_window_sec change should be followed by a ratio re-check (the iterative tweaker re-tunes over rounds). REVERSIBILITY: averaging_enabled=false reverts to snapshot OFI hot (the rollback) and is BYTE-IDENTICAL to v45 (proven via the harness regression). CODE: new Core/OfiAccumulator.vb (+ OfiAverageSnapshot struct); IndicatorEngine.ComputeOfiImbalance/ClassifyOfiRatio extracted from CalcOFI; MarketState owns the accumulator (FoldOfi/ResetOfiAccumulator/GetOfiAverage under its SyncLock); DeribitWsFeed folds on each book update + resets in SeedAsync (every connect); RunAnalysisAsync sources the OFI fields from the average on the WS-live path when warmed up. Display-parity: no new/removed/renamed rendered line — the OFI breakdown row + card binding render the same r.OFIRatio (value source unchanged), so no card-surface change. Acceptance: solution(Release) + AutoTweaker + OrderCheck build 0/0; A1-A19e unregressed + A20a-h (CalcOFI refactor byte-identical, EMA steady-state + time-aware step, warmup gate, reset re-arm, tweaker reject averaging_enabled / accept avg_window_sec) + new A20i (geometric symmetry: alternating 2.0/0.5 converges to ~1.0, not the ~1.25 an arithmetic mean would give); averaging_enabled=false byte-identical to v45. No CSV-schema/eval-cache change; CSV stays v0.7, eval cache v4. AMENDMENT (2026-07-01, docs/ofi-geometric-construction-spec.md): the ratio construction switched arithmetic → geometric (log-ratio) per the NY DIAG test — folds INTO this v46 entry, no version bump (dataset boundary lands on the final construction; v47 stays reserved for the data-gated threshold re-baseline).",
    "v45 (2026-06-29): LIVE microstructure strip — P4 #3 (docs/live-microstructure-strip-proposal.md, APPROVED 2026-06-25; §9 #1 visual checkpoint cleared by the trader 2026-06-29 → enabled default flipped false→true + the toggle made a VISIBLE checkbox). ONE new top-level block live_strip {enabled:true, refresh_sec:2, tape_window_sec:10}. DISPLAY/AWARENESS ONLY — a continuously-updating one-line TAPE strip (last price vs nearest structural level above+below, TFI, spread, top-book imbalance, tape speed in tr/s + $/s) reconstructed from the live WS MarketState every refresh_sec between full runs, at ~2s freshness. Deliberately NOT a verdict (no score / no direction call — §11 discipline: the verdict stays a deliberate full-pipeline product; the strip shows raw microstructure INPUTS, visually distinct, so it informs without tempting a marginal entry). NEVER calls ScoringEngine.Calculate, never writes the CSV, no schema change, NO re-baseline. CODE: new host-agnostic LiveMicrostructureEvaluator.vb (Evaluate(state, lastRun, cfg) -> MicrostructureSnapshot — reuses the existing pure CalcTFI / CalcSpread / CalcOFI fns + carried last-run levels LastSwingHigh/Low5m + VPFRNearestHvnAbove/Below + a small tape-speed window scan; identical methodology to the full run, only fresher; never throws, degenerate/empty buffer -> safe blanks). Levels are CARRIED not recomputed (5m swing / VPFR HVN are slow, refresh each full run); price/TFI/spread/imbalance/tape-speed recomputed live. WinForms host = a ~2s System.Windows.Forms.Timer (MainForm_LiveStrip.vb; created at form load, disposed on close; self-gates each tick — renders only when enabled AND the WS feed is healthy+fresh; transport=rest / no _marketState -> 'TAPE · WS only') + a thin full-width TAPE strip row directly under the verdict header (neutral/dim, never the verdict colour ramp) + a VISIBLE 'TAPE' checkbox toggle for live_strip.enabled (mirrors the SINGLE/REPEAT + INTERVAL/ON-CLOSE radio toggles; data label hides when off so 'disabled→hidden' (§7) holds while the toggle stays reachable (§10)). ALWAYS-ON when enabled (NOT position-gated — useful flat watching a level for entry or in a hold), independent of the exit-guard timer + the on-close watcher. OFF the auto-tweaker surface: 'live_strip.' added to SettingsDiffApplier.RejectedPathPrefixes + PromptBuilder HARD CONSTRAINT 15 (display preference, no failure-rate linkage — same class as kelly.*/exit_guard.*/auto_run.*). Live status-bar element like BuildWsStatusSegment / the exit-guard strip — NOT an RTF/snapshot/card surface, so no card-binding obligation (engine display-string parity rule). Runtime toggle saves bumpVersion:=False (operational); only this one-time key ADD bumps v44->v45. Acceptance: solution(Release) + AutoTweaker + OrderCheck build 0/0; A1-A18e unregressed (engine path untouched — the strip never calls Calculate) + new A19a-e evaluator fixtures (TFI/spread/imbalance correct, nearest-level above-vs-below selection, tape-speed window [recent counted / older excluded / lull -> 0], empty buffer -> blanks no throw); transport=rest / disabled -> strip inert, verdict path byte-identical. No CSV/eval-cache change; CSV stays v0.7, eval cache v4. §9 #1 checkpoint (overall look) CLEARED 2026-06-29 (default->enabled:true + visible checkbox toggle); §9 #4 (both-levels) behaves as designed — the strip shows the NEAREST level on EACH side, so when price is below ALL carried structure (a strong down-leg that broke the 5m swing low) only the nearest-above shows and the below-bracket is genuinely empty until the next full run maps a level there.",
    "v44 (2026-06-25): on-close analysis mode — P4 #2 (docs/on-close-analysis-mode-proposal.md, APPROVED 2026-06-25). ONE new key auto_run.trigger_mode (\"interval\" | \"on_close\", default \"interval\"). PURE RUN-TRIGGER change — RunAnalysisAsync / ScoringEngine.Calculate() are BYTE-IDENTICAL; only WHEN a run fires moves, from the fixed-interval auto-run timer to each execution-resolution bar close (NY 1-min / Asia-London 3-min — v36 exec resolution). No CSV-schema change, no scoring change, NO re-baseline. On-close does NOT drop the forming bar (that would be a re-baseline, out of scope) — the verdict at an on-close fire is exactly what a timer fire at that instant produces. CODE: new host-agnostic Core/BarCloseDetector.DetectBarRoll(state, execRes, lastSeenOpen) — reads MarketState.GetCandles(execRes), fires once when the forming-bar open-time advances (multi-bar reconnect gap → single catch-up fire, not a burst); reuses ExecutionResolution.ResolveResolution (re-resolved each tick so a session 3↔1 boundary is honoured live). WinForms host = a ~1s Threading.Timer in MainForm_AutoRun (StartOnCloseWatcher/StopOnCloseWatcher, created in StartAutoRun's on_close branch, disposed in StopAutoRun + OnFormClosing) that marshals RunAutoAnalysis on a detected roll OR an interval BACKSTOP (now − lastFire ≥ interval — never goes silent on a WS feed stall; lastFire also double-fire-guards). SINGLE → next close then stop; REPEAT → every close. Countdown label shows 'Next close: M:SS' (ceil(now/execRes) − now). WS-mode feature: transport=rest or no _marketState/_wsFeed → falls back to interval mode for the session (status note) — never silent. interval_* keys keep their meaning AND double as the backstop ceiling. Operational/UI toggle (mode radio beside SINGLE/REPEAT) — runtime saves with bumpVersion:=False (v36 §10a precedent); only this one-time key ADD bumps v43→v44. OFF the auto-tweaker surface: 'auto_run.' added to SettingsDiffApplier.RejectedPathPrefixes + PromptBuilder HARD CONSTRAINT 14 (operational preference, no failure-rate linkage — same class as kelly.*/network.*/exit_guard.*). NOT an RTF/snapshot/card surface (lblCountdown status label only) → no card-binding obligation (engine display-string parity rule). Acceptance: solution(Release) + AutoTweaker + OrderCheck build 0/0; A1–A17h unregressed (RunAnalysisAsync/Calculate untouched) + new A18a–e DetectBarRoll fixtures (no-roll / +1 roll / multi-bar gap → single fire / resolution switch / backstop arithmetic); interval mode byte-identical. No CSV/eval-cache change; CSV stays v0.7, eval cache v4.",
    "v43 (2026-06-25): realtime exit guard — P4 #1 (docs/realtime-exit-guard-proposal.md, APPROVED 2026-06-24). ONE new top-level block exit_guard {enabled:true, interval_sec:3, debounce_evals:2, sound_enabled:true}. DISPLAY/ALERT ONLY — re-runs the fast microstructure exit checks (the two EXIT-producing CalcHoldStatus layers: 2+-adverse fast exit + structural-break exit; a SINGLE adverse signal maps to Clear, NOT a Warn tier — D3 coordinator ruling) against the live WS MarketState every interval_sec while a position is declared, surfacing an EXIT GUARD status strip + optional alarm at tick freshness instead of the 10-60s poll cadence. NEVER calls ScoringEngine.Calculate, never writes the CSV, never changes the verdict — no scoring/dataset/re-baseline impact. CODE: shared ScoringEngine.ComputeFastExitPrimitives extracted from CalcHoldStatus (Layers 1/1.5/3 refactored to consume it — OUTPUT BYTE-IDENTICAL, the single source of truth so the overlay can't drift on 'what counts as adverse'); new host-agnostic ExitGuardEvaluator.vb (recomputes MicroCVD/TFI/OFI/CVD from MarketState via the existing pure indicator fns, same cfg params + same 500-trade window as the full run — fresher data, identical method); WinForms host = a System.Windows.Forms.Timer (started at form load, decoupled from auto-run — D6) + a full-width EXIT GUARD strip in the SETTINGS & TOOLS card (visible only when posState != None; paused on stale/down feed — never fires on stale data) + System.Media on the EXIT-latch transition (debounce_evals consecutive ticks). WS-mode feature by nature (reads MarketState); at transport=rest it shows 'WS only'/hidden. OFF the auto-tweaker surface: 'exit_guard.' added to SettingsDiffApplier RejectedPathPrefixes + PromptBuilder HARD CONSTRAINT 13 (trader-risk/display preference, same class as kelly.*). Live status-bar element like BuildWsStatusSegment — NOT an RTF/snapshot/card surface, so no card-binding obligation (engine display-string parity rule). Acceptance: solution(Release) + AutoTweaker + OrderCheck build 0/0; CalcHoldStatus byte-identical (hold-status harness fixtures unchanged); evaluator fixtures (2-adverse/structural -> Exit, 1-adverse -> Clear [D3, A17h], 0 -> Clear, mirror short, empty buffer -> Clear); transport=rest byte-identical. No CSV/eval-cache change; CSV stays v0.7, eval cache v4.",
    "v42 (2026-06-24): WebSocket transport CUTOVER — network.transport 'rest' -> 'ws'. shadow_parity STAYS false -- the ShadowParityComparer only runs at transport!='ws' (it is skipped while live on WS, so it cannot log there); shadow_parity=true at ws is inert and would show a frozen 'parity 0/50' on the status line. The >=50-consecutive parity gate was the PRE-flip validation, already passed at transport='rest' (peak 79/50). Dataset-boundary marker for the WS migration (docs/websocket-migration-p3-cutover-spec.md §5; spec-back ...-p3-spec-back.md, coordinator-APPROVED; sequencing §8). The live verdict path now reads all 8 live shapes from the WS feed (DeribitWsFeed/MarketState) via ResolveSource(), with per-run REST fallback on DeribitWsFeed.IsDegraded(); Early-WS monitoring is the WS-health status line (WS OK/DEGRADED/DOWN + reconnect counter), the skip count, and the §12 volume-spike watch -- NOT the parity comparer (off at transport='ws'). SEMANTICS UNCHANGED BY DESIGN — closed-bar OHLCV byte-identical across the 12h soak + 214-run gate trial. GATE (GO 2026-06-24): parity held >=50 consecutive (peak 79/50); live transport=ws smoke 214 runs, no skips, no WS-specific desync. The only residual is the systematic WS 3-min closed-bar VOLUME undercount ~2.5% (OHLC-exact, 78/78 ws-low) — accepted as immaterial to scoring (decision (a), §7) behind a relative-volume tolerance on the parity comparer's closed-bar check + a §12 volume-spike standing watch. Funding + book moving-field tolerances widened (funding -> Math.Max(5e-8, |rate|*5%); book 5 -> 8 ticks) to absorb the comparer's own REST-run-start-vs-WS-read-later read-skew (gap directions track price -> instrument artifact, NOT WS staleness; settled OHLC never trips). POCO default stays 'rest' (config-absent/corrupt falls back to the proven REST path; a 'ws' default would degrade unsafe). No CSV transport column (no header rotation -> preserves the live book + tweaker NY×1 accumulation). Rollback = transport back to 'rest' (hot, next run; the comparer auto-resumes there if shadow_parity is re-enabled). No scoring/indicator change; CSV stays v0.7, eval cache v4. Builds 0/0 (solution + AutoTweaker + OrderCheck); harness A1-A15h + A16a-A16e pass; transport=rest byte-identical.",
    "v41 (2026-06-22): (B) Monday recalibration — ASIA roc_magnitude_threshold 0.20 -> 0.17 (settings-only, one value; LONDON 0.11 + shared slope 0.06 both CONFIRMED, unchanged). The v40 ASIA 0.20 was anchored on a 2-weekday (Thu+Fri) sample skewed by a hot Thursday; the 3rd weekday session (Mon 06-22) ran calmer, confirming the v40 provisional caveat. Re-measured on 3 weekday Asia/London sessions (Thu 06-18 + Fri 06-19 + Mon 06-22), firing-rate-matched to NY-1m's stable ~53.7% @0.10: ASIA hr6-7 (n=575) pooled matched 0.170 — per-day 0.21/0.16/0.15, real session-vol variance — fired only 49% at the old 0.20 (mild over-suppression vs the 54% target). LONDON hr8-12 (n=1180) pooled matched 0.106, fires 52%, stable across all 3 days. SLOPE re-derived on the 3-day book (reusing IndicatorEngine.CalcROCSeries; fidelity re-validated NY active@0.05 = 46.5% vs logged 47.6%): ASIA pooled |delta| matched 0.061 / LONDON 0.062 — still shared + near-identical, both fire ~48% vs the 46.5% target → 0.06 kept. Data-hygiene note: Mon also logged hr5 (early/dead Asia outside the trader's hr6-7) — excluded from the measurement; those rows are noise in the live book. ASIA stays the noisier session (day-to-day vol variance); revisit if it keeps compressing. No code change; no CSV/eval-cache/render change. Acceptance: solution + AutoTweaker + OrderCheck 0/0; harness A1-A15h + A14j (value updated 0.20->0.17).",
    "v40 (2026-06-20): Asia/London ROC re-baseline — workstream (B) of v36, settling resolution_profiles['3'] with MEASURED 3-min values on the weekday-only book (Thu 06-18 + Fri 06-19; the v34 weekend confound gone). Spec docs/asia-london-roc-rebaseline-proposal.md (trader-approved 2026-06-20). Method = firing-rate-matching to NY-1m selectivity, NOT ATR-rescaling (no session runs both resolutions, so resolution-scaling can't be isolated from session-vol). FINDING: the single ×2.1 proxy broke session-specifically. MAGNITUDE (|ROC| active gate) tracks session volatility — Asia hr6-7 runs ~1.8× hotter than London hr8-12 (whose 3-min |ROC| ≈ NY-1m), so a single resolution_profiles['3'] magnitude can't serve both → made PER-SESSION: new nullable session_volume.sessions[].roc_magnitude_threshold (ASIA 0.20 ≈ unchanged from 0.21; LONDON 0.11 — the real fix, current 0.21 suppressed ROC on 78% of London bars / fired only 22% vs NY's 54%). resolution_profiles['3'].roc_magnitude_threshold KEPT at 0.21 as the 3-min fallback for any 3-min session without a bucket override. SLOPE (RISING/FALLING vs FLAT gate) is ONE SHARED 3-min value — bar-to-bar ROC *delta* distributions are near-identical across the two sessions (p50 |delta| 0.059 Asia vs 0.054 London) even though the *levels* differ 1.8×, so resolution_profiles['3'].roc_slope_delta_threshold 0.105 -> 0.06 (both sessions; current 0.105 over-suppressed to ~25% active vs NY's 46.5%). Slope was re-derived from live Deribit 3-min candles reusing the exact IndicatorEngine.CalcROCSeries (the delta isn't CSV-logged) — fidelity validated: re-derived NY active@0.05 = 46.5% vs engine-logged 47.6%. CODE (magnitude routing only): SessionBucketSettings + nullable RocMagnitudeThreshold; ExecutionResolution.ResolveRocMagnitudeForHour (bucket override -> resolution_profiles -> base); IndicatorResults.RocMagnitudeThreshold (0=inherit) stamped in RunAnalysisAsync; ScoringEngine EffRocMag at the 3 magnitude read sites (fallback keeps harness fixtures byte-identical). NY byte-identical (no bucket override -> base 0.10). PROVISIONAL — 2 weekday days only; re-measure after Monday 06-22. New key OFF the auto-tweaker surface (PromptBuilder HARD CONSTRAINT 11 + array-unreachable in SettingsDiffApplier). Acceptance: solution + AutoTweaker + OrderCheck build 0/0; harness A1-A15h unregressed + new A14e (per-session magnitude resolution). No CSV/eval-cache change; no rendered-line change (ROC thresholds aren't displayed).",
    "v39 (2026-06-19): WebSocket migration P2 — consumer routing + shadow parity + WS-health status + network.* tweaker hardening (docs/websocket-migration-p2-implementer-handoff.md, parent docs/websocket-migration-proposal.md §7/§8). ONE new key network.shadow_parity (false) — dev/validation flag; when true (with transport='rest') the WS feed runs alongside REST and each run logs a field-level WS-vs-REST parity comparison to a side log (ws_parity_log.txt) + console, NEVER the CSV and NEVER scoring. The >=50-consecutive-pass result is the proposal §7 cutover gate. RunAnalysisAsync now fetches the 7 live shapes (1m/5m/15m/funding/book/orderbook/trades + the v36 exec-resolution candles) through an IMarketDataSource chosen per-run by network.transport via ResolveSource(): transport='rest' (the P2 default) ALWAYS returns the verified pass-through RestMarketDataSource so a normal run is byte-identical to v38; transport='ws' adds the per-run REST fallback (new DeribitWsFeed.IsDegraded() — not connected / cooling down / all primary streams stale). transport STAYS 'rest' in P2 — the cutover is P3 (data-gated). Backfill paths (OhlcCache / FetchGapChunked / time-range GetCandlesAsync) stay direct-REST. MainForm owns the feed lifecycle (start at form load when transport='ws' OR shadow_parity; Stop on close) — the only host glue; the feed/comparer stay host-agnostic. WS-health status line added to the LOG cascade (renders only when the feed is active). Auto-tweaker hardening: 'network.' added to SettingsDiffApplier.Validate RejectedPathPrefixes (code-level reject) + PromptBuilder HARD CONSTRAINT 12 (never propose network.* — transport plumbing, no failure-rate linkage). No scoring/indicator change; CSV stays v0.7. Acceptance: solution + AutoTweaker + OrderCheck build 0/0; harness A1-A15g unregressed + new A15h; transport=rest byte-identical regression check; >=50-run shadow-parity report with the three P1 watches (chart bar roll, ticker-OI vs book-summary-OI, seed->subscribe gap) characterized.",
    "v38 (2026-06-19): WebSocket migration P1 — additive-only foundation (docs/websocket-migration-p1-implementer-handoff.md, parent docs/websocket-migration-proposal.md). SIX new network.* keys, all transport plumbing (NOT scoring): transport ('rest'|'ws' cutover flag, stays 'rest' through P1/P2 — P3 flips it), ws_url (wss://www.deribit.com/ws/api/v2, public channels only no auth), ws_heartbeat_sec (30, Deribit min 10), ws_stale_after_sec (10, book/trades/ticker staleness gate; candle series defer to IndicatorEngine.IsFresh), ws_cooldown_sec (300, reconnect-storm hold), ws_fallback_to_rest (true, P2 routing). FIVE new dormant root .vb files (IMarketDataSource/RestMarketDataSource/MarketState/WsMarketDataSource/DeribitWsFeed) — public but unreferenced; the live verdict path is UNTOUCHED and runs pure REST byte-identical to v37. DeribitWsFeed subscribes 1/3/5/15 chart (v36 3-min exec) + trades + ticker + depth-limited book, REST-seeds full windows on connect/reconnect, replies to heartbeat test_request (the #1 foot-gun), and reconnects with 1→60s backoff + storm cooldown. funding served from funding_8h (NOT current_funding — parity with DeribitClient). Channel names + payload fields verified against the current Deribit API docs (2026-06-19). Auto-tweaker surface: network.* keys NOT yet blanket-excluded in PromptBuilder (same status as the pre-existing 3 REST keys + all display keys) — recommended as a separate hardening commit, see spec-back. Consumer routing = P2; cutover = P3 (data-gated). EngineSettings.vb NetworkSettings POCO gains the 6 fields with matching defaults. No scoring/indicator/CSV change.",
    "v37 (2026-06-17): ATR-band recalibration for the current price regime (P11, docs section 16.6). indicators.ATR.static_ref 115.0 -> 38.0 -- the v14 anchor (midpoint of trader-profile Normal band 80-150, BTC ~$80-100k) was ~3x the live 1-min ATR mean (38.8 over 1640 rows at BTC ~$62-67k; p25/p50/p75 = 18/35/53, p90 72). static_ref is the COLD-START fallback for DynamicNorms.ComputeATRRef only (the live ATR reference self-calibrates from a recent average), so this affects only the first runs before the window fills -- not a scoring knob. Settings-only; EngineSettings.vb POCO default deliberately untouched (rides next code commit, v33/v34 precedent). Paired trader-profile section 5 update: 1-min bands Low<20 / Normal 20-55 / High>55 (from live p25/p75); bands are now resolution-dependent -- Asia/London run 3-min where ATR ~2.1x, so ~Low<42 / Normal 42-115 / High>115. No code change.",
    "v36 (2026-06-16): session-conditional execution timeframe — Phase 1 (docs/session-timeframe-resolution-implementer-handoff.md). The engine reads a per-session execution_resolution (1/3/5 min) and fetches + computes the WHOLE execution-indicator stack (ROC, RSI, RSI-div, EMA ribbon, Volume SMA, VWAP+bands, BBW/TTM, OBV, Donchian, VPFR, CVD divergence-gate, AND ATR(7)) at that resolution. ASIA=3 / LONDON=3 / NY=1 (trader-signed 2026-06-15; Path B). The 5m DMI/ADX regime, 15m MTF gate, and 5m/15m swing pivots are UNCHANGED (the valid higher-timeframe layer above a 3-min chart). The min-tradeable-move gate, ATR levels, eval barriers, and Kelly inherit the resolution automatically (all derive from r.ATR, now on the execution candles) — this is the whole point: low-vol Asia/London 1-min verdicts that the v35 gate correctly suppressed now run on a tradeable 3-min timeframe. CONFIG: each session_volume.sessions[] gains execution_resolution (default 1 = unchanged 1-min). New top-level resolution_profiles map keyed by resolution string ('1' empty/inherits global; '3' overrides only the two ROC keys). THRESHOLD PROFILE (trader-confirmed 2026-06-15): ONLY the 2 ROC keys scale x2.1 on 3-min — roc_magnitude_threshold 0.1->0.21, roc_slope_delta_threshold 0.05->0.105 (candle-return-gated; 3-min ROC(9) spans 27min vs 9min). CVD slope_min_usd (12000) and MicroCVD accel_threshold (10000) DO NOT scale — they gate the FIXED 500/50-trade stream, not candles, so they're resolution-independent (correction to the proposal's 4-key list: code review settled it to 2). 2.1x is the measured 1->3min ATR ratio used as a PROXY for ROC scaling (true ROC scaling bracketed ~1.7x noise to ~3x trend) — both ROC keys are the Phase-2 re-baseline priority; Phase-1 Asia/London verdicts are PROVISIONAL by design. DynamicNorms vol/VWAP-dev/ATR-ref self-scale (computed from the candle window). Bar-count windows (ATR 7, RSI 9, etc.) keep their counts — a 3-min RSI(9) IS the standard 3-min RSI. session_volume multipliers stay 1-min-calibrated (VolumeRatio ~scale-invariant) and OFF the resolution profile; the weekday-ASIA re-verify becomes a 3-min re-verify. DATA: per-row ExecResolution stamp — analysis_log.csv v0.6->v0.7 (+ExecResolution +CVDWeightedSlope, appended); eval cache analysis_eval_cache.csv v3->v4 (+ExecResolution; legacy rows default 1); live perf-strip session windows filtered by (session x resolution) so 3-min Asia never blends with 1-min NY or pre-v36 1-min Asia. execution_resolution is OFF the auto-tweaker surface (PromptBuilder HARD CONSTRAINT 11). Resolver in host-agnostic Core/ExecutionResolution.vb (shared MatchSessionBucket — DynamicNorms.ApplySessionVolume + the display ResolveSessionLabel now route through it, fixing the v34 hour-7 off-by-one). Mandatory EXEC 1m/3m display tag on the ATR header (card + snapshot). §10 hygiene folded in: operational/UI saves (auto_run interval, perf metric_mode, output-dump) no longer bump version/change_log (SettingsLoader.Save bumpVersion flag); tweaker settings_path realigned to the bin-copy the live app reads. Acceptance: A1-A13 unregressed + A14a-A14i. Phase 2 (per-resolution re-baseline + tweaker session x resolution filter) is OUT of scope.",
    "v35 (2026-06-14): minimum-tradeable-move gate + eval-metric de-confound (paired). Specs: docs/min-tradeable-move-gate-proposal.md, docs/eval-metric-deconfound-proposal.md. ONE new key: scoring.min_tradeable_move_pct = 0.0008 (0.08%, price-relative, ~=$50 at $62k, sized to clear slippage). Shared, UI-editable, hot-reloadable, OFF the auto-tweaker surface (trader risk preference, same exclusion class as kelly.*; new HARD CONSTRAINT 11 in PromptBuilder). (1) SCORING GATE (behaviour change, approval-gated): at the end of Calculate() after Step 5b, a directional verdict whose EFFECTIVE (post-cap) take-profit distance < min_tradeable_move_pct x price is overridden to NO TRADE with VerdictContext = BELOW_MIN_MOVE; scores/breakdown/levels preserved for display. Catches both low-ATR (small ATR target) and near-swing (cap pulls TP below floor) causes. At ~$62k low-vol this suppresses most Asia (ATR ~13) and low-ATR NY verdicts BY DESIGN -> slower low-vol accumulation, cleaner calibration book. (2) EVAL DE-CONFOUND (analysis-layer; no scoring votes/thresholds/vetoes touched): favourable barrier floored at max(k x ATR, min_tradeable_move_pct x price) so 'success' requires a tradeable move; gate-killed historical directional trades (AtrTargetMultiplier x ATR < floor) reclassified EXCLUDED_BELOW_MIN_MOVE and removed from success/fail counts (NOT re-scored as failures, mirroring the live gate), survivors still scored with the floor. Eval cache schema v2 -> v3 (re-evaluate on load; store the floor pct used; re-walk if it changes). One-time forensic logs how many directional trades got excluded and how many were SUCCESS under the old confounded barrier. Both must land before the supervised auto-tweaker first fire. No CSV scoring-schema change (VerdictContext already logged; BELOW_MIN_MOVE rides existing column). Acceptance: harness A13a/b/c + editability; eval before/after Asia-ATR table in spec-back.",
    "v34 (2026-06-13): clean-data FULL re-baseline (docs/clean-data-rebaseline-v34-proposal.md). CalibrationReport READY (975 rows, 3 regimes >=50, 3 session days; ASIA 233 / LONDON 254 / NY 488). THREE changes. (1) session_volume ASIA 0.80/0.85 -> 1.10/1.05 (trader chose pre-emptive over neutral 1.00): ASIA over-trades the choppiest session (trade-rate 63.1% / RANGE_BOUND 27.5%, both highest) and the 0.80/0.85 multiplier *lowered* the volume-confirm floor (clamp 2.0x0.80=1.6) into ASIA's fatter thin-denominator VolumeRatio tail (p90 2.01 vs NY 1.37); 1.10/1.05 lifts the floor above neutral. CONFOUND (trader-flagged): ASIA sample is 100% Saturday 06-13 (weekend), LONDON ~99% Saturday, NY 100% weekday (Thu/Fri) -> the session comparison is weekday/weekend-confounded; 0.80/0.85 is backwards on any day (direction safe) but the magnitude is weekend-set -> re-verify vs weekday ASIA (WATCHING). Barrier win-rate does NOT show Asia losing (73.7% success is ATR-barrier-confounded) -> selectivity fix not loss fix; partial-bar volume starvation caps the lever (code spec). (2) indicators.funding.momentum_threshold 0.00001 -> 0.00000005: FundingMomentum FLAT 975/975; 3-sample funding delta nonzero on only 4.2% of rows (15 distinct rates, +/-1.1e-5); 5e-8 makes it live but hard-capped ~4% (low signal, do not over-invest). (3) indicators.CVD.slope_pct_of_value 0.05 -> 0.10: v33 pre-committed follow-up: CVDSlope FLAT still 5.5% (<10% target), NY 2.7% the drag; 0.10 doubles the proportional arm, floor kept 12k; weightedSlope unlogged -> monitor (revert to 0.07 if FLAT>20%). KEPT (re-confirmed multi-session): OBV 18.0 (regime ordering now healthy, trend 87% > range 52% directional), MicroCVD 0.30 (FLAT 20.4%), spread 5.0 (REST-dead, accept), volume clamps, RSI/OFI/funding-bands/verdict-pcts (sanity), ATR 2.0/1.2 (adverse-hit 3.4%). OISignal 95% NEUTRAL -> WATCHING. Code specs flagged: partial-bar volume sampling, log weightedSlope, continuous spread (WS P4), ResolveSessionLabel hour-7 off-by-one. POCO defaults untouched (rides next code commit). No keys added/removed; CSV stays v0.6.",
    "v33 (2026-06-13): clean-data re-baseline pass (docs/clean-data-rebaseline-v33-proposal.md). Partial re-baseline on 489 clean NY-session rows (Step 0: ASIA/LONDON + TRANSITIONAL/TRENDING_DOWN too thin — session_volume and clamps deferred). indicators.OBV.trend_gate 10.0 → 18.0 — offline recompute (96.5% validation vs logged) put |obvChange| p50 at 18.5; seed left OBV directional on 84% of runs (93% in RANGE_BOUND). indicators.CVD.slope_pct_of_value 0.01 → 0.05 — proportional arm was dead (exceeded the 12k floor on 10% of rows; CVDSlope FLAT 2.7%, no forward-return discrimination); 0.05 activates scaling on 60% of rows. indicators.MicroCVD.accel_threshold_dynamic_pct 0.03 → 0.30 — exact E/M/L reclassification sweep; FLAT 3.7% → est. 25-30%; dead-band was ~3k vs |late-early| p50 ~50k. All three are normalized/ratio quantities supportable from a one-session sample. atr multipliers, Donchian quartile, mtf_gate, verdict pcts reviewed — no change (rationale in proposal §3.5-3.9).",
    "v32 (2026-06-11): engine Tier D display & hygiene pass (docs/engine-tier-d-hygiene-proposal.md). Added kelly.max_leverage (5.0) — D1/H4 fixes the inverse-contract Kelly sizing (risk per contract = face x stopDistance / entryPrice, was face x stopDistance, so contracts were always 0) and adds a leverage cap that binds before the $ risk cap; new KellyLevCapped surfaces it. Removed dead auto_run.enabled key (D4 - never read; auto-run always starts stopped). Display-only D2 linearises ATR stop/target distances (drop ATRScaleFactor; distance = ATR x multiplier) and relabels the sizing display to AvgATR/CurrATR; D3 adds VPFR-lite to the context-tag structural set; D5 adds a 1m/5m candle-freshness skip gate. No scoring votes, thresholds, or vetoes changed. CSV stays v0.6.",
    "v31 (2026-06-11): engine correctness pass (docs/engine-correctness-pass-proposal.md). One value changed: indicators.OBV.trend_gate 0.001 → 10.0 — the OBV normalisation basis moved from first-bar volume to mean per-bar volume (F5), so the gate's units are now 'net OBV drift in average-bar-volumes over the window' (~0.6σ of 250-bar random-walk noise). SEEDED GUESS, not calibrated — re-baseline with the §8 WATCHING table at ≥300 clean v0.5 rows. No keys added/removed. Ships with the fix bundle F1–F9 (trade-stream ascending normalisation, recent-window norms, direction-aware MTF gate, dominant-side cascade, OBV/Donchian fixes, funding-boost cap, TRANSITIONAL penalty arm, header-map CSV backfill), CSV schema v0.4→v0.5, and the pre-authorised calibration-data reset (data-archive/pre-orderfix-20260611).",
    "v30 (2026-05-17): display polish pass — output dump now captures perf-strip values (PERF STRIP header line after each '## Run'), R:R rendering uses '< 0.1' for sub-1dp ratios, sub-noise CAPPED labels suppressed (threshold = max(0.5, ATR × 0.02)), negative-zero funding clamped, OI delta precision unified at 3dp in SIGNAL BREAKDOWN to match OPEN INTEREST section, NO TRADE + CONFIRMED relabelled to ALIGNED (new VerdictContext value), per-side missing-target/stop wording in structural rows. No new config keys; thresholds hardcoded.",
    "v29 (2026-05-17): auto_tweaker switched from sliding to fixed (non-overlapping) windows. New tweaker_config.json key window_mode (\"fixed\" | \"sliding\", default \"fixed\"). min_tier_eligible_rows is now nullable — null auto-scales to max(15, ceil(WindowSize × 0.5)). cooldown_rows retained but is a no-op when window_mode=fixed; logged once per process. state.json gains last_evaluated_row_index (default -1 = uninitialised; seeded to currentRowCount on first v29 run so historical sliding-era CSV data is preserved but not re-evaluated). RoundHistory entries gain window_start_row / window_end_row (already present) plus a new outcome value SKIPPED_INSUFFICIENT_TIER — skipped rounds advance the row index but do NOT tick the BELOW_THRESHOLD streak. RoundHistory cap raised 50 → 1000. Tweak Settings dialog exposes the Min tier-eligible rows row with save-time validation: reject < 5, reject > WindowSize, warning > WindowSize × 0.7.",
    "v28 (2026-05-17): target-hit metric toggle for the live performance strip. Decouples direction-prediction quality (target-hit) from stop-placement quality (barrier-hit). Eval cache schema bumped v1 → v2 with new TargetEverHit column; first load detects v1 and back-fills the column from in-memory OHLC via new FailureRateMatrix.TargetHitWalk helper, rewriting the file with the v2 schema comment. Same denominator for both rates — rows where TargetEverHit is Nothing are excluded from BOTH numerator and denominator. New top-level key performance_display.metric_mode (\"barrier\" | \"target\", default \"barrier\"). Live UI: small [B]/[T] mode indicator just before lblPerfWeek (dim when matches default, amber when ephemerally overridden); left-click on any perf label toggles mode in memory (no settings.json write); right-click opens a 2-item context menu that persists via SettingsLoader.Save. Tooltip second line carries the other metric's rate so both are visible without toggling.",
    "v27 (2026-05-15): OHLC cache gap-fill on startup. InitialiseAsync now detects interior gaps within the 7-day window and back-fills from Deribit. New keys gap_backfill_enabled, max_gap_fill_calls, max_gap_fill_minutes. Addresses the \"engine running but bars missing\" failure mode.",
    "v26 — live-performance-display: six-window success/fail rate strip, updated after every analysis run.",
    "     New top-level block: performance_display (4 keys).",
    "       enabled: true                    — master switch; false = no strip rendered, no cache I/O.",
    "       min_sample_for_render: 4         — below this row count per window, display --% instead of a rate.",
    "       eager_backfill_on_startup: true  — fetch 7-day OHLC gap and backfill eval cache on startup.",
    "       session_block_semantic: most_recent — reserved; currently always most-recent-block per §2b.",
    "     New sidecar files (gitignored): analysis_eval_cache.csv, ohlc_1m_cache.csv.",
    "v25 — output-dump: persistent markdown record of full rendered analysis text per run.",
    "     New top-level block: analysis_logging (2 keys).",
    "       output_dump_enabled: true  — master switch; false = no I/O on analysis runs.",
    "       output_dump_max_runs: 3000 — rolling-trim cap; 0 = unlimited.",
    "     At 3000 runs / 60s cadence: ~9 MB / ~50 hours of coverage.",
    "v24 — d1-trend-structure: HH/HL/LH/LL swing-structure classification + Pass 2c structure bonus.",
    "     New indicators.trend_structure block (4 keys):",
    "       enabled: true        — master switch for ClassifyTrendStructure and Pass 2c bonus.",
    "       pivot_wing: 3        — confirmation bars left/right (default matches swing.pivot_wing_5m).",
    "       pivot_count: 6       — total pivot events to collect before classifying (~3H + 3L).",
    "       structure_bonus: 1   — score added when UPTREND+LongDominant or DOWNTREND+ShortDominant.",
    "     Bonus capped at regimeMax; suppressed in TRANSITIONAL regime.",
    "     EXPANSION/CONTRACTION/UNDEFINED fire no score change.",
    "     CSV column 87 added: TrendStructure5m (string).",
    "     CalibrationReport: new TREND STRUCTURE DISTRIBUTION and BEST VOLUME PIVOT DISTRIBUTION sections.",
    "     D2 (volume-weighted pivots): no settings change. Populates reserved CSV columns 85-86.",
    "v23 — csv-expansion-v0.4: schema bump from v0.3 to v0.4. Added 18 columns:",
    "     SpreadBps, OFIMomentum, FundingDelta, VPFRVAH, VPFRVAL, VPFRNearestHvnAbove,",
    "     VPFRNearestHvnBelow, LastSwingHigh5m, LastSwingLow5m, LastSwingHigh15m,",
    "     LastSwingLow15m, SwingTargetLong, SwingTargetShort, SwingStopLong,",
    "     SwingStopShort, TargetCapReason, BestPivotByVolume5m, BestPivotVolumeRatio5m.",
    "     Old log rotated to analysis_log.csv.v0.3.bak on first startup.",
    "v22: [funding-calibration] Recalibrate funding bands + momentum threshold against broader",
    "     BTC-PERPETUAL funding scale (Deribit 1m/7d/8h chart context, April 2026).",
    "     v19 was tuned for an ultra-quiet 618-row sub-window where rate was stable at -0.0009%.",
    "     v19's thresholds saturated at HEAVILY CROWDED on routine 3 bp moves and fired",
    "     CROWDED on basically any non-zero reading. v22 widens to match the actual scale:",
    "     LOW threshold ±0.001% (1 bp) — fires on mild crowding (a few per session)",
    "     HIGH threshold ±0.008% (8 bp) — fires on real spike events (rare per month)",
    "     momentum_threshold 0.001% (1 bp) — meaningful build-up vs micro-noise",
    "     During current quiet windows, v22 returns NEUTRAL/FLAT more often (correct).",
    "     During typical regimes, v22 differentiates mild vs spike crowding properly.",
    "     scoring.funding_high_positive: 0.00003 -> 0.00008 (0.003% -> 0.008%).",
    "     scoring.funding_low_positive:  0.000005 -> 0.00001 (0.0005% -> 0.001%).",
    "     scoring.funding_high_negative: -0.00003 -> -0.00008.",
    "     scoring.funding_low_negative:  -0.000005 -> -0.00001.",
    "     indicators.funding.momentum_threshold: 0.000005 -> 0.00001 (0.0005% -> 0.001%).",
    "     No code change. No new keys. No tunables removed.",
    "v21: [algorithm-fixes] RSI divergence rewrite + ROC slope_sensitivity split.",
    "     Fix 1 — CalcRSIDivergence semantic rewrite. Three bugs fixed:",
    "       (a) Direction inverted: BEARISH now fires when current price is AT OR ABOVE",
    "           prior pivot (testing/breaking the high), not below it.",
    "       (b) Overbought/oversold gate added: pivot RSI must have been >= 65 (BEARISH)",
    "           or <= 35 (BULLISH) to qualify as an exhaustion signal.",
    "       (c) Most-recent pivot scan: walk backward to find the most recent confirmed",
    "           swing high/low rather than the highest/lowest in the lookback.",
    "     Expected: RSIDivergence NONE rate rises from ~20% to ~80-90%.",
    "     indicators.RSI.divergence_rsi_delta: 2.0 -> 5.0 (meaningful compression required).",
    "     indicators.RSI.divergence_overbought_threshold: 65.0 (new key).",
    "     indicators.RSI.divergence_oversold_threshold: 35.0 (new key).",
    "     indicators.RSI.div_penalty_rsi_high: 65.0 (existing key, now explicit in settings.json).",
    "     indicators.RSI.div_penalty_rsi_low: 35.0 (existing key, now explicit in settings.json).",
    "     indicators.RSI.pivot_wing: 2 (existing key, now explicit in settings.json).",
    "     indicators.RSI.lookback_bars: 20 (existing key, now explicit in settings.json).",
    "     Fix 2 — ROC slope_sensitivity split into two semantically-distinct keys:",
    "       slope_delta_threshold: 0.05 — ROCSlope bar-to-bar delta classification.",
    "       magnitude_threshold: 0.1   — ROC absolute magnitude for partial scoring,",
    "                                     Pass 2c regime-alignment activation, and",
    "                                     spread penalty direction check.",
    "     indicators.ROC.slope_sensitivity REMOVED.",
    "     indicators.ROC.slope_delta_threshold: 0.05 (new key; preserves v19 ROCSlope intent).",
    "     indicators.ROC.magnitude_threshold: 0.1 (new key; restores v18 magnitude conservatism).",
    "v20: [calibration-tuning-2] OI threshold further lowered based on 499-row post-v19 dataset.",
    "     indicators.OI.change_threshold_pct: 0.003 -> 0.002 (effective 0.3% -> 0.2%).",
    "     v19 threshold was still above the observed 15m peak of ~0.23% in the new dataset.",
    "     Funding thresholds unchanged -- BTC is in a near-zero basis regime; thresholds correct.",
    "v19: [calibration-tuning] Empirical threshold recalibration from 618-row CalibrationReport audit.",
    "     Five classifiers were stuck on a single value due to threshold mismatches vs observed BTC scale.",
    "     scoring.funding_high_positive: 0.0003 -> 0.00003 (3 bp -> 0.3 bp).",
    "     scoring.funding_low_positive:  0.00005 -> 0.000005 (0.5 bp -> 0.05 bp).",
    "     scoring.funding_high_negative: -0.0003 -> -0.00003.",
    "     scoring.funding_low_negative:  -0.00005 -> -0.000005.",
    "     indicators.funding.momentum_threshold: 0.0001 -> 0.000005 (10 bp -> 0.5 bp; matches observed max delta of 0.9 bp).",
    "     indicators.OI.change_threshold_pct: 0.01 -> 0.003 (1.0% -> 0.3%; observed peak was 0.61%).",
    "     indicators.ROC.slope_sensitivity: 0.1 -> 0.05 (conservative halving; affects slope classification,",
    "       partial ROC scoring, and Pass 2c activation; 0.03 deferred to v20 if ROCSlope still >80% FLAT).",
    "     UI/MainForm_Analysis.vb: GetRecentTradesAsync(100) -> GetRecentTradesAsync(500) (liq window fix).",
    "     No new cfg keys. No scoring logic changes. No CSV schema change.",
    "v18: [api-resilience] Added top-level 'network' block (3 keys). DeribitClient wraps all fetches",
    "     with ExecuteWithRetry; RunAnalysisAsync validates results and skips cleanly on failure.",
    "     network.request_timeout_seconds: 15 -- per-call HttpClient timeout (raised from 10s).",
    "     network.retry_count: 1 -- additional retries on 5xx / timeout / network drop.",
    "     network.retry_backoff_ms: 1000 -- delay between retries in ms.",
    "     GetFundingRateAsync: Double -> Double?  |  GetBookSummaryAsync: tuple -> nullable tuple.",
    "     15m cache preserved on fetch failure (stale data better than no data for MTF gate).",
    "     Skip counter increments per session; shown in status bar when > 0. No CSV change.",
    "v17: [settings-exposure] Lift 19 hardcoded scoring literals to settings.json. Zero behaviour change at defaults.",
    "     scoring.regime_max_score: {trending:19, range_bound:18, transitional:15} -- RegimeMaxScore() base ceilings.",
    "     scoring.tier_floor: {high_threshold:12, high_floor:9, med_threshold:9, med_floor:6, low_threshold:6, low_floor:3}",
    "       -- TRANSITIONAL ADX penalty graceful-degradation caps.",
    "     scoring.context_tag_thresholds: {momentum_fading_decay_ratio:0.5, momentum_fading_count_min:2,",
    "       structurally_weak_struct_min:2, structurally_weak_flow_min:2} -- CalcVerdictContext thresholds.",
    "     indicators.RSI.pass2c_midline: 50.0 -- RSI midline for Pass 2c RANGE_BOUND alignment check.",
    "     indicators.Donchian.quartile_pct: 0.25 -- Donchian channel quartile for LONG/SHORT_PARTIAL classification.",
    "     indicators.BBW.series_window_multiplier: 5, squeeze_percentile: 0.20 -- CalcBBW internal parameters.",
    "     indicators.TTM.sma_period: 20, lin_reg_period: 7 -- CalcTTMSqueeze internal periods.",
    "     indicators.CVD.late_segment_weight: 2.0, early_segment_weight: 1.0 -- 3-segment CVD slope weights.",
    "v16: [swing-pivots] Added indicators.swing block (4 keys) for CalcSwingPivots().",
    "     pivot_wing_5m: 3  -- bars left/right confirming a 5m swing (~15 min each side).",
    "     lookback_bars_5m: 30  -- scan back ~150 min for most recent confirmed 5m swing.",
    "     pivot_wing_15m: 2  -- bars left/right on 15m (~30 min each side).",
    "     lookback_bars_15m: 20  -- scan back ~5 hours on 15m for higher-TF context.",
    "     No schema changes to other blocks. No scoring impact (display + arbitration + exit only).",
    "v15: [cleanup] Strip silently-ignored keys + drop unused config fields. No behavioural change.",
    "     Removed scoring.long_threshold / short_threshold / strong_long_threshold /",
    "       strong_short_threshold / medium_long_threshold / medium_short_threshold (superseded",
    "       by verdict_*_pct since v0.30; never deserialised onto a POCO field).",
    "     Removed scoring.weights block (ScoringWeights class deleted long ago; never read).",
    "     Removed scoring.transitional_penalty_enabled (no consumer; gate runs unconditionally).",
    "     Removed regime_gates.suppress_long_in_trending_down / suppress_short_in_trending_up",
    "       (Step 4 regime veto applies unconditionally; flags were never read).",
    "     Removed indicators.EMA200 block (CalcEMA(candles5m, 200) is hardcoded).",
    "     Removed indicators.OBV.lookback (CalcOBV uses trend_gate / divergence_gate only).",
    "     Removed indicators.CVD.trade_lookback (CalcCVD walks the full trade list).",
    "     Removed indicators.VWAP.dev_threshold_pct (UI uses dynamic norms.VWAPDevThreshold).",
    "     Removed indicators.VWAP.session1_start_hour / session1_start_minute (only Session 2 boundary used).",
    "     Removed indicators.BBW.releasing_roc_threshold (CalcBBW uses 20th-percentile threshold).",
    "     Removed indicators.Liquidations.long_liq_threshold / short_liq_threshold (CalcLiquidations",
    "       uses dominance_ratio + large_liq_size only).",
    "     Removed indicators.ATR.ref_period (only consumer was ATRAvg20d field, also removed).",
    "     Removed mtf_gate.ema_period_fast / ema_period_slow (CalcMTFGate hardcodes 9/21/50).",
    "v14: [calibration-pass] HIGH-confidence tuning sweep. No schema changes.",
    "     S1 OFI:      buy_dominant_ratio 3.0 -> 2.0, sell_dominant_ratio 0.333 -> 0.5 (less aggressive dominance filter).",
    "     S2 RSI:      partial_overbought 50.0 -> 55.0, partial_oversold 50.0 -> 45.0 (restore 45-55 dead-band).",
    "     S3 Funding:  funding_high_positive 0.0001 -> 0.0003, funding_high_negative -0.0001 -> -0.0003 (BTC routinely sits at 0.01%/8h; extreme-tier should fire less often).",
    "     S4 CVD:      slope_min_usd 1000.0 -> 12000.0 (approved interim; 1000 was noise on BTC perp trade flow).",
    "     S5 Volume:   dynamic_high_clamp_min 1.5 -> 2.0, dynamic_mid_clamp_min 1.2 -> 1.5 (raise dynamic floor toward trader's 3x breakout rule).",
    "     S6 ATR:      static_ref 150.0 -> 115.0 (midpoint of trader-profile Normal band 80-150; removes downward bias on ATRScaleFactor).",
    "     S7 MicroCVD: accel_threshold 5000.0 -> 10000.0 (interim static bump; dynamic scaling deferred).",
    "     S10 Kelly:   account_size_usd 1000.0 (confirmed; placeholder pending trader input).",
    "v13: [adaptive-regime-weights] Added 'regime_weights' top-level block (Pass 2c).",
    "     enabled: true  -- master switch; set false to bypass gate entirely.",
    "     trending.alignment_bonus: 1   -- added when EMA+ROC(gated)+CVD all align with direction.",
    "     trending.conflict_penalty: 1  -- deducted when all three conflict.",
    "     range_bound.alignment_bonus/conflict_penalty: 1/1 for VWAP dev+RSI(9)vs50+Donchian.",
    "     TRANSITIONAL: gate suppressed. Zero-net-score run: gate suppressed.",
    "     ROC active only when Abs(ROC) >= indicators.ROC.slope_sensitivity.",
    "     VWAP active only when VWAP not in warmup (VWAPSessionCandles >= VWAP.warmup_candles).",
    "     RegimeMaxScore bumped +1 for TRENDING (19→20) and RANGE_BOUND (18→19).",
    "     Verdict % thresholds auto-adjust via Math.Ceiling(regimeMax * pct).",
    "v12: [session-volume-norms] Added top-level 'session_volume' block.",
    "     enabled: true  -- master switch; set false to bypass session scaling entirely.",
    "     sessions: 3 UTC bucket entries (ASIA 00-07, LONDON 08-12, NY 13-23).",
    "     Each bucket carries high_multiplier and mid_multiplier applied to",
    "     DynamicNorms.VolHighThreshold / VolMidThreshold after dynamic computation.",
    "     ASIA defaults 0.8/0.85 (lower liquidity), LONDON 1.0/1.0 (neutral),",
    "     NY 1.15/1.1 (elevated liquidity). Tune after 50+ runs per session.",
    "v11: [oi-cvd-cross-confirm] Added 'oi_cvd_cross' block under 'indicators' with 3 keys:",
    "     enabled: true        -- master switch for Pass 2b gate in ScoringEngine_Calculate_Scoring",
    "     upgrade_bonus: 1     -- score added when OI full/partial and CVD direction confirm each other",
    "     conflict_penalty: 1  -- score deducted when OI full signal and CVD directly oppose",
    "     Set upgrade_bonus or conflict_penalty to 0 to disable that half without code change.",
    "v10: [funding-momentum] Added 'funding' block under 'indicators' with 5 keys:",
    "     momentum_enabled: true  -- master switch for Step 3b / CalcFundingMomentum()",
    "     momentum_window: 3      -- samples back to compare against most recent rate",
    "     momentum_threshold: 0.0001 -- min abs delta (1 bp) to classify RISING/FALLING",
    "     momentum_amplify: 1     -- extra penalty when momentum confirms crowding",
    "     momentum_soften: 1      -- score restored when momentum signals de-crowding",
    "v9: [fix] Funding thresholds recalibrated from current_funding scale to funding_8h scale.",
    "     funding_high_positive:  0.001  -> 0.0001  (0.01%/8h)",
    "     funding_low_positive:   0.0005 -> 0.00005 (0.005%/8h)",
    "     funding_high_negative: -0.001  -> -0.0001",
    "     funding_low_negative:  -0.0005 -> -0.00005",
    "     Conservative starting points for BTC-PERPETUAL. Review after 50+ runs.",
    "v8: [Kelly] Added kelly block (account_size_usd, use_half_kelly, max_risk_fraction, contract_face_usd, est_prob_floor, est_prob_scale). CAL mode removed; EST mode only until backtesting module ships.",
    "v7: [Step 5b] Added context_tag_structural_min (default 3) and context_tag_flow_max (default 1) to scoring block. Used by CalcVerdictContext() to classify FLOW_UNCONFIRMED / MOMENTUM_FADING / STRUCTURALLY_WEAK.",
    "v6: [P10/P11/P12] v0.49: Added scoring penalty/multiplier keys (atr_target_multiplier, atr_stop_multiplier, bbw_squeeze_penalty, liq_standard_penalty, liq_large_penalty, funding_high_penalty, funding_high_boost, funding_low_penalty)",
    "v5: Added auto_run block (enabled, interval_minutes, interval_seconds)",
    "v4: Added mtf_gate block (soft gate, 2-of-3, 60 candles lookback, adx_min 20)",
    "v3: Added VWAP session timing fields (session1_start_hour/minute, session2_start_hour/minute)",
    "v3: Added VWAP warmup_candles threshold (was hardcoded 15)",
    "v2: Added CVD indicator settings block",
    "v2: Added CVD to scoring.weights",
    "v2: Exposed RSI divergence gates (divergence_price_gate, divergence_rsi_delta)",
    "v2: Exposed OBV trend/divergence gates (trend_gate, divergence_gate)",
    "v2: Exposed ROC series_lookback (was hardcoded 3)",
    "v2: Exposed OI change_threshold_pct (was hardcoded 1%)",
    "v2: Exposed verdict % thresholds (strong_pct, med_pct, weak_pct)",
    "v2: Added scoring.weights.CVD"
  ],
  "indicators": {
    "ADX":      { "period": 9,  "trend_threshold": 25.0, "range_threshold": 20.0 },
    "RSI":      { "period": 9,  "oversold": 40.0, "overbought": 60.0,
                  "partial_oversold": 45.0, "partial_overbought": 55.0,
                  "divergence_price_gate": 0.001,
                  "divergence_rsi_delta": 5.0,
                  "div_penalty_rsi_high": 65.0,
                  "div_penalty_rsi_low": 35.0,
                  "pivot_wing": 2,
                  "lookback_bars": 20,
                  "pass2c_midline": 50.0,
                  "divergence_overbought_threshold": 65.0,
                  "divergence_oversold_threshold": 35.0 },
    "ROC":      { "period": 9,  "slope_delta_threshold": 0.05, "magnitude_threshold": 0.1, "series_lookback": 3 },
    "VWAP":     {
                  "session2_start_hour": 13,
                  "session2_start_minute": 30,
                  "warmup_candles": 15
                },
    "BBW":      { "period": 20, "std_dev": 2.0,
                  "series_window_multiplier": 5, "squeeze_percentile": 0.20 },
    "EMA":      { "fast": 9,   "mid": 21,  "slow": 50 },
    "Donchian": { "period": 20, "quartile_pct": 0.25 },
    "OBV":      { "trend_gate": 18.0, "divergence_gate": 0.001 },
    "ATR":      { "period": 7,
                  "static_ref": 38.0, "scale_min": 0.25, "scale_max": 4.0 },
    "OFI":      { "book_depth": 5,
                  "buy_dominant_ratio": 1.60, "sell_dominant_ratio": 0.625,
                  "momentum_enabled": false,
                  "momentum_window": 3,
                  "momentum_threshold": 0.15,
                  "momentum_bonus": 1,
                  "averaging_enabled": true,
                  "avg_window_sec": 10 },
    "Volume":   { "sma_period": 9,
                  "static_high": 3.0, "static_mid": 2.0,
                  "dynamic_high_clamp_min": 2.0, "dynamic_high_clamp_max": 6.0,
                  "dynamic_mid_clamp_min": 1.5, "dynamic_mid_clamp_max": 4.0 },
    "VWAPDynamic": { "dev_clamp_min": 0.30, "dev_clamp_max": 3.0,
                     "static_fallback": 1.5 },
    "Liquidations": { "large_liq_size": 200.0, "dominance_ratio": 2.0 },
    "OI":       { "neutral_band_pct": 0.05, "change_threshold_pct": 0.002 },
    "DMI":      { "period": 9 },
    "CVD":      { "slope_min_usd": 12000.0,
                  "slope_pct_of_value": 0.10,
                  "divergence_price_gate": 0.0005,
                  "divergence_penalty": 1,
                  "late_segment_weight": 2.0,
                  "early_segment_weight": 1.0 },
    "TFI":      { "window_size": 30, "threshold": 0.15 },
    "MicroCVD": { "window_size": 50, "accel_threshold": 10000.0,
                  "accel_threshold_dynamic_pct": 0.30,
                  "accel_threshold_floor_pct": 0.25,
                  "decel_penalty": 1 },
    "TTM":      { "flat_threshold": 0.5, "sma_period": 20, "lin_reg_period": 7 },
    "VPFR":     { "num_buckets": 50,
                  "value_area_pct": 0.70,
                  "hvn_vol_pct": 0.60,
                  "lvn_vol_pct": 0.20,
                  "hvn_proximity_pct": 0.002,
                  "decay_base": 0.985,
                  "value_area_scoring_enabled": false },
    "funding":  {
                  "momentum_enabled": true,
                  "momentum_window_minutes": 5,
                  "momentum_threshold": 0.0000002,
                  "momentum_amplify": 1,
                  "momentum_soften": 1
                },
    "oi_cvd_cross": {
                  "enabled": true,
                  "upgrade_bonus": 1,
                  "conflict_penalty": 1
                },
    "spread": {
                  "wide_threshold_bps": 5.0,
                  "tight_threshold_bps": 1.5
                },
    "swing": {
                  "pivot_wing_5m": 3,
                  "lookback_bars_5m": 30,
                  "pivot_wing_15m": 2,
                  "lookback_bars_15m": 20
                },
    "trend_structure": {
                  "enabled": true,
                  "pivot_wing": 3,
                  "pivot_count": 6,
                  "structure_bonus": 1
                },
    "aggressor_velocity": {
                  "enabled": true,
                  "scoring_enabled": true,
                  "fast_window_sec": 5,
                  "direction_lean_floor": 0.2,
                  "gross_floor_usd_per_sec": 50,
                  "upgrade_bonus": 1,
                  "contra_penalty": 1,
                  "default":  { "norm_window_sec": 120, "burst_ratio_threshold": 2.5 },
                  "sessions": {
                    "NY":     { "norm_window_sec": 60, "burst_ratio_threshold": 4.5 },
                    "LONDON": { "burst_ratio_threshold": 5.5 },
                    "ASIA":   { }
                  }
                },
    "absorption": {
                  "enabled": true,
                  "scoring_enabled": false,
                  "proximity_atr_frac": 0.30,
                  "band_atr_frac": 0.10,
                  "window_sec": 10,
                  "break_tol_atr_frac": 0.05,
                  "absorb_ratio": 1.5,
                  "depletion_floor_usd": 5000,
                  "max_pull_frac": 0.75,
                  "penalty": 1,
                  "default":  { "min_aggr_usd": 20000 },
                  "sessions": {
                    "NY":     { },
                    "LONDON": { },
                    "ASIA":   { }
                  }
                }
  },
  "session_volume": {
    "enabled": true,
    "sessions": [
      {
        "name": "ASIA",
        "start_hour": 0,
        "end_hour": 7,
        "high_multiplier": 1.00,
        "mid_multiplier": 1.00,
        "execution_resolution": 3,
        "roc_magnitude_threshold": 0.17
      },
      {
        "name": "LONDON",
        "start_hour": 8,
        "end_hour": 12,
        "high_multiplier": 1.00,
        "mid_multiplier": 1.00,
        "execution_resolution": 3,
        "roc_magnitude_threshold": 0.11
      },
      {
        "name": "NY",
        "start_hour": 13,
        "end_hour": 23,
        "high_multiplier": 1.15,
        "mid_multiplier": 1.10,
        "execution_resolution": 1
      }
    ]
  },
  "resolution_profiles": {
    "1": {},
    "3": {
      "roc_magnitude_threshold": 0.21,
      "roc_slope_delta_threshold": 0.06
    }
  },
  "mtf_gate": {
    "enabled": true,
    "candle_lookback": 60,
    "adx_period": 9,
    "adx_min": 20.0,
    "min_of": 2
  },
  "auto_run": {
    "interval_minutes": 1,
    "interval_seconds": 0,
    "trigger_mode": "on_close"
  },
  "scoring": {
    "verdict_strong_pct": 0.70,
    "verdict_med_pct":    0.53,
    "verdict_weak_pct":   0.35,
    "funding_high_positive":  0.00008,
    "funding_low_positive":   0.00001,
    "funding_high_negative": -0.00008,
    "funding_low_negative":  -0.00001,
    "bbw_squeeze_penalty": 2,
    "liq_standard_penalty": 1,
    "liq_large_penalty": 2,
    "funding_high_penalty": 2,
    "funding_high_boost": 1,
    "funding_low_penalty": 1,
    "atr_target_multiplier": 1.75,
    "atr_stop_multiplier": 1.6,
    "structural_levels": {
      "enabled": true,
      "target_max_atr_mult": 3.5,
      "stop_max_atr_mult": 1.6,
      "stop_min_floor_ticks": 4,
      "stop_too_loose_mode": "clamp",
      "target_arbitration_mode": 0,
      "stop_arbitration_mode": 0,
      "target_buffer_pct": 0.0,
      "stop_buffer_pct": 0.0,
      "sessions": {
        "NY": {},
        "LONDON": { "fallback_target_atr_mult": 2.0 },
        "ASIA": { "fallback_target_atr_mult": 1.25 }
      }
    },
    "min_tradeable_move_pct": 0.0008,
    "hold_roc_take_profit_long": 0.6,
    "hold_roc_take_profit_short": -0.6,
    "hold_rsi_hold_long": 60,
    "hold_rsi_evaluate_long": 40,
    "hold_rsi_hold_short": 40,
    "hold_rsi_evaluate_short": 60,
    "context_tag_structural_min": 3,
    "context_tag_flow_max": 1,
    "spread_wide_penalty": 1,
    "regime_max_score": {
      "trending": 19,
      "range_bound": 18,
      "transitional": 15
    },
    "tier_floor": {
      "high_threshold": 12, "high_floor": 9,
      "med_threshold":   9, "med_floor":  6,
      "low_threshold":   6, "low_floor":  3
    },
    "context_tag_thresholds": {
      "momentum_fading_decay_ratio": 0.5,
      "momentum_fading_count_min": 2,
      "structurally_weak_struct_min": 2,
      "structurally_weak_flow_min": 2
    }
  },
  "kelly": {
    "account_size_usd": 1000.0,
    "use_half_kelly": true,
    "max_risk_fraction": 0.05,
    "contract_face_usd": 10.0,
    "est_prob_floor": 0.45,
    "est_prob_scale": 0.20,
    "max_leverage": 5.0
  },
  "regime_gates": {
    "transitional_adx_penalty_mid": 22.5,
    "transitional_adx_penalty_high": 25.0,
    "transitional_penalty_low": 2,
    "transitional_penalty_mid": 1
  },
  "regime_weights": {
    "enabled": true,
    "trending": {
      "alignment_bonus": 1,
      "conflict_penalty": 1
    },
    "range_bound": {
      "alignment_bonus": 1,
      "conflict_penalty": 1
    }
  },
  "network": {
    "request_timeout_seconds": 15,
    "retry_count": 1,
    "retry_backoff_ms": 1000,
    "transport": "ws",
    "ws_url": "wss://www.deribit.com/ws/api/v2",
    "ws_heartbeat_sec": 30,
    "ws_stale_after_sec": 10,
    "ws_cooldown_sec": 300,
    "ws_fallback_to_rest": true,
    "shadow_parity": false
  },
  "analysis_logging": {
    "output_dump_enabled": true,
    "output_dump_max_runs": 3000
  },
  "performance_display": {
    "enabled": true,
    "min_sample_for_render": 10,
    "eager_backfill_on_startup": true,
    "session_block_semantic": "most_recent",
    "gap_backfill_enabled": true,
    "max_gap_fill_calls": 10,
    "max_gap_fill_minutes": 5000,
    "metric_mode": "barrier"
  },
  "exit_guard": {
    "enabled": true,
    "interval_sec": 3,
    "debounce_evals": 2,
    "sound_enabled": true
  },
  "live_strip": {
    "enabled": true,
    "refresh_sec": 2,
    "tape_window_sec": 10
  },
  "signal_bridge": {
    "enabled": true,
    "output_path": "C:\\Dev\\DeribitBridge\\verdict_signal.json"
  },
  "alerts": {
    "enabled": true,
    "cascade_min_trades": 3,
    "cascade_window_sec": 10,
    "level_ticks": 12,
    "sound_enabled": false
  }
}
